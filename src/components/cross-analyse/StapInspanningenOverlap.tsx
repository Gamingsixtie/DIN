"use client";

import type { DINSession, Stap3Result } from "@/lib/types";

interface StapInspanningenOverlapProps {
  session: DINSession;
  result?: Stap3Result;
}

export default function StapInspanningenOverlap({ session, result }: StapInspanningenOverlapProps) {
  // Stub -- will be implemented in Task 2
  return <div>StapInspanningenOverlap placeholder</div>;
}
