"use client";

import type { DINSession, Stap2Result } from "@/lib/types";

interface StapGedeeldeVermogensProps {
  session: DINSession;
  result?: Stap2Result;
}

export default function StapGedeeldeVermogens({ session, result }: StapGedeeldeVermogensProps) {
  // Stub -- will be implemented in Task 2
  return <div>StapGedeeldeVermogens placeholder</div>;
}
