---
phase: 13-stap-5-cross-analyse-prioriteitsview-eerste-doel
plan: 03
type: execute
wave: 2
depends_on: [13-02]
files_modified:
  - src/app/api/cross-analyse/route.ts
  - src/components/cross-analyse/StapSectorVertaling.tsx
autonomous: true
requirements: [R-CROSS-01, R-CROSS-02]
must_haves:
  truths:
    - "API route voor stap 5 stuurt versmalde structuredData (alleen focusdoel + baten onder focusdoel + cross-sector vermogens + gedeelde inspanningen) naar Claude"
    - "API route en client gebruiken identieke focus-selectie (getFocusGoal uit stap5-focus.ts)"
    - "StapSectorVertaling.tsx toont het focusdoel, baten per sector met SectorBadge + geraakt/risico badges, cross-sector vermogens met hefboom-callouts, gedeelde inspanningen met breedte-badges, AI-samenvatting, en een inklapbare buiten-scope footer"
    - "Oude totaaloverzicht (volledige DIN-keten, domeinbalans, gap-analyse) is verwijderd uit StapSectorVertaling.tsx"
    - "Structurele view rendert ook zonder AI-resultaat — AI-callouts verschijnen pas na Analyseer"
    - "npm run build slaagt zonder errors"
  artifacts:
    - path: "src/app/api/cross-analyse/route.ts"
      provides: "Stap 5 branch versmalt structuredData naar payloadForPrompt via getFocusGoal + focus-scope filters"
      contains: "payloadForPrompt"
    - path: "src/components/cross-analyse/StapSectorVertaling.tsx"
      provides: "Volledig herschreven focusview component — FocusDoelCard + BatenPerSectorGroup + CrossSectorVermogensList + GedeeldeInspanningenList + AISamenvattingBlock + BuitenScopeFooter"
      contains: "computeFocusView"
      min_lines: 200
  key_links:
    - from: "src/app/api/cross-analyse/route.ts"
      to: "src/lib/stap5-focus.ts getFocusGoal"
      via: "import and call on body.goals for stap===5 branch"
      pattern: "getFocusGoal"
    - from: "src/components/cross-analyse/StapSectorVertaling.tsx"
      to: "src/lib/stap5-focus.ts computeFocusView"
      via: "import and call on session prop"
      pattern: "computeFocusView"
    - from: "src/components/cross-analyse/StapSectorVertaling.tsx"
      to: "result.batenDekking / result.vermogenReview / result.inspanningReview"
      via: "find by id to decorate each item with AI callouts"
      pattern: "result\\?\\.(batenDekking|vermogenReview|inspanningReview)"
    - from: "src/components/cross-analyse/StapSectorVertaling.tsx"
      to: "SectorBadge component"
      via: "relative import ./shared"
      pattern: "from \"\\./shared\""
---

<objective>
Wave 2 implementation: Laat de API-route voor stap 5 een versmalde payload sturen naar Claude (alleen focusdoel + focus-scope items), en herschrijf `StapSectorVertaling.tsx` volledig naar de focusview conform UI-SPEC. Beide taken gebruiken de Wave 1 pure helpers uit `src/lib/stap5-focus.ts` als single source of truth voor filter-semantiek.

**Purpose:** Client en server filteren identiek (D-01 + D-09). Component toont de methodische keten voor precies het eerste doel met AI-gekoppelde beoordelingsbadges. Oude totaaloverzicht (DIN-keten render, domeinbalans, gap-analyse) verdwijnt volledig uit stap 5.

**Output:**
- `src/app/api/cross-analyse/route.ts` — versmalde `payloadForPrompt` in `stap === 5` branch
- `src/components/cross-analyse/StapSectorVertaling.tsx` — volledige rewrite volgens UI-SPEC layout: FocusDoelCard → BatenPerSectorGroup → CrossSectorVermogensList → GedeeldeInspanningenList → AISamenvattingBlock → BuitenScopeFooter
- `npm run build` slaagt

**Scope disclaimer:** Geen wijzigingen aan `CrossAnalyseWizard.tsx` (Wave 3). Geen apply-knoppen, geen inline editing (CONTEXT.md deferred). Apply-flow voor AI-suggesties is expliciet deferred.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-CONTEXT.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-RESEARCH.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-UI-SPEC.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-VALIDATION.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-02-PLAN.md
@src/app/api/cross-analyse/route.ts
@src/components/cross-analyse/StapSectorVertaling.tsx
@src/components/cross-analyse/CrossAnalyseWizard.tsx
@src/components/cross-analyse/shared.tsx

<interfaces>
<!-- Imports beschikbaar vanuit src/lib/stap5-focus.ts (Wave 1) -->

```typescript
import { getFocusGoal, computeFocusView, type FocusView } from "@/lib/stap5-focus";

// getFocusGoal<T extends { rank?: number }>(goals: T[]): T | undefined
// computeFocusView(session: DINSession): FocusView | null
// FocusView = { focusGoal, focusBenefits, focusCaps, focusEfforts, outOfScopeCaps, outOfScopeEfforts }
```

<!-- Existing SectorBadge from ./shared -->

```typescript
// src/components/cross-analyse/shared.tsx
import type { SectorName } from "@/lib/types";
export function SectorBadge({ sector }: { sector: SectorName | string }): JSX.Element;
// Padding: px-1.5 py-0.5, text-[10px], uses SECTOR_COLORS lookup
```

<!-- Stap5Result shape (Wave 1 delivered) -->

```typescript
interface Stap5Result {
  focusDoelId: string;
  focusDoelNaam: string;
  vermogenReview: Array<{
    vermogenId: string;
    hefboomAnalyse: string;
    suggestieAanscherping?: string;
  }>;
  inspanningReview: Array<{
    inspanningId: string;
    breedteOordeel: "dekt_volledig" | "moet_verbreed" | "mist_aspect";
    toelichting: string;
    suggestieVerbreding?: string;
  }>;
  batenDekking: Array<{
    baatId: string;
    sector: string;
    wordtGeraakt: boolean;
    redenering: string;
    risico?: string;
  }>;
  samenvatting: string;
}
```
</interfaces>

<codebase_context>
### route.ts huidige structuur (voor context)
- Lines 1-32: Imports + getStepConfig map (stap→prompt+schema — Stap5ResultSchema wiring al in place, geen wijziging)
- Lines 34-59: buildCumulativeContext helper
- Lines 61-108: POST handler start, env check, verrijkt-sectorplan branch, basis body data extraction (goalsData, benefitsData, capsData, effortsData, projectsData)
- Lines 109-120: structuredData object assemblen (volledig — voor alle stappen 1-4)
- Lines 122-160: Per-step wizard invocation (`stap >= 1 && stap <= 5`), assembleSystemPrompt + callClaudeWithValidation + response formatting
- **Kritiek punt:** Line 134 serialiseert `structuredData` in userMessage. Voor `stap === 5` moeten we een `payloadForPrompt` variabele introduceren die versmald is.
- Lines 162-197: Legacy fallback branch (backward compat) — NIET raken

### StapSectorVertaling.tsx huidige file (wordt volledig vervangen)
- Lines 1-11: "use client" + imports (SectorBadge, findGaps, getDomainBalance, DOMAIN_LABELS, DINSession, Stap5Result, EffortDomain)
- Lines 13-25: DOMAIN_COLORS en CHAIN_COLORS constants
- Lines 27-89: Active items logic, gaps, domain balance, empty state
- Lines 91-405: Consolidatie-samenvatting card + DIN-keten secties + Domeinbalans + Gap analyse
- **Alles weg**, behalve CHAIN_COLORS constante (wordt aangepast tot plain string map per UI-SPEC).

### Imports die bewaard moeten blijven
- `SectorBadge` (relative `./shared` — Pitfall 5)
- `DINSession`, `Stap5Result` uit `@/lib/types`

### Imports die weg moeten
- `findGaps`, `getDomainBalance` (Pitfall 3 — lint fail als ze blijven)
- `DOMAIN_LABELS` (niet meer nodig; de nieuwe "Gedeelde inspanningen" sectie groepeert optioneel per domein maar UI-SPEC staat ook flat list toe)
- `EffortDomain` type (niet meer nodig tenzij je het domein-grouping doet)

### UI-SPEC strict rules (herhaald)
- Alleen `font-normal` (400) en `font-semibold` (600)
- Alleen sizes: `text-lg` (18), `text-sm` (14), `text-[13px]` (13), `text-[11px]` (11)
- Geen `font-medium`, geen `text-xl`, geen `text-xs`
- Accent alleen als `border-l-4` op DIN-keten items (10% rule)
- Kleurbadges per D-05 — groene "geraakt", rode "risico", groene "dekt volledig", amber "moet verbreed", rode "mist aspect"
- Pre-AI placeholder dot moet `aria-label="Wachten op AI-analyse"` hebben
- Hard-coded sector volgorde: `["PO", "VO", "Zakelijk"] as const` (Open Question 4)
- Min touch target 44px voor Toon details toggle
</codebase_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API route — versmalde payloadForPrompt voor stap===5</name>
  <files>src/app/api/cross-analyse/route.ts</files>
  <read_first>
    - src/app/api/cross-analyse/route.ts (volledig, focus op lines 92-160)
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-RESEARCH.md §Code Examples → Pattern 4 (API route narrowing) en Example 3 (reference to Pattern 4)
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-CONTEXT.md §Decisions D-01, D-09
    - src/lib/stap5-focus.ts (Wave 1 — import getFocusGoal)
  </read_first>
  <action>
    Open `src/app/api/cross-analyse/route.ts`. Voer precies deze wijzigingen door:

    **Wijziging 1: Import `getFocusGoal`**

    Voeg na de bestaande `import { assembleSystemPrompt, extractKiBContext } from "@/lib/prompt-assembly";` regel (line 20) toe:

    ```typescript
    import { getFocusGoal } from "@/lib/stap5-focus";
    ```

    **Wijziging 2: Introduceer payloadForPrompt in de stap===5 branch**

    Tussen de bestaande lines 120 (`};` — einde van `structuredData` object literal) en line 122 (`// Per-step wizard invocation (D-11)`), VOEG TOE een nieuwe sectie die `payloadForPrompt` voorbereidt maar alleen voor `stap === 5`. De bestaande `structuredData` blijft intact voor stap 1-4.

    Daarna, in de bestaande stap-branch (lines 122-160), WIJZIG de `userMessage` assembly (line 134) om `payloadForPrompt` te gebruiken in plaats van `structuredData`.

    De volledige vervanging van lines 122-160 moet dit worden (behoud bestaande logica, voeg alleen de stap===5 narrowing toe):

    ```typescript
        // Per-step wizard invocation (D-11)
        const stap = body.stap as number | undefined;
        if (stap && stap >= 1 && stap <= 5) {
          const config = getStepConfig(stap);
          if (!config) {
            return NextResponse.json(
              { success: false, error: `Ongeldige stap: ${stap}` },
              { status: 400 }
            );
          }

          // D-09 — Stap 5 versmalt payload tot focusdoel + focus-scope items
          // getFocusGoal gebruikt dezelfde expressie als client (src/lib/stap5-focus.ts)
          let payloadForPrompt: unknown = structuredData;
          if (stap === 5) {
            const rawGoals = (body.goals || []) as Array<{ id: string; name?: string; description?: string; rank?: number }>;
            const focusGoal = getFocusGoal(rawGoals);
            if (focusGoal) {
              const goalBenefitMaps = (body.goalBenefitMaps || []) as Array<{ goalId: string; benefitId: string }>;
              const benefitCapabilityMaps = (body.benefitCapabilityMaps || []) as Array<{ benefitId: string; capabilityId: string }>;
              const capabilityEffortMaps = (body.capabilityEffortMaps || []) as Array<{ capabilityId: string; effortId: string }>;

              const focusBenefitIds = new Set(
                goalBenefitMaps.filter((m) => m.goalId === focusGoal.id).map((m) => m.benefitId)
              );
              const focusBenefits = benefitsData.filter((b) => focusBenefitIds.has(b.id));

              const rawCaps = (body.capabilities || []) as Array<{ id: string; relatedSectors?: string[]; consolidated?: boolean }>;
              const activeCapIds = new Set(rawCaps.filter((c) => !c.consolidated).map((c) => c.id));
              const sharedCapIds = new Set(
                rawCaps
                  .filter((c) => !c.consolidated && (c.relatedSectors?.length ?? 0) > 1)
                  .map((c) => c.id)
              );
              const focusCapIds = new Set(
                benefitCapabilityMaps
                  .filter((m) => focusBenefitIds.has(m.benefitId) && sharedCapIds.has(m.capabilityId))
                  .map((m) => m.capabilityId)
              );
              const focusCaps = capsData.filter((c) => focusCapIds.has(c.id) && activeCapIds.has(c.id));

              const rawEfforts = (body.efforts || []) as Array<{ id: string; responsibleSector?: string; consolidated?: boolean }>;
              const activeEffortIds = new Set(rawEfforts.filter((e) => !e.consolidated).map((e) => e.id));
              const sharedEffortIds = new Set(
                rawEfforts
                  .filter((e) => !e.consolidated && (e.responsibleSector?.includes(",") ?? false))
                  .map((e) => e.id)
              );
              const focusEffortIds = new Set(
                capabilityEffortMaps
                  .filter((m) => focusCapIds.has(m.capabilityId) && sharedEffortIds.has(m.effortId))
                  .map((m) => m.effortId)
              );
              const focusEfforts = effortsData.filter((e) => focusEffortIds.has(e.id) && activeEffortIds.has(e.id));

              payloadForPrompt = {
                focusDoel: { id: focusGoal.id, naam: focusGoal.name ?? focusGoal.description ?? "" },
                baten: focusBenefits,
                gedeeldeVermogens: focusCaps,
                gedeeldeInspanningen: focusEfforts,
                koppelingen: {
                  goalBenefitMaps: goalBenefitMaps.filter((m) => m.goalId === focusGoal.id),
                  benefitCapabilityMaps: benefitCapabilityMaps.filter(
                    (m) => focusBenefitIds.has(m.benefitId) && focusCapIds.has(m.capabilityId)
                  ),
                  capabilityEffortMaps: capabilityEffortMaps.filter(
                    (m) => focusCapIds.has(m.capabilityId) && focusEffortIds.has(m.effortId)
                  ),
                },
              };
            }
          }

          const cumulativeContext = buildCumulativeContext(body);
          let userMessage = `Analyseer de volgende DIN-data over alle sectoren heen.\nGebruik de id-velden om items te identificeren in je clusters.\n\n${JSON.stringify(payloadForPrompt, null, 2).slice(0, 20000)}`;
          if (cumulativeContext) {
            userMessage += `\n\n${cumulativeContext}`;
          }
          if (body.userFeedback) {
            userMessage += `\n\nExtra instructies van de gebruiker: ${body.userFeedback}`;
          }

          const result = await callClaudeWithValidation(
            config.schema,
            assembleSystemPrompt(config.prompt, "cross-analyse", undefined, kibContext),
            userMessage,
            { maxTokens: 16384, model: "claude-opus-4-6" }
          );

          if (!result.success) {
            return NextResponse.json(
              { success: false, error: result.error, retryable: true },
              { status: 422 }
            );
          }

          return NextResponse.json({
            success: true,
            data: { analysis: result.data, stap },
          });
        }
    ```

    **Kritieke details:**
    - `payloadForPrompt` default = `structuredData` — stap 1-4 ongewijzigd
    - Voor stap 5: ALLEEN als `focusGoal` bestaat wordt er versmald (fallback: volledige payload, Claude gaat dan toch niks vinden)
    - Gebruik bestaande `benefitsData`, `capsData`, `effortsData` mapped arrays (lines 96-104) — die zijn al gedestructureerd. Gebruik NIET `body.benefits` direct voor het finale filter.
    - Gebruik `body.capabilities` en `body.efforts` (raw arrays met `relatedSectors`, `consolidated`, `responsibleSector`) voor het BEPALEN van IDs, daarna filter op de mapped `capsData`/`effortsData` voor de payload zodat alleen de verwachte velden naar Claude gaan.
    - NIET raken: lines 162-197 (backward compat fallback).

    **Verwacht gedrag na task:** Stap 1-4 blijven 100% identiek werken. Stap 5 krijgt een versmalde JSON payload in de Claude prompt (max ~2KB voor realistische sessies).
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/stap5-focus-filter.test.ts src/lib/__tests__/schemas-stap5.test.ts --reporter=default</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/cross-analyse/route.ts` importeert `getFocusGoal` van `@/lib/stap5-focus` (via grep: `grep -c "from \"@/lib/stap5-focus\"" src/app/api/cross-analyse/route.ts` ≥ 1)
    - Bevat `let payloadForPrompt: unknown = structuredData;` exact
    - Bevat `if (stap === 5)` conditie
    - Bevat `const focusGoal = getFocusGoal(` call
    - Bevat `focusBenefitIds`, `focusCapIds`, `focusEffortIds` variable names
    - Bevat `payloadForPrompt = {` met `focusDoel`, `baten`, `gedeeldeVermogens`, `gedeeldeInspanningen`, `koppelingen` velden
    - `JSON.stringify(payloadForPrompt, null, 2)` in de userMessage template (NIET `JSON.stringify(structuredData` in de stap-branch)
    - Stap 1-4 branches ongewijzigd (verwijder NIKS van lines 162-197 en de bestaande `structuredData` definitie bij lines 109-120)
    - `npm run lint` op dit bestand zonder nieuwe errors (existing tests mogen niet falen; rest van het project mag build fails hebben door Wave 2 Task 2 dependencies)
    - Wave 0 tests blijven groen (23/23 PASS)
  </acceptance_criteria>
  <done>
    route.ts bevat de versmalling voor stap 5. Stap 1-4 onverlet. Wave 0 tests blijven groen.
  </done>
</task>

<task type="auto">
  <name>Task 2: StapSectorVertaling.tsx volledig herschrijven naar focusview per UI-SPEC</name>
  <files>src/components/cross-analyse/StapSectorVertaling.tsx</files>
  <read_first>
    - src/components/cross-analyse/StapSectorVertaling.tsx (HUIDIG — weet wat er weg moet)
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-UI-SPEC.md (VOLLEDIG — dit is de design contract, elk onderdeel moet matchen: copy, typografie, kleuren, badges, layout order, accessibility)
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-RESEARCH.md §Code Examples → Example 4 (imports + helper reference), Example 5 (SectorBadge per-baat group), Example 6 (buiten-scope collapsible footer) — deze leveren concrete JSX patronen
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-CONTEXT.md §Decisions D-03, D-04, D-05, D-06, D-11, D-12
    - src/components/cross-analyse/shared.tsx (SectorBadge export — check exacte signature)
    - src/lib/stap5-focus.ts (Wave 1 — computeFocusView signature)
    - src/lib/types.ts (DINSession, Stap5Result, SECTOR_COLORS, SECTORS)
  </read_first>
  <action>
    Vervang de volledige inhoud van `src/components/cross-analyse/StapSectorVertaling.tsx` door de onderstaande implementatie. Dit is een end-to-end herschrijving.

    **Context:** SectorName = `"PO" | "VO" | "Zakelijk"`. Copy komt verbatim uit UI-SPEC §Copywriting Contract. Kleuren komen verbatim uit UI-SPEC §Color en §Semantic badge palette. Typografie rules: alleen `font-normal`, `font-semibold`, `text-lg`, `text-sm`, `text-[13px]`, `text-[11px]`.

    ```typescript
    "use client";

    import { useState } from "react";
    import { SectorBadge } from "./shared";
    import { computeFocusView } from "@/lib/stap5-focus";
    import type { DINSession, Stap5Result, SectorName } from "@/lib/types";

    interface StapSectorVertalingProps {
      session: DINSession;
      result?: Stap5Result;
    }

    // CHAIN_COLORS — accent (10% rule), border-l-4 only
    const CHAIN_BORDER = {
      doelen: "border-l-[#003366]",
      baten: "border-l-[#0066cc]",
      vermogens: "border-l-[#0891b2]",
      inspanningen: "border-l-[#059669]",
    } as const;

    const SECTORS_ORDER: readonly SectorName[] = ["PO", "VO", "Zakelijk"] as const;

    export default function StapSectorVertaling({ session, result }: StapSectorVertalingProps) {
      const [buitenScopeOpen, setBuitenScopeOpen] = useState(false);

      // Empty state A — geen doelen
      if (session.goals.length === 0) {
        return (
          <div className="flex flex-col items-center justify-center py-12 text-center">
            <p className="text-sm font-semibold text-gray-700">Nog geen doelen beschikbaar</p>
            <p className="text-[13px] text-gray-500 mt-1">
              Importeer eerst de KiB-uitkomsten in stap 1.
            </p>
          </div>
        );
      }

      const view = computeFocusView(session);
      if (!view) {
        // Should not happen when goals.length > 0, but TypeScript narrowing
        return null;
      }

      const { focusGoal, focusBenefits, focusCaps, focusEfforts, outOfScopeCaps, outOfScopeEfforts } = view;

      // Empty state B — focusdoel maar geen cross-sector vermogens
      const hasCrossSectorData = focusCaps.length > 0;

      // Group baten per sector
      const batenGroupedBySector = SECTORS_ORDER.map((sectorKey) => ({
        sector: sectorKey,
        baten: focusBenefits.filter((b) => b.sectorId === sectorKey),
      }));

      return (
        <div className="space-y-8">
          {/* Intro */}
          <div>
            <p className="text-[13px] text-gray-700 leading-relaxed">
              Dit is de scherpste hefboom: de keten doel → baten → vermogens → inspanningen voor uw hoogste prioriteit.
              We tonen alleen het eerste doel omdat daar het meeste mandaat en de hoogste urgentie zit. Niet-geconsolideerde
              items zijn niet verdwenen — u vindt ze onderaan in &lsquo;Buiten scope voor nu&rsquo; en pakt ze later op.
            </p>
          </div>

          {/* Block 1: FocusDoelCard */}
          <section
            className={`bg-white border border-[#e2e8f0] rounded-lg p-6 border-l-4 ${CHAIN_BORDER.doelen}`}
            aria-labelledby="focus-doel-title"
          >
            <p className="text-[11px] font-semibold text-gray-500 uppercase tracking-wider">
              Focusdoel — prioriteit 1
            </p>
            <h4 id="focus-doel-title" className="text-lg font-semibold text-[#003366] mt-1">
              {focusGoal.name || focusGoal.description}
            </h4>
          </section>

          {/* Block 2: BatenPerSectorGroup */}
          <section aria-labelledby="baten-heading">
            <h4 id="baten-heading" className="text-sm font-semibold text-gray-700">
              Baten onder dit doel — per sector
            </h4>
            <p className="text-[13px] text-gray-500 mt-1 mb-4">
              Groen = deze baat wordt geraakt door de cross-sector aanpak. Rood = risico dat deze baat niet wordt gerealiseerd.
            </p>
            <div className="space-y-4">
              {batenGroupedBySector.map(({ sector, baten }) => (
                <div key={sector} className="space-y-2">
                  <div className="flex items-center gap-2">
                    <SectorBadge sector={sector} />
                    <span className="text-[11px] font-semibold text-gray-500">
                      {baten.length} {baten.length === 1 ? "baat" : "baten"}
                    </span>
                  </div>
                  {baten.length === 0 ? (
                    <p className="text-[13px] text-gray-400 pl-3">Geen baten in deze sector onder dit doel.</p>
                  ) : (
                    <div className="space-y-2">
                      {baten.map((baat) => {
                        const dekking = result?.batenDekking.find((d) => d.baatId === baat.id);
                        const wordtGeraakt = dekking?.wordtGeraakt;
                        return (
                          <div
                            key={baat.id}
                            className={`border-l-4 ${CHAIN_BORDER.baten} bg-white border border-[#e2e8f0] rounded-r-lg pl-3 pr-3 py-2`}
                          >
                            <div className="flex items-start justify-between gap-2">
                              <span className="text-[13px] text-gray-700 flex-1">
                                {baat.title || baat.description}
                              </span>
                              {dekking ? (
                                wordtGeraakt ? (
                                  <span className="text-[11px] font-semibold bg-green-50 text-green-700 border border-green-200 rounded px-1.5 py-0.5 whitespace-nowrap">
                                    geraakt
                                  </span>
                                ) : (
                                  <span className="text-[11px] font-semibold bg-red-50 text-red-700 border border-red-200 rounded px-1.5 py-0.5 whitespace-nowrap">
                                    risico
                                  </span>
                                )
                              ) : (
                                <span
                                  className="w-2 h-2 rounded-full bg-gray-300 shrink-0 mt-1.5"
                                  aria-label="Wachten op AI-analyse"
                                />
                              )}
                            </div>
                            {dekking && !wordtGeraakt && dekking.risico && (
                              <p className="text-[13px] text-red-700 mt-1">
                                <span className="font-semibold">Risico: </span>
                                {dekking.risico}
                              </p>
                            )}
                          </div>
                        );
                      })}
                    </div>
                  )}
                </div>
              ))}
            </div>
          </section>

          {/* Block 3: CrossSectorVermogensList */}
          <section aria-labelledby="vermogens-heading">
            <h4 id="vermogens-heading" className="text-sm font-semibold text-gray-700 mb-3">
              Cross-sector vermogens die hefboom leveren
            </h4>
            {focusCaps.length === 0 ? (
              <div className="bg-white border border-[#e2e8f0] rounded-lg p-4">
                <p className="text-[13px] text-gray-600">
                  Dit doel heeft nog geen cross-sector vermogens. Voer stap 4 (Consolidatie) uit of voeg gedeelde vermogens
                  toe in de DIN-mapping, en kom daarna terug naar stap 5.
                </p>
              </div>
            ) : (
              <div className="space-y-2">
                {focusCaps.map((cap) => {
                  const review = result?.vermogenReview.find((r) => r.vermogenId === cap.id);
                  return (
                    <div
                      key={cap.id}
                      className={`border-l-4 ${CHAIN_BORDER.vermogens} bg-white border border-[#e2e8f0] rounded-r-lg pl-3 pr-3 py-3`}
                    >
                      <div className="flex items-start justify-between gap-2">
                        <span className="text-[13px] text-gray-700 flex-1">
                          {cap.title || cap.description}
                        </span>
                        <div className="flex gap-0.5 shrink-0">
                          {(cap.relatedSectors ?? []).map((s) => (
                            <SectorBadge key={s} sector={s} />
                          ))}
                        </div>
                      </div>
                      {review ? (
                        <div className="mt-2 text-[13px] text-gray-600">
                          <p className="leading-relaxed">{review.hefboomAnalyse}</p>
                          {review.suggestieAanscherping && (
                            <p className="mt-1 text-gray-500">
                              <span className="font-semibold">Aanscherping: </span>
                              {review.suggestieAanscherping}
                            </p>
                          )}
                        </div>
                      ) : (
                        <div className="mt-2 flex items-center gap-1.5">
                          <span
                            className="w-2 h-2 rounded-full bg-gray-300"
                            aria-label="Wachten op AI-analyse"
                          />
                          <span className="text-[11px] text-gray-400">AI-analyse nog niet gedraaid</span>
                        </div>
                      )}
                    </div>
                  );
                })}
              </div>
            )}
          </section>

          {/* Block 4: GedeeldeInspanningenList */}
          <section aria-labelledby="inspanningen-heading">
            <h4 id="inspanningen-heading" className="text-sm font-semibold text-gray-700 mb-3">
              Gedeelde inspanningen onder deze vermogens
            </h4>
            {focusEfforts.length === 0 ? (
              <div className="bg-white border border-[#e2e8f0] rounded-lg p-4">
                <p className="text-[13px] text-gray-600">
                  Nog geen gedeelde inspanningen. Stap 3 (Inspanningen-overlap) moet zijn doorlopen.
                </p>
              </div>
            ) : (
              <div className="space-y-2">
                {focusEfforts.map((effort) => {
                  const review = result?.inspanningReview.find((r) => r.inspanningId === effort.id);
                  const badgeClass =
                    review?.breedteOordeel === "dekt_volledig"
                      ? "bg-green-50 text-green-700 border-green-200"
                      : review?.breedteOordeel === "moet_verbreed"
                      ? "bg-amber-50 text-amber-700 border-amber-200"
                      : review?.breedteOordeel === "mist_aspect"
                      ? "bg-red-50 text-red-700 border-red-200"
                      : "";
                  const badgeLabel =
                    review?.breedteOordeel === "dekt_volledig"
                      ? "dekt volledig"
                      : review?.breedteOordeel === "moet_verbreed"
                      ? "moet verbreed"
                      : review?.breedteOordeel === "mist_aspect"
                      ? "mist aspect"
                      : null;
                  const sectors = (effort.responsibleSector ?? "")
                    .split(",")
                    .map((s) => s.trim())
                    .filter(Boolean);
                  return (
                    <div
                      key={effort.id}
                      className={`border-l-4 ${CHAIN_BORDER.inspanningen} bg-white border border-[#e2e8f0] rounded-r-lg pl-3 pr-3 py-3`}
                    >
                      <div className="flex items-start justify-between gap-2">
                        <div className="flex-1">
                          <p className="text-[13px] text-gray-700">{effort.title || effort.description}</p>
                          {sectors.length > 0 && (
                            <div className="flex gap-0.5 mt-1">
                              {sectors.map((s) => (
                                <SectorBadge key={s} sector={s} />
                              ))}
                            </div>
                          )}
                        </div>
                        {badgeLabel ? (
                          <span
                            className={`text-[11px] font-semibold border rounded px-1.5 py-0.5 whitespace-nowrap ${badgeClass}`}
                          >
                            {badgeLabel}
                          </span>
                        ) : (
                          <span
                            className="w-2 h-2 rounded-full bg-gray-300 shrink-0 mt-1.5"
                            aria-label="Wachten op AI-analyse"
                          />
                        )}
                      </div>
                      {review?.toelichting && (
                        <p className="mt-2 text-[13px] text-gray-600 leading-relaxed">{review.toelichting}</p>
                      )}
                      {review?.breedteOordeel === "moet_verbreed" && review.suggestieVerbreding && (
                        <p className="mt-1 text-[13px] text-amber-700">
                          <span className="font-semibold">Suggestie: </span>
                          {review.suggestieVerbreding}
                        </p>
                      )}
                      {review?.breedteOordeel === "mist_aspect" && review.suggestieVerbreding && (
                        <p className="mt-1 text-[13px] text-red-700">
                          <span className="font-semibold">Mist: </span>
                          {review.suggestieVerbreding}
                        </p>
                      )}
                    </div>
                  );
                })}
              </div>
            )}
          </section>

          {/* Block 5: AISamenvattingBlock */}
          <section aria-labelledby="samenvatting-heading">
            <h4 id="samenvatting-heading" className="text-sm font-semibold text-gray-700 mb-2">
              AI-samenvatting
            </h4>
            {result?.samenvatting ? (
              <div className="bg-white border border-[#e2e8f0] rounded-lg p-4">
                <p className="text-[13px] text-gray-700 leading-relaxed">{result.samenvatting}</p>
              </div>
            ) : (
              <div className="bg-gray-50 border border-[#e2e8f0] rounded-lg p-4">
                <p className="text-[13px] text-gray-500">
                  {hasCrossSectorData
                    ? "De structurele keten staat klaar. Klik op 'Analyseer eerste doel' voor de AI-review van hefboomwerking en baten-dekking."
                    : "Klik op 'Analyseer' om de hefboomanalyse, breedtebeoordeling en baten-dekking voor dit doel te zien."}
                </p>
              </div>
            )}
          </section>

          {/* Block 6: BuitenScopeFooter */}
          <div className="mt-12">
            <button
              onClick={() => setBuitenScopeOpen((v) => !v)}
              className="w-full min-h-[44px] flex items-center justify-between px-4 py-3 bg-gray-50 border border-[#e2e8f0] rounded-lg text-left hover:bg-gray-100 transition-colors"
              aria-expanded={buitenScopeOpen}
            >
              <span className="text-[13px] text-gray-700">
                <span className="font-semibold">Buiten scope voor nu</span>
                {" — "}
                {outOfScopeCaps.length} vermogens en {outOfScopeEfforts.length} inspanningen
              </span>
              <span className="text-[11px] font-semibold text-gray-500">
                {buitenScopeOpen ? "Verberg details" : "Toon details"}
              </span>
            </button>
            {buitenScopeOpen && (
              <div className="mt-3 px-4 py-3 bg-white border border-[#e2e8f0] rounded-lg space-y-3">
                <p className="text-[13px] text-gray-600">
                  Deze items zijn niet geconsolideerd tot cross-sector hefbomen voor dit doel. Ze blijven beschikbaar voor
                  latere doelen of vervolgstappen.
                </p>
                {outOfScopeCaps.length > 0 && (
                  <div>
                    <h5 className="text-[11px] font-semibold text-gray-500 uppercase tracking-wider mb-1">Vermogens</h5>
                    <ul className="space-y-1">
                      {outOfScopeCaps.map((c) => (
                        <li key={c.id} className="text-[13px] text-gray-700 flex items-center gap-2">
                          <span className="flex-1">{c.title || c.description}</span>
                          {c.sectorId && <SectorBadge sector={c.sectorId} />}
                        </li>
                      ))}
                    </ul>
                  </div>
                )}
                {outOfScopeEfforts.length > 0 && (
                  <div>
                    <h5 className="text-[11px] font-semibold text-gray-500 uppercase tracking-wider mb-1">Inspanningen</h5>
                    <ul className="space-y-1">
                      {outOfScopeEfforts.map((e) => (
                        <li key={e.id} className="text-[13px] text-gray-700 flex items-center gap-2">
                          <span className="flex-1">{e.title || e.description}</span>
                          {e.sectorId && <SectorBadge sector={e.sectorId} />}
                        </li>
                      ))}
                    </ul>
                  </div>
                )}
                {outOfScopeCaps.length === 0 && outOfScopeEfforts.length === 0 && (
                  <p className="text-[13px] text-gray-500">Er zijn geen items buiten scope.</p>
                )}
              </div>
            )}
          </div>
        </div>
      );
    }
    ```

    **Kritieke checks na schrijven:**

    1. GEEN `font-medium` ergens in het bestand (Pitfall 4, UI-SPEC Dimension 4)
    2. GEEN `text-xs` of `text-xl` (alleen `text-lg`, `text-sm`, `text-[13px]`, `text-[11px]`)
    3. GEEN imports van `findGaps`, `getDomainBalance`, `DOMAIN_LABELS`, `EffortDomain` (Pitfall 3)
    4. SectorBadge import is `from "./shared"` (relative — Pitfall 5)
    5. `computeFocusView` import is `from "@/lib/stap5-focus"` (absolute — cross-module)
    6. Elk `badge` voor `wordtGeraakt`/`breedteOordeel` heeft een tekst-label (niet alleen kleur — accessibility §Accessibility)
    7. Placeholder dot heeft `aria-label="Wachten op AI-analyse"`
    8. Sectors order is hardcoded `["PO", "VO", "Zakelijk"] as const` (Open Question 4)
    9. Intro copy + alle section headings matchen UI-SPEC §Copywriting Contract verbatim
    10. Geen mention van "Sectorvertaling", "Nieuw DIN-netwerk", "Gap-analyse", "Domeinbalans" in copy
    11. CrossAnalyseWizard.tsx passes `result={wizardState.stepResults.stap5}` al — geen prop-contract wijziging (check of existing `session` prop ongewijzigd blijft)

    **Draai na de rewrite:**
    ```
    npm run lint
    npm run build
    ```
    Beide MOETEN slagen. Als lint error: vaak `no-unused-vars` op overgeleven imports — verwijder. Als build error: type mismatch op `result?.batenDekking` etc. — check dat Wave 1 `Stap5ResultSchema` correct is en `Stap5Result` de nieuwe shape heeft.

    **CrossAnalyseWizard.tsx dependency:** De wizard rendert al `<StapSectorVertaling session={session!} result={wizardState.stepResults.stap5} />` (check `CrossAnalyseWizard.tsx` — reeds zo in huidige code). Als de wizard dit nog niet doet: **NIET** aanpassen in deze task — dat hoort bij Wave 3 Plan 04. Je taak is alleen de component rewrite zelf.
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/cross-analyse/StapSectorVertaling.tsx` importeert `computeFocusView` van `@/lib/stap5-focus` (via grep)
    - Importeert `SectorBadge` van `./shared` (relative path — via grep)
    - Bevat GEEN `findGaps` of `getDomainBalance` imports (via grep: 0 matches)
    - Bevat GEEN `DOMAIN_LABELS` import (via grep: 0 matches)
    - Bevat GEEN `font-medium` klasse (via grep: 0 matches)
    - Bevat GEEN `text-xl` klasse (via grep: 0 matches)
    - Bevat GEEN `text-xs` klasse (via grep: 0 matches)
    - Bevat `font-normal` OF geen font-weight (default) — en `font-semibold` voor labels
    - Bevat exact `"Focusdoel — prioriteit 1"` (UI-SPEC copy)
    - Bevat exact `"Baten onder dit doel — per sector"` (UI-SPEC copy)
    - Bevat exact `"Cross-sector vermogens die hefboom leveren"` (UI-SPEC copy)
    - Bevat exact `"Gedeelde inspanningen onder deze vermogens"` (UI-SPEC copy)
    - Bevat exact `"Buiten scope voor nu"` (UI-SPEC copy)
    - Bevat `aria-label="Wachten op AI-analyse"` (accessibility)
    - Bevat `min-h-[44px]` voor de buiten-scope toggle button (accessibility touch target)
    - Bevat `border-l-4 border-l-[#003366]` (of equivalent) voor focus doel
    - Bevat `border-l-4` met `border-l-[#0066cc]`, `border-l-[#0891b2]`, `border-l-[#059669]` voor baten/vermogens/inspanningen
    - Bevat drie enum checks `"dekt_volledig"`, `"moet_verbreed"`, `"mist_aspect"` (via grep op elk)
    - Bevat `SECTORS_ORDER` of equivalent hardcoded `["PO", "VO", "Zakelijk"]`
    - `npm run lint` passes zonder errors en warnings toegerekend aan dit bestand
    - `npm run build` passes zonder errors
    - Wave 0 tests blijven GROEN (23/23)
    - Bestand heeft ≥ 200 regels (rewrite is niet korter dan de UI-SPEC blokken toestaan)
  </acceptance_criteria>
  <done>
    StapSectorVertaling.tsx volledig herschreven. `npm run build` en `npm run lint` beide groen. Alle 23 Wave 0 tests nog steeds groen. Geen oude totaaloverzicht code overgebleven.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` — MOET nu slagen (alle breakages van Wave 1 gefixt)
2. `npm run lint` — geen errors
3. `npx vitest run src/lib/__tests__/stap5-*.test.ts` — alle 23 tests GROEN
4. `npx vitest run` — volledige suite groen (geen regressies in andere tests)
5. `grep -c "sectorVertalingen\|totaalSamenvatting\|findGaps\|getDomainBalance" src/components/cross-analyse/StapSectorVertaling.tsx` = 0
6. `grep -c "font-medium\|text-xl\|text-xs" src/components/cross-analyse/StapSectorVertaling.tsx` = 0
7. `grep -c "payloadForPrompt" src/app/api/cross-analyse/route.ts` ≥ 2
8. Handmatig (niet blocking): check in browser dat stap 5 een focusview toont met empty state correct (maar dit kan pas in Wave 3 na wizard integration)
</verification>

<success_criteria>
- API route stuurt versmalde payload voor stap 5 (alle 4 entities: focusdoel, baten, gedeeldeVermogens, gedeeldeInspanningen + koppelingen)
- Component toont alle 6 UI-SPEC blokken in correcte order: FocusDoelCard → BatenPerSectorGroup → CrossSectorVermogensList → GedeeldeInspanningenList → AISamenvattingBlock → BuitenScopeFooter
- Copy, typografie, kleuren matchen UI-SPEC verbatim
- Accessibility: aria-labels, heading hierarchy, touch targets
- npm run build en lint slagen
- Wave 0 tests blijven groen
</success_criteria>

<output>
After completion, create `.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-03-SUMMARY.md` with:
- Exact line changes in route.ts (welke regels toegevoegd, welke lines nu de branch-structuur vormen)
- StapSectorVertaling.tsx: nieuwe regelcount vs oude
- `npm run build` output: slagen + eventuele waarschuwingen
- `npm run lint` output: slagen
- Wave 0 test status: 23/23 GREEN
- Eventuele aanpassingen die tijdens implementatie nodig bleken (bijv. extra type imports)
- Vooruitblik naar Wave 3 Plan 04: wizard integration needed (STEP_INFO[5] update, restore guard import, CTA condition change, intro copy)
</output>
