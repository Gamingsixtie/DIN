---
phase: 13-stap-5-cross-analyse-prioriteitsview-eerste-doel
plan: 02
type: execute
wave: 1
depends_on: [13-01]
files_modified:
  - src/lib/schemas.ts
  - src/lib/prompts.ts
  - src/lib/stap5-focus.ts
autonomous: true
requirements: [R-CROSS-01, R-CROSS-02]
must_haves:
  truths:
    - "Stap5ResultSchema heeft de nieuwe shape (focusDoelId, focusDoelNaam, vermogenReview[], inspanningReview[], batenDekking[], samenvatting)"
    - "CROSS_ANALYSE_STAP5_PROMPT instrueert Claude op hefboomwerking, breedte, baten-dekking, samenvatting — met expliciete Nederlandse enum waardes"
    - "getFocusGoal en computeFocusView zijn pure functions, testbaar, en gebruikt in zowel client als server (single source of truth voor filter-semantiek)"
    - "restoreStap5Result pure function wijst oude sectorVertalingen[] shape af en accepteert nieuwe shape"
    - "Wave 0 tests (schemas-stap5, stap5-focus-filter, stap5-restore-guard) slagen allemaal GROEN"
  artifacts:
    - path: "src/lib/schemas.ts"
      provides: "Nieuwe Stap5ResultSchema (lines 430-444 vervangen)"
      contains: "focusDoelId"
    - path: "src/lib/prompts.ts"
      provides: "Nieuwe CROSS_ANALYSE_STAP5_PROMPT (lines 253-284 vervangen)"
      contains: "hefboomwerking"
    - path: "src/lib/stap5-focus.ts"
      provides: "Pure helpers getFocusGoal, computeFocusView, restoreStap5Result"
      exports: ["getFocusGoal", "computeFocusView", "restoreStap5Result"]
  key_links:
    - from: "src/lib/stap5-focus.ts"
      to: "src/lib/schemas.ts Stap5ResultSchema"
      via: "import Stap5ResultSchema for restoreStap5Result safeParse"
      pattern: "Stap5ResultSchema\\.safeParse"
    - from: "src/lib/schemas.ts"
      to: "src/lib/types.ts Stap5Result re-export (line 47)"
      via: "z.infer automatic type propagation"
      pattern: "z\\.infer<typeof Stap5ResultSchema>"
---

<objective>
Wave 1 foundation: Vervang het `Stap5ResultSchema` met het nieuwe shape, herschrijf `CROSS_ANALYSE_STAP5_PROMPT` rond hefboomwerking/breedte/baten-dekking/samenvatting, en extract de focus-filter logica naar een nieuwe pure module `src/lib/stap5-focus.ts`.

**Purpose:** Levert het datamodel, de AI-instructies, en de pure filter-helpers die Wave 2 (API route + component rewrite) en Wave 3 (wizard integration) consumeren. Pure helpers in `stap5-focus.ts` zijn de single source of truth zodat client en server dezelfde filter-semantiek hebben (D-01 identieke logica, D-09 versmalde payload).

**Output:**
- `src/lib/schemas.ts` — `Stap5ResultSchema` volledig vervangen (lines 430-444)
- `src/lib/prompts.ts` — `CROSS_ANALYSE_STAP5_PROMPT` volledig vervangen (lines 253-284)
- `src/lib/stap5-focus.ts` — nieuw bestand met `getFocusGoal`, `computeFocusView`, `restoreStap5Result`
- Alle Wave 0 tests zijn GREEN

**Scope disclaimer:** Geen wijzigingen aan `types.ts` (re-export via `z.infer` gebeurt automatisch). Geen wijzigingen aan API route (Wave 2). Geen UI wijzigingen (Wave 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-CONTEXT.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-RESEARCH.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-VALIDATION.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-01-PLAN.md
@src/lib/schemas.ts
@src/lib/prompts.ts
@src/lib/types.ts

<interfaces>
<!-- Pure module exports from src/lib/stap5-focus.ts (NIEUW) -->

```typescript
// src/lib/stap5-focus.ts

import type { Stap5Result } from "./types";
import { Stap5ResultSchema } from "./schemas";
import type {
  DINSession,
  ProgrammeGoal,
  DINBenefit,
  DINCapability,
  DINEffort,
} from "./types";

export function getFocusGoal<T extends { rank?: number }>(goals: T[]): T | undefined;

export interface FocusView {
  focusGoal: ProgrammeGoal;
  focusBenefits: DINBenefit[];
  focusCaps: DINCapability[];
  focusEfforts: DINEffort[];
  outOfScopeCaps: DINCapability[];
  outOfScopeEfforts: DINEffort[];
}

export function computeFocusView(session: DINSession): FocusView | null;

export function restoreStap5Result(rawStap5: unknown): Stap5Result | undefined;
```

<!-- Existing types that stap5-focus consumes — from src/lib/schemas.ts / src/lib/types.ts -->

```typescript
// src/lib/schemas.ts (existing)
export const DINCapabilitySchema = z.object({
  id: z.string(),
  sectorId: z.string(),
  title: z.string().optional(),
  description: z.string(),
  relatedSectors: z.array(z.string()).optional(),   // used for cross-sector filter
  consolidated: z.boolean().optional(),              // filter: !consolidated
  // ... other fields
});

export const DINEffortSchema = z.object({
  id: z.string(),
  sectorId: z.string(),
  title: z.string().optional(),
  description: z.string(),
  domain: z.enum(["mens", "processen", "data_systemen", "cultuur"]),
  responsibleSector: z.string().optional(),          // multi-sector: .includes(",")
  consolidated: z.boolean().optional(),
  // ... other fields
});

// Stap5Result is re-exported from types.ts via z.infer (types.ts line 47)
```
</interfaces>

<codebase_context>
### Hoe schemas.ts is georganiseerd
- Lines 1-429: Andere schemas (benefits, capabilities, efforts, Stap1-4, etc.)
- Lines 430-444: HUIDIGE Stap5ResultSchema (OUDE shape met sectorVertalingen) — VERVANGEN
- Lines 446-456: CrossAnalyseWizardStateSchema (gebruikt Stap5ResultSchema — geen wijziging nodig, referentie blijft intact)
- Lines 458+: DINSessionSchema en downstream types

### Hoe prompts.ts is georganiseerd
- Lines 1-250: Andere prompts (DIN_MAPPING, BENEFIT, etc.)
- Lines 253-284: HUIDIGE CROSS_ANALYSE_STAP5_PROMPT (oude instructies over sectorVertalingen) — VERVANGEN
- Lines 286+: SECTOR_INTEGRATIE_PROMPT en andere

### Hoe stap5-focus.ts NIEUW moet zijn
- Geen `"use client"` directive — pure function module, werkt in Node en browser
- Importeert types via `import type { ... } from "./types"` (absolute path via tsconfig `@/lib/*` = `src/lib/*`; ook relative import werkt want beide liggen in `src/lib/`)
- Gebruikt `import type` waar mogelijk (project convention)
- Wordt geconsumeerd door: Wave 2 Plan 03 (StapSectorVertaling.tsx en/of route.ts kunnen deze helpers direct importeren) en Wave 3 Plan 04 (CrossAnalyseWizard.tsx restore guard via restoreStap5Result)
</codebase_context>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stap5ResultSchema vervangen + CROSS_ANALYSE_STAP5_PROMPT herschrijven</name>
  <files>src/lib/schemas.ts, src/lib/prompts.ts</files>
  <read_first>
    - src/lib/schemas.ts (volledig — snap structuur, vooral lines 420-456)
    - src/lib/prompts.ts lines 200-290 (Stap4 prompt als referentie voor enum-constraint idioom, huidige Stap5 die vervangen wordt)
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-RESEARCH.md §Code Examples → Example 1 (nieuwe Stap5ResultSchema verbatim) en Example 2 (nieuwe CROSS_ANALYSE_STAP5_PROMPT verbatim)
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-CONTEXT.md §Decisions D-07, D-08
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-RESEARCH.md §Pitfall 6 (breedteOordeel enum hallucinatie — prompt moet exacte waardes opnemen)
    - src/lib/__tests__/schemas-stap5.test.ts (Wave 0 — weet welke assertions groen moeten worden)
  </read_first>
  <behavior>
    - Test 1 (schemas-stap5.test.ts): `Stap5ResultSchema.safeParse` accepteert volledige valide shape met vermogenReview, inspanningReview (alle 3 enum waardes), batenDekking (met risico), samenvatting → success=true
    - Test 2: Minimale shape met alleen focusDoelId + focusDoelNaam + samenvatting → success=true en arrays default naar `[]`
    - Test 3: Oude sectorVertalingen[] shape → success=false
    - Test 4: `breedteOordeel: "needs_broadening"` → success=false (enum constraint)
    - Test 5: Ontbrekende focusDoelId of focusDoelNaam → success=false
  </behavior>
  <action>
    **Step A: schemas.ts**

    Open `src/lib/schemas.ts`. Vervang lines 430-444 (de huidige `Stap5ResultSchema`) volledig door:

    ```typescript
    export const Stap5ResultSchema = z.object({
      focusDoelId: z.string(),
      focusDoelNaam: z.string(),
      vermogenReview: z.array(z.object({
        vermogenId: z.string(),
        hefboomAnalyse: z.string(),
        suggestieAanscherping: z.string().optional(),
      })).default([]),
      inspanningReview: z.array(z.object({
        inspanningId: z.string(),
        breedteOordeel: z.enum(["dekt_volledig", "moet_verbreed", "mist_aspect"]),
        toelichting: z.string(),
        suggestieVerbreding: z.string().optional(),
      })).default([]),
      batenDekking: z.array(z.object({
        baatId: z.string(),
        sector: z.string(),
        wordtGeraakt: z.boolean(),
        redenering: z.string(),
        risico: z.string().optional(),
      })).default([]),
      samenvatting: z.string(),
    });
    ```

    Laat `CrossAnalyseWizardStateSchema` (lines 446-456) ongewijzigd — `stap5: Stap5ResultSchema.optional()` blijft werken met de nieuwe shape.

    **Step B: prompts.ts**

    Open `src/lib/prompts.ts`. Vervang lines 253-284 (de huidige `CROSS_ANALYSE_STAP5_PROMPT`) volledig door:

    ```typescript
    export const CROSS_ANALYSE_STAP5_PROMPT = `Je bent een expert in programmamanagement (DIN-methodiek, Werken aan Programma's, Hfst 8 — Hefboomwerking).

    Je krijgt het EERSTE DOEL (focusdoel) met:
    - de baten die eraan gekoppeld zijn, per sector
    - de GECONSOLIDEERDE cross-sector vermogens die hefboom moeten leveren op die baten
    - de GEDEELDE inspanningen die die vermogens opbouwen

    Je taak is KWALITATIEF beoordelen — niet tellen, niet rangschikken, niet filteren. De client heeft de items al geselecteerd.

    Beoordeel:

    1. HEFBOOMWERKING PER VERMOGEN
       Voor elk vermogen: welke baten (in welke sectoren) raakt dit vermogen en WAAROM levert cross-sector consolidatie hefboom op?
       Optioneel: een korte aanscherpings-suggestie voor de formulering.

    2. BREEDTE VAN INSPANNINGEN
       Omdat de vermogens nu cross-sector zijn, moeten inspanningen mogelijk BREDER worden geformuleerd.
       Per inspanning:
       - "dekt_volledig" -> inspanning past al bij het gedeelde vermogen
       - "moet_verbreed" -> inspanning dekt maar een deel; geef concrete herformulering
       - "mist_aspect" -> er ontbreekt een relevant aspect; benoem welk aspect
       Geef altijd een toelichting; bij "moet_verbreed" en "mist_aspect" een suggestieVerbreding.

    3. BATEN-DEKKING (kritisch — stakeholder-mandaat)
       Loop elke baat onder het focusdoel langs.
       - wordtGeraakt: true als de huidige cross-sector vermogens en inspanningen deze baat daadwerkelijk realiseren
       - wordtGeraakt: false als er een MISMATCH is — deze baat dreigt buiten schot te raken
       Geef altijd een redenering. Bij false: een concrete risico-tekst ("wat missen we als het zo blijft").

    4. SAMENVATTING
       3-5 zinnen die vastleggen: welke hefbomen trekken we, waar zit het risico, waarom gaat dit het focusdoel realiseren.

    Antwoord ALLEEN als JSON-object met EXACT deze structuur:
    {
      "focusDoelId": "uuid-van-doel",
      "focusDoelNaam": "Naam van het focusdoel",
      "vermogenReview": [
        {
          "vermogenId": "uuid",
          "hefboomAnalyse": "Concrete analyse over welke baten dit vermogen raakt en waarom consolidatie hefboom geeft.",
          "suggestieAanscherping": "Optionele herformulering"
        }
      ],
      "inspanningReview": [
        {
          "inspanningId": "uuid",
          "breedteOordeel": "dekt_volledig",
          "toelichting": "Waarom dit oordeel",
          "suggestieVerbreding": "Concrete bredere herformulering (alleen bij moet_verbreed of mist_aspect)"
        }
      ],
      "batenDekking": [
        {
          "baatId": "uuid",
          "sector": "PO",
          "wordtGeraakt": true,
          "redenering": "Waarom wel/niet",
          "risico": "Alleen bij wordtGeraakt=false: wat verliezen we"
        }
      ],
      "samenvatting": "3-5 zinnen over de kern van de cross-sector hefboomwerking voor dit focusdoel."
    }

    breedteOordeel MOET exact een van: "dekt_volledig", "moet_verbreed", "mist_aspect" zijn.
    Gebruik de werkelijke id-velden uit de data. Verwijs nooit naar doelen, baten, vermogens of inspanningen die niet in de input staan.
    BELANGRIJK: Antwoord in het Nederlands.`;
    ```

    **Step C: verify**

    Draai direct na beide replacements:
    ```
    npx vitest run src/lib/__tests__/schemas-stap5.test.ts
    ```
    Alle 5 tests MOETEN groen worden. Zo niet: lees de error, fix het schema (niet de tests).

    **Bewaar** dat `types.ts` line 47 al `Stap5Result` re-exporteert via `z.infer<typeof Stap5ResultSchema>`. Geen wijziging aan `types.ts` nodig. TypeScript propageert de nieuwe shape automatisch — wat betekent dat downstream TypeScript-fouten zullen verschijnen in:
    - `src/components/cross-analyse/StapSectorVertaling.tsx` (oude shape usage — Wave 2 fix)
    - `src/components/cross-analyse/CrossAnalyseWizard.tsx` (imports + type references — Wave 3 fix)
    - `src/app/api/cross-analyse/route.ts` (schema wired, geen body compile issues verwacht)

    **`npm run build` zal FALEN na deze task** tot Wave 2 de downstream consumers fixt. Dat is expected en correct. De verify command is vitest (niet build) om deze reden.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/schemas-stap5.test.ts --reporter=default</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/schemas.ts` bevat exact de string `"dekt_volledig"` (via grep: `grep -c '"dekt_volledig"' src/lib/schemas.ts` ≥ 1)
    - `src/lib/schemas.ts` bevat `focusDoelId: z.string()` (via grep: `grep -c 'focusDoelId: z\.string' src/lib/schemas.ts` ≥ 1)
    - `src/lib/schemas.ts` bevat GEEN `sectorVertalingen` meer (via grep: `grep -c 'sectorVertalingen' src/lib/schemas.ts` = 0)
    - `src/lib/schemas.ts` bevat `batenDekking: z.array` (via grep)
    - `src/lib/schemas.ts` bevat `z.enum(["dekt_volledig", "moet_verbreed", "mist_aspect"])` exact
    - `src/lib/prompts.ts` bevat `CROSS_ANALYSE_STAP5_PROMPT` export
    - `src/lib/prompts.ts` bevat de tekst `"HEFBOOMWERKING PER VERMOGEN"` (uppercase)
    - `src/lib/prompts.ts` bevat de tekst `"Werken aan Programma's, Hfst 8"`
    - `src/lib/prompts.ts` bevat GEEN `"aantalBaten"` meer (oude prompt referentie, via grep)
    - `src/lib/prompts.ts` bevat de exacte enum instructie `breedteOordeel MOET exact een van: "dekt_volledig", "moet_verbreed", "mist_aspect" zijn.`
    - `npx vitest run src/lib/__tests__/schemas-stap5.test.ts` → alle 5 tests PASS (groen)
  </acceptance_criteria>
  <done>
    Schemas + prompts vervangen. Schemas test-suite groen. `types.ts` ongewijzigd. `npm run build` mag falen (expected — downstream consumers komen in Wave 2/3).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure module src/lib/stap5-focus.ts creëren met getFocusGoal, computeFocusView, restoreStap5Result</name>
  <files>src/lib/stap5-focus.ts</files>
  <read_first>
    - src/lib/__tests__/stap5-focus-filter.test.ts (Wave 0 — weet exact welke test assertions groen moeten worden, inclusief mockSession structuur)
    - src/lib/__tests__/stap5-restore-guard.test.ts (Wave 0 — weet exact welke restoreStap5Result behaviors getest worden)
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-RESEARCH.md §Code Examples → Example 4 (computeFocusView volledig, 30+ regels) en §Pattern 7 (restoreStap5Result logica met console.warn)
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-CONTEXT.md §Decisions D-01 (exact expressie), D-09 (filter semantiek)
    - src/lib/din-service.ts (bestaande filter patterns — `findSharedCapabilities` gebruikt zelfde `relatedSectors.length > 1` idioom; NIET importeren, alleen referentie)
    - src/lib/types.ts (DINSession, ProgrammeGoal, DINBenefit, DINCapability, DINEffort types)
    - src/lib/schemas.ts (Stap5ResultSchema — voor restoreStap5Result import)
    - src/components/cross-analyse/StapSectorVertaling.tsx lines 37-42 (bestaande `.relatedSectors && .relatedSectors.length > 1` idioom dat je herhaalt)
  </read_first>
  <behavior>
    - getFocusGoal: sorteert op rank asc, returnt [0]. Rank undefined fallback op 999. Lege array → undefined. Muteert input niet (spread kopie).
    - computeFocusView: returnt null als geen doelen. Anders: filtert benefits via goalBenefitMaps waar goalId===focusGoal.id. Filtert capabilities op `!consolidated && relatedSectors.length > 1` EN gekoppeld via benefitCapabilityMaps aan focus benefits. Filtert efforts op `!consolidated && responsibleSector.includes(",")` EN gekoppeld via capabilityEffortMaps aan focus caps. outOfScopeCaps = active caps (!consolidated) NIET in focusCaps. outOfScopeEfforts = active efforts (!consolidated) NIET in focusEfforts.
    - restoreStap5Result: `undefined`/`null` input → undefined. Anders: `Stap5ResultSchema.safeParse(raw)` → op success data, op failure `console.warn` + undefined.
  </behavior>
  <action>
    Maak nieuw bestand `src/lib/stap5-focus.ts` met exact deze structuur:

    ```typescript
    import { Stap5ResultSchema } from "./schemas";
    import type {
      DINSession,
      Stap5Result,
      ProgrammeGoal,
      DINBenefit,
      DINCapability,
      DINEffort,
    } from "./types";

    // ============================================================
    // getFocusGoal — D-01 locked expression
    // EXACT dezelfde logica in client (StapSectorVertaling.tsx) en server (route.ts)
    // ============================================================

    export function getFocusGoal<T extends { rank?: number }>(goals: T[]): T | undefined {
      if (goals.length === 0) return undefined;
      return [...goals].sort((a, b) => (a.rank ?? 999) - (b.rank ?? 999))[0];
    }

    // ============================================================
    // computeFocusView — R-CROSS-01, R-CROSS-02
    // Filter semantiek conform D-09: focusdoel → baten → cross-sector vermogens → gedeelde inspanningen
    // ============================================================

    export interface FocusView {
      focusGoal: ProgrammeGoal;
      focusBenefits: DINBenefit[];
      focusCaps: DINCapability[];
      focusEfforts: DINEffort[];
      outOfScopeCaps: DINCapability[];
      outOfScopeEfforts: DINEffort[];
    }

    export function computeFocusView(session: DINSession): FocusView | null {
      const focusGoal = getFocusGoal(session.goals);
      if (!focusGoal) return null;

      // Step 1: baten onder focusdoel via goalBenefitMaps
      const focusBenefitIds = new Set(
        (session.goalBenefitMaps ?? [])
          .filter((m) => m.goalId === focusGoal.id)
          .map((m) => m.benefitId)
      );
      const focusBenefits = session.benefits.filter((b) => focusBenefitIds.has(b.id));

      // Step 2: active capabilities (niet geconsolideerd)
      const activeCaps = session.capabilities.filter((c) => !c.consolidated);

      // Step 3: cross-sector capabilities (relatedSectors.length > 1)
      const sharedCaps = activeCaps.filter(
        (c) => (c.relatedSectors?.length ?? 0) > 1
      );

      // Step 4: focusCaps = shared caps gekoppeld aan focus benefits via benefitCapabilityMaps
      const sharedCapIdSet = new Set(sharedCaps.map((c) => c.id));
      const focusCapIds = new Set(
        (session.benefitCapabilityMaps ?? [])
          .filter(
            (m) => focusBenefitIds.has(m.benefitId) && sharedCapIdSet.has(m.capabilityId)
          )
          .map((m) => m.capabilityId)
      );
      const focusCaps = sharedCaps.filter((c) => focusCapIds.has(c.id));

      // Step 5: active efforts (niet geconsolideerd)
      const activeEfforts = session.efforts.filter((e) => !e.consolidated);

      // Step 6: shared efforts (multi-sector responsibleSector via comma)
      const sharedEfforts = activeEfforts.filter(
        (e) => e.responsibleSector?.includes(",") ?? false
      );

      // Step 7: focusEfforts = shared efforts gekoppeld aan focus caps via capabilityEffortMaps
      const sharedEffortIdSet = new Set(sharedEfforts.map((e) => e.id));
      const focusEffortIds = new Set(
        (session.capabilityEffortMaps ?? [])
          .filter(
            (m) => focusCapIds.has(m.capabilityId) && sharedEffortIdSet.has(m.effortId)
          )
          .map((m) => m.effortId)
      );
      const focusEfforts = sharedEfforts.filter((e) => focusEffortIds.has(e.id));

      // Step 8: out-of-scope = active items niet in focus
      const outOfScopeCaps = activeCaps.filter((c) => !focusCapIds.has(c.id));
      const outOfScopeEfforts = activeEfforts.filter((e) => !focusEffortIds.has(e.id));

      return {
        focusGoal,
        focusBenefits,
        focusCaps,
        focusEfforts,
        outOfScopeCaps,
        outOfScopeEfforts,
      };
    }

    // ============================================================
    // restoreStap5Result — D-10 migratie (oude shape → undefined)
    // Pure function, gebruikt door CrossAnalyseWizard.tsx useEffect bij session load
    // ============================================================

    export function restoreStap5Result(rawStap5: unknown): Stap5Result | undefined {
      if (rawStap5 === undefined || rawStap5 === null) return undefined;
      const parsed = Stap5ResultSchema.safeParse(rawStap5);
      if (!parsed.success) {
        console.warn(
          "[stap5-focus] stap5 result heeft oude shape, wordt gereset",
          parsed.error.issues
        );
        return undefined;
      }
      return parsed.data;
    }
    ```

    **Draai direct:**
    ```
    npx vitest run src/lib/__tests__/stap5-focus-filter.test.ts src/lib/__tests__/stap5-restore-guard.test.ts
    ```

    Alle 11 focus-filter tests + 7 restore-guard tests MOETEN groen worden. Als iets faalt:
    - Check dat `getFocusGoal` geen mutatie doet op input array
    - Check dat `computeFocusView` correcte `undefined` → null returnt voor lege goals
    - Check dat outOfScopeCaps GEEN consolidated items bevat (Test 4 in focus-filter verwacht leeg)
    - Check dat `restoreStap5Result` `console.warn` mocked correct wordt aangeroepen (Test 7 in restore-guard)

    **Zoek naar de specifieke edge case in Test 4 van focus-filter:** Wanneer een capability `consolidated: true` heeft, valt hij BUITEN zowel focusCaps ALS outOfScopeCaps (omdat `activeCaps = filter(!consolidated)`). Deze semantiek is correct en is een bewuste keuze — D-03 "buiten scope" geldt alleen voor niet-geconsolideerde items.

    **TypeScript notes:**
    - `ProgrammeGoal`, `DINBenefit`, `DINCapability`, `DINEffort`, `DINSession`, `Stap5Result` moeten allemaal beschikbaar zijn via `./types` (types.ts re-exporteert deze uit schemas.ts)
    - Gebruik `import type` voor types, `import` (zonder type) voor `Stap5ResultSchema` omdat die een runtime value is
    - Gebruik NIET `any`; gebruik `unknown` voor `restoreStap5Result` parameter

    **Na task:** `npm run build` zal nog steeds falen (Wave 2 dependencies) MAAR `npm run lint` op alleen `src/lib/stap5-focus.ts` moet schoon zijn.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/stap5-focus-filter.test.ts src/lib/__tests__/stap5-restore-guard.test.ts src/lib/__tests__/schemas-stap5.test.ts --reporter=default</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/stap5-focus.ts` bestaat
    - Bevat exact drie exports: `getFocusGoal`, `computeFocusView`, `restoreStap5Result` (via grep: `grep -c '^export ' src/lib/stap5-focus.ts` ≥ 3)
    - Bevat interface export `FocusView` (via grep: `grep -c 'export interface FocusView' src/lib/stap5-focus.ts` = 1)
    - Bevat exact de D-01 expressie: `[...goals].sort((a, b) => (a.rank ?? 999) - (b.rank ?? 999))[0]`
    - Bevat `Stap5ResultSchema.safeParse` (in restoreStap5Result)
    - Bevat `console.warn` call in restoreStap5Result
    - `.relatedSectors?.length ?? 0) > 1` pattern aanwezig (cross-sector filter)
    - `responsibleSector?.includes(",")` pattern aanwezig (shared effort filter)
    - `!consolidated` filter aanwezig (twee keer: voor caps en efforts)
    - Gebruikt `import type` voor types, `import` voor `Stap5ResultSchema`
    - GEEN `any` types in het bestand (via grep: `grep -c ': any' src/lib/stap5-focus.ts` = 0 of alleen in comments)
    - Vitest: alle Wave 0 tests GROEN (5 schemas + 11 focus-filter + 7 restore-guard = 23 PASS)
  </acceptance_criteria>
  <done>
    `src/lib/stap5-focus.ts` is gecreëerd en alle 23 Wave 0 tests zijn GROEN. Pure function module is klaar voor import door Wave 2 (route.ts, StapSectorVertaling.tsx) en Wave 3 (CrossAnalyseWizard.tsx).
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/__tests__/stap5-*.test.ts` → alle 23 tests GROEN
2. `npx vitest run` (volledige suite) → blijft groen; geen regressie op bestaande tests zoals `cross-analyse-schema.test.ts` of `schemas.test.ts` (deze testen andere schemas of backward compat van AICrossAnalyseSchema, niet Stap5ResultSchema)
3. `grep -c 'sectorVertalingen' src/lib/schemas.ts` = 0
4. `grep -c 'hefboomwerking\|HEFBOOMWERKING' src/lib/prompts.ts` ≥ 1
5. `src/lib/stap5-focus.ts` bestaat en exporteert drie functies + FocusView interface
6. `src/lib/types.ts` ongewijzigd (`z.infer` propagation vangt de nieuwe shape automatisch)
7. **`npm run build` faalt** — dit is verwacht en correct; Wave 2 fixt de downstream consumers (StapSectorVertaling.tsx, CrossAnalyseWizard.tsx)
</verification>

<success_criteria>
- Stap5ResultSchema vervangen door nieuwe shape (5 velden + samenvatting)
- CROSS_ANALYSE_STAP5_PROMPT herschreven rond 4 beoordelingspunten + Nederlandse enum waardes expliciet
- Pure module `src/lib/stap5-focus.ts` biedt getFocusGoal, computeFocusView, restoreStap5Result
- Alle 23 Wave 0 tests GROEN
- Geen nieuwe lint errors in de drie gewijzigde/nieuwe bestanden
- Build mag falen (Wave 2 repareert downstream)
</success_criteria>

<output>
After completion, create `.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-02-SUMMARY.md` with:
- Exacte line ranges van de vervangingen in schemas.ts en prompts.ts
- Aantal regels in src/lib/stap5-focus.ts
- Wave 0 test-resultaten: 23 GREEN confirmed
- Welke bestanden door `npm run build` stuk gaan zijn (voor Wave 2 scope)
- Bevestiging dat `types.ts` line 47 ongewijzigd is en `z.infer` propagatie werkt
</output>
