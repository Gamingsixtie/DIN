---
phase: 13-stap-5-cross-analyse-prioriteitsview-eerste-doel
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/__tests__/schemas-stap5.test.ts
  - src/lib/__tests__/stap5-focus-filter.test.ts
  - src/lib/__tests__/stap5-restore-guard.test.ts
autonomous: true
requirements: [R-CROSS-01, R-CROSS-02]
must_haves:
  truths:
    - "Executor heeft concrete test-assertions die het nieuwe Stap5ResultSchema shape vastleggen voordat code wordt geschreven"
    - "getFocusGoal en computeFocusView zijn testbaar als pure functions vóór Wave 1 implementatie"
    - "De D-10 restore-guard logica heeft een regressie-test die oude sectorVertalingen[] shape afwijst"
  artifacts:
    - path: "src/lib/__tests__/schemas-stap5.test.ts"
      provides: "Regression guard voor nieuwe Stap5ResultSchema shape — accepteert nieuwe velden, wijst oude sectorVertalingen[] shape af"
      contains: "Stap5ResultSchema.safeParse"
    - path: "src/lib/__tests__/stap5-focus-filter.test.ts"
      provides: "Unit tests voor getFocusGoal (rank sort + fallback) en computeFocusView (cross-sector filtering voor focusdoel)"
      contains: "getFocusGoal"
    - path: "src/lib/__tests__/stap5-restore-guard.test.ts"
      provides: "Unit test voor pure restoreStap5Result helper die oude shape naar undefined reset"
      contains: "restoreStap5Result"
  key_links:
    - from: "src/lib/__tests__/schemas-stap5.test.ts"
      to: "src/lib/schemas.ts Stap5ResultSchema"
      via: "Zod safeParse assertions"
      pattern: "Stap5ResultSchema\\.safeParse"
    - from: "src/lib/__tests__/stap5-focus-filter.test.ts"
      to: "src/lib/stap5-focus.ts (Wave 1)"
      via: "import getFocusGoal, computeFocusView"
      pattern: "from \"@/lib/stap5-focus\""
---

<objective>
Wave 0 test-scaffolding voor Phase 13. Creëer drie test-bestanden die de contracten van Wave 1 (nieuwe `Stap5ResultSchema`, pure helpers `getFocusGoal`/`computeFocusView`, en `restoreStap5Result`) vastleggen. De tests FALEN aanvankelijk — Wave 1 code maakt ze groen.

**Purpose:** TDD-achtige regressie-guards voor een breaking schema change. Voorkomt dat Wave 1 stilzwijgend de oude shape breekt of dat Wave 2 runtime-errors introduceert via ongevalideerde oude data.

**Output:** Drie `.test.ts` bestanden in `src/lib/__tests__/` die via `npx vitest run src/lib/__tests__/stap5-*.test.ts` draaien en expliciete assertions geven voor het nieuwe datamodel + D-10 restore behavior.

**Scope disclaimer:** Deze Wave 0 bevat GEEN productiecode. Alleen test-bestanden. Het is verwacht en correct dat alle drie de tests RED zijn na Wave 0 — Wave 1 maakt ze GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-CONTEXT.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-RESEARCH.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-VALIDATION.md
@src/lib/__tests__/cross-analyse-schema.test.ts
@src/lib/__tests__/schemas.test.ts
@vitest.config.ts

<interfaces>
<!-- Contracts Wave 1 will implement — extracted from 13-RESEARCH.md Code Examples -->

### NIEUW Stap5ResultSchema shape (RESEARCH Example 1, landt in src/lib/schemas.ts lines 430-444)
```typescript
export const Stap5ResultSchema = z.object({
  focusDoelId: z.string(),
  focusDoelNaam: z.string(),
  vermogenReview: z.array(z.object({
    vermogenId: z.string(),
    hefboomAnalyse: z.string(),
    suggestieAanscherping: z.string().optional(),
  })).default([]),
  inspanningReview: z.array(z.object({
    inspanningId: z.string(),
    breedteOordeel: z.enum(["dekt_volledig", "moet_verbreed", "mist_aspect"]),
    toelichting: z.string(),
    suggestieVerbreding: z.string().optional(),
  })).default([]),
  batenDekking: z.array(z.object({
    baatId: z.string(),
    sector: z.string(),
    wordtGeraakt: z.boolean(),
    redenering: z.string(),
    risico: z.string().optional(),
  })).default([]),
  samenvatting: z.string(),
});
```

### NIEUW src/lib/stap5-focus.ts exports (Wave 1 — RESEARCH Example 4)
```typescript
export function getFocusGoal<T extends { rank?: number }>(goals: T[]): T | undefined;
// Implementeert [...goals].sort((a,b) => (a.rank ?? 999) - (b.rank ?? 999))[0], fallback goals[0]

export function computeFocusView(session: DINSession): {
  focusGoal: ProgrammeGoal;
  focusBenefits: DINBenefit[];
  focusCaps: DINCapability[];         // sharedCaps (relatedSectors.length > 1) gekoppeld aan focus baten
  focusEfforts: DINEffort[];          // sharedEfforts (responsibleSector.includes(",")) gekoppeld aan focus caps
  outOfScopeCaps: DINCapability[];    // activeCaps (!consolidated) niet in focusCaps
  outOfScopeEfforts: DINEffort[];     // activeEfforts (!consolidated) niet in focusEfforts
} | null;

export function restoreStap5Result(rawStap5: unknown): Stap5Result | undefined;
// Pakt Zod safeParse: bij success return data, bij fail return undefined (D-10 migratie)
```

### OUDE shape die MOET falen (schemas.ts lines 430-444, wordt vervangen)
```typescript
// OUD — na Wave 1 niet meer geldig
{
  sectorVertalingen: [{ sector, dinKeten: {...}, domeinBalans: [...], gaps: [...], samenvatting }],
  totaalSamenvatting: string
}
```
</interfaces>

<codebase_context>
### Hoe vitest wordt aangeroepen in deze codebase
- `vitest.config.ts` include glob: `src/**/*.test.ts` (alias `@` → `./src`)
- Bestaande test files importeren met `@/lib/...` absolute paths (zie `cross-analyse-schema.test.ts` line 3)
- Tests gebruiken `describe`, `test`, `expect` uit `vitest`
- Runner commando: `npx vitest run src/lib/__tests__/<file>.test.ts` (NIET `vitest` zonder `run` — watch mode verboden)

### Waarom drie test files (niet één)
- `schemas-stap5.test.ts` valideert de Zod schema change zelf (D-05, D-07)
- `stap5-focus-filter.test.ts` valideert de pure filter logica (R-CROSS-01, R-CROSS-02, D-01)
- `stap5-restore-guard.test.ts` valideert de D-10 migratie pure function
- Bestaand patroon: `schemas.test.ts`, `cross-analyse-schema.test.ts` zijn al per-schema georganiseerd — deze splitsing past in dat patroon
</codebase_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nieuwe Stap5ResultSchema contract test schrijven</name>
  <files>src/lib/__tests__/schemas-stap5.test.ts</files>
  <read_first>
    - src/lib/__tests__/cross-analyse-schema.test.ts (bestaand pattern voor schema backward compat tests)
    - src/lib/__tests__/schemas.test.ts (bestaand pattern voor Stap1-4 schema assertions)
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-RESEARCH.md §Code Examples → Example 1 (nieuwe Stap5ResultSchema definitie verbatim)
    - src/lib/schemas.ts lines 430-444 (HUIDIGE oude shape — dit test-bestand moet deze shape EXPLICIET afwijzen)
    - vitest.config.ts (alias `@` → `./src`, include `src/**/*.test.ts`)
  </read_first>
  <action>
    Maak nieuw bestand `src/lib/__tests__/schemas-stap5.test.ts` met exact deze structuur:

    ```typescript
    import { describe, test, expect } from "vitest";
    import { Stap5ResultSchema } from "@/lib/schemas";

    describe("Stap5ResultSchema (nieuw shape — Phase 13)", () => {
      test("Test 1: accepteert valid nieuwe shape met alle velden", () => {
        const valid = {
          focusDoelId: "goal-1",
          focusDoelNaam: "Outside-in competentie verankeren",
          vermogenReview: [
            {
              vermogenId: "cap-1",
              hefboomAnalyse: "Dit vermogen raakt baat X in PO en VO.",
              suggestieAanscherping: "Maak formulering concreter",
            },
          ],
          inspanningReview: [
            {
              inspanningId: "eff-1",
              breedteOordeel: "dekt_volledig",
              toelichting: "Past bij het gedeelde vermogen",
            },
            {
              inspanningId: "eff-2",
              breedteOordeel: "moet_verbreed",
              toelichting: "Dekt maar een deel",
              suggestieVerbreding: "Breid uit naar Zakelijk",
            },
            {
              inspanningId: "eff-3",
              breedteOordeel: "mist_aspect",
              toelichting: "Mist data-infrastructuur",
              suggestieVerbreding: "Voeg data-laag toe",
            },
          ],
          batenDekking: [
            { baatId: "ben-1", sector: "PO", wordtGeraakt: true, redenering: "Cross-sector vermogen raakt direct" },
            { baatId: "ben-2", sector: "VO", wordtGeraakt: false, redenering: "Mismatch", risico: "Baat niet gerealiseerd zonder extra inspanning" },
          ],
          samenvatting: "Kern van de cross-sector hefboomwerking voor dit focusdoel in 3-5 zinnen.",
        };
        const parsed = Stap5ResultSchema.safeParse(valid);
        expect(parsed.success).toBe(true);
      });

      test("Test 2: accepteert minimale shape met lege arrays (default [])", () => {
        const minimal = {
          focusDoelId: "goal-1",
          focusDoelNaam: "Doel",
          samenvatting: "Korte samenvatting",
        };
        const parsed = Stap5ResultSchema.safeParse(minimal);
        expect(parsed.success).toBe(true);
        if (parsed.success) {
          expect(parsed.data.vermogenReview).toEqual([]);
          expect(parsed.data.inspanningReview).toEqual([]);
          expect(parsed.data.batenDekking).toEqual([]);
        }
      });

      test("Test 3: wijst OUDE sectorVertalingen[] shape af (D-07 breaking change)", () => {
        const oldShape = {
          sectorVertalingen: [
            {
              sector: "PO",
              dinKeten: { aantalBaten: 6, aantalVermogens: 4, aantalInspanningen: 8, waarvanGedeeld: 2 },
              domeinBalans: [],
              gaps: [],
              samenvatting: "oud",
            },
          ],
          totaalSamenvatting: "oud totaal",
        };
        const parsed = Stap5ResultSchema.safeParse(oldShape);
        expect(parsed.success).toBe(false);
      });

      test("Test 4: wijst breedteOordeel buiten enum af", () => {
        const invalidEnum = {
          focusDoelId: "goal-1",
          focusDoelNaam: "Doel",
          inspanningReview: [
            {
              inspanningId: "eff-1",
              breedteOordeel: "needs_broadening", // Engelse hallucinatie — moet falen
              toelichting: "Fout",
            },
          ],
          samenvatting: "x",
        };
        const parsed = Stap5ResultSchema.safeParse(invalidEnum);
        expect(parsed.success).toBe(false);
      });

      test("Test 5: vereist focusDoelId en focusDoelNaam (non-optional)", () => {
        const missingFocus = {
          samenvatting: "x",
        };
        const parsed = Stap5ResultSchema.safeParse(missingFocus);
        expect(parsed.success).toBe(false);
      });
    });
    ```

    **Verwacht:** Na commit draait de test en faalt elke assertion (schema is nog de oude shape). Dit is correct — Wave 1 maakt ze groen.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/schemas-stap5.test.ts --reporter=default</automated>
  </verify>
  <acceptance_criteria>
    - Bestand bestaat: `src/lib/__tests__/schemas-stap5.test.ts`
    - Bestand bevat exact deze 5 test-namen: "Test 1: accepteert valid nieuwe shape met alle velden", "Test 2: accepteert minimale shape met lege arrays (default [])", "Test 3: wijst OUDE sectorVertalingen[] shape af (D-07 breaking change)", "Test 4: wijst breedteOordeel buiten enum af", "Test 5: vereist focusDoelId en focusDoelNaam (non-optional)"
    - Bestand importeert `Stap5ResultSchema` van `@/lib/schemas`
    - `grep -c "Stap5ResultSchema.safeParse" src/lib/__tests__/schemas-stap5.test.ts` ≥ 5
    - `grep -c 'breedteOordeel' src/lib/__tests__/schemas-stap5.test.ts` ≥ 4 (gebruikt in test 1, 4)
    - Bevat alle drie enum waardes exact: `"dekt_volledig"`, `"moet_verbreed"`, `"mist_aspect"`
    - Vitest runt het bestand (geen syntax errors, geen import errors). Test-resultaten zijn RED (expected — Wave 1 fixt dit); aanvaardbaar mits het bestand UITVOERT. Als vitest crashed vóór test execution: blocker.
  </acceptance_criteria>
  <done>
    `schemas-stap5.test.ts` gecommit, vitest leest het bestand in, tests zijn RED omdat `Stap5ResultSchema` nog oude shape heeft. Geen type errors bij `npm run lint`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Focus-filter pure function contract test schrijven</name>
  <files>src/lib/__tests__/stap5-focus-filter.test.ts</files>
  <read_first>
    - src/lib/__tests__/consolidation.test.ts (bestaand pattern voor pure function tests met mock DINSession)
    - src/lib/__tests__/goal-completion.test.ts (bestaand pattern voor filter logic tests)
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-RESEARCH.md §Code Examples → Example 4 (getFocusGoal + computeFocusView verbatim signatures)
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-CONTEXT.md §Decisions D-01 (focusdoel expressie), D-09 (filtering semantiek)
    - src/lib/types.ts lines 40-80 (DINSession, SECTORS, SECTOR_COLORS structures)
    - src/lib/schemas.ts (DINCapability, DINEffort, DINBenefit, ProgrammeGoal shapes met relatedSectors, responsibleSector, consolidated, rank velden)
  </read_first>
  <action>
    Maak nieuw bestand `src/lib/__tests__/stap5-focus-filter.test.ts`. Het test-bestand importeert van een Wave 1 module die nog NIET bestaat: `@/lib/stap5-focus`. Dit is intentioneel — de import zorgt dat Wave 1 precies dit bestand moet creëren met exact deze exports.

    ```typescript
    import { describe, test, expect } from "vitest";
    import { getFocusGoal, computeFocusView } from "@/lib/stap5-focus";
    import type { DINSession } from "@/lib/types";

    // ============================================================
    // getFocusGoal — D-01 expression contract
    // ============================================================

    describe("getFocusGoal (D-01)", () => {
      test("Test 1: sorteert op rank oplopend en retourneert laagste", () => {
        const goals = [
          { id: "g1", name: "Doel 1", description: "", rank: 3 },
          { id: "g2", name: "Doel 2", description: "", rank: 1 },
          { id: "g3", name: "Doel 3", description: "", rank: 2 },
        ];
        const result = getFocusGoal(goals);
        expect(result?.id).toBe("g2");
      });

      test("Test 2: rank undefined valt terug op 999 in sort", () => {
        const goals = [
          { id: "g1", name: "Doel 1", description: "" }, // geen rank
          { id: "g2", name: "Doel 2", description: "", rank: 5 },
        ];
        const result = getFocusGoal(goals);
        expect(result?.id).toBe("g2"); // g1 valt op 999, g2 op 5 → g2 wint
      });

      test("Test 3: lege array retourneert undefined", () => {
        const result = getFocusGoal([]);
        expect(result).toBeUndefined();
      });

      test("Test 4: single goal retourneert dat goal (fallback path)", () => {
        const goals = [{ id: "g1", name: "Only", description: "" }];
        const result = getFocusGoal(goals);
        expect(result?.id).toBe("g1");
      });

      test("Test 5: muteert source array NIET (spread kopie)", () => {
        const goals = [
          { id: "g1", name: "A", description: "", rank: 3 },
          { id: "g2", name: "B", description: "", rank: 1 },
        ];
        getFocusGoal(goals);
        expect(goals[0].id).toBe("g1"); // ongewijzigd
      });
    });

    // ============================================================
    // computeFocusView — R-CROSS-01, R-CROSS-02
    // ============================================================

    function mockSession(overrides: Partial<DINSession> = {}): DINSession {
      const base: DINSession = {
        id: "s1",
        name: "Test",
        createdAt: "2026-04-05T00:00:00Z",
        updatedAt: "2026-04-05T00:00:00Z",
        currentStep: 1,
        goals: [],
        sectorPlans: [],
        pmcEntries: [],
        benefits: [],
        capabilities: [],
        efforts: [],
        goalBenefitMaps: [],
        benefitCapabilityMaps: [],
        capabilityEffortMaps: [],
      };
      return { ...base, ...overrides };
    }

    describe("computeFocusView (R-CROSS-01, R-CROSS-02)", () => {
      test("Test 1: filtert baten op focusdoel via goalBenefitMaps", () => {
        const session = mockSession({
          goals: [
            { id: "g1", name: "Focus", description: "", rank: 1 },
            { id: "g2", name: "Ander", description: "", rank: 2 },
          ],
          benefits: [
            { id: "b1", goalId: "g1", sectorId: "PO", description: "Baat onder focus", title: "B1", createdAt: "", updatedAt: "" },
            { id: "b2", goalId: "g2", sectorId: "VO", description: "Baat onder ander", title: "B2", createdAt: "", updatedAt: "" },
          ],
          goalBenefitMaps: [
            { goalId: "g1", benefitId: "b1" },
            { goalId: "g2", benefitId: "b2" },
          ],
        });
        const view = computeFocusView(session);
        expect(view).not.toBeNull();
        expect(view!.focusGoal.id).toBe("g1");
        expect(view!.focusBenefits.map((b) => b.id)).toEqual(["b1"]);
      });

      test("Test 2: filtert vermogens op relatedSectors.length > 1 EN koppeling aan focus-baten", () => {
        const session = mockSession({
          goals: [{ id: "g1", name: "Focus", description: "", rank: 1 }],
          benefits: [
            { id: "b1", goalId: "g1", sectorId: "PO", description: "B1", title: "", createdAt: "", updatedAt: "" },
          ],
          capabilities: [
            { id: "c1", sectorId: "PO", description: "Cross", title: "", relatedSectors: ["PO", "VO"], consolidated: false, createdAt: "", updatedAt: "" },
            { id: "c2", sectorId: "PO", description: "Solo", title: "", relatedSectors: ["PO"], consolidated: false, createdAt: "", updatedAt: "" },
            { id: "c3", sectorId: "VO", description: "Cross niet gekoppeld", title: "", relatedSectors: ["VO", "Zakelijk"], consolidated: false, createdAt: "", updatedAt: "" },
          ],
          goalBenefitMaps: [{ goalId: "g1", benefitId: "b1" }],
          benefitCapabilityMaps: [
            { benefitId: "b1", capabilityId: "c1" },
            { benefitId: "b1", capabilityId: "c2" },
          ],
        });
        const view = computeFocusView(session);
        expect(view!.focusCaps.map((c) => c.id)).toEqual(["c1"]); // alleen cross-sector EN gekoppeld
        expect(view!.outOfScopeCaps.map((c) => c.id).sort()).toEqual(["c2", "c3"].sort());
      });

      test("Test 3: filtert inspanningen op responsibleSector multi-sector EN koppeling aan focus-vermogens", () => {
        const session = mockSession({
          goals: [{ id: "g1", name: "Focus", description: "", rank: 1 }],
          benefits: [
            { id: "b1", goalId: "g1", sectorId: "PO", description: "B1", title: "", createdAt: "", updatedAt: "" },
          ],
          capabilities: [
            { id: "c1", sectorId: "PO", description: "Cross", title: "", relatedSectors: ["PO", "VO"], consolidated: false, createdAt: "", updatedAt: "" },
          ],
          efforts: [
            { id: "e1", sectorId: "PO", description: "Shared", title: "", domain: "mens", responsibleSector: "PO, VO", consolidated: false, createdAt: "", updatedAt: "" },
            { id: "e2", sectorId: "PO", description: "Solo", title: "", domain: "mens", responsibleSector: "PO", consolidated: false, createdAt: "", updatedAt: "" },
          ],
          goalBenefitMaps: [{ goalId: "g1", benefitId: "b1" }],
          benefitCapabilityMaps: [{ benefitId: "b1", capabilityId: "c1" }],
          capabilityEffortMaps: [
            { capabilityId: "c1", effortId: "e1" },
            { capabilityId: "c1", effortId: "e2" },
          ],
        });
        const view = computeFocusView(session);
        expect(view!.focusEfforts.map((e) => e.id)).toEqual(["e1"]); // alleen multi-sector EN gekoppeld
        expect(view!.outOfScopeEfforts.map((e) => e.id)).toEqual(["e2"]);
      });

      test("Test 4: negeert consolidated=true items (zowel caps als efforts)", () => {
        const session = mockSession({
          goals: [{ id: "g1", name: "Focus", description: "", rank: 1 }],
          benefits: [
            { id: "b1", goalId: "g1", sectorId: "PO", description: "B1", title: "", createdAt: "", updatedAt: "" },
          ],
          capabilities: [
            { id: "c1", sectorId: "PO", description: "Cross consolidated", title: "", relatedSectors: ["PO", "VO"], consolidated: true, createdAt: "", updatedAt: "" },
          ],
          goalBenefitMaps: [{ goalId: "g1", benefitId: "b1" }],
          benefitCapabilityMaps: [{ benefitId: "b1", capabilityId: "c1" }],
        });
        const view = computeFocusView(session);
        expect(view!.focusCaps).toEqual([]);
        expect(view!.outOfScopeCaps).toEqual([]); // consolidated items vallen OOK buiten active set
      });

      test("Test 5: geen doelen → null", () => {
        const session = mockSession({ goals: [] });
        const view = computeFocusView(session);
        expect(view).toBeNull();
      });

      test("Test 6: focusdoel maar geen cross-sector vermogens → lege focusCaps, non-null view", () => {
        const session = mockSession({
          goals: [{ id: "g1", name: "Focus", description: "", rank: 1 }],
          benefits: [
            { id: "b1", goalId: "g1", sectorId: "PO", description: "B1", title: "", createdAt: "", updatedAt: "" },
          ],
          goalBenefitMaps: [{ goalId: "g1", benefitId: "b1" }],
        });
        const view = computeFocusView(session);
        expect(view).not.toBeNull();
        expect(view!.focusCaps).toEqual([]);
        expect(view!.focusEfforts).toEqual([]);
        expect(view!.focusBenefits.length).toBe(1);
      });
    });
    ```

    **Verwacht gedrag:** Vitest faalt met `Failed to resolve import "@/lib/stap5-focus"` tot Wave 1 het bestand creëert. Dat is een correcte RED-state.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/stap5-focus-filter.test.ts --reporter=default</automated>
  </verify>
  <acceptance_criteria>
    - Bestand bestaat: `src/lib/__tests__/stap5-focus-filter.test.ts`
    - Importeert `getFocusGoal, computeFocusView` van `@/lib/stap5-focus`
    - Importeert `DINSession` type van `@/lib/types`
    - Bevat exact 11 tests: 5 in `describe("getFocusGoal ...")` en 6 in `describe("computeFocusView ...")`
    - `grep -c 'test(' src/lib/__tests__/stap5-focus-filter.test.ts` ≥ 11
    - Test-namen bevatten: "sorteert op rank", "rank undefined valt terug op 999", "lege array", "muteert source array NIET", "filtert baten op focusdoel", "relatedSectors.length > 1", "responsibleSector multi-sector", "consolidated=true", "lege focusCaps"
    - `grep -n 'relatedSectors: \["PO", "VO"\]' src/lib/__tests__/stap5-focus-filter.test.ts` retourneert ≥ 1 match
    - `grep -n 'responsibleSector: "PO, VO"' src/lib/__tests__/stap5-focus-filter.test.ts` retourneert ≥ 1 match
    - `mockSession` helper aanwezig voor DINSession construction
  </acceptance_criteria>
  <done>
    `stap5-focus-filter.test.ts` gecommit. Vitest faalt bij `npx vitest run src/lib/__tests__/stap5-focus-filter.test.ts` met een import-resolution error op `@/lib/stap5-focus` (verwacht — Wave 1 creëert het bestand). Geen TypeScript errors in het test-bestand zelf (types zijn correct).
  </done>
</task>

<task type="auto">
  <name>Task 3: Restore guard pure function test schrijven + Wave 0 final verify</name>
  <files>src/lib/__tests__/stap5-restore-guard.test.ts</files>
  <read_first>
    - src/lib/__tests__/sector-migration.test.ts (bestaand pattern voor migratie/restore tests)
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-RESEARCH.md §Pattern 7 (Pitfall 1 + Pattern 7 restore guard code)
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-CONTEXT.md §Decisions D-10 (geen destructieve migratie)
    - src/lib/schemas.ts lines 430-444 (HUIDIGE oude shape die moet worden gerejectiond)
  </read_first>
  <action>
    Maak nieuw bestand `src/lib/__tests__/stap5-restore-guard.test.ts` dat test dat de pure function `restoreStap5Result` correct oude shapes naar `undefined` reset en valide nieuwe shapes ongewijzigd doorlaat.

    ```typescript
    import { describe, test, expect, vi, beforeEach, afterEach } from "vitest";
    import { restoreStap5Result } from "@/lib/stap5-focus";

    describe("restoreStap5Result (D-10 migratie)", () => {
      // Unterdrückt de console.warn die restoreStap5Result emits bij fail (zie RESEARCH Pattern 7)
      let warnSpy: ReturnType<typeof vi.spyOn>;
      beforeEach(() => {
        warnSpy = vi.spyOn(console, "warn").mockImplementation(() => {});
      });
      afterEach(() => {
        warnSpy.mockRestore();
      });

      test("Test 1: valide nieuwe shape wordt doorgegeven als getypeerd object", () => {
        const valid = {
          focusDoelId: "g1",
          focusDoelNaam: "Focus",
          vermogenReview: [],
          inspanningReview: [],
          batenDekking: [],
          samenvatting: "OK",
        };
        const result = restoreStap5Result(valid);
        expect(result).toBeDefined();
        expect(result?.focusDoelId).toBe("g1");
        expect(result?.samenvatting).toBe("OK");
      });

      test("Test 2: oude sectorVertalingen[] shape wordt naar undefined gereset", () => {
        const oldShape = {
          sectorVertalingen: [
            { sector: "PO", dinKeten: { aantalBaten: 1, aantalVermogens: 1, aantalInspanningen: 1, waarvanGedeeld: 0 }, samenvatting: "x" },
          ],
          totaalSamenvatting: "oud",
        };
        const result = restoreStap5Result(oldShape);
        expect(result).toBeUndefined();
      });

      test("Test 3: undefined input retourneert undefined", () => {
        expect(restoreStap5Result(undefined)).toBeUndefined();
      });

      test("Test 4: null input retourneert undefined", () => {
        expect(restoreStap5Result(null)).toBeUndefined();
      });

      test("Test 5: partial nieuwe shape met ontbrekende required fields → undefined", () => {
        const partial = {
          focusDoelId: "g1",
          // focusDoelNaam ontbreekt
          samenvatting: "x",
        };
        const result = restoreStap5Result(partial);
        expect(result).toBeUndefined();
      });

      test("Test 6: valide minimale shape (lege default arrays) wordt geaccepteerd", () => {
        const minimal = {
          focusDoelId: "g1",
          focusDoelNaam: "Focus",
          samenvatting: "minimale",
        };
        const result = restoreStap5Result(minimal);
        expect(result).toBeDefined();
        expect(result?.vermogenReview).toEqual([]);
        expect(result?.inspanningReview).toEqual([]);
        expect(result?.batenDekking).toEqual([]);
      });

      test("Test 7: emitteert console.warn bij afgewezen oude shape", () => {
        const oldShape = { sectorVertalingen: [], totaalSamenvatting: "x" };
        restoreStap5Result(oldShape);
        expect(warnSpy).toHaveBeenCalled();
      });
    });
    ```

    Na commit van alle drie Wave 0 bestanden: draai `npx vitest run src/lib/__tests__/stap5-*.test.ts` om te bevestigen dat vitest alle drie files leest. Draai ook `npm run lint` om te bevestigen dat er GEEN nieuwe lint errors zijn (tests mogen niet `any` gebruiken zonder reden; de mockSession helper in Task 2 gebruikt correct typed `Partial<DINSession>`).

    **Let op voor lint:** Als `npm run lint` klaagt over ongebruikte variabelen of `any` in de test-files: fix direct. Deze tests moeten de project lint-rules respecteren.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/stap5-restore-guard.test.ts --reporter=default && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Bestand bestaat: `src/lib/__tests__/stap5-restore-guard.test.ts`
    - Importeert `restoreStap5Result` van `@/lib/stap5-focus` (NIET van schemas.ts — moet een aparte pure helper zijn in Wave 1)
    - Bevat exact 7 tests met de namen: "valide nieuwe shape wordt doorgegeven", "oude sectorVertalingen[] shape wordt naar undefined gereset", "undefined input", "null input", "partial nieuwe shape", "valide minimale shape", "emitteert console.warn"
    - Bevat `beforeEach`/`afterEach` met `vi.spyOn(console, "warn")` mock
    - `grep -c 'test(' src/lib/__tests__/stap5-restore-guard.test.ts` ≥ 7
    - `grep -n 'sectorVertalingen' src/lib/__tests__/stap5-restore-guard.test.ts` retourneert ≥ 2 matches (oude shape reference)
    - `npm run lint` passes zonder nieuwe errors of warnings veroorzaakt door de drie nieuwe test-bestanden
    - Alle drie bestanden worden door vitest geregistreerd (check via `npx vitest run src/lib/__tests__/stap5-*.test.ts --reporter=default` → output toont 3 test files listed, ongeacht RED/GREEN status)
  </acceptance_criteria>
  <done>
    Alle drie Wave 0 test-bestanden gecommit. `npm run lint` groen. Vitest registreert alle drie files (RED status is expected en correct). Wave 0 is klaar voor Wave 1 om de tests te laten slagen.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/__tests__/stap5-*.test.ts` draait zonder framework-crashes (RED-tests zijn expected)
2. `npm run lint` slaagt zonder nieuwe errors
3. Drie nieuwe bestanden bestaan in `src/lib/__tests__/`:
   - schemas-stap5.test.ts
   - stap5-focus-filter.test.ts
   - stap5-restore-guard.test.ts
4. Elk bestand bevat de verwachte tests (5 + 11 + 7 = 23 tests totaal)
5. Import paths gebruiken `@/lib/...` alias patroon (conform vitest.config.ts)
6. `npm run build` blijft slagen — test files worden niet door Next.js build meegenomen
</verification>

<success_criteria>
- Drie test files bestaan en runnen via vitest
- Tests zijn RED (expected — Wave 1 maakt groen)
- Geen lint errors, geen build errors
- Tests gebruiken concrete waarden voor `Stap5ResultSchema` velden, pure function signatures, en D-10 migratie gedrag (niet abstracte beschrijvingen)
- Tests leggen contracten vast die Wave 1 MOET implementeren (schema shape, helper exports, restore behavior)
</success_criteria>

<output>
After completion, create `.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-01-SUMMARY.md` with:
- Which test files were created and how many tests each contains
- RED state confirmation (verwacht gedrag)
- Confirmation that `npm run lint` and the vitest framework registration succeeded
- Any lint fixes needed in test files
- Reference to Wave 1 dependencies: `src/lib/stap5-focus.ts` (new module with getFocusGoal, computeFocusView, restoreStap5Result) and `src/lib/schemas.ts` (Stap5ResultSchema rewrite)
</output>
