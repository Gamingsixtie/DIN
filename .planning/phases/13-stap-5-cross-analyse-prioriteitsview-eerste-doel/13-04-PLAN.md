---
phase: 13-stap-5-cross-analyse-prioriteitsview-eerste-doel
plan: 04
type: execute
wave: 3
depends_on: [13-02, 13-03]
files_modified:
  - src/components/cross-analyse/CrossAnalyseWizard.tsx
autonomous: false
requirements: [R-CROSS-01, R-CROSS-02]
must_haves:
  truths:
    - "CrossAnalyseWizard valideert stap5 shape bij session restore via restoreStap5Result (D-10) — oude sectorVertalingen[] data wordt silent-reset naar undefined"
    - "STEP_INFO[5] bevat de actuele copy: title 'Prioriteitsview — eerste doel', analyseLabel 'Analyseer eerste doel', placeholder/loadingTitle/loadingDescription ingevuld"
    - "De 'Analyseer' CTA-knop verschijnt op stap 5 (CrossAnalyseWizard line ~437 condition van <= 4 naar <= 5) — Open Question 2 locked"
    - "Intro-tekst / description voor stap 5 actualiseert 'Sector-vertaling' / 'Nieuw DIN-netwerk' naar 'Prioriteitsview — eerste doel' (D-11)"
    - "Handmatige UAT bevestigt: focusview rendert, Analyseer triggert AI, badges verschijnen, buiten-scope footer werkt, wizard terug-navigatie behoudt stap 5 state"
    - "npm run build slaagt, npm run lint groen"
  artifacts:
    - path: "src/components/cross-analyse/CrossAnalyseWizard.tsx"
      provides: "Stap 5 wizard integration — restore guard, updated STEP_INFO[5], CTA condition <= 5, import Stap5ResultSchema/restoreStap5Result"
      contains: "restoreStap5Result"
  key_links:
    - from: "src/components/cross-analyse/CrossAnalyseWizard.tsx useEffect (lines 98-113)"
      to: "src/lib/stap5-focus.ts restoreStap5Result"
      via: "validate stap5 shape bij session load"
      pattern: "restoreStap5Result\\("
    - from: "src/components/cross-analyse/CrossAnalyseWizard.tsx STEP_INFO[5]"
      to: "UI-SPEC Copywriting Contract"
      via: "exact copy voor title/analyseLabel/placeholder/loading"
      pattern: "Prioriteitsview — eerste doel"
    - from: "src/components/cross-analyse/CrossAnalyseWizard.tsx CTA conditional (line ~437)"
      to: "stap 5 Analyseer button rendering"
      via: "currentStep <= 5 predicate"
      pattern: "currentStep <= 5"
---

<objective>
Wave 3 integration: Wire de `StapSectorVertaling` focusview in `CrossAnalyseWizard.tsx`. Drie wijzigingen:

1. **D-10 restore guard:** Bij session load valideer `stap5` result via `restoreStap5Result` pure helper; oude shape wordt silent-reset naar `undefined` (Pitfall 1 — runtime crash voorkomen op pages met oude localStorage).
2. **STEP_INFO[5] update + intro copy (D-11):** Vervang "Nieuw DIN-netwerk" door "Prioriteitsview — eerste doel" en vul de lege `placeholder`, `analyseLabel`, `loadingTitle`, `loadingDescription` velden met UI-SPEC copy.
3. **CTA condition extension (Open Question 2):** Verander `wizardState.currentStep <= 4` naar `<= 5` op line ~437 zodat de Analyseer-knop óók op stap 5 zichtbaar is. Dit is de minst-invasieve oplossing, locked in RESEARCH Open Question 2.

**Purpose:** Stap 5 is nu een volwaardige AI-stap in de wizard. Gebruiker ziet op stap 5 een structurele focusview (zelfs zonder AI-resultaat), klikt "Analyseer eerste doel", krijgt badges en callouts, en kan buiten-scope items inklapbaar bekijken. De UAT checkpoint bevestigt dat dit end-to-end werkt.

**Output:**
- `src/components/cross-analyse/CrossAnalyseWizard.tsx` — 3 targeted wijzigingen
- Handmatige UAT checkpoint: focusview flow end-to-end verified
- Commit + push na groene build + UAT approval

**Scope disclaimer:** Alleen `CrossAnalyseWizard.tsx`. Geen wijzigingen aan andere wizard stappen (1-4). Geen wijzigingen aan de rendering van `StapSectorVertaling` zelf (dat is Wave 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-CONTEXT.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-RESEARCH.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-UI-SPEC.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-VALIDATION.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-02-PLAN.md
@.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-03-PLAN.md
@src/components/cross-analyse/CrossAnalyseWizard.tsx

<interfaces>
<!-- From Wave 1 — stap5-focus.ts -->
```typescript
import { restoreStap5Result } from "@/lib/stap5-focus";
// restoreStap5Result(rawStap5: unknown): Stap5Result | undefined
// Pure function: null/undefined → undefined, oude shape → undefined (+ console.warn), nieuwe valid shape → Stap5Result
```

<!-- UI-SPEC copy voor STEP_INFO[5] (from 13-UI-SPEC.md §Copywriting Contract) -->
```
title:                "Prioriteitsview — eerste doel"
description:          "Dit is de scherpste hefboom: de keten doel → baten → vermogens → inspanningen voor uw hoogste prioriteit."
placeholder:          "Bijv. specifieke aandachtspunten voor hefboomwerking of baten-dekking..."
analyseLabel:         "Analyseer eerste doel"
loadingTitle:         "AI beoordeelt hefboomwerking…"
loadingDescription:   "De AI loopt vermogens, inspanningen en baten-dekking voor het eerste doel langs..."
```
</interfaces>

<codebase_context>
### CrossAnalyseWizard.tsx structuur (huidige lines relevant voor deze plan)

- **Lines 1-21:** Imports — inclusief `Stap5Result` type en `StapSectorVertaling` component
- **Lines 23-33:** `WizardState` interface (stap5 already typed als Stap5Result — Wave 1 z.infer propagatie werkt automatisch)
- **Lines 35-83:** `STEP_INFO` map — lines 75-82 zijn STEP_INFO[5] met "Nieuw DIN-netwerk" en lege strings voor placeholder/analyseLabel/loadingTitle/loadingDescription
- **Lines 85-95:** Component function start, useSession, wizardState
- **Lines 97-113:** `useEffect` voor restore — **dit is waar D-10 restore guard moet landen**
- **Lines 115-141:** `handleStepComplete` — NIET wijzigen (generieke `result: unknown`)
- **Lines 143+:** `handleMarkViewed`, `handleAnalyse`, andere handlers — NIET wijzigen
- **Line ~437:** CTA conditional `{wizardState.currentStep <= 4 && !error && (...)}` — moet `<= 5` worden
- **Line ~471:** Closing tags

### Kritieke code die NIET gewijzigd mag worden
- `handleStepComplete` (generiek, werkt met nieuwe shape)
- `handleAnalyse` (POST naar /api/cross-analyse met stap: 5 — backend doet de rest)
- `findSharedCapabilities` usage op line ~318 (oude consolidatie helper, gebruikt door andere wizard steps)
- Rendering van stap 1-4 components

### Waarom restore guard in CrossAnalyseWizard.tsx en NIET in session-context.tsx
Pitfall 1 + Pattern 7 uit RESEARCH: `session-context.tsx:loadSession` doet geen `DINSessionSchema.safeParse` — validatie gebeurt op gebruiks-niveau. Fixen in session-context zou een grote refactor zijn die buiten scope valt. De restore-useEffect in de wizard is het juiste level.
</codebase_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: STEP_INFO[5] update + restore guard + CTA condition in CrossAnalyseWizard.tsx</name>
  <files>src/components/cross-analyse/CrossAnalyseWizard.tsx</files>
  <read_first>
    - src/components/cross-analyse/CrossAnalyseWizard.tsx (volledig — gebruik offset/limit als te groot; focus op lines 1-150 en lines 420-475)
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-RESEARCH.md §Pattern 7 (restore guard code exact) en §Open Question 2 (CTA condition rationale)
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-UI-SPEC.md §Copywriting Contract (step title, primary CTA, loading labels)
    - .planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-CONTEXT.md §Decisions D-10 (migratie), D-11 (copy)
    - src/lib/stap5-focus.ts (Wave 1 — restoreStap5Result export signature)
  </read_first>
  <action>
    Voer exact drie wijzigingen door in `src/components/cross-analyse/CrossAnalyseWizard.tsx`:

    **Wijziging 1: Import restoreStap5Result**

    Na de bestaande `import { findSharedCapabilities } from "@/lib/din-service";` regel (line 13), voeg toe:

    ```typescript
    import { restoreStap5Result } from "@/lib/stap5-focus";
    ```

    **Wijziging 2: STEP_INFO[5] update (lines ~75-82)**

    Zoek het bestaande STEP_INFO[5] object:
    ```typescript
    5: {
      title: "Nieuw DIN-netwerk",
      description: "Het geconsolideerde DIN-netwerk — het resultaat van de cross-analyse dat doorgaat naar prioritering.",
      placeholder: "",
      analyseLabel: "",
      loadingTitle: "",
      loadingDescription: "",
    },
    ```

    Vervang het door:
    ```typescript
    5: {
      title: "Prioriteitsview — eerste doel",
      description: "Dit is de scherpste hefboom: de keten doel → baten → vermogens → inspanningen voor uw hoogste prioriteit. We tonen alleen het eerste doel omdat daar het meeste mandaat en de hoogste urgentie zit.",
      placeholder: "Bijv. specifieke aandachtspunten voor hefboomwerking of baten-dekking...",
      analyseLabel: "Analyseer eerste doel",
      loadingTitle: "AI beoordeelt hefboomwerking…",
      loadingDescription: "De AI loopt vermogens, inspanningen en baten-dekking voor het eerste doel langs...",
    },
    ```

    **Wijziging 3: Restore useEffect met D-10 guard (lines ~97-113)**

    Zoek het bestaande restore `useEffect`:
    ```typescript
    useEffect(() => {
      if (!session) return;

      const wizData = session.crossAnalyseWizard;
      if (wizData) {
        setWizardState({
          currentStep: wizData.currentStep || 1,
          completedSteps: new Set(wizData.completedSteps || []),
          stepResults: wizData.stepResults || {},
        });
      } else if (session.crossAnalyse) {
        setLegacyMode(true);
      }
    }, [session?.id]); // eslint-disable-line react-hooks/exhaustive-deps
    ```

    Vervang door:
    ```typescript
    useEffect(() => {
      if (!session) return;

      const wizData = session.crossAnalyseWizard;
      if (wizData) {
        // D-10 — stap5 schema is breaking changed; valideer bij restore via pure helper
        const restoredStap5 = restoreStap5Result(wizData.stepResults?.stap5);

        setWizardState({
          currentStep: wizData.currentStep || 1,
          completedSteps: new Set(wizData.completedSteps || []),
          stepResults: {
            ...(wizData.stepResults || {}),
            stap5: restoredStap5,
          },
        });
      } else if (session.crossAnalyse) {
        setLegacyMode(true);
      }
    }, [session?.id]); // eslint-disable-line react-hooks/exhaustive-deps
    ```

    **Wijziging 4: CTA condition van `<= 4` naar `<= 5` (line ~437)**

    Zoek exact deze regel:
    ```jsx
    {wizardState.currentStep <= 4 && !error && (
    ```

    Vervang door:
    ```jsx
    {wizardState.currentStep <= 5 && !error && (
    ```

    **Verificatie na wijzigingen:**

    1. Draai `npm run lint` — moet groen zijn. Specifiek: geen `no-unused-vars` op `restoreStap5Result`.
    2. Draai `npm run build` — moet groen zijn.
    3. Draai `npx vitest run` (volledige suite) — alle tests moeten groen blijven.
    4. Grep-check:
       - `grep -c 'restoreStap5Result' src/components/cross-analyse/CrossAnalyseWizard.tsx` ≥ 2 (import + call)
       - `grep -c 'Prioriteitsview — eerste doel' src/components/cross-analyse/CrossAnalyseWizard.tsx` ≥ 1
       - `grep -c 'Analyseer eerste doel' src/components/cross-analyse/CrossAnalyseWizard.tsx` ≥ 1
       - `grep -c 'currentStep <= 5' src/components/cross-analyse/CrossAnalyseWizard.tsx` ≥ 1
       - `grep -c 'currentStep <= 4' src/components/cross-analyse/CrossAnalyseWizard.tsx` = 0
       - `grep -c 'Nieuw DIN-netwerk' src/components/cross-analyse/CrossAnalyseWizard.tsx` = 0

    **Bewaar NIET aan te passen:**
    - `handleStepComplete` werkt generiek — wijzig niet
    - `handleAnalyse` — wijzig niet (backend doet de focus-narrowing)
    - Alle rendering code voor stap 1-4
    - `StapSectorVertaling` JSX element (bestaande `<StapSectorVertaling session={session} result={wizardState.stepResults.stap5} />` — als deze al bestaat laat hem staan; als hij nog de oude props heeft update ALLEEN de props-shape naar `session` + `result`)
  </action>
  <verify>
    <automated>npm run lint && npm run build && npx vitest run --reporter=default</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/cross-analyse/CrossAnalyseWizard.tsx` importeert `restoreStap5Result` van `@/lib/stap5-focus`
    - Bevat de string `"Prioriteitsview — eerste doel"` exact (em-dash character)
    - Bevat de string `"Analyseer eerste doel"` exact
    - Bevat de string `"AI beoordeelt hefboomwerking…"` (let op het …-karakter, niet drie losse punten — maar drie losse punten zijn ook acceptabel mits consistent)
    - Bevat `restoreStap5Result(wizData.stepResults?.stap5)` call
    - Bevat GEEN `"Nieuw DIN-netwerk"` meer (via grep)
    - Bevat `currentStep <= 5` (via grep: exact 1 match minimaal)
    - Bevat GEEN `currentStep <= 4` meer (via grep: 0 matches)
    - `npm run lint` passes zonder errors/warnings geattribueerd aan dit bestand
    - `npm run build` passes zonder errors
    - `npx vitest run` — alle tests groen (inclusief de 23 Wave 0 tests)
    - Existing stap 1-4 rendering paths intact (handleStepComplete, handleAnalyse, andere STEP_INFO entries ongewijzigd)
  </acceptance_criteria>
  <done>
    CrossAnalyseWizard.tsx bevat de restore guard, de nieuwe STEP_INFO[5] copy, en de `<= 5` CTA condition. Build en lint slagen, full vitest suite groen.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end UAT — Phase 13 focusview flow</name>
  <files>N/A — checkpoint task (geen file changes; verificatie van Wave 0-3 output)</files>
  <action>Human-verify checkpoint. Claude heeft alle geautomatiseerde werk voltooid (Waves 0-3). Deze task voert handmatige UAT uit — zie <what-built> voor de context en <how-to-verify> voor de exacte checklist van 10 checks. Claude pauzeert hier en wacht op het resume-signal ("approved" of issue-description).</action>
  <what-built>
    Volledige Phase 13 implementation is nu af. Claude heeft geautomatiseerd:
    - Wave 0: 3 test-bestanden (23 tests totaal, nu alle GROEN)
    - Wave 1: Stap5ResultSchema vervangen, CROSS_ANALYSE_STAP5_PROMPT herschreven, pure helpers (getFocusGoal, computeFocusView, restoreStap5Result) in src/lib/stap5-focus.ts
    - Wave 2: API route versmalt payload voor stap 5, StapSectorVertaling.tsx volledig herschreven naar focusview per UI-SPEC
    - Wave 3 Task 1: CrossAnalyseWizard.tsx restore guard + STEP_INFO[5] + CTA condition

    Geautomatiseerde verificatie: `npm run build` groen, `npm run lint` groen, `npx vitest run` alle tests groen.

    Deze checkpoint verifieert handmatig wat niet door unit tests gedekt wordt:
    - D-09: API route stap===5 payload narrowing (integration — geen unit tests in codebase)
    - D-12: Empty states rendering (geen React Testing Library in codebase)
    - D-06: Baten per sector met kleurbadges (visual verification)
    - D-04/D-08: AI review content render per UI-SPEC
    - D-03: Buiten-scope inklapbare footer
    - Open Question 2: CTA button visible en clickable op stap 5
    - D-10: Oude localStorage data wordt gereset (regressie check)
  </what-built>
  <how-to-verify>
    **Setup:**
    1. Start de app: `npm run dev` (open http://localhost:3000)
    2. Open een bestaande sessie met DIN-data, OF maak een nieuwe sessie met:
       - Minimaal 2 doelen met rank (bijv. doel 1 rank=1, doel 2 rank=2)
       - Per doel ≥ 2 baten gekoppeld via goalBenefitMaps (bij voorkeur verdeeld over PO/VO/Zakelijk)
       - ≥ 1 cross-sector vermogen (via cross-analyse stap 2/3/4: capability met `relatedSectors = ["PO", "VO"]` of meer)
       - ≥ 1 gedeelde inspanning (responsibleSector = "PO, VO")
       - De cross-sector vermogens gekoppeld via benefitCapabilityMaps aan de focus-doel baten
       - De gedeelde inspanningen gekoppeld via capabilityEffortMaps aan die vermogens

    **Als zo'n sessie niet bestaat:** Gebruik de demo-data via de homepage "Demo laden" knop en voer minimaal stap 1-4 van de cross-analyse wizard door zodat er consolidatie-flags en cross-sector data is.

    ---

    **Verificatie checklist:**

    **Check 1 — Navigeer naar stap 5 van de cross-analyse wizard**
    - Open de sessie, ga naar het tabblad / stap "Cross-analyse"
    - Navigeer door stappen 1-4 tot stap 5 actief is
    - **Verwacht:** De header bovenaan toont "Prioriteitsview — eerste doel" (NIET "Nieuw DIN-netwerk" of "Sector-vertaling")
    - **Verwacht:** Onder de header een beschrijvende tekst over hefboomwerking, eerste doel, mandaat

    **Check 2 — Focusdoel card**
    - **Verwacht:** Bovenaan een card met label "Focusdoel — prioriteit 1" en daaronder de naam van het doel met laagste rank (doel 1 uit je setup)
    - **Verwacht:** De card heeft een linker accent rand (donkerblauw #003366)

    **Check 3 — Baten per sector**
    - **Verwacht:** Sectie "Baten onder dit doel — per sector"
    - **Verwacht:** Drie sub-groepen (PO, VO, Zakelijk) elk met een SectorBadge + aantal baten
    - **Verwacht:** Alleen baten die aan doel 1 gekoppeld zijn (NIET baten onder doel 2)
    - **Verwacht:** Per baat een linker accent rand (#0066cc) en rechts een neutrale placeholder dot (pre-AI)
    - **Accessibility:** Hover/inspect op een placeholder dot — moet `aria-label="Wachten op AI-analyse"` hebben

    **Check 4 — Cross-sector vermogens**
    - **Verwacht:** Sectie "Cross-sector vermogens die hefboom leveren"
    - **Verwacht:** Alleen capabilities met `relatedSectors.length > 1` EN gekoppeld aan de focus-baten
    - **Verwacht:** Per vermogen een linker accent (#0891b2), de titel, en rechts de SectorBadges voor de gedeelde sectoren
    - **Verwacht:** Pre-AI: "AI-analyse nog niet gedraaid" placeholder tekst

    **Check 5 — Gedeelde inspanningen**
    - **Verwacht:** Sectie "Gedeelde inspanningen onder deze vermogens"
    - **Verwacht:** Alleen efforts met `responsibleSector` die een comma bevat EN gekoppeld aan de focus-vermogens
    - **Verwacht:** Per inspanning linker accent (#059669), titel, sector-badges

    **Check 6 — Empty states (test één van deze scenario's)**
    - Scenario A: Maak een nieuwe sessie zonder doelen → **Verwacht:** "Nog geen doelen beschikbaar. Importeer eerst de KiB-uitkomsten in stap 1."
    - Scenario B: Sessie met doelen maar GEEN cross-sector vermogens (verwijder `relatedSectors` op alle caps) → **Verwacht:** In de "Cross-sector vermogens" sectie: "Dit doel heeft nog geen cross-sector vermogens. Voer stap 4 (Consolidatie) uit of voeg gedeelde vermogens toe..."

    **Check 7 — AI Analyseer flow (D-02, D-04, D-05, D-08)**
    - Onderaan de wizard panel moet de "Analyseer eerste doel" CTA-knop zichtbaar zijn (BELANGRIJK: deze was er voorheen NIET op stap 5 — dit is Open Question 2 fix)
    - Klik op de knop
    - **Verwacht:** Loading overlay verschijnt met tekst "AI beoordeelt hefboomwerking…" (of gelijkwaardig uit loadingTitle)
    - **Verwacht:** Na 10-30 seconden verschijnt AI-resultaat:
      - Per baat een groene "geraakt" of rode "risico" badge (met risico-tekst onder rode)
      - Per vermogen een hefboomAnalyse tekst in plaats van de placeholder
      - Per inspanning een groene/amber/rode badge ("dekt volledig" / "moet verbreed" / "mist aspect") met toelichting
      - Onderaan een "AI-samenvatting" blok met 3-5 zinnen
    - **Verwacht:** Layout blijft stabiel, geen content jump

    **Check 8 — Buiten scope footer (D-03)**
    - Onderaan de view: een inklapbare knop "Buiten scope voor nu — X vermogens en Y inspanningen"
    - Klik op de knop
    - **Verwacht:** Uitklappen toont een simpele lijst met titel + SectorBadge per item, met tekst "Deze items zijn niet geconsolideerd..."
    - **Verwacht:** GEEN volledige cards, GEEN AI-review per item (dat is bewust — D-03)
    - Klik nog een keer → inklappen werkt

    **Check 9 — Page reload / sessie persistence (D-10)**
    - Reload de browser (F5)
    - **Verwacht:** Sessie laadt opnieuw, stap 5 view toont nog steeds de AI-resultaten (badges, samenvatting) — deze zijn gecached in localStorage
    - **Verwacht:** Geen console errors, geen white screen
    - **Open dev-tools console** — check of er `[stap5-focus]` warnings verschijnen (mag als er oude shape data was)
    - (Optioneel) Handmatig oude shape in localStorage injecteren via devtools:
      ```javascript
      // In browser console:
      const s = JSON.parse(localStorage.getItem('din_session_<session-id>'));
      s.crossAnalyseWizard.stepResults.stap5 = { sectorVertalingen: [], totaalSamenvatting: "oud" };
      localStorage.setItem('din_session_<session-id>', JSON.stringify(s));
      location.reload();
      ```
    - **Verwacht:** Na reload zie je console.warn `[stap5-focus] stap5 result heeft oude shape, wordt gereset`, en stap 5 view toont de structurele view maar ZONDER AI-callouts (omdat restoreStap5Result het naar undefined heeft gereset). Geen crash.

    **Check 10 — Styling conformance**
    - Bekijk of de algemene styling klopt: neutrale achtergronden, linker accenten, kleur-badges volgens D-05
    - Geen paarse/regenboog/gradient overdaad — ingetogen zakelijk uiterlijk

    ---

    **Als alles correct is:** Type "approved" en beschrijf eventuele kleine gebreken als tekst.
    **Als iets fundamenteel fout is:** Beschrijf precies wat er niet werkt zodat een follow-up task het kan fixen.
  </how-to-verify>
  <resume-signal>Type "approved" om af te ronden, of beschrijf issues voor een follow-up fix</resume-signal>
  <verify>
    <automated>MISSING — handmatige UAT checkpoint, geen automated command. Wave 3 Task 1 <verify> heeft de build/lint/vitest checks al geautomatiseerd voor deze wave.</automated>
  </verify>
  <done>Gebruiker heeft "approved" getypt of alle 10 checks als conform bevestigd. Eventuele follow-up issues zijn genoteerd in het SUMMARY bestand. Phase 13 goal is bereikt.</done>
</task>

</tasks>

<verification>
1. `npm run build` slaagt
2. `npm run lint` slaagt
3. `npx vitest run` — alle tests GROEN (inclusief 23 Wave 0 tests)
4. Handmatige UAT (Task 2) succesvol: alle 10 checks conform
5. `grep -c "restoreStap5Result\|Prioriteitsview — eerste doel\|currentStep <= 5" src/components/cross-analyse/CrossAnalyseWizard.tsx` ≥ 3 (één per feature)
6. `grep -c "Nieuw DIN-netwerk\|currentStep <= 4" src/components/cross-analyse/CrossAnalyseWizard.tsx` = 0
7. End-to-end flow getest: sessie met data → navigeer naar stap 5 → focusview rendert → Analyseer klikbaar en werkt → badges verschijnen → buiten-scope footer werkt → reload behoudt state
</verification>

<success_criteria>
- Alle drie wijzigingen in CrossAnalyseWizard.tsx doorgevoerd (import, STEP_INFO[5], restore guard, CTA condition)
- Build + lint groen
- Volledige vitest suite groen
- UAT checkpoint approved door gebruiker
- Phase 13 goal bereikt: focusview rond eerste doel met AI-review voor hefboomwerking en baten-dekking, oude totaaloverzicht weg, buiten-scope inklapbaar
</success_criteria>

<output>
After completion, create `.planning/phases/13-stap-5-cross-analyse-prioriteitsview-eerste-doel/13-04-SUMMARY.md` with:
- De 4 wijzigingen in CrossAnalyseWizard.tsx (import, STEP_INFO[5], useEffect restore guard, CTA conditional)
- UAT checkpoint resultaat (approved / issues found)
- Bevestiging dat alle 23 Wave 0 tests groen zijn gebleven
- Build + lint status: beide groen
- Opmerkingen uit de UAT (eventuele kleine gebreken of follow-up items)
- Totaal-overzicht phase 13: welke files zijn gewijzigd/toegevoegd (schemas.ts, prompts.ts, stap5-focus.ts NEW, route.ts, StapSectorVertaling.tsx, CrossAnalyseWizard.tsx, 3 test files NEW)
</output>
