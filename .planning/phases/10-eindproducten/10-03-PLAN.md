---
phase: 10-eindproducten
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai-client.ts
  - src/lib/prompts.ts
  - src/app/api/export/route.ts
autonomous: true
requirements:
  - EXP-01

must_haves:
  truths:
    - "De ongebruikte /api/export route bestaat niet meer"
    - "generateProgrammaPlan() functie is verwijderd uit ai-client.ts"
    - "PROGRAMMAPLAN_PROMPT is verwijderd uit prompts.ts"
    - "Geen imports refereren aan verwijderde code"
  artifacts:
    - path: "src/lib/ai-client.ts"
      provides: "AI client without deprecated generateProgrammaPlan function"
    - path: "src/lib/prompts.ts"
      provides: "Prompts without deprecated PROGRAMMAPLAN_PROMPT"
  key_links: []
---

<objective>
Remove the unused AI-generated prose export pipeline per D-04. The Word document is the eindproduct -- no separate AI-generated text document needed. Delete /api/export/route.ts entirely, remove generateProgrammaPlan() from ai-client.ts, and remove PROGRAMMAPLAN_PROMPT from prompts.ts.

Purpose: Clean up dead code that was superseded by the direct Word generation approach. Reduces confusion and maintenance burden.

Output: Three files cleaned up, one file deleted. No new functionality.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-eindproducten/10-CONTEXT.md

@src/lib/ai-client.ts
@src/lib/prompts.ts
@src/app/api/export/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused AI prose export pipeline (D-04)</name>
  <files>src/lib/ai-client.ts, src/lib/prompts.ts, src/app/api/export/route.ts</files>
  <read_first>
    - src/app/api/export/route.ts (full file -- verify it is the unused AI prose export route, ~49 lines)
    - src/lib/ai-client.ts (full file -- find generateProgrammaPlan function around line 243 and the PROGRAMMAPLAN_PROMPT import)
    - src/lib/prompts.ts (full file -- find PROGRAMMAPLAN_PROMPT export around line 201)
  </read_first>
  <action>
    **Per D-04: Remove all three pieces of the unused AI prose export pipeline.**

    1. **Delete `src/app/api/export/route.ts` entirely.** This is the unused API route that called `generateProgrammaPlan()` to create AI-generated prose. The Word document export uses `word-export.ts` directly via dynamic import in ExportStep.tsx -- this route is not called anywhere.

    2. **Remove `generateProgrammaPlan()` from `src/lib/ai-client.ts`:**
    - Find and delete the `export async function generateProgrammaPlan(sessionData: Record<string, unknown>): Promise<string>` function (starts around line 243).
    - Remove the `PROGRAMMAPLAN_PROMPT` from the import statement at the top of ai-client.ts (it imports from `./prompts`).
    - Do NOT remove any other functions or imports -- only the generateProgrammaPlan function and its specific prompt import.

    3. **Remove `PROGRAMMAPLAN_PROMPT` from `src/lib/prompts.ts`:**
    - Find and delete the `export const PROGRAMMAPLAN_PROMPT = ...` constant definition (starts around line 201).
    - This is a multi-line template literal. Delete from the `export const PROGRAMMAPLAN_PROMPT` line through the closing backtick and semicolon.
    - Do NOT remove any other prompt constants.

    4. **Verify no remaining references:**
    - Search the codebase for any remaining imports of `generateProgrammaPlan` or `PROGRAMMAPLAN_PROMPT`.
    - Search for any fetch calls to `/api/export`.
    - If any references are found, remove them (they should be dead code).

    5. **Delete the empty directory** `src/app/api/export/` after removing route.ts (if the directory becomes empty).
  </action>
  <verify>
    <automated>npm run build</automated>
    Also verify: grep -r "generateProgrammaPlan\|PROGRAMMAPLAN_PROMPT\|api/export" src/ should return no results.
  </verify>
  <acceptance_criteria>
    - src/app/api/export/route.ts does NOT exist (file deleted)
    - src/lib/ai-client.ts does NOT contain `generateProgrammaPlan`
    - src/lib/ai-client.ts does NOT contain `PROGRAMMAPLAN_PROMPT` in import statement
    - src/lib/prompts.ts does NOT contain `PROGRAMMAPLAN_PROMPT`
    - No file in src/ directory references `generateProgrammaPlan`, `PROGRAMMAPLAN_PROMPT`, or `/api/export`
    - `npm run build` exits 0
    - `npm test` exits 0 (no regressions)
  </acceptance_criteria>
  <done>
    The unused AI prose export pipeline is completely removed: /api/export route deleted, generateProgrammaPlan() function deleted from ai-client.ts, PROGRAMMAPLAN_PROMPT deleted from prompts.ts. No remaining references in codebase. Build and tests pass.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` -- no build errors
2. `npm test` -- all tests pass (no regressions from removed code)
3. Grep for removed identifiers returns no results
</verification>

<success_criteria>
- All three artifacts removed (route file, function, prompt constant)
- No dangling references
- Build and test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/10-eindproducten/10-03-SUMMARY.md`
</output>
