---
phase: 09-wizard-cleanup-export-voorbereiding
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/components/steps/DINMappingStep.tsx
  - src/components/steps/ExportStep.tsx
  - src/lib/word-export.ts
autonomous: false
requirements:
  - EXP-04

must_haves:
  truths:
    - "DINMappingStep renders with only the AI: Genereer DIN-netwerk button in the action bar"
    - "DINMappingStep has no slide-out advies panel overlay"
    - "DINMappingStep has no samengevoegd/per-sector phase toggle"
    - "ExportStep renders sector sections without integratie-advies subsection"
    - "Word export generates document without integratie-advies per-sector section"
    - "SectorIntegratieStep.tsx file does not exist"
    - "SamengevoegdDINStep.tsx file does not exist"
    - "MergedDINView.tsx file does not exist"
    - "No unused imports remain in any edited file"
    - "npm run build succeeds with zero errors"
  artifacts:
    - path: "src/components/steps/DINMappingStep.tsx"
      provides: "DIN mapping UI without integratieAdvies panel, samengevoegd toggle, or MergedDINView"
    - path: "src/components/steps/ExportStep.tsx"
      provides: "Export preview without IntegratieAdviesSubSection"
    - path: "src/lib/word-export.ts"
      provides: "Word export without integratie-advies per-sector rendering"
  key_links:
    - from: "src/components/steps/DINMappingStep.tsx"
      to: "src/lib/schemas.ts"
      via: "no IntegratieAdvies type imports"
      pattern: "no IntegratieAdvies.*import"
    - from: "src/components/steps/ExportStep.tsx"
      to: "src/lib/schemas.ts"
      via: "no IntegratieAdviesResult import"
      pattern: "no IntegratieAdviesResult.*import"
    - from: "src/lib/word-export.ts"
      to: "src/lib/schemas.ts"
      via: "no IntegratieAdviesResult import"
      pattern: "no IntegratieAdviesResult.*import"
---

<objective>
Remove all integratieadvies UI elements from DINMappingStep, ExportStep, and word-export, then delete orphaned component files.

Purpose: Complete the deep clean of integratieadvies by removing all UI rendering, state management, and export code. This plan depends on Plan 01 (data layer cleanup) because the types/schemas referenced here were removed in Plan 01.
Output: Clean DINMappingStep with only the core DIN editing flow, clean ExportStep without advies subsections, clean word-export, and 3 orphaned files deleted.
</objective>

<execution_context>
@C:\Users\pdebu\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\pdebu\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-wizard-cleanup-export-voorbereiding/09-CONTEXT.md
@.planning/phases/09-wizard-cleanup-export-voorbereiding/09-RESEARCH.md
@.planning/phases/09-wizard-cleanup-export-voorbereiding/09-UI-SPEC.md
@.planning/phases/09-wizard-cleanup-export-voorbereiding/09-01-SUMMARY.md

<interfaces>
<!-- Elements to REMOVE from DINMappingStep.tsx — exact locations from RESEARCH.md -->

DINMappingStep.tsx removals (per D-02, D-03, D-10):
- Line 14-15: IntegratieAdviesItem/IntegratieAdviesResult type imports from @/lib/schemas
- Line 37: import MergedDINView from "@/components/din/MergedDINView"
- Line 42: type DINPhase = "per-sector" | "samengevoegd"
- Lines 81-93: ADVIES_SECTIONS constant array
- Lines 95-130: AdviesCard inner component function
- Line 439: isAnalyzingIntegratie state
- Line 448: showAdviesPanel state
- Lines 449-451: integratieAdvies state
- Lines 498-504: setIntegratieAdvies wrapper function
- Lines 1217-1272: handleIntegratieAdvies async function
- Lines 1279-1293, 1320: integratieAdvies in handleGenerateVerrijktPlan (adviesText construction + passing)
- Lines 1436-1458: Phase toggle UI between per-sector and samengevoegd
- Lines 1460-1474: samengevoegd phase rendering section (MergedDINView usage)
- Lines 1637-1652: Integratie-advies button and clipboard icon button
- Lines 2182-2330+: Slide-out advies panel overlay

ExportStep.tsx removals (per D-07, D-08):
- Line 7: IntegratieAdviesResult import
- Lines 651-681: IntegratieAdviesSubSection function component
- Lines 701-704: advies lookup (session.integratieAdvies?.[sector])
- Line 835: IntegratieAdviesSubSection rendering

word-export.ts removals (per D-07):
- Line 20: IntegratieAdviesResult import
- Lines 1137-1169: Integratie-advies per-sector rendering section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip integratieadvies from DINMappingStep, ExportStep, and word-export</name>
  <files>src/components/steps/DINMappingStep.tsx, src/components/steps/ExportStep.tsx, src/lib/word-export.ts</files>
  <read_first>
    - src/components/steps/DINMappingStep.tsx (full file — understand all integratieAdvies state, functions, UI, MergedDINView import and usage, DINPhase type and phase toggle)
    - src/components/steps/ExportStep.tsx (full file — find IntegratieAdviesResult import, IntegratieAdviesSubSection function, advies lookup, and rendering)
    - src/lib/word-export.ts (full file — find IntegratieAdviesResult import and integratie-advies per-sector section)
    - .planning/phases/09-wizard-cleanup-export-voorbereiding/09-UI-SPEC.md (Post-Removal UI State section — confirms target button bar and layout)
  </read_first>
  <action>
    **Per D-02, D-03, D-07, D-08, D-10: Remove all integratieadvies UI, MergedDINView toggle, and export rendering.**

    **In src/components/steps/DINMappingStep.tsx (~300+ lines removed):**

    IMPORTS to remove:
    1. Delete `IntegratieAdviesItem` and `IntegratieAdviesResult` from the type imports (around line 14-15). These types no longer exist in schemas.ts.
    2. Delete the `import MergedDINView from "@/components/din/MergedDINView"` import (line 37). The file will be deleted.
    3. Delete the `type DINPhase = "per-sector" | "samengevoegd"` type alias (line 42). No longer needed.

    CONSTANTS and INNER COMPONENTS to remove:
    4. Delete the `ADVIES_SECTIONS` constant array (lines 81-93). This is the array of `{ key, title, description }` objects for the advies panel sections.
    5. Delete the `AdviesCard` inner component function (lines 95-130). This is the card rendering function used in the slide-out panel.

    STATE VARIABLES to remove:
    6. Delete `const [isAnalyzingIntegratie, setIsAnalyzingIntegratie] = useState(false);` (line 439)
    7. Delete `const [showAdviesPanel, setShowAdviesPanel] = useState(false);` (line 448)
    8. Delete the `integratieAdvies` state and its `Record<string, IntegratieAdviesResult>` type (lines 449-451)
    9. Delete the `setIntegratieAdvies` wrapper function that persists to session (lines 498-504)

    FUNCTIONS to remove:
    10. Delete the `handleIntegratieAdvies` async function (lines 1217-1272). This is the function that calls the sector-integratie API endpoint.
    11. In `handleGenerateVerrijktPlan` function: delete the `adviesText` variable construction (lines 1279-1293) that builds text from `integratieAdvies[selectedSector]`, and delete the `integratieAdvies: adviesText` property from the fetch body (line 1320). Keep the rest of the handleGenerateVerrijktPlan function intact — it still calls the verrijkt-sectorplan API.

    PHASE TOGGLE UI to remove (per D-03 — MergedDINView removal):
    12. Delete the phase toggle UI section (lines 1436-1458) — the `flex gap-2` div with the "Per Sector Invullen" and "Samengevoegd DIN-Netwerk" radio-style toggle buttons that switch between `phase === "per-sector"` and `phase === "samengevoegd"`.
    13. Delete the samengevoegd phase rendering section (lines 1460-1474) — the conditional block `{phase === "samengevoegd" && <MergedDINView ... />}`.
    14. Delete the `const [phase, setPhase] = useState<DINPhase>("per-sector");` state variable. Since there is no DINPhase type anymore and no toggle, the component always renders the per-sector view.
    15. Remove any `phase === "per-sector" &&` conditional wrapping from the main per-sector content. After removal, the per-sector content should render unconditionally (since it is the only view).

    BUTTONS to remove:
    16. Delete the "Integratie-advies" button (lines 1637-1643) — `bg-white border border-cito-blue text-cito-blue rounded-lg text-sm font-medium` button.
    17. Delete the clipboard icon button (lines 1644-1652) — `bg-cito-blue/10 text-cito-blue rounded-lg text-sm` button with clipboard emoji.
    18. After removal, the button bar should contain only the "AI: Genereer DIN-netwerk" primary CTA button per the UI-SPEC Post-Removal UI State.

    SLIDE-OUT PANEL to remove:
    19. Delete the entire slide-out advies panel overlay (lines 2182-2330+) — the fixed-position panel that appears from the right side with advies cards, close button, and "Verwerk in sectorplan" CTA.

    CLEANUP:
    20. Remove any remaining references to `showAdviesPanel`, `isAnalyzingIntegratie`, `integratieAdvies`, `setIntegratieAdvies`, `handleIntegratieAdvies`, `phase`, `setPhase`, `DINPhase`, `ADVIES_SECTIONS`, `AdviesCard`, or `MergedDINView` that may exist elsewhere in the file.
    21. Clean up any now-unused imports (e.g., if any React hooks like useState were only used for the removed state variables, clean them up — but useState is certainly still used elsewhere).

    **In src/components/steps/ExportStep.tsx (~35 lines removed):**

    1. Delete `IntegratieAdviesResult` from the type import (line 7). This type no longer exists.
    2. Delete the `IntegratieAdviesSubSection` function component (lines 651-681). This is the function that renders advies sections in the export preview.
    3. Delete the advies data lookup (lines 701-704) — the `const adviesData = session.integratieAdvies?.[sector]` and related type cast.
    4. Delete the `<IntegratieAdviesSubSection ... />` rendering call (line 835). Per D-09, no replacement section is added.
    5. Clean up any now-unused imports.

    **In src/lib/word-export.ts (~35 lines removed):**

    1. Delete `IntegratieAdviesResult` from the type import (line 20). This type no longer exists.
    2. Delete the integratie-advies per-sector rendering section (lines 1137-1169). This is the code block that generates Word document paragraphs for each sector's integratie-advies. Per D-07, this section is removed entirely.
    3. Clean up any now-unused imports or variables.

    **After all edits:** Run `npm run build` to verify no TypeScript errors remain. The build is the ultimate validator — if it passes, all import/type references are clean.
  </action>
  <verify>
    <automated>cd "C:/Users/pdebu/Projects VS code/DIN" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - DINMappingStep.tsx does NOT contain the string "IntegratieAdvies"
    - DINMappingStep.tsx does NOT contain the string "MergedDINView"
    - DINMappingStep.tsx does NOT contain the string "DINPhase"
    - DINMappingStep.tsx does NOT contain the string "samengevoegd"
    - DINMappingStep.tsx does NOT contain the string "showAdviesPanel"
    - DINMappingStep.tsx does NOT contain the string "ADVIES_SECTIONS"
    - DINMappingStep.tsx does NOT contain the string "AdviesCard"
    - DINMappingStep.tsx does NOT contain the string "isAnalyzingIntegratie"
    - DINMappingStep.tsx does NOT contain the string "handleIntegratieAdvies"
    - DINMappingStep.tsx does NOT contain the string "adviesText"
    - ExportStep.tsx does NOT contain the string "IntegratieAdvies"
    - ExportStep.tsx does NOT contain the string "integratieAdvies"
    - word-export.ts does NOT contain the string "IntegratieAdvies"
    - word-export.ts does NOT contain the string "integratieAdvies"
    - `npm run build` exits 0 with no errors
  </acceptance_criteria>
  <done>DINMappingStep cleaned: single AI button, no advies panel, no phase toggle, no MergedDINView. ExportStep cleaned: no IntegratieAdviesSubSection. word-export cleaned: no integratie-advies rendering. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Delete orphaned component files</name>
  <files>src/components/steps/SectorIntegratieStep.tsx, src/components/steps/SamengevoegdDINStep.tsx, src/components/din/MergedDINView.tsx</files>
  <read_first>
    - src/components/steps/DINMappingStep.tsx (verify no MergedDINView import remains after Task 1)
  </read_first>
  <action>
    **Per D-03: Delete three orphaned component files.**

    1. Delete `src/components/steps/SectorIntegratieStep.tsx` — orphaned step component, not referenced in wizard or any import.
    2. Delete `src/components/steps/SamengevoegdDINStep.tsx` — orphaned step component, not referenced in wizard or any import.
    3. Delete `src/components/din/MergedDINView.tsx` — orphaned after Task 1 removed the DINMappingStep samengevoegd toggle and import.

    After deletion, verify build still passes.
  </action>
  <verify>
    <automated>cd "C:/Users/pdebu/Projects VS code/DIN" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/steps/SectorIntegratieStep.tsx does NOT exist
    - File src/components/steps/SamengevoegdDINStep.tsx does NOT exist
    - File src/components/din/MergedDINView.tsx does NOT exist
    - `npm run build` exits 0 with no errors
  </acceptance_criteria>
  <done>Three orphaned component files deleted. Build passes with zero errors. No remaining references to deleted files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify wizard flow after cleanup</name>
  <files>none</files>
  <action>
    Human verifies the wizard flow works correctly after all integratieadvies code has been removed. This is a visual and functional verification that cannot be automated.

    What was built: Removed all integratieadvies functionality across 13 files (~760 lines). Schemas, types, prompts, AI client function, API route handler, DINMappingStep advies panel + buttons + phase toggle, ExportStep advies subsection, word-export advies section, and 3 orphaned component files — all deleted.
  </action>
  <verify>
    Steps for human verification:
    1. Run `npm run dev` and open http://localhost:3000
    2. Open an existing session (or create new one with demo data)
    3. Navigate to Stap 3: DIN-Mapping
    4. Verify: Only the "AI: Genereer DIN-netwerk" button appears in the action bar (no "Integratie-advies" button, no clipboard icon)
    5. Verify: No slide-out panel appears anywhere
    6. Verify: No "Per Sector Invullen" / "Samengevoegd DIN-Netwerk" toggle appears
    7. Verify: DIN editing (baten, vermogens, inspanningen) still works normally
    8. Navigate to Stap 6: Export
    9. Verify: Per-sector sections render baten/vermogens/inspanningen WITHOUT an integratie-advies subsection
    10. Verify: The wizard steps 1-6 all navigate correctly with no gaps
  </verify>
  <done>Human confirms wizard flow works end-to-end. DIN-Mapping shows single AI button, no advies panel, no phase toggle. Export shows sector sections without advies. All 6 wizard steps navigate correctly.</done>
</task>

</tasks>

<verification>
After all tasks:
1. `npm run build` — clean build, zero errors
2. `npx vitest run --reporter=verbose` — all tests pass
3. `grep -r "integratieAdvies\|IntegratieAdvies\|SECTOR_INTEGRATIE\|MergedDINView\|SectorIntegratieStep\|SamengevoegdDINStep" src/` — returns NO matches (full codebase clean)
4. Human verification: wizard flow works end-to-end without integratieadvies remnants
</verification>

<success_criteria>
- DINMappingStep renders single "AI: Genereer DIN-netwerk" button only
- No slide-out advies panel exists
- No samengevoegd/per-sector phase toggle exists
- ExportStep renders without integratie-advies subsection
- Word export generates without integratie-advies section
- Three orphaned files (SectorIntegratieStep, SamengevoegdDINStep, MergedDINView) deleted
- No remaining references to integratieAdvies, IntegratieAdvies, SECTOR_INTEGRATIE_PROMPT, or MergedDINView anywhere in src/
- npm run build succeeds
- All vitest tests pass
- Wizard flow verified by human
</success_criteria>

<output>
After completion, create `.planning/phases/09-wizard-cleanup-export-voorbereiding/09-02-SUMMARY.md`
</output>
