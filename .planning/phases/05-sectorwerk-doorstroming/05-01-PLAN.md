---
phase: 05-sectorwerk-doorstroming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/schemas.ts
  - src/lib/types.ts
  - src/lib/session-context.tsx
  - src/components/steps/SectorWerkStep.tsx
  - src/lib/__tests__/sector-migration.test.ts
autonomous: true
requirements: [DATA-04]
must_haves:
  truths:
    - "sectorAnalyses in DINSession slaat getypeerde SectorplanAnalyseResult objecten op, niet strings"
    - "Bestaande sessies met string-data worden stilletjes gemigreerd naar getypeerde objecten bij laden"
    - "Ongeldige of markdown string-data wordt verwijderd bij laden met een toast-melding"
    - "SectorWerkStep toont analyse-resultaten direct vanuit getypeerde objecten zonder JSON.parse"
  artifacts:
    - path: "src/lib/schemas.ts"
      provides: "sectorAnalyses schema changed to Record<string, AISectorplanAnalyseSchema>"
      contains: "z.record(z.string(), AISectorplanAnalyseSchema)"
    - path: "src/lib/__tests__/sector-migration.test.ts"
      provides: "Unit tests for migration logic"
      min_lines: 40
    - path: "src/lib/session-context.tsx"
      provides: "migrateSectorAnalyses function for legacy data"
      contains: "migrateSectorAnalyses"
    - path: "src/components/steps/SectorWerkStep.tsx"
      provides: "Typed object storage and display without JSON.parse"
  key_links:
    - from: "src/lib/schemas.ts"
      to: "src/lib/types.ts"
      via: "z.infer re-export"
      pattern: "SectorplanAnalyseResult"
    - from: "src/lib/session-context.tsx"
      to: "src/lib/schemas.ts"
      via: "AISectorplanAnalyseSchema import for migration validation"
      pattern: "AISectorplanAnalyseSchema.safeParse"
    - from: "src/components/steps/SectorWerkStep.tsx"
      to: "src/lib/session-context.tsx"
      via: "updateSession stores typed objects"
      pattern: "sectorAnalyses"
---

<objective>
Wijzig de opslagstructuur van sectorwerk-analyse van strings naar getypeerde SectorplanAnalyseResult objecten, inclusief migratie van bestaande data en het opschonen van de SectorWerkStep weergave.

Purpose: DATA-04 vereist dat sectorwerk-analyse als getypeerd object wordt opgeslagen. Dit elimineert de fragiele JSON.stringify/parse cyclus en maakt typed downstream consumption mogelijk voor Plan 02 (suggestiepaneel + AI context).

Output: Schema gewijzigd, migratie-logica werkend, SectorWerkStep leest direct van typed objects, tests groen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sectorwerk-doorstroming/05-CONTEXT.md
@.planning/phases/05-sectorwerk-doorstroming/05-RESEARCH.md
@.planning/phases/05-sectorwerk-doorstroming/05-UI-SPEC.md
@.planning/phases/05-sectorwerk-doorstroming/05-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/schemas.ts (line 195-220):
```typescript
export const SectorplanAnalyseResultSchema = z.object({
  samenvatting: z.string(),
  aansluiting: z.object({ titel: z.string(), toelichting: z.string(), punten: z.array(z.string()) }),
  baten: z.object({ titel: z.string(), toelichting: z.string(), punten: z.array(z.string()) }),
  vermogens: z.object({ titel: z.string(), toelichting: z.string(), punten: z.array(z.string()) }),
  inspanningen: z.object({
    titel: z.string(), toelichting: z.string(),
    mens: z.array(z.string()), processen: z.array(z.string()),
    data_systemen: z.array(z.string()), cultuur: z.array(z.string()),
  }),
  aandachtspunten: z.object({ titel: z.string(), toelichting: z.string(), punten: z.array(z.string()) }).optional(),
});
```

From src/lib/schemas.ts (line 455-479):
```typescript
export const AISectorplanAnalyseSchema = z.object({
  samenvatting: z.string().optional().default(""),
  aansluiting: AIAnalyseSectieSchema.optional().default(aiAnalyseSectieDefault),
  baten: AIAnalyseSectieSchema.optional().default(aiAnalyseSectieDefault),
  vermogens: AIAnalyseSectieSchema.optional().default(aiAnalyseSectieDefault),
  inspanningen: z.object({
    titel: z.string().optional().default(""), toelichting: z.string().optional().default(""),
    mens: z.array(z.string()).optional().default([]),
    processen: z.array(z.string()).optional().default([]),
    data_systemen: z.array(z.string()).optional().default([]),
    cultuur: z.array(z.string()).optional().default([]),
  }).optional().default(/* factory */),
  aandachtspunten: AIAnalyseSectieSchema.optional().default(aiAnalyseSectieDefault),
});
```

From src/lib/schemas.ts (line 337 - CURRENT, to be changed):
```typescript
sectorAnalyses: z.record(z.string(), z.string()).optional(),
```

From src/lib/session-context.tsx (line 106-129):
```typescript
const updateSession = useCallback(
  (updater: (prev: DINSession) => Partial<DINSession>) => {
    setSession((prev) => {
      if (!prev) return prev;
      const updates = updater(prev);
      const updated = { ...prev, ...updates, updatedAt: new Date().toISOString() };
      const saved = saveLocal(`session_${prev.id}`, updated);
      // toast on error, setLastSaved on success
      return updated;
    });
  }, []);
```

From src/lib/session-context.tsx (line 66-73):
```typescript
const loadSession = useCallback((id: string) => {
  const loaded = loadLocal<DINSession>(`session_${id}`);
  if (loaded) {
    setSession(loaded);
    const step = APP_STEPS[loaded.currentStep]?.key || "import";
    setCurrentStepState(step);
  }
}, []);
```

From src/components/steps/SectorWerkStep.tsx (line 13-14 - CURRENT local state type):
```typescript
const [planAnalysis, setPlanAnalysisState] = useState<Record<string, string | null>>(
  session?.sectorAnalyses || {}
);
```

From src/components/steps/SectorWerkStep.tsx (line 27-39 - CURRENT setPlanAnalysis helper):
```typescript
function setPlanAnalysis(updater: (prev: Record<string, string | null>) => Record<string, string | null>) {
  setPlanAnalysisState((prev) => {
    const next = updater(prev);
    const cleaned: Record<string, string> = {};
    Object.entries(next).forEach(([k, v]) => {
      if (v) cleaned[k] = v;
    });
    updateSession(prev => ({ ...prev, sectorAnalyses: cleaned }));
    return next;
  });
}
```

From src/lib/din-service.ts (line 23-43):
```typescript
export function createBenefit(goalId: string, sectorId: string, description: string, title?: string): DINBenefit;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema migratie + migratielogica + tests</name>
  <files>
    src/lib/schemas.ts,
    src/lib/session-context.tsx,
    src/lib/__tests__/sector-migration.test.ts
  </files>
  <read_first>
    src/lib/schemas.ts,
    src/lib/session-context.tsx,
    src/lib/types.ts,
    src/lib/__tests__/prompt-assembly.test.ts
  </read_first>
  <behavior>
    - Test 1 "stores typed object": Een SectorplanAnalyseResult object dat door AISectorplanAnalyseSchema wordt gevalideerd, wordt geaccepteerd door het bijgewerkte sectorAnalyses schema veld.
    - Test 2 "migrates legacy string to typed object": Een string die valid JSON bevat met samenvatting/baten/vermogens velden wordt door migrateSectorAnalyses() omgezet naar een getypeerd SectorplanAnalyseResult object.
    - Test 3 "discards invalid string data": Een plain markdown string (niet-JSON) of een JSON string die niet aan het AISectorplanAnalyseSchema voldoet, wordt door migrateSectorAnalyses() verwijderd (retourneert null voor die entry) met needsToast=true.
    - Test 4 "keeps valid objects unchanged": Een object dat al een SectorplanAnalyseResult is, wordt ongewijzigd doorgelaten door migrateSectorAnalyses().
  </behavior>
  <action>
    **Stap 1: Schrijf tests EERST** in `src/lib/__tests__/sector-migration.test.ts`.

    Creeer een nieuw testbestand met 4 test cases:
    1. Importeer `AISectorplanAnalyseSchema` uit `@/lib/schemas`.
    2. Definieer een `validAnalysis` fixture: `{ samenvatting: "Test samenvatting", aansluiting: { titel: "Aansluiting", toelichting: "...", punten: ["punt 1"] }, baten: { titel: "Baten", toelichting: "...", punten: ["baat 1", "baat 2"] }, vermogens: { titel: "Vermogens", toelichting: "...", punten: ["vermogen 1"] }, inspanningen: { titel: "Inspanningen", toelichting: "...", mens: ["training"], processen: ["werkwijze"], data_systemen: [], cultuur: [] } }`.
    3. Test "stores typed object": `AISectorplanAnalyseSchema.safeParse(validAnalysis)` succeeds.
    4. Test "migrates legacy": `migrateSectorAnalyses({ "PO": JSON.stringify(validAnalysis) })` returns `{ migrated: { "PO": validAnalysis }, needsToast: false }`.
    5. Test "discards invalid": `migrateSectorAnalyses({ "PO": "# Markdown analyse\nGeen JSON" })` returns `{ migrated: {}, needsToast: true }`.
    6. Test "keeps valid objects": `migrateSectorAnalyses({ "PO": validAnalysis })` returns `{ migrated: { "PO": validAnalysis }, needsToast: false }`.

    **Stap 2: Schema wijzigen** in `src/lib/schemas.ts`.

    Wijzig lijn 337 van:
    ```typescript
    sectorAnalyses: z.record(z.string(), z.string()).optional(),
    ```
    naar:
    ```typescript
    sectorAnalyses: z.record(z.string(), AISectorplanAnalyseSchema).optional(),
    ```

    Per D-01: `AISectorplanAnalyseSchema` wordt gebruikt (niet `SectorplanAnalyseResultSchema`) omdat dit schema soepelere defaults heeft voor optionele velden — geschikt voor zowel AI output als opgeslagen data die mogelijk niet alle velden heeft.

    **Stap 3: Migratiefunctie** in `src/lib/session-context.tsx`.

    Voeg BOVEN de SessionProvider component een exported functie toe:
    ```typescript
    import { AISectorplanAnalyseSchema } from "@/lib/schemas";
    import type { SectorplanAnalyseResult } from "@/lib/types";

    export function migrateSectorAnalyses(
      raw: Record<string, unknown> | undefined
    ): { migrated: Record<string, SectorplanAnalyseResult>; needsToast: boolean } {
      if (!raw) return { migrated: {}, needsToast: false };
      const migrated: Record<string, SectorplanAnalyseResult> = {};
      let needsToast = false;

      for (const [key, value] of Object.entries(raw)) {
        if (typeof value === "object" && value !== null) {
          const result = AISectorplanAnalyseSchema.safeParse(value);
          if (result.success) {
            migrated[key] = result.data;
          } else {
            console.error(`[migration] Ongeldige sectorAnalyse object voor ${key}:`, result.error.issues);
            needsToast = true;
          }
        } else if (typeof value === "string") {
          try {
            const parsed = JSON.parse(value);
            const result = AISectorplanAnalyseSchema.safeParse(parsed);
            if (result.success) {
              migrated[key] = result.data;
            } else {
              console.error(`[migration] Sectoranalyse string voor ${key} valideert niet:`, result.error.issues);
              needsToast = true;
            }
          } catch {
            console.error(`[migration] Sectoranalyse voor ${key} is geen geldig JSON, wordt verwijderd`);
            needsToast = true;
          }
        }
      }

      return { migrated, needsToast };
    }
    ```

    **Stap 4: Migratie aanroepen bij session laden**.

    In de `loadSession` callback (huidige lijn 66-73), na `const loaded = loadLocal<DINSession>(...)`:
    ```typescript
    const loadSession = useCallback((id: string) => {
      const loaded = loadLocal<DINSession>(`session_${id}`);
      if (loaded) {
        // Migratie: sectorAnalyses string -> typed object (D-08)
        if (loaded.sectorAnalyses) {
          const { migrated, needsToast } = migrateSectorAnalyses(
            loaded.sectorAnalyses as Record<string, unknown>
          );
          loaded.sectorAnalyses = migrated;
          if (needsToast) {
            queueMicrotask(() =>
              addToastRef.current(
                "Eerdere sectorwerk-analyses konden niet worden geladen. Voer de analyse opnieuw uit.",
                "warning"
              )
            );
          }
          // Sla gemigreerde data terug op
          saveLocal(`session_${id}`, loaded);
        }
        setSession(loaded);
        const step = APP_STEPS[loaded.currentStep]?.key || "import";
        setCurrentStepState(step);
      }
    }, []);
    ```

    Let op: `addToastRef` is al beschikbaar in het component scope (Phase 2 patroon). De `queueMicrotask` zorgt dat de toast pas na render wordt getoond.

    **Stap 5: Tests laten slagen** door `migrateSectorAnalyses` te exporteren en te importeren in het testbestand.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/sector-migration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/schemas.ts bevat `sectorAnalyses: z.record(z.string(), AISectorplanAnalyseSchema).optional()` (geen `z.string()` meer)
    - src/lib/session-context.tsx bevat `export function migrateSectorAnalyses(`
    - src/lib/session-context.tsx bevat `AISectorplanAnalyseSchema.safeParse(` (minstens 2 aanroepen)
    - src/lib/session-context.tsx loadSession bevat `migrateSectorAnalyses(` aanroep
    - src/lib/session-context.tsx loadSession bevat `addToastRef.current(` met "warning" type
    - src/lib/__tests__/sector-migration.test.ts bevat minstens 4 `it(` of `test(` blokken
    - `npx vitest run src/lib/__tests__/sector-migration.test.ts` exit code 0
  </acceptance_criteria>
  <done>
    Schema gewijzigd naar getypeerde objecten, migratielogica geschreven en getest, legacy string data wordt automatisch gemigreerd of verwijderd bij session laden. 4 tests groen.
  </done>
</task>

<task type="auto">
  <name>Task 2: SectorWerkStep typed object opslag en weergave</name>
  <files>
    src/components/steps/SectorWerkStep.tsx
  </files>
  <read_first>
    src/components/steps/SectorWerkStep.tsx,
    src/lib/schemas.ts,
    src/lib/types.ts,
    src/lib/session-context.tsx
  </read_first>
  <action>
    Refactor SectorWerkStep.tsx om direct met getypeerde SectorplanAnalyseResult objecten te werken in plaats van strings. Per D-02 en D-09.

    **Stap 1: Local state type wijzigen** (lijn 13-14).

    Van:
    ```typescript
    const [planAnalysis, setPlanAnalysisState] = useState<Record<string, string | null>>(
      session?.sectorAnalyses || {}
    );
    ```
    Naar:
    ```typescript
    const [planAnalysis, setPlanAnalysisState] = useState<Record<string, SectorplanAnalyseResult | null>>(
      (session?.sectorAnalyses as Record<string, SectorplanAnalyseResult | null>) || {}
    );
    ```

    **Stap 2: setPlanAnalysis helper wijzigen** (lijn 27-39).

    Van string-gebaseerde helper naar typed object helper:
    ```typescript
    function setPlanAnalysis(
      updater: (prev: Record<string, SectorplanAnalyseResult | null>) => Record<string, SectorplanAnalyseResult | null>
    ) {
      setPlanAnalysisState((prev) => {
        const next = updater(prev);
        // Sla op in sessie (filter null waarden)
        const cleaned: Record<string, SectorplanAnalyseResult> = {};
        Object.entries(next).forEach(([k, v]) => {
          if (v) cleaned[k] = v;
        });
        updateSession(() => ({ sectorAnalyses: cleaned }));
        return next;
      });
    }
    ```

    Let op: `updateSession(() => ({ sectorAnalyses: cleaned }))` zonder `...prev` spread -- de `updateSession` callback doet de merge al (Phase 2 patroon).

    **Stap 3: Analyse opslaan als object** (lijn 100-108).

    Van:
    ```typescript
    const analysisStr = typeof data.data.analysis === "string"
      ? data.data.analysis
      : JSON.stringify(data.data.analysis);
    setPlanAnalysis((prev) => ({
      ...prev,
      [activeSector]: analysisStr,
    }));
    ```
    Naar (per D-02):
    ```typescript
    const analysisObj: SectorplanAnalyseResult = data.data.analysis;
    setPlanAnalysis((prev) => ({
      ...prev,
      [activeSector]: analysisObj,
    }));
    ```

    **Stap 4: Error/fallback handling** (lijn 115-118).

    De fallback `setPlanAnalysis((prev) => ({ ...prev, [activeSector]: data.data?.message || "..." }))` slaat een string op in een typed veld. Dit moet null worden:
    ```typescript
    // Bij niet-succesvol resultaat: geen analyse opslaan
    setPlanAnalysis((prev) => ({
      ...prev,
      [activeSector]: null,
    }));
    ```
    Toon de error via `setAnalyzeError(data.data?.message || "Analyse niet beschikbaar.")` zodat de gebruiker feedback krijgt.

    **Stap 5: JSON.parse blok verwijderen** (lijn 538-546).

    Verwijder het volledige try/catch blok dat `JSON.parse(currentAnalysis)` doet. De `currentAnalysis` variabele is nu al een `SectorplanAnalyseResult | null` object. Wijzig:

    Van:
    ```typescript
    {currentAnalysis && (() => {
      let parsed: SectorplanAnalyseResult | null = null;
      try {
        const obj = JSON.parse(currentAnalysis);
        if (obj.samenvatting && obj.baten && obj.vermogens) {
          parsed = obj;
        }
      } catch { /* fallback naar platte tekst */ }

      const handleClose = () => setPlanAnalysis((prev) => ({ ...prev, [activeSector]: null }));

      if (parsed) {
        // ... structured rendering ...
      }

      // Fallback: platte tekst (oud formaat)
      return ( ... );
    })()}
    ```

    Naar:
    ```typescript
    {currentAnalysis && (() => {
      const handleClose = () => setPlanAnalysis((prev) => ({ ...prev, [activeSector]: null }));

      const domainColors: Record<string, { bg: string; text: string; dot: string }> = {
        mens: { bg: "bg-blue-50", text: "text-blue-700", dot: "bg-blue-500" },
        processen: { bg: "bg-green-50", text: "text-green-700", dot: "bg-green-500" },
        data_systemen: { bg: "bg-purple-50", text: "text-purple-700", dot: "bg-purple-500" },
        cultuur: { bg: "bg-amber-50", text: "text-amber-700", dot: "bg-amber-500" },
      };
      const domainLabels: Record<string, string> = {
        mens: "Mens",
        processen: "Processen",
        data_systemen: "Data & Systemen",
        cultuur: "Cultuur",
      };

      return (
        <div className="space-y-3">
          {/* Header */}
          {/* ... exact dezelfde JSX als het huidige parsed blok (lijn 564-715) ... */}
        </div>
      );
    })()}
    ```

    De hele structured rendering (lijn 564-715) blijft exact hetzelfde, maar leest nu van `currentAnalysis` direct in plaats van `parsed`. Vervang elke referentie naar `parsed.samenvatting` met `currentAnalysis.samenvatting`, `parsed.baten` met `currentAnalysis.baten`, etc.

    **Stap 6: Verwijder de fallback platte-tekst rendering** (lijn 719-739).

    Het hele `// Fallback: platte tekst (oud formaat)` blok wordt verwijderd. Na de schema-migratie in session-context.tsx zijn er geen string-waarden meer -- ongeldige data is al gefilterd.

    **Stap 7: Controleer type import**.

    Bovenaan het bestand importeert de component al `SectorplanAnalyseResult` uit `@/lib/types` (lijn 4). Controleer dat deze import aanwezig is.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/steps/SectorWerkStep.tsx bevat `useState<Record<string, SectorplanAnalyseResult | null>>` (niet `string | null`)
    - src/components/steps/SectorWerkStep.tsx bevat NIET `JSON.stringify(data.data.analysis)` of `JSON.parse(currentAnalysis)`
    - src/components/steps/SectorWerkStep.tsx bevat NIET de string "Fallback: platte tekst" of de markdown fallback `whitespace-pre-wrap` rendering
    - src/components/steps/SectorWerkStep.tsx bevat `const analysisObj: SectorplanAnalyseResult = data.data.analysis`
    - src/components/steps/SectorWerkStep.tsx bevat `updateSession(() => ({ sectorAnalyses: cleaned }))` (functionele updater zonder ...prev spread)
    - `npm run build` exit code 0 (geen TypeScript fouten door type mismatches)
  </acceptance_criteria>
  <done>
    SectorWerkStep slaat analyse-resultaten direct op als getypeerde objecten, de JSON.parse/stringify cyclus is verwijderd, en de weergave leest direct van het getypeerde object. Build slaagt zonder fouten.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/__tests__/sector-migration.test.ts` -- 4 tests groen
2. `npm run build` -- geen TypeScript compilatiefouten
3. Geen `JSON.stringify(data.data.analysis)` meer in SectorWerkStep.tsx
4. Geen `JSON.parse(currentAnalysis)` meer in SectorWerkStep.tsx
5. Schema lijn 337 bevat `AISectorplanAnalyseSchema` in plaats van `z.string()`
</verification>

<success_criteria>
- sectorAnalyses schema gewijzigd van Record<string, string> naar Record<string, AISectorplanAnalyseSchema>
- Migratielogica test 4/4 groen
- SectorWerkStep slaat objecten direct op (geen stringify)
- SectorWerkStep toont analyse direct vanuit object (geen parse)
- Build slaagt
</success_criteria>

<output>
After completion, create `.planning/phases/05-sectorwerk-doorstroming/05-01-SUMMARY.md`
</output>
