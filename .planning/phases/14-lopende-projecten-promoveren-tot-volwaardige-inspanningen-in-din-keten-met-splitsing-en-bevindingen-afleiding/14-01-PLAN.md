---
phase: 14-lopende-projecten-promoveren-tot-volwaardige-inspanningen-in-din-keten-met-splitsing-en-bevindingen-afleiding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/schemas.ts
  - src/lib/__tests__/external-project-schema.test.ts
  - src/lib/__tests__/project-promotion-schema.test.ts
  - src/lib/__tests__/project-promotion.test.ts
  - src/lib/din-service.ts
autonomous: true
requirements:
  - D-05
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-14
must_haves:
  truths:
    - "ExternalProjectSchema.parse accepts promotedAt + promotedToEffortIds; sessions zonder deze velden blijven parseable"
    - "DINEffortSchema.parse accepts originProjectId; bestaande efforts zonder veld blijven parseable"
    - "ProjectPromotieResultSchema enforced: capabilityMatches.min(1), splitEfforts.min(1).max(4), findings type enum ('baat'|'vermogen'|'inspanning')"
    - "promoteProjectToEfforts(session, id, result, selections) retourneert Partial<DINSession> die project markeert als promoted, nieuwe efforts + capabilityEffortMaps aanmaakt, oude projectCapabilityMaps voor dat project verwijdert"
    - "undoProjectPromotion(session, id) retourneert Partial<DINSession> die efforts verwijdert, capabilityEffortMaps opruimt, projectCapabilityMaps herstelt, promotedAt/promotedToEffortIds wist"
  artifacts:
    - path: src/lib/schemas.ts
      provides: "FindingSuggestionSchema, AIPromotedEffortSchema, AIPromotedBenefitMatchSchema, AIPromotedCapabilityMatchSchema, ProjectPromotieResultSchema + type exports; ExternalProjectSchema extended; DINEffortSchema extended"
      contains: "ProjectPromotieResultSchema"
    - path: src/lib/din-service.ts
      provides: "promoteProjectToEfforts() + undoProjectPromotion() pure functions"
      contains: "export function promoteProjectToEfforts"
    - path: src/lib/__tests__/project-promotion-schema.test.ts
      provides: "Zod schema tests voor ProjectPromotieResultSchema (valid, min 1 capability, min 1 / max 4 efforts, finding type enum, default status=in_uitvoering)"
    - path: src/lib/__tests__/project-promotion.test.ts
      provides: "Unit tests voor promoteProjectToEfforts() en undoProjectPromotion()"
  key_links:
    - from: src/lib/din-service.ts
      to: src/lib/schemas.ts
      via: "import type { ProjectPromotieResult } from './schemas'"
      pattern: "ProjectPromotieResult"
    - from: src/lib/__tests__/project-promotion.test.ts
      to: src/lib/din-service.ts
      via: "import { promoteProjectToEfforts, undoProjectPromotion }"
      pattern: "promoteProjectToEfforts"
---

<objective>
Fundament voor project-promotie: Zod schemas + TypeScript types + pure mutation helpers met volledige unit-test dekking. Geen AI-calls, geen UI — puur de data-laag zodat latere waves op een stabiele basis bouwen.

Purpose: Phase 14 positioneert lopende projecten als volwaardige inspanningen in de DIN-keten. Deze plan levert de single source of truth voor het promotie-resultaat (ProjectPromotieResultSchema) en de pure, testbare mutatie-helpers die Wave 2 (AI) en Wave 3 (UI) gebruiken. Door alles in één plan te houden blijft het schema + de mutatie + de tests commit-bare unit die atomisch correct is.

Output: Uitgebreide schemas.ts, twee nieuwe test-bestanden, uitgebreide external-project-schema.test.ts, twee nieuwe pure functies in din-service.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-lopende-projecten-promoveren-tot-volwaardige-inspanningen-in-din-keten-met-splitsing-en-bevindingen-afleiding/14-CONTEXT.md
@.planning/phases/14-lopende-projecten-promoveren-tot-volwaardige-inspanningen-in-din-keten-met-splitsing-en-bevindingen-afleiding/14-RESEARCH.md
@.planning/phases/14-lopende-projecten-promoveren-tot-volwaardige-inspanningen-in-din-keten-met-splitsing-en-bevindingen-afleiding/14-VALIDATION.md
@src/lib/schemas.ts
@src/lib/din-service.ts
@src/lib/__tests__/external-project-schema.test.ts

<interfaces>
<!-- Exact schema shapes that must be added — copied verbatim from RESEARCH.md §Example 1 and §Example 3 -->

Reference (existing, line numbers in schemas.ts):
- Line 136-153: DINEffortSchema (add originProjectId)
- Line 155-166: ExternalProjectSchema (add promotedAt + promotedToEffortIds)
- Line 475: efforts: z.array(DINEffortSchema) in DINSessionSchema
- Line 486: externalProjects: z.array(ExternalProjectSchema).optional() in DINSessionSchema
- Line 778-779: type exports DINEffort, ExternalProject

Existing related schemas to reuse:
- SectorNameSchema (for FindingSuggestion.targetSector)
- EffortDomainSchema (for AIPromotedEffort.domain and FindingSuggestion.domain)
- EffortStatusSchema (for AIPromotedEffort.status)
- InspanningsDossierSchema (for AIPromotedEffort.dossier — already optional)

Reference implementation in callClaudeWithValidation:
- `src/lib/ai-client.ts` §62-155 — existing Zod-validation pipeline pattern
- `src/lib/ai-client.ts` line 751+ — matchProjectsToCapabilities as nearest sibling function
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ExternalProjectSchema + DINEffortSchema + write Zod schema tests (RED → GREEN)</name>
  <files>src/lib/schemas.ts, src/lib/__tests__/external-project-schema.test.ts</files>
  <read_first>
    - src/lib/schemas.ts (current state of DINEffortSchema §136-153 and ExternalProjectSchema §155-166; type exports §778-779)
    - src/lib/__tests__/external-project-schema.test.ts (existing Test 1-10 pattern, append Tests 11-16)
    - .planning/phases/14-lopende-projecten-promoveren-tot-volwaardige-inspanningen-in-din-keten-met-splitsing-en-bevindingen-afleiding/14-RESEARCH.md §Example 3 (exact diff) and §Example 4 (test pattern)
    - .planning/phases/14-lopende-projecten-promoveren-tot-volwaardige-inspanningen-in-din-keten-met-splitsing-en-bevindingen-afleiding/14-RESEARCH.md Pitfall 1 (`.optional()` WITHOUT `.default()` — critical)
  </read_first>
  <behavior>
    - Test 11: ExternalProjectSchema.parse accepts promotedAt ISO string → result.promotedAt === "2026-04-05T12:34:56.000Z"
    - Test 12: accepts promotedToEffortIds array → result.promotedToEffortIds equals ["eff-1", "eff-2"]
    - Test 13: backward-compat — baseProject without promoted fields still parses; promotedAt/promotedToEffortIds undefined
    - Test 14: promotedToEffortIds NOT defaulted to [] (Pitfall 1) — parsed minimal project has promotedToEffortIds === undefined
    - Test 15: DINEffortSchema accepts originProjectId → result.originProjectId === "proj-uuid-xyz"
    - Test 16: DINEffortSchema backward-compat — baseEffort without originProjectId parses; result.originProjectId undefined
  </behavior>
  <action>
    RED first: append the test block from 14-RESEARCH.md §Example 4 verbatim to `src/lib/__tests__/external-project-schema.test.ts` (lines 877-938). It adds two `describe` blocks: "ExternalProjectSchema Phase 14 promotion fields" with Tests 11-14, and "DINEffortSchema Phase 14 originProjectId" with Tests 15-16. Run `npx vitest run src/lib/__tests__/external-project-schema.test.ts` → expect RED on all 6 new tests.

    GREEN: edit `src/lib/schemas.ts`:

    1. In `DINEffortSchema` (§136-153), append **ONE** new line before the closing `})`:
       ```
       originProjectId: z.string().optional(), // Phase 14 D-09
       ```

    2. In `ExternalProjectSchema` (§155-166), append **TWO** new lines before the closing `})`:
       ```
       promotedAt: z.string().optional(), // Phase 14 D-08
       promotedToEffortIds: z.array(z.string()).optional(), // Phase 14 D-08 — NO .default() per Pitfall 1
       ```

    CRITICAL: Do NOT add `.default([])` to `promotedToEffortIds`. Pitfall 1 in RESEARCH.md documents that `.optional().default([])` breaks TypeScript inference and causes createEffort/createBenefit/createCapability helpers to fail compilation. Use bare `.optional()`.

    Run `npx vitest run src/lib/__tests__/external-project-schema.test.ts` → expect GREEN on all 16 tests. Then run `npm run build` to verify no TypeScript regressions.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/external-project-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep `originProjectId: z.string().optional()` in src/lib/schemas.ts returns 1 match
    - grep `promotedAt: z.string().optional()` in src/lib/schemas.ts returns 1 match
    - grep `promotedToEffortIds: z.array(z.string()).optional()` in src/lib/schemas.ts returns 1 match
    - grep `promotedToEffortIds` in src/lib/schemas.ts does NOT contain `.default`
    - `npx vitest run src/lib/__tests__/external-project-schema.test.ts` exits 0 with "Test 16" in output (16 tests pass)
    - `npm run build` exits 0 with no TypeScript errors
  </acceptance_criteria>
  <done>6 nieuwe tests GREEN, schemas backward-compatible, build succeeds zonder regressies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ProjectPromotieResultSchema family + tests (RED → GREEN)</name>
  <files>src/lib/schemas.ts, src/lib/__tests__/project-promotion-schema.test.ts</files>
  <read_first>
    - src/lib/schemas.ts (updated after Task 1 — find location after AIProjectCapabilityMatchResponseSchema §714 for insert point)
    - .planning/phases/14-lopende-projecten-promoveren-tot-volwaardige-inspanningen-in-din-keten-met-splitsing-en-bevindingen-afleiding/14-RESEARCH.md §Pattern 1 (verbatim schema code lines 182-237) and §Example 5 (verbatim test code lines 942-1028)
    - src/lib/__tests__/schemas.test.ts (follow describe/it naming + import style)
  </read_first>
  <behavior>
    - accepts full valid response (benefitMatches + capabilityMatches + 1 splitEffort + 1 finding + samenvatting)
    - rejects 0 split efforts (`.min(1)` enforcement per D-05)
    - rejects 5 split efforts (`.max(4)` enforcement per D-05)
    - rejects 0 capability matches (`.min(1)` — project must land in keten)
    - accepts empty benefitMatches AND empty findings (soft defaults via `.optional().default([])`)
    - defaults status to `in_uitvoering` when AI omits it on splitEfforts[0] (D-07)
    - rejects finding with invalid type (e.g., "risico" — not in enum 'baat'|'vermogen'|'inspanning')
  </behavior>
  <action>
    RED first: create `src/lib/__tests__/project-promotion-schema.test.ts`. Copy the test code verbatim from 14-RESEARCH.md §Example 5 (lines 942-1028). Top imports:
    ```typescript
    import { describe, it, expect } from "vitest";
    import { ProjectPromotieResultSchema } from "@/lib/schemas";
    ```
    Run `npx vitest run src/lib/__tests__/project-promotion-schema.test.ts` → expect RED (ProjectPromotieResultSchema does not exist).

    GREEN: in `src/lib/schemas.ts`, add the schema block below `AIProjectCapabilityMatchResponseSchema` (search for that export to find insert point, around §714). Copy verbatim from 14-RESEARCH.md §Pattern 1 lines 182-237 — this includes 5 schemas and 5 type exports:

    1. `FindingSuggestionSchema` — z.object({ type: z.enum(["baat","vermogen","inspanning"]), beschrijving: z.string(), toelichting: z.string(), targetSector: SectorNameSchema, domain: EffortDomainSchema.optional() })
    2. `AIPromotedEffortSchema` — with title, description, domain (EffortDomainSchema), status (EffortStatusSchema.optional().default("in_uitvoering")), quarter optional, responsibleSector optional, dossier (InspanningsDossierSchema.optional() — NOTE: RESEARCH.md shows inline shape, but prefer reusing existing InspanningsDossierSchema.optional() for consistency; if that schema has different field names, fall back to the inline shape from RESEARCH.md lines 199-207), rationale optional
    3. `AIPromotedBenefitMatchSchema` — z.object({ benefitId: z.string(), toelichting: z.string() })
    4. `AIPromotedCapabilityMatchSchema` — z.object({ capabilityId: z.string(), toelichting: z.string() })
    5. `ProjectPromotieResultSchema` — z.object({ benefitMatches: z.array(AIPromotedBenefitMatchSchema).optional().default([]), capabilityMatches: z.array(AIPromotedCapabilityMatchSchema).min(1), splitEfforts: z.array(AIPromotedEffortSchema).min(1).max(4), findings: z.array(FindingSuggestionSchema).optional().default([]), samenvatting: z.string() })

    Type exports (below each or together):
    ```typescript
    export type FindingSuggestion = z.infer<typeof FindingSuggestionSchema>;
    export type AIPromotedEffort = z.infer<typeof AIPromotedEffortSchema>;
    export type AIPromotedBenefitMatch = z.infer<typeof AIPromotedBenefitMatchSchema>;
    export type AIPromotedCapabilityMatch = z.infer<typeof AIPromotedCapabilityMatchSchema>;
    export type ProjectPromotieResult = z.infer<typeof ProjectPromotieResultSchema>;
    ```

    If `InspanningsDossierSchema` does not exist or has incompatible fields, use the inline object shape from RESEARCH.md lines 199-207 (eigenaar/inspanningsleider/verwachtResultaat/kostenraming/randvoorwaarden — all z.string().optional().default("")).

    Run tests → expect GREEN on all 7 tests.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/project-promotion-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - file src/lib/__tests__/project-promotion-schema.test.ts exists
    - grep `export const ProjectPromotieResultSchema` in src/lib/schemas.ts returns 1 match
    - grep `export const FindingSuggestionSchema` in src/lib/schemas.ts returns 1 match
    - grep `export const AIPromotedEffortSchema` in src/lib/schemas.ts returns 1 match
    - grep `export type ProjectPromotieResult` in src/lib/schemas.ts returns 1 match
    - grep `splitEfforts: z.array(AIPromotedEffortSchema).min(1).max(4)` in src/lib/schemas.ts returns 1 match
    - grep `capabilityMatches: z.array(AIPromotedCapabilityMatchSchema).min(1)` in src/lib/schemas.ts returns 1 match
    - `npx vitest run src/lib/__tests__/project-promotion-schema.test.ts` exits 0 with 7 tests passing
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>ProjectPromotieResultSchema family volledig gedefinieerd, 7 Zod tests GREEN, type exports beschikbaar voor downstream plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pure mutation helpers promoteProjectToEfforts + undoProjectPromotion + unit tests</name>
  <files>src/lib/din-service.ts, src/lib/__tests__/project-promotion.test.ts</files>
  <read_first>
    - src/lib/din-service.ts (complete file — understand existing export pattern, generateId usage, where to append new functions at end)
    - src/lib/types.ts (DINSession, DINEffort, DINBenefit, DINCapability, ExternalProject, CapabilityEffortMap, ProjectCapabilityMap type shapes)
    - src/lib/schemas.ts (updated — ProjectPromotieResult, AIPromotedEffort, FindingSuggestion types from Task 2)
    - .planning/phases/14-lopende-projecten-promoveren-tot-volwaardige-inspanningen-in-din-keten-met-splitsing-en-bevindingen-afleiding/14-RESEARCH.md §Pattern 3 (verbatim function code lines 313-510)
    - src/lib/__tests__/consolidation.test.ts (mirror this file's structure: import pure functions from din-service, build minimal DINSession fixture, assert Partial<DINSession> return shape)
  </read_first>
  <behavior>
    - promoteProjectToEfforts with 1 keptEffort + 1 acceptedCapability: returns Partial<DINSession> where externalProjects[project].promotedAt is an ISO string, promotedToEffortIds has 1 entry, efforts has +1 new entry with originProjectId === project.id, capabilityEffortMaps has +1 row linking capId→effortId, projectCapabilityMaps for this project are removed
    - promoteProjectToEfforts with 3 keptEfforts + 2 acceptedCapabilities: 3 new efforts, 6 new capabilityEffortMap rows (cartesian product)
    - promoteProjectToEfforts with acceptedFindings of type 'vermogen': pushes 1 new DINCapability with sectorId=targetSector
    - promoteProjectToEfforts with acceptedFindings of type 'inspanning': pushes 1 new DINEffort with originProjectId set
    - promoteProjectToEfforts returns {} when projectId not found in session.externalProjects
    - undoProjectPromotion: removes efforts with originProjectId === projectId, removes their capabilityEffortMaps, restores projectCapabilityMaps with deduped capabilityId entries, clears promotedAt + promotedToEffortIds on project
    - undoProjectPromotion returns {} when project.promotedAt is undefined (no-op)
    - undoProjectPromotion: findings-created entities are NOT rolled back (documented behavior — test asserts finding-created DINCapability from a prior promote call is still present after undo)
  </behavior>
  <action>
    RED first: create `src/lib/__tests__/project-promotion.test.ts`. Structure:
    ```typescript
    import { describe, it, expect } from "vitest";
    import { promoteProjectToEfforts, undoProjectPromotion } from "@/lib/din-service";
    import type { DINSession } from "@/lib/types";
    import type { ProjectPromotieResult, AIPromotedEffort, FindingSuggestion } from "@/lib/schemas";
    ```
    Build a minimal `makeSession()` fixture helper that returns a DINSession with: 1 externalProject (id="proj1", sectorId="PO", name="Test", description="...", status="in_uitvoering"), 2 existing capabilities (cap1, cap2), 1 existing projectCapabilityMap ({projectId:"proj1", capabilityId:"cap1"}), empty efforts/benefits/capabilityEffortMaps.

    Write 8 tests matching the behavior block above. Each test:
    1. constructs a ProjectPromotieResult fixture
    2. constructs a PromotionSelections fixture
    3. calls `promoteProjectToEfforts(session, "proj1", result, selections)` or `undoProjectPromotion(...)`
    4. asserts on the returned Partial<DINSession>

    Run → expect RED (functions don't exist yet).

    GREEN: append both functions to `src/lib/din-service.ts`. Copy verbatim from 14-RESEARCH.md §Pattern 3 lines 313-510. The file already imports/exports generateId so no new import needed for that. Add imports at top if missing:
    ```typescript
    import type { DINSession, ExternalProject, DINEffort, CapabilityEffortMap, ProjectCapabilityMap, DINBenefit, DINCapability } from "./types";
    import type { ProjectPromotieResult, AIPromotedEffort, FindingSuggestion } from "./schemas";
    ```
    The `PromotionSelections` interface goes inline in din-service.ts (exported) per RESEARCH.md lines 318-323:
    ```typescript
    export interface PromotionSelections {
      acceptedBenefitIds: string[];
      acceptedCapabilityIds: string[];
      keptEfforts: AIPromotedEffort[];
      acceptedFindings: FindingSuggestion[];
    }
    ```

    For findings of type 'baat', use `goalId: ""` placeholder as documented in RESEARCH.md line 413 (open question #2 — goal picker is handled in UI Wave 3). For this task, testing baat-finding behavior is out of scope; focus on vermogen + inspanning finding tests only.

    Ensure the function returns a Partial<DINSession> (not a full mutation), matching the signature in RESEARCH.md line 340-344. The test asserts `result.externalProjects[0].promotedAt` is a non-empty string (not exact match, use `expect.any(String)`).

    For undoProjectPromotion: mirror RESEARCH.md lines 465-510. Critical detail: `restoredProjectCapMaps` uses a `seen` Set to dedupe per `projectId:capabilityId` (lines 483-490 of RESEARCH.md).

    Run tests → expect GREEN on all 8 tests.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/project-promotion.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - file src/lib/__tests__/project-promotion.test.ts exists with at least 8 `it(` blocks
    - grep `export function promoteProjectToEfforts` in src/lib/din-service.ts returns 1 match
    - grep `export function undoProjectPromotion` in src/lib/din-service.ts returns 1 match
    - grep `export interface PromotionSelections` in src/lib/din-service.ts returns 1 match
    - grep `originProjectId: projectId` in src/lib/din-service.ts returns at least 1 match (from promote + finding-inspanning)
    - `npx vitest run src/lib/__tests__/project-promotion.test.ts` exits 0 with 8 tests passing
    - `npx vitest run` (full suite) exits 0 — no regressions in other tests
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Twee pure mutation functies volledig getest; data-laag klaar voor Wave 2 (AI) en Wave 3 (UI).</done>
</task>

</tasks>

<verification>
Run all three automated commands in sequence:
1. `npx vitest run src/lib/__tests__/external-project-schema.test.ts` (Task 1 — 16 tests)
2. `npx vitest run src/lib/__tests__/project-promotion-schema.test.ts` (Task 2 — 7 tests)
3. `npx vitest run src/lib/__tests__/project-promotion.test.ts` (Task 3 — 8 tests)
4. `npx vitest run` (full suite regression check)
5. `npm run build` (TypeScript + Next.js build must pass)
</verification>

<success_criteria>
- 31 nieuwe tests GREEN (16 schema ext + 7 result schema + 8 mutation)
- Geen regressies in bestaande 200+ tests
- `npm run build` succeeds
- schemas.ts, din-service.ts, en beide nieuwe test-bestanden committed
</success_criteria>

<output>
After completion, create `.planning/phases/14-lopende-projecten-promoveren-tot-volwaardige-inspanningen-in-din-keten-met-splitsing-en-bevindingen-afleiding/14-01-SUMMARY.md` documenting: schemas added, mutation functions added, test counts, any deviations from the RESEARCH.md templates (e.g., InspanningsDossierSchema reuse vs inline).
</output>
