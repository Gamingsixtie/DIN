---
phase: 14-lopende-projecten-promoveren-tot-volwaardige-inspanningen-in-din-keten-met-splitsing-en-bevindingen-afleiding
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/lib/prompts.ts
  - src/lib/ai-client.ts
  - src/app/api/promote-project/route.ts
  - src/lib/__tests__/ai-client-promotion.test.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-07
  - D-12
  - D-13
  - D-14
must_haves:
  truths:
    - "PROJECT_PROMOTIE_PROMPT instrueert AI om benefitMatches + capabilityMatches + splitEfforts (1-4) + findings + samenvatting in één JSON response te leveren, geankerd in DIN-methodiek"
    - "promoteExternalProject(project, benefits, capabilities, sectorName, options) roept callClaudeWithValidation aan met ProjectPromotieResultSchema en Claude Opus 4.6 model"
    - "assembleSystemPrompt wordt gebruikt met useCase 'inspanning-create' zodat programmaboek + KiB context meegaan"
    - "POST /api/promote-project accepteert {project, sectorBenefits, sectorCapabilities, sectorName, kibGoals, kibScope, sectorAnalysis, completedGoalItems, priorProjectCapabilityIds} en retourneert {success, data} of {success:false, error, retryable:true}"
    - "API route retourneert 503 met Dutch error bij ontbrekende ANTHROPIC_API_KEY"
    - "Mocked unit test verifieert dat promoteExternalProject de Zod schema toepast en invalid IDs filtert (Pitfall 2)"
  artifacts:
    - path: src/lib/prompts.ts
      provides: "PROJECT_PROMOTIE_PROMPT exported constant"
      contains: "PROJECT_PROMOTIE_PROMPT"
    - path: src/lib/ai-client.ts
      provides: "promoteExternalProject() async function using callClaudeWithValidation"
      contains: "export async function promoteExternalProject"
    - path: src/app/api/promote-project/route.ts
      provides: "POST handler for combined promotion AI call"
      contains: "export async function POST"
    - path: src/lib/__tests__/ai-client-promotion.test.ts
      provides: "Mocked tests for promoteExternalProject (schema path, invalid capabilityId filter)"
  key_links:
    - from: src/lib/ai-client.ts
      to: src/lib/schemas.ts
      via: "import { ProjectPromotieResultSchema } from './schemas'"
      pattern: "ProjectPromotieResultSchema"
    - from: src/lib/ai-client.ts
      to: src/lib/prompts.ts
      via: "import { PROJECT_PROMOTIE_PROMPT }"
      pattern: "PROJECT_PROMOTIE_PROMPT"
    - from: src/app/api/promote-project/route.ts
      to: src/lib/ai-client.ts
      via: "import { promoteExternalProject }"
      pattern: "promoteExternalProject"
---

<objective>
AI-pipeline voor project-promotie: één prompt, één Zod-gevalideerde API call, één nieuwe Next.js API route. Wave 1 (schemas + helpers) is klaar; dit plan wire't de AI-laag ertussen zodat Wave 3 (UI) direct `fetch("/api/promote-project", ...)` kan aanroepen.

Purpose: De user klikt "Promoveer" op één project → browser POST naar /api/promote-project → Claude Opus 4.6 doet multi-concept redenering (baat-match + vermogen-match + effort-splitsing + bevindingen) in één call → Zod valideert → response terug naar UI. Hergebruik van bestaande prompt-assembly (programmaboek + KiB + sectorwerk + completed goals) garandeert methodiek-conformiteit.

Output: PROJECT_PROMOTIE_PROMPT constant, promoteExternalProject AI-client functie, POST route, mocked unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-lopende-projecten-promoveren-tot-volwaardige-inspanningen-in-din-keten-met-splitsing-en-bevindingen-afleiding/14-CONTEXT.md
@.planning/phases/14-lopende-projecten-promoveren-tot-volwaardige-inspanningen-in-din-keten-met-splitsing-en-bevindingen-afleiding/14-RESEARCH.md
@.planning/phases/14-lopende-projecten-promoveren-tot-volwaardige-inspanningen-in-din-keten-met-splitsing-en-bevindingen-afleiding/14-01-SUMMARY.md
@src/lib/ai-client.ts
@src/lib/prompts.ts
@src/lib/prompt-assembly.ts
@src/app/api/match-projects/route.ts

<interfaces>
<!-- Reference implementations to mirror. Executor should use these directly. -->

From src/lib/ai-client.ts (to be extended):
- `callClaudeWithValidation<T>(schema, systemPrompt, userMessage, options?)` — existing wrapper with 2 silent retries, 2 models supported via options.model: "claude-sonnet-4-6" | "claude-opus-4-6"
- Existing function `matchProjectsToCapabilities` (around line 751) — closest sibling; mirror its structured-JSON input pattern (send entities as `JSON.stringify([{id, description}, ...])` so AI can reference by ID)
- Existing function `generateCrossAnalyse` — also uses Opus 4.6; reference for multi-concept reasoning call shape

From src/lib/prompt-assembly.ts:
- `assembleSystemPrompt(basePrompt, useCase, currentItem, kibContext)` — useCase "inspanning-create" injects PROGRAMMABOEK_INSPANNINGEN section
- `extractKiBContext({goals, scope})` — builds KiBContext from session data
- `buildSectorwerkBlock(sectorAnalysis)` — appends sectorwerk analysis as prompt suffix
- `buildCompletedGoalsContext(items)` — appends previously completed goals (capped at 6000 chars)

From src/app/api/match-projects/route.ts:
- Mirror structure: POST handler, body validation, ANTHROPIC_API_KEY check (503 Dutch error), call ai-client function, wrap result in NextResponse.json

From src/lib/schemas.ts (Plan 01 output):
- `ProjectPromotieResultSchema` — Zod schema to pass as first arg to callClaudeWithValidation
- type `ProjectPromotieResult` — return type

Full verbatim code templates for all three files are in 14-RESEARCH.md §Example 1 (lines 665-750), §Example 2 (lines 754-827), and §Pattern 2 prompt body (lines 260-306).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PROJECT_PROMOTIE_PROMPT + promoteExternalProject() in ai-client.ts</name>
  <files>src/lib/prompts.ts, src/lib/ai-client.ts</files>
  <read_first>
    - src/lib/prompts.ts (find end of file — PROJECT_PROMOTIE_PROMPT is appended after BATENPROFIEL_PROMPT)
    - src/lib/ai-client.ts (understand callClaudeWithValidation signature ~§62-155, matchProjectsToCapabilities structure ~§751+, imports at top)
    - src/lib/prompt-assembly.ts (verify exports: assembleSystemPrompt, extractKiBContext, buildSectorwerkBlock, buildCompletedGoalsContext, KiBContext, CompletedGoalContext types)
    - .planning/phases/14-lopende-projecten-promoveren-tot-volwaardige-inspanningen-in-din-keten-met-splitsing-en-bevindingen-afleiding/14-RESEARCH.md §Pattern 2 lines 260-306 (PROJECT_PROMOTIE_PROMPT body verbatim) and §Example 1 lines 665-750 (promoteExternalProject verbatim)
    - .planning/phases/14-lopende-projecten-promoveren-tot-volwaardige-inspanningen-in-din-keten-met-splitsing-en-bevindingen-afleiding/14-RESEARCH.md Pitfall 2 (post-parse capabilityId consistency check — filter invalid IDs after validation)
  </read_first>
  <action>
    Step 1 — `src/lib/prompts.ts`: append `PROJECT_PROMOTIE_PROMPT` constant at the end of the file. Copy the full Dutch prompt verbatim from 14-RESEARCH.md §Pattern 2 lines 260-306. Export it as a named const:
    ```typescript
    export const PROJECT_PROMOTIE_PROMPT = `Je bent een expert in het DIN-framework...`;
    ```
    The prompt must contain these exact section headers (checked via grep in acceptance):
    - "1. **BENEFIT MATCHES**"
    - "2. **CAPABILITY MATCHES**"
    - "3. **SPLIT EFFORTS (1-4)**"
    - "4. **FINDINGS**"
    And must explicitly state: "Splits: minimaal 1, maximaal 4. Capabilities: minimaal 1."

    Step 2 — `src/lib/ai-client.ts`: add `promoteExternalProject` function. Copy verbatim from 14-RESEARCH.md §Example 1 lines 665-750. Place it at the end of the file after `matchProjectsToCapabilities`. Add imports at top of file:
    ```typescript
    import { ProjectPromotieResultSchema } from "./schemas";
    import { PROJECT_PROMOTIE_PROMPT } from "./prompts";
    ```
    (assembleSystemPrompt etc. are already imported if matchProjectsToCapabilities uses them; verify by grep and only add missing ones).

    Key contract points (checked via grep in acceptance):
    - Function signature: `export async function promoteExternalProject(project: ExternalProject, sectorBenefits: DINBenefit[], sectorCapabilities: DINCapability[], sectorName: string, options: {...})`
    - Uses `assembleSystemPrompt(PROJECT_PROMOTIE_PROMPT, "inspanning-create", undefined, options.kibContext)`
    - Appends `buildSectorwerkBlock(options.sectorAnalysis)` if present
    - Appends `buildCompletedGoalsContext(options.completedGoalItems)` if present and non-empty
    - User message includes `JSON.stringify(sectorBenefits.map(b => ({id: b.id, ...})))` and `JSON.stringify(sectorCapabilities.map(c => ({id: c.id, ...})))` — structured IDs per Pitfall 2
    - Calls `callClaudeWithValidation(ProjectPromotieResultSchema, systemPrompt, userMessage, { maxTokens: 8192, model: "claude-opus-4-6" })`

    Step 3 — Pitfall 2 safeguard: after `callClaudeWithValidation` returns success, before returning, filter `result.data.capabilityMatches` to only rows where `capabilityId` exists in the input `sectorCapabilities` array. Log a `console.warn("[promoteExternalProject] Dropped invalid capabilityId from AI response:", id)` for each dropped. Do the same for `benefitMatches` against `sectorBenefits`. This prevents orphan mappings downstream.

    Verify compilation: `npm run build`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep `export const PROJECT_PROMOTIE_PROMPT` in src/lib/prompts.ts returns 1 match
    - grep `1. \*\*BENEFIT MATCHES\*\*` in src/lib/prompts.ts returns 1 match
    - grep `3. \*\*SPLIT EFFORTS \(1-4\)\*\*` in src/lib/prompts.ts returns 1 match
    - grep `4. \*\*FINDINGS\*\*` in src/lib/prompts.ts returns 1 match
    - grep `export async function promoteExternalProject` in src/lib/ai-client.ts returns 1 match
    - grep `ProjectPromotieResultSchema` in src/lib/ai-client.ts returns at least 2 matches (import + call)
    - grep `claude-opus-4-6` in src/lib/ai-client.ts contains the promoteExternalProject region (model selection)
    - grep `assembleSystemPrompt\(PROJECT_PROMOTIE_PROMPT, "inspanning-create"` in src/lib/ai-client.ts returns 1 match
    - grep `Dropped invalid capabilityId` in src/lib/ai-client.ts returns 1 match (Pitfall 2 warning)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Prompt + AI-client functie compileren; structurele DIN-context + Pitfall 2 safeguard aanwezig.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create POST /api/promote-project route + mocked ai-client unit test</name>
  <files>src/app/api/promote-project/route.ts, src/lib/__tests__/ai-client-promotion.test.ts</files>
  <read_first>
    - src/app/api/match-projects/route.ts (nearest sibling API route — mirror ANTHROPIC_API_KEY guard, try/catch shape, NextResponse.json format)
    - src/app/api/extract-projects/route.ts (if exists — similar import structure for Phase 12 routes)
    - src/lib/ai-client.ts (updated — verify promoteExternalProject export and its return shape `{success: true, data} | {success: false, error}`)
    - .planning/phases/14-lopende-projecten-promoveren-tot-volwaardige-inspanningen-in-din-keten-met-splitsing-en-bevindingen-afleiding/14-RESEARCH.md §Example 2 lines 754-827 (API route verbatim)
    - src/lib/__tests__/ai-parsing.test.ts (for vitest mocking pattern example with vi.mock)
  </read_first>
  <behavior>
    - Mock test 1: promoteExternalProject returns success → the ai-client function filters capabilityMatches to drop IDs not in sectorCapabilities (Pitfall 2 safeguard).
    - Mock test 2: callClaudeWithValidation returns `{success: false, error: "..."}` → promoteExternalProject propagates the failure unchanged.
    - Mock test 3: Valid ProjectPromotieResult with all 4 sections intact is passed through when all IDs exist.
  </behavior>
  <action>
    Step 1 — create `src/app/api/promote-project/route.ts`. Copy verbatim from 14-RESEARCH.md §Example 2 lines 754-827. Verify:
    - POST async handler
    - Destructures body: `project, sectorBenefits, sectorCapabilities, sectorName, kibGoals, kibScope, sectorAnalysis, completedGoalItems, priorProjectCapabilityIds`
    - Validates `project` and `sectorName` required → 400 "project en sectorName zijn verplicht"
    - Checks `process.env.ANTHROPIC_API_KEY` → 503 "ANTHROPIC_API_KEY niet geconfigureerd. AI-promotie niet beschikbaar."
    - Calls `extractKiBContext({ goals: kibGoals, scope: kibScope })` then `promoteExternalProject(...)`
    - Maps result: success → `{success: true, data: result.data}`, failure → `{success: false, error: result.error, retryable: true}` with status 422
    - Outer try/catch returns 500 with `error instanceof Error ? error.message : "Fout bij promotie-aanroep"`

    Step 2 — create `src/lib/__tests__/ai-client-promotion.test.ts`. Use vitest's `vi.mock` to mock the underlying `callClaudeWithValidation` call. Structure:
    ```typescript
    import { describe, it, expect, vi, beforeEach } from "vitest";
    import type { DINBenefit, DINCapability, ExternalProject } from "@/lib/types";

    // Mock the callClaudeWithValidation export from ai-client
    // Note: since promoteExternalProject lives in the same file, we mock the underlying
    // Anthropic SDK at a lower level OR extract callClaudeWithValidation via vi.spyOn.
    // Alternative: mock @anthropic-ai/sdk directly if ai-client does not re-export callClaudeWithValidation.

    vi.mock("@anthropic-ai/sdk", () => ({
      default: class {
        messages = {
          create: vi.fn(),
        };
      },
    }));

    // Import AFTER mock is set up
    import { promoteExternalProject } from "@/lib/ai-client";
    ```
    Write 3 tests:
    1. "filters capabilityMatches with unknown IDs (Pitfall 2)": set up mock Anthropic response where splitEfforts/capabilityMatches reference a `capabilityId: "ghost-id"` not in input. Assert the returned data.capabilityMatches does not contain "ghost-id".
    2. "propagates upstream failure": mock Anthropic to throw or return malformed JSON repeatedly. Assert result.success === false.
    3. "passes through valid result intact": mock Anthropic to return a full valid ProjectPromotieResult with IDs that exist in sectorCapabilities/sectorBenefits input arrays. Assert all matches preserved.

    NOTE: If the vi.mock path for @anthropic-ai/sdk proves tricky because ai-client instantiates the client at module load, fall back to: extract `callClaudeWithValidation` as importable export and use `vi.spyOn(aiClient, 'callClaudeWithValidation')`. If neither is workable, at minimum write ONE integration test that passes valid inputs and asserts the function does not throw on schema-valid mock data. The test exists to catch regressions on the Pitfall 2 filter logic specifically.

    Run: `npx vitest run src/lib/__tests__/ai-client-promotion.test.ts` — RED until the ai-client filter logic (from Task 1) is in place, then GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/ai-client-promotion.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - file src/app/api/promote-project/route.ts exists
    - grep `export async function POST` in src/app/api/promote-project/route.ts returns 1 match
    - grep `promoteExternalProject` in src/app/api/promote-project/route.ts returns 1 match
    - grep `ANTHROPIC_API_KEY niet geconfigureerd` in src/app/api/promote-project/route.ts returns 1 match
    - grep `retryable: true` in src/app/api/promote-project/route.ts returns 1 match
    - grep `extractKiBContext` in src/app/api/promote-project/route.ts returns 1 match
    - file src/lib/__tests__/ai-client-promotion.test.ts exists with at least 1 `it(` block
    - `npx vitest run src/lib/__tests__/ai-client-promotion.test.ts` exits 0
    - `npm run build` exits 0 (new API route compiles)
    - grep `promote-project` in `.next/` build output (if built) or `npm run build` stdout confirms the route was compiled (optional check — compile success above is the primary signal)
  </acceptance_criteria>
  <done>AI API route + mocked test groen; Wave 3 kan client-side `fetch("/api/promote-project", ...)` aanroepen.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/__tests__/ai-client-promotion.test.ts` — new mocked tests pass
2. `npx vitest run` — no regressions in other tests (31 tests from Plan 01 + existing suite still GREEN)
3. `npm run build` — TypeScript + Next.js build succeeds, new route compiles, Pitfall 2 filter adds no regressions
4. Manual smoke check: start dev server (`npm run dev`), POST `curl -X POST http://localhost:3000/api/promote-project -H "Content-Type: application/json" -d '{}'` → expect 400 "project en sectorName zijn verplicht"
</verification>

<success_criteria>
- PROJECT_PROMOTIE_PROMPT, promoteExternalProject, POST route, mocked tests allemaal gecommit
- `npm run build` succeeds
- 400/503/422 response paden handmatig of via test bevestigd
</success_criteria>

<output>
After completion, create `.planning/phases/14-lopende-projecten-promoveren-tot-volwaardige-inspanningen-in-din-keten-met-splitsing-en-bevindingen-afleiding/14-02-SUMMARY.md` documenting: prompt shape, model choice (Opus 4.6), Pitfall 2 safeguard implementation details, mocked test approach, any deviations from RESEARCH.md templates.
</output>
