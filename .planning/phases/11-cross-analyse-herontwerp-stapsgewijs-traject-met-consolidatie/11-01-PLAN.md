---
phase: 11-cross-analyse-herontwerp-stapsgewijs-traject-met-consolidatie
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/schemas.ts
  - src/lib/prompts.ts
  - src/app/api/cross-analyse/route.ts
autonomous: true
requirements:
  - R-CROSS-01
  - R-CROSS-02

must_haves:
  truths:
    - "Per-stap AI-call is mogelijk via /api/cross-analyse met stap parameter (D-11)"
    - "Cumulatieve context van eerdere stappen wordt meegestuurd naar latere stappen (D-12)"
    - "Bestaande cross-analyse flow zonder stap parameter werkt nog (backward compat)"
  artifacts:
    - path: "src/lib/schemas.ts"
      provides: "Per-step AI response schemas + CrossAnalyseWizardState + DINSession extension"
      contains: "Stap1ResultSchema"
    - path: "src/lib/prompts.ts"
      provides: "5 per-step cross-analyse prompts"
      contains: "CROSS_ANALYSE_STAP1_PROMPT"
    - path: "src/app/api/cross-analyse/route.ts"
      provides: "Refactored API route accepting stap parameter with per-step prompt/schema selection"
      contains: "body.stap"
  key_links:
    - from: "src/app/api/cross-analyse/route.ts"
      to: "src/lib/schemas.ts"
      via: "per-step schema import"
      pattern: "Stap[1-5]ResultSchema"
    - from: "src/app/api/cross-analyse/route.ts"
      to: "src/lib/prompts.ts"
      via: "per-step prompt import"
      pattern: "CROSS_ANALYSE_STAP[1-5]_PROMPT"
---

<objective>
Data layer foundation for the cross-analyse wizard: per-step Zod schemas, per-step AI prompts, and API route refactoring to support step-by-step AI calls with cumulative context.

Purpose: The wizard needs per-step AI calls (D-11) with cumulative context (D-12). This plan creates the data contracts and API infrastructure that all 5 wizard step components will call.
Output: Updated schemas.ts, prompts.ts, and route.ts supporting per-step AI invocation while maintaining backward compatibility.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cross-analyse-herontwerp-stapsgewijs-traject-met-consolidatie/11-CONTEXT.md
@.planning/phases/11-cross-analyse-herontwerp-stapsgewijs-traject-met-consolidatie/11-RESEARCH.md
@.planning/phases/11-cross-analyse-herontwerp-stapsgewijs-traject-met-consolidatie/11-UI-SPEC.md

<interfaces>
<!-- Existing schemas that will be extended -->

From src/lib/schemas.ts (current AICrossAnalyseSchema structure):
```typescript
// Current single-schema approach — all fields in one response:
export const AICrossAnalyseSchema = z.object({
  synergie: z.object({ titel, toelichting, items: CrossAnalyseSynergieItemSchema[] }),
  gaps: z.object({ titel, toelichting, doelenZonderBaten[], batenZonderVermogens[], vermogensZonderInspanningen[] }),
  hefboomwerking: z.object({ titel, toelichting, items: CrossAnalyseHefboomItemSchema[] }),
  domeinBalans: z.object({ titel, toelichting, domeinen: CrossAnalyseDomeinItemSchema[] }),
  sectorOverlap: z.object({ titel, toelichting, items: CrossAnalyseSectorOverlapItemSchema[] }),
  externeProjecten: z.object({ titel, toelichting, items: CrossAnalyseExternItemSchema[] }),
  vermogenClusters: z.object({ titel, toelichting, items: VermogenClusterItemSchema[] }),
  inspanningClusters: z.object({ titel, toelichting, items: InspanningClusterItemSchema[] }),
  projectMatching: z.object({ titel, toelichting, items: ProjectMatchItemSchema[] }),
});

// Current DINSession — crossAnalyse stored as string:
export const DINSessionSchema = z.object({
  // ... other fields ...
  crossAnalyse: z.string().optional(),
});
```

From src/lib/prompts.ts:
```typescript
export const CROSS_ANALYSE_PROMPT = `...single monolithic prompt...`;
```

From src/app/api/cross-analyse/route.ts:
```typescript
// Current: single AI call with AICrossAnalyseSchema
const result = await callClaudeWithValidation(
  AICrossAnalyseSchema,
  assembleSystemPrompt(CROSS_ANALYSE_PROMPT, "cross-analyse", undefined, kibContext),
  userMessage,
  { maxTokens: 16384, model: "claude-opus-4-6" }
);
```

From src/lib/ai-client.ts:
```typescript
export function callClaudeWithValidation<T>(
  schema: z.ZodSchema<T>,
  systemPrompt: string,
  userMessage: string,
  options?: { maxTokens?: number; model?: string }
): Promise<{ success: boolean; data?: T; error?: string }>;
```

From src/lib/prompt-assembly.ts:
```typescript
export function assembleSystemPrompt(
  basePrompt: string,
  useCase: ProgrammaboekUseCase,
  sectorwerkContext?: string,
  kibContext?: string
): string;
export function extractKiBContext(data: { goals?: unknown; scope?: unknown }): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-step Zod schemas + DINSession wizard state extension</name>
  <files>src/lib/schemas.ts</files>
  <read_first>
    - src/lib/schemas.ts
    - .planning/phases/11-cross-analyse-herontwerp-stapsgewijs-traject-met-consolidatie/11-RESEARCH.md
    - .planning/phases/11-cross-analyse-herontwerp-stapsgewijs-traject-met-consolidatie/11-CONTEXT.md
  </read_first>
  <action>
Add 5 per-step AI response schemas and a wizard state schema to schemas.ts. Insert AFTER the existing AICrossAnalyseSchema block (around line 603) and BEFORE the AIDomainRecommendSchema.

**Step 1 schema (Stap1ResultSchema):** Baten-overloop analysis per sector + gaps + synergies.
```typescript
export const Stap1ResultSchema = z.object({
  batenPerSector: z.array(z.object({
    sector: z.string(),
    baten: z.array(z.object({
      titel: z.string(),
      doelId: z.string().optional(),
      doelNaam: z.string().optional(),
    })),
  })),
  synergieën: z.array(z.object({
    beschrijving: z.string(),
    sectoren: z.array(z.string()),
    impact: z.string(),
  })).optional().default([]),
  gaps: z.object({
    doelenZonderBaten: z.array(z.string()).optional().default([]),
    batenZonderVermogens: z.array(z.string()).optional().default([]),
  }).optional().default(() => ({ doelenZonderBaten: [], batenZonderVermogens: [] })),
  samenvatting: z.string(),
});
```

**Step 2 schema (Stap2ResultSchema):** Vermogen clusters + hefboomwerking.
```typescript
export const Stap2ResultSchema = z.object({
  vermogenClusters: z.array(VermogenClusterItemSchema).optional().default([]),
  hefboomwerking: z.array(CrossAnalyseHefboomItemSchema).optional().default([]),
  samenvatting: z.string(),
});
```

**Step 3 schema (Stap3ResultSchema):** Inspanning clusters + project matching.
```typescript
export const Stap3ResultSchema = z.object({
  inspanningClusters: z.array(InspanningClusterItemSchema).optional().default([]),
  projectMatching: z.array(ProjectMatchItemSchema).optional().default([]),
  samenvatting: z.string(),
});
```

**Step 4 schema (Stap4ResultSchema):** Consolidation advice per cluster.
```typescript
export const Stap4ResultSchema = z.object({
  consolidatieAdvies: z.array(z.object({
    clusterTitel: z.string(),
    type: z.enum(["vermogen", "inspanning"]),
    aanbeveling: z.enum(["combineren", "afstemmen", "apart_houden"]),
    reden: z.string(),
    voorgesteldeNaam: z.string().optional(),
  })).optional().default([]),
  samenvatting: z.string(),
});
```

**Step 5 schema (Stap5ResultSchema):** Per-sector translation summary.
```typescript
export const Stap5ResultSchema = z.object({
  sectorVertalingen: z.array(z.object({
    sector: z.string(),
    dinKeten: z.object({
      aantalBaten: z.number(),
      aantalVermogens: z.number(),
      aantalInspanningen: z.number(),
      waarvanGedeeld: z.number(),
    }),
    domeinBalans: z.array(CrossAnalyseDomeinItemSchema).optional().default([]),
    gaps: z.array(z.string()).optional().default([]),
    samenvatting: z.string(),
  })),
  totaalSamenvatting: z.string(),
});
```

**Wizard state schema (CrossAnalyseWizardStateSchema):** Persists wizard progress in session.
```typescript
export const CrossAnalyseWizardStateSchema = z.object({
  currentStep: z.number().min(1).max(5),
  completedSteps: z.array(z.number()),
  stepResults: z.object({
    stap1: Stap1ResultSchema.optional(),
    stap2: Stap2ResultSchema.optional(),
    stap3: Stap3ResultSchema.optional(),
    stap4: Stap4ResultSchema.optional(),
    stap5: Stap5ResultSchema.optional(),
  }).optional(),
});
```

**DINSession extension:** Add `crossAnalyseWizard` field to DINSessionSchema (around line 400, after `crossAnalyse: z.string().optional()`):
```typescript
crossAnalyseWizard: CrossAnalyseWizardStateSchema.optional(),
```

**Type exports:** Add at end of type exports section (around line 696):
```typescript
export type Stap1Result = z.infer<typeof Stap1ResultSchema>;
export type Stap2Result = z.infer<typeof Stap2ResultSchema>;
export type Stap3Result = z.infer<typeof Stap3ResultSchema>;
export type Stap4Result = z.infer<typeof Stap4ResultSchema>;
export type Stap5Result = z.infer<typeof Stap5ResultSchema>;
export type CrossAnalyseWizardState = z.infer<typeof CrossAnalyseWizardStateSchema>;
```

Also add re-exports in src/lib/types.ts for these new types.
  </action>
  <verify>
    <automated>cd "C:/Users/pdebu/Projects VS code/DIN" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - schemas.ts contains `export const Stap1ResultSchema`
    - schemas.ts contains `export const Stap2ResultSchema`
    - schemas.ts contains `export const Stap3ResultSchema`
    - schemas.ts contains `export const Stap4ResultSchema`
    - schemas.ts contains `export const Stap5ResultSchema`
    - schemas.ts contains `export const CrossAnalyseWizardStateSchema`
    - schemas.ts DINSessionSchema contains `crossAnalyseWizard: CrossAnalyseWizardStateSchema.optional()`
    - schemas.ts contains `export type Stap1Result`
    - schemas.ts contains `export type CrossAnalyseWizardState`
    - types.ts re-exports `Stap1Result, Stap2Result, Stap3Result, Stap4Result, Stap5Result, CrossAnalyseWizardState`
    - `npx tsc --noEmit` passes without errors
  </acceptance_criteria>
  <done>All 5 per-step schemas, wizard state schema, DINSession extension, and type exports in place. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Per-step prompts + API route refactoring with cumulative context</name>
  <files>src/lib/prompts.ts, src/app/api/cross-analyse/route.ts</files>
  <read_first>
    - src/lib/prompts.ts
    - src/app/api/cross-analyse/route.ts
    - src/lib/ai-client.ts
    - src/lib/prompt-assembly.ts
    - src/lib/schemas.ts
    - .planning/phases/11-cross-analyse-herontwerp-stapsgewijs-traject-met-consolidatie/11-CONTEXT.md
  </read_first>
  <action>
**Part A: Per-step prompts in prompts.ts**

Add 5 new prompt constants AFTER the existing CROSS_ANALYSE_PROMPT (keep the original for backward compat). Each prompt asks for output matching the corresponding Stap*ResultSchema.

```typescript
export const CROSS_ANALYSE_STAP1_PROMPT = `Je bent een expert in programmamanagement (DIN-methodiek).
Analyseer de baten per sector en identificeer synergieën en ontbrekende ketens.

Vergelijk de baten van PO, VO en Zakelijk naast elkaar.
Identificeer waar dezelfde baat in meerdere sectoren terugkomt (synergieën).
Detecteer ontbrekende koppelingen (doelen zonder baten, baten zonder vermogens).

Antwoord ALLEEN als JSON-object met EXACT deze structuur:
{
  "batenPerSector": [
    { "sector": "PO", "baten": [{ "titel": "...", "doelId": "...", "doelNaam": "..." }] }
  ],
  "synergieën": [
    { "beschrijving": "Welk vermogen/baat gedeeld wordt", "sectoren": ["PO","VO"], "impact": "Effect" }
  ],
  "gaps": {
    "doelenZonderBaten": ["Doel X..."],
    "batenZonderVermogens": ["Baat Y..."]
  },
  "samenvatting": "Korte samenvatting van de baten-overloop analyse (2-3 zinnen)"
}

BELANGRIJK: Verwijs naar specifieke doelen en baten uit de data. Antwoord in het Nederlands.`;

export const CROSS_ANALYSE_STAP2_PROMPT = `Je bent een expert in programmamanagement (DIN-methodiek).
Analyseer welke vermogens door meerdere sectoren gedeeld worden en waar hefboomwerking zit.

Cluster vermogens die semantisch op hetzelfde neerkomen over sectoren heen.
Gebruik de id-velden om items te identificeren in je clusters.
Identificeer inspanningen die aan meerdere baten bijdragen (hefboomwerking).

Antwoord ALLEEN als JSON-object met EXACT deze structuur:
{
  "vermogenClusters": [
    {
      "clusterTitel": "Titel van het gedeelde vermogen",
      "items": [{ "id": "uuid", "beschrijving": "...", "sector": "PO" }],
      "batenContext": [{ "baat": "naam baat", "sector": "PO" }],
      "advies": "Waarom deze combineren/afstemmen/apart houden",
      "aanbeveling": "combineren"
    }
  ],
  "hefboomwerking": [
    { "inspanning": "Naam", "bijdraagtAan": ["Baat A"], "prioriteit": "hoog" }
  ],
  "samenvatting": "Korte samenvatting van de vermogen-analyse (2-3 zinnen)"
}

aanbeveling MOET exact een van: "combineren", "afstemmen", "apart_houden" zijn.
BELANGRIJK: Gebruik de werkelijke id-velden uit de data. Antwoord in het Nederlands.`;

export const CROSS_ANALYSE_STAP3_PROMPT = `Je bent een expert in programmamanagement (DIN-methodiek).
Analyseer overlap in inspanningen over sectoren en koppel lopende projecten aan het DIN-netwerk.

Cluster inspanningen die semantisch overlappen over sectoren.
Gebruik de id-velden om items te identificeren in je clusters.
Koppel lopende projecten aan DIN-inspanningen (match/geen match).

Antwoord ALLEEN als JSON-object met EXACT deze structuur:
{
  "inspanningClusters": [
    {
      "clusterTitel": "Titel van de overlap",
      "items": [{ "id": "uuid", "beschrijving": "...", "sector": "PO", "domein": "mens" }],
      "batenContext": [{ "baat": "naam baat", "sector": "PO" }],
      "advies": "Waarom combineren/afstemmen/apart houden",
      "aanbeveling": "combineren"
    }
  ],
  "projectMatching": [
    {
      "project": "Projectnaam",
      "heeftMatch": true,
      "gekoppeldAan": "Inspanning X",
      "type": "directe match",
      "advies": "Synergie benutten"
    }
  ],
  "samenvatting": "Korte samenvatting van de inspanningen-analyse (2-3 zinnen)"
}

aanbeveling MOET exact een van: "combineren", "afstemmen", "apart_houden" zijn.
BELANGRIJK: Gebruik de werkelijke id-velden uit de data. Antwoord in het Nederlands.`;

export const CROSS_ANALYSE_STAP4_PROMPT = `Je bent een expert in programmamanagement (DIN-methodiek).
Geef per cluster een concreet consolidatie-advies: samenvoegen, afstemmen, of apart houden.

Je krijgt de vermogen-clusters en inspanning-clusters uit eerdere analyse-stappen.
Beoordeel per cluster of samenvoegen zinvol is op basis van:
- Semantische overlap (hetzelfde vermogen/inspanning in andere woorden)
- Organisatorische haalbaarheid (kunnen sectoren dit samen?)
- Methodische correctheid (mag dit volgens DIN-methodiek gedeeld?)

Antwoord ALLEEN als JSON-object met EXACT deze structuur:
{
  "consolidatieAdvies": [
    {
      "clusterTitel": "Titel van het cluster",
      "type": "vermogen",
      "aanbeveling": "combineren",
      "reden": "Concrete onderbouwing waarom dit advies",
      "voorgesteldeNaam": "Voorgestelde naam na samenvoeging (alleen bij combineren)"
    }
  ],
  "samenvatting": "Korte samenvatting van het consolidatie-advies (2-3 zinnen)"
}

type MOET exact "vermogen" of "inspanning" zijn.
aanbeveling MOET exact een van: "combineren", "afstemmen", "apart_houden" zijn.
BELANGRIJK: Antwoord in het Nederlands.`;

export const CROSS_ANALYSE_STAP5_PROMPT = `Je bent een expert in programmamanagement (DIN-methodiek).
Maak per sector een vertaling: hoe komt hun input terug in het geheel?

Per sector (PO, VO, Zakelijk):
- Tel baten, vermogens, inspanningen (eigen + gedeeld)
- Beoordeel de domeinbalans (Mens/Processen/Data & Systemen/Cultuur)
- Identificeer gaps specifiek voor deze sector
- Geef een korte samenvatting

Antwoord ALLEEN als JSON-object met EXACT deze structuur:
{
  "sectorVertalingen": [
    {
      "sector": "PO",
      "dinKeten": {
        "aantalBaten": 6,
        "aantalVermogens": 4,
        "aantalInspanningen": 8,
        "waarvanGedeeld": 2
      },
      "domeinBalans": [
        { "domein": "Mens", "beoordeling": "voldoende", "advies": "..." }
      ],
      "gaps": ["Vermogen X heeft geen inspanning"],
      "samenvatting": "Korte samenvatting voor deze sector (2-3 zinnen)"
    }
  ],
  "totaalSamenvatting": "Overkoepelende samenvatting over alle sectoren (2-3 zinnen)"
}

Beoordeling MOET "voldoende", "te weinig", of "oververtegenwoordigd" zijn.
BELANGRIJK: Antwoord in het Nederlands.`;
```

**Part B: API route refactoring in route.ts**

Refactor the route to accept an optional `stap` parameter (1-5). When `stap` is provided, use the per-step prompt and schema. When `stap` is NOT provided, fall back to the existing full cross-analyse behavior (backward compat per Pitfall 5).

Key changes:
1. Import all 5 per-step schemas (`Stap1ResultSchema` through `Stap5ResultSchema`) and prompts from their respective modules.
2. Add a `getStepConfig(stap: number)` helper function that returns `{ prompt, schema }` for the given step.
3. Build cumulative context from prior step results:
   - Step 2 receives step 1 `samenvatting`
   - Step 3 receives step 1+2 `samenvatting`
   - Step 4 receives step 1+2+3 `samenvatting` + all clusters
   - Step 5 receives step 1-4 `samenvatting` + consolidation decisions
4. Cap cumulative context at ~3000 characters using string slicing at sentence boundary.
5. Add optional `userFeedback` to the user message per step (D-13).
6. Keep the existing `type === "verrijkt-sectorplan"` branch untouched.
7. Keep the existing no-stap branch using `CROSS_ANALYSE_PROMPT` + `AICrossAnalyseSchema`.

The route structure becomes:
```typescript
import { Stap1ResultSchema, Stap2ResultSchema, Stap3ResultSchema, Stap4ResultSchema, Stap5ResultSchema } from "@/lib/schemas";
import { CROSS_ANALYSE_PROMPT, CROSS_ANALYSE_STAP1_PROMPT, CROSS_ANALYSE_STAP2_PROMPT, CROSS_ANALYSE_STAP3_PROMPT, CROSS_ANALYSE_STAP4_PROMPT, CROSS_ANALYSE_STAP5_PROMPT } from "@/lib/prompts";

function getStepConfig(stap: number) {
  const configs: Record<number, { prompt: string; schema: z.ZodSchema }> = {
    1: { prompt: CROSS_ANALYSE_STAP1_PROMPT, schema: Stap1ResultSchema },
    2: { prompt: CROSS_ANALYSE_STAP2_PROMPT, schema: Stap2ResultSchema },
    3: { prompt: CROSS_ANALYSE_STAP3_PROMPT, schema: Stap3ResultSchema },
    4: { prompt: CROSS_ANALYSE_STAP4_PROMPT, schema: Stap4ResultSchema },
    5: { prompt: CROSS_ANALYSE_STAP5_PROMPT, schema: Stap5ResultSchema },
  };
  return configs[stap];
}

function buildCumulativeContext(body: Record<string, unknown>): string {
  let context = "";
  if (body.stap1Result) {
    const s1 = body.stap1Result as { samenvatting?: string };
    context += `\nEERDERE ANALYSE - Stap 1 (Baten-overloop):\n${s1.samenvatting || ""}`;
  }
  if (body.stap2Result) {
    const s2 = body.stap2Result as { samenvatting?: string };
    context += `\nEERDERE ANALYSE - Stap 2 (Vermogen-clusters):\n${s2.samenvatting || ""}`;
  }
  if (body.stap3Result) {
    const s3 = body.stap3Result as { samenvatting?: string };
    context += `\nEERDERE ANALYSE - Stap 3 (Inspanning-clusters):\n${s3.samenvatting || ""}`;
  }
  if (body.stap4Result) {
    const s4 = body.stap4Result as { samenvatting?: string };
    context += `\nEERDERE ANALYSE - Stap 4 (Consolidatie-advies):\n${s4.samenvatting || ""}`;
  }
  // Cap at ~3000 chars to prevent token budget explosion
  if (context.length > 3000) {
    const truncated = context.slice(0, 3000);
    const lastSentence = truncated.lastIndexOf(".");
    context = lastSentence > 2500 ? truncated.slice(0, lastSentence + 1) : truncated;
  }
  return context;
}

// In POST handler, after verrijkt-sectorplan branch:
const stap = body.stap as number | undefined;
if (stap && stap >= 1 && stap <= 5) {
  const config = getStepConfig(stap);
  const cumulativeContext = buildCumulativeContext(body);
  let userMessage = `Analyseer de volgende DIN-data...\n\n${JSON.stringify(structuredData, null, 2).slice(0, 20000)}`;
  if (cumulativeContext) userMessage += `\n\n${cumulativeContext}`;
  if (body.userFeedback) userMessage += `\n\nExtra instructies: ${body.userFeedback}`;

  const result = await callClaudeWithValidation(
    config.schema,
    assembleSystemPrompt(config.prompt, "cross-analyse", undefined, kibContext),
    userMessage,
    { maxTokens: 16384, model: "claude-opus-4-6" }
  );
  // Return { success, data: { analysis: result.data, stap } }
}
// else: fall through to existing full cross-analyse
```

Also add "cross-analyse" to the ProgrammaboekUseCase type import if not already there (it should already be listed).
  </action>
  <verify>
    <automated>cd "C:/Users/pdebu/Projects VS code/DIN" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - prompts.ts contains `export const CROSS_ANALYSE_STAP1_PROMPT`
    - prompts.ts contains `export const CROSS_ANALYSE_STAP2_PROMPT`
    - prompts.ts contains `export const CROSS_ANALYSE_STAP3_PROMPT`
    - prompts.ts contains `export const CROSS_ANALYSE_STAP4_PROMPT`
    - prompts.ts contains `export const CROSS_ANALYSE_STAP5_PROMPT`
    - prompts.ts still contains `export const CROSS_ANALYSE_PROMPT` (backward compat)
    - route.ts contains `body.stap`
    - route.ts contains `getStepConfig`
    - route.ts contains `buildCumulativeContext`
    - route.ts contains `Stap1ResultSchema`
    - route.ts still handles `type === "verrijkt-sectorplan"` (unchanged)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>5 per-step prompts exported from prompts.ts. API route accepts optional `stap` parameter (1-5), selects per-step prompt and schema, builds cumulative context from earlier step results, caps at 3000 chars. Backward compat maintained for calls without `stap` parameter.</done>
</task>

</tasks>

<verification>
- `npm run build` passes without errors
- `npx tsc --noEmit` passes
- Existing cross-analyse API calls (without stap parameter) still work unchanged
- Per-step schemas are importable from schemas.ts
- Per-step prompts are importable from prompts.ts
</verification>

<success_criteria>
- All 5 per-step Zod schemas validate the expected AI response shapes
- DINSession type includes crossAnalyseWizard field
- API route supports both legacy (no stap) and new (stap 1-5) invocation patterns
- Cumulative context builder caps at ~3000 chars
- Build succeeds with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-cross-analyse-herontwerp-stapsgewijs-traject-met-consolidatie/11-01-SUMMARY.md`
</output>
