---
phase: 04-ai-output-kwaliteit
plan: 03
type: execute
wave: 1
depends_on: ["04-01", "04-02"]
files_modified:
  - src/components/steps/DINMappingStep.tsx
autonomous: true
gap_closure: true
requirements: [AI-02, AI-03, AI-04]

must_haves:
  truths:
    - "Elke AI-aanroep vanuit DINMappingStep bevat kibGoals en kibScope in de request body"
    - "API routes ontvangen daadwerkelijke goals en scope data (niet undefined)"
    - "Het KiB-blok verschijnt in de system prompt bij AI-generatie"
  artifacts:
    - path: "src/components/steps/DINMappingStep.tsx"
      provides: "KiB context doorvoering in alle 4 fetch-aanroepen"
      contains: "kibGoals"
  key_links:
    - from: "src/components/steps/DINMappingStep.tsx"
      to: "/api/din-mapping"
      via: "kibGoals en kibScope in request body"
      pattern: "kibGoals.*session"
    - from: "src/components/steps/DINMappingStep.tsx"
      to: "/api/din-suggest"
      via: "kibGoals en kibScope in request body"
      pattern: "kibGoals.*session"
    - from: "src/components/steps/DINMappingStep.tsx"
      to: "/api/cross-analyse"
      via: "kibGoals en kibScope in request body (beide aanroepen)"
      pattern: "kibGoals.*session"
---

<objective>
Fix the single verification gap from Phase 04: DINMappingStep.tsx never sends kibGoals/kibScope to the API routes. The server-side infrastructure (extractKiBContext, assembleSystemPrompt with KiB block, all 3 API routes accepting kibGoals/kibScope) is fully wired and tested. But the client component never includes these fields in its fetch request bodies, so extractKiBContext always receives undefined and produces empty KiB context.

Purpose: Close the client-server wiring gap so that KiB programmadoelen and scope actually reach the AI prompts (AI-03 requirement).
Output: DINMappingStep.tsx with kibGoals and kibScope in all 4 API fetch calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-output-kwaliteit/04-01-SUMMARY.md
@.planning/phases/04-ai-output-kwaliteit/04-VERIFICATION.md

<interfaces>
<!-- Server-side API routes expect these fields in the request body -->

From src/app/api/din-mapping/route.ts (line 11):
```typescript
const { goal, sectorPlan, sector, allGoals, sectorAnalysis, kibGoals, kibScope } = body;
const kibContext = extractKiBContext({ goals: kibGoals, scope: kibScope });
```

From src/app/api/din-suggest/route.ts (line 41):
```typescript
const kibContext = extractKiBContext({ goals: body.kibGoals, scope: body.kibScope });
```

From src/app/api/cross-analyse/route.ts (line 12):
```typescript
const kibContext = extractKiBContext({ goals: body.kibGoals, scope: body.kibScope });
```

From src/lib/prompt-assembly.ts (lines 106-108):
```typescript
export function extractKiBContext(session: {
  goals?: { name: string; description: string; rank: number }[];
  scope?: { id?: string; inScope: string[]; outScope: string[] } | null;
}): KiBContext
```

From src/lib/schemas.ts (lines 77-96) — session.goals shape:
```typescript
// ProgrammeGoal: { id: string; name: string; description: string; rank: number; sourceSessionId?: string }
// ProgrammeScope: { id: string; inScope: string[]; outScope: string[] }
// DINSession.goals: ProgrammeGoal[]
// DINSession.scope: ProgrammeScope | undefined
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire kibGoals and kibScope into all 4 API fetch calls in DINMappingStep</name>
  <files>src/components/steps/DINMappingStep.tsx</files>
  <read_first>
    - src/components/steps/DINMappingStep.tsx (current state of all 4 fetch calls)
    - src/app/api/din-mapping/route.ts (line 11: confirms field names kibGoals, kibScope)
    - src/app/api/din-suggest/route.ts (line 41: confirms body.kibGoals, body.kibScope)
    - src/app/api/cross-analyse/route.ts (line 12: confirms body.kibGoals, body.kibScope)
  </read_first>
  <action>
    Add `kibGoals: session!.goals` and `kibScope: session!.scope` to 4 fetch request bodies in DINMappingStep.tsx:

    1. **fetchAISuggestion** (~line 796): The current body is `JSON.stringify({ type, context })`. Change to `JSON.stringify({ type, context, kibGoals: session!.goals, kibScope: session!.scope })`. The fields go at the top level alongside `type` and `context`, NOT inside the `context` object.

    2. **handleAIGenerate** (~line 887-900): The current `requestBody` object has fields `goal`, `sectorPlan`, `sector`, `allGoals`, `sectorAnalysis`. Add two fields to the object literal: `kibGoals: session!.goals` and `kibScope: session!.scope`. Add them right after the existing fields (before the conditional `userFeedback` addition).

    3. **handleIntegratieAdvies** (~line 998-1008): The current `requestBody` object has fields `type`, `sector`, `sectorPlan`, `goals`, etc. Add `kibGoals: session!.goals` and `kibScope: session!.scope` to this object literal.

    4. **handleVerrijktSectorplan** (~line 1069-1096): The current inline object in `JSON.stringify({...})` has fields `type`, `sector`, `sectorPlan`, `goals`, `benefits`, etc. Add `kibGoals: session!.goals` and `kibScope: session!.scope` to this object literal.

    Per D-05: session.goals contains all top-doelen with name, description, and rank. session.scope contains inScope/outScope arrays. Both are available on the session object which is already used extensively in these functions.

    Per D-06: ALL AI-aanroepen must include KiB context — this covers all 4 fetch calls in the component.

    Important: `session!.scope` may be `undefined` when no KiB import has been done. This is fine — `extractKiBContext` on the server side handles `undefined` gracefully (returns `{ goals: [], scope: null }`).
  </action>
  <verify>
    <automated>cd "c:/Users/pdebu/Projects VS code/DIN" && npx grep -c "kibGoals" src/components/steps/DINMappingStep.tsx | grep -q "4" && echo "PASS: 4 kibGoals references found" || echo "FAIL: expected 4 kibGoals references"</automated>
    <automated>cd "c:/Users/pdebu/Projects VS code/DIN" && npm run build 2>&1 | tail -5</automated>
    <automated>cd "c:/Users/pdebu/Projects VS code/DIN" && npx vitest run src/lib/__tests__/prompt-assembly.test.ts src/lib/__tests__/din-validation.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - DINMappingStep.tsx contains exactly 4 occurrences of "kibGoals" (one per fetch call)
    - DINMappingStep.tsx contains exactly 4 occurrences of "kibScope" (one per fetch call)
    - Each occurrence is `kibGoals: session!.goals` (not session.goals without assertion, not inside a nested object)
    - Each occurrence is `kibScope: session!.scope` (not session.scope without assertion)
    - `npm run build` exits with code 0 (no TypeScript errors)
    - All existing tests pass (npx vitest run)
    - The fetchAISuggestion body has kibGoals/kibScope at the same level as `type` and `context` (NOT nested inside `context`)
    - The handleAIGenerate requestBody has kibGoals/kibScope at the same level as `goal`, `sectorPlan`, `sector`
    - The handleIntegratieAdvies requestBody has kibGoals/kibScope at the same level as `type`, `sector`
    - The handleVerrijktSectorplan inline object has kibGoals/kibScope at the same level as `type`, `sector`
  </acceptance_criteria>
  <done>All 4 API fetch calls in DINMappingStep.tsx include kibGoals and kibScope. Build passes. Server-side extractKiBContext will now receive actual goal and scope data instead of undefined, enabling the KiB block to appear in AI system prompts.</done>
</task>

</tasks>

<verification>
After task completion, verify the full chain works:

1. **Static verification:** grep DINMappingStep.tsx for "kibGoals" — must find exactly 4 matches
2. **Static verification:** grep DINMappingStep.tsx for "kibScope" — must find exactly 4 matches
3. **Build check:** `npm run build` succeeds
4. **Test suite:** `npx vitest run` — all tests pass
5. **Code review:** Each kibGoals/kibScope pair is at the correct nesting level in the request body (top-level, not nested in context or other sub-objects)
</verification>

<success_criteria>
- DINMappingStep.tsx sends kibGoals and kibScope in all 4 API calls (din-mapping, din-suggest, both cross-analyse calls)
- Build succeeds without TypeScript errors
- All existing tests continue to pass
- The verification gap "DINMappingStep.tsx verzend nooit kibGoals of kibScope naar de API-routes" is resolved
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-output-kwaliteit/04-03-SUMMARY.md`
</output>
