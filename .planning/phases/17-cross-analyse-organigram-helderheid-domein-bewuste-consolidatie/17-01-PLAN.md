---
phase: 17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/schemas.ts
  - src/lib/consolidation-guards.ts
  - src/lib/__tests__/cross-analyse-schema.test.ts
  - src/lib/__tests__/consolidation-guards.test.ts
autonomous: true
requirements:
  - R-CROSS-01
  - R-CROSS-02
requirements_addressed:
  - R-CROSS-01
  - R-CROSS-02
user_setup: []

must_haves:
  truths:
    - "Executor kan `zod` direct importeren vanuit `package.json` dependencies (geen transitive-only resolutie meer)"
    - "Nieuwe entiteit `VermogenGelijkenisGroep` is een gevalideerd Zod schema met `id`, `vermogenIds`, `gezamenlijkeOmschrijving`, `reden`"
    - "`Stap2ResultSchema.parse()` op legacy data zonder `vermogenGelijkenisGroepen` slaagt (backward compat, default [])"
    - "`VermogenClusterItemSchema.aanbeveling` accepteert `markeer_gelijkenis` naast bestaande enum-waardes"
    - "`SubEffortAdviesSchema` valideert `{groepId, domein, actie, items, reden, voorgesteldeNaam}` shape"
    - "`Stap4ResultSchema` accepteert `subEffortAnalysis` optioneel + `context` op elk `consolidatieAdvies` item"
    - "Pure helper `computeAutoApplyResult(clusters, drieluikCtx): { mergedIds, failedKeys, reasons }` is unit-testbaar zonder React"
  artifacts:
    - path: "src/lib/consolidation-guards.ts"
      provides: "SECTOR_NAME_REGEX, MIN_TITLE_LENGTH, validateNeutralTitle, validateSameDomain, validateDrieluikThreshold, computeAutoApplyResult types"
      exports:
        - "SECTOR_NAME_REGEX"
        - "MIN_TITLE_LENGTH"
        - "validateNeutralTitle"
        - "validateSameDomain"
        - "validateDrieluikThreshold"
        - "computeAutoApplyResult"
        - "DrieluikContext"
    - path: "src/lib/schemas.ts"
      provides: "VermogenGelijkenisGroepSchema, SubEffortAdviesSchema, uitgebreide Stap2ResultSchema + Stap4ResultSchema + VermogenClusterItemSchema"
      contains: "export const VermogenGelijkenisGroepSchema"
    - path: "src/lib/__tests__/cross-analyse-schema.test.ts"
      provides: "Schema tests voor nieuwe velden + backward compat"
      min_lines: 80
    - path: "src/lib/__tests__/consolidation-guards.test.ts"
      provides: "Unit tests voor auto-apply helper + schema helpers die NIET afhangen van CrossAnalyseStep.tsx"
      min_lines: 30
  key_links:
    - from: "src/lib/schemas.ts"
      to: "src/lib/types.ts"
      via: "z.infer<typeof VermogenGelijkenisGroepSchema> export"
      pattern: "export type VermogenGelijkenisGroep = z.infer"
    - from: "src/lib/consolidation-guards.ts"
      to: "src/lib/types.ts"
      via: "type imports (DINEffort, VermogenGelijkenisGroep, CapabilityEffortMap)"
      pattern: "import type"
---

<objective>
Wave 0 — schema extensies, dependency hardening en test-scaffolding die Wave 1..3 unit-testbaar maken. Dit plan is de fundering: geen guards, geen AI, geen UI — alleen Zod-schemas, `zod` als expliciete dependency, een nieuwe pure-helper module `consolidation-guards.ts` (stubs + types + één testbare helper `computeAutoApplyResult`), en uitbreiding van twee bestaande test-files.

**Purpose:** Zonder expliciete `zod` dep is de build fragiel (transitive resolutie via `@anthropic-ai/sdk`). Zonder schema-extensies kunnen Wave 2/3 geen `VermogenGelijkenisGroep` opslaan of valideren. Zonder pure-helper module heeft de auto-apply loop in `StapConsolidatie.tsx` nergens een export-anker waarop Wave 2 tests kunnen landen. Dit plan levert alle drie in één Wave zodat Wave 1 (guards) meteen groen kan starten.

**Output:** Expliciete `zod` in `package.json`. Uitgebreide `schemas.ts` met `VermogenGelijkenisGroepSchema` + `SubEffortAdviesSchema` + enum-uitbreiding `markeer_gelijkenis` + `context` veld op `consolidatieAdvies`. Nieuwe `consolidation-guards.ts` met helper-signatures (guards zijn stubs die in Wave 1 implementatie krijgen) + werkende `computeAutoApplyResult`. Uitgebreide `cross-analyse-schema.test.ts` + nieuwe `consolidation-guards.test.ts` — quick-run (`npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts src/lib/__tests__/consolidation-guards.test.ts`) groen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-CONTEXT.md
@.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-RESEARCH.md
@.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-VALIDATION.md
@CLAUDE.md
@src/lib/schemas.ts
@src/lib/types.ts
@src/lib/__tests__/cross-analyse-schema.test.ts
@src/lib/__tests__/consolidation.test.ts
@src/components/steps/CrossAnalyseStep.tsx
@src/components/cross-analyse/StapConsolidatie.tsx

<interfaces>
<!-- Key existing schema shapes Wave 0 extends. Extracted from src/lib/schemas.ts -->

From src/lib/schemas.ts (regel 300-329, 433-462, 899-916):
```typescript
// Bestaande schemas die uitgebreid worden:

export const VermogenClusterItemSchema = z.object({
  clusterTitel: z.string(),
  items: z.array(z.object({ id: z.string(), beschrijving: z.string(), sector: z.string() })),
  batenContext: z.array(z.object({ baat: z.string(), sector: z.string() })),
  advies: z.string(),
  aanbeveling: z.enum(["combineren", "afstemmen", "apart_houden"]),   // ← enum uitbreiden met "markeer_gelijkenis"
});

export const Stap2ResultSchema = z.object({
  vermogenClusters: z.array(VermogenClusterItemSchema).optional().default([]),
  hefboomwerking: z.array(CrossAnalyseHefboomItemSchema).optional().default([]),
  samenvatting: z.string(),
  // ← toevoegen: vermogenGelijkenisGroepen: z.array(VermogenGelijkenisGroepSchema).optional().default([])
});

export const Stap4ResultSchema = z.object({
  consolidatieAdvies: z.array(z.object({
    clusterTitel: z.string(),
    type: z.enum(["vermogen", "inspanning"]),
    aanbeveling: z.enum(["combineren", "afstemmen", "apart_houden"]),
    reden: z.string(),
    voorgesteldeNaam: z.string().nullable().optional(),
    afstemmingsStappen: z.array(z.string()).optional().default([]),
    // ← toevoegen: context: z.string().optional()
  })).optional().default([]),
  citobreedInzicht: z.array(/* ... onveranderd ... */).optional().default([]),
  samenvatting: z.string(),
  // ← toevoegen: subEffortAnalysis: z.array(SubEffortAdviesSchema).optional().default([])
});

export type VermogenClusterItem = z.infer<typeof VermogenClusterItemSchema>;
export type Stap2Result = z.infer<typeof Stap2ResultSchema>;
export type Stap4Result = z.infer<typeof Stap4ResultSchema>;
```

From src/lib/types.ts (reuse voor drieluik-context typing):
```typescript
export type EffortDomain = "mens" | "processen" | "data_systemen" | "cultuur";
export type SectorName = "PO" | "VO" | "Zakelijk";
export interface DINEffort { id: string; sectorId: string; domain: EffortDomain; /* ... */ }
export interface CapabilityEffortMap { capabilityId: string; effortId: string; }
```

From src/components/cross-analyse/StapConsolidatie.tsx (regel 108-162 — bron voor `computeAutoApplyResult`):
```typescript
// De bestaande useEffect code-structuur:
// - Input: inspanningClusters[], findSuggestedTitle(clusterTitel)
// - State: mergedEffClusters: Map<key, sharedId>
// - Per cluster: ids=cluster.items.map(it=>it.id); if(ids.length<2) continue; key=[...ids].sort().join(",")
// - Try mergeEfforts(updated, ids, title[, ctx]); push to merged or catch → failedKeys
// - Return { mergedIds: string[], failedKeys: string[], reasons: string[] }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expliciete `zod` dependency + Zod schema uitbreidingen (VermogenGelijkenisGroepSchema, SubEffortAdviesSchema, enum + context velden)</name>
  <files>
    package.json,
    package-lock.json,
    src/lib/schemas.ts,
    src/lib/__tests__/cross-analyse-schema.test.ts
  </files>
  <read_first>
    - src/lib/schemas.ts (regel 1-50 voor imports, 295-330 voor VermogenClusterItemSchema, 433-462 voor Stap2ResultSchema + Stap4ResultSchema, 895-918 voor type exports)
    - src/lib/__tests__/cross-analyse-schema.test.ts (volledig — pattern overnemen voor nieuwe tests)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-CONTEXT.md (D-26, D-30 — schema contracten verbatim)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-RESEARCH.md (regel 584-645 voor Example 1 schema-code, regel 540-544 voor Pitfall 3 optional().default([]) vereisten)
    - package.json (verifieer `zod` ontbreekt in dependencies, alleen transitief via @anthropic-ai/sdk)
  </read_first>
  <behavior>
    - Test 1 ("zod dependency explicit"): `import { z } from "zod"` resolveert; `require("zod/package.json").version` matcht `^4.3.6` of bestaande versie.
    - Test 2 ("VermogenGelijkenisGroepSchema valid input"): `VermogenGelijkenisGroepSchema.parse({ id: "g1", vermogenIds: ["cap-po-1","cap-vo-1","cap-zak-1"], gezamenlijkeOmschrijving: "Medewerker-wendbaarheid", reden: "Alle drie sectoren vereisen…" })` slaagt.
    - Test 3 ("VermogenGelijkenisGroepSchema requires at least one vermogenId"): `.parse({ id: "g1", vermogenIds: [], … })` faalt (ZodError).
    - Test 4 ("Stap2ResultSchema backward compat"): `.parse({ vermogenClusters: [], hefboomwerking: [], samenvatting: "x" })` slaagt EN `result.data.vermogenGelijkenisGroepen` is `[]` (default).
    - Test 5 ("Stap2ResultSchema met nieuw veld"): `.parse({ ..., vermogenGelijkenisGroepen: [{ id:"g1", vermogenIds:["c1"], gezamenlijkeOmschrijving:"x", reden:"y" }] })` slaagt, lengte 1.
    - Test 6 ("VermogenClusterItemSchema accepteert markeer_gelijkenis"): `.parse({ clusterTitel:"x", items:[], batenContext:[], advies:"y", aanbeveling:"markeer_gelijkenis" })` slaagt. Bestaande enum-waardes blijven werken.
    - Test 7 ("SubEffortAdviesSchema valid"): `.parse({ groepId:"g1", domein:"mens", actie:"combineren", items:["eff-1","eff-2"], reden:"gedeelde training", voorgesteldeNaam:"Sector-overstijgende training" })` slaagt. Ook `voorgesteldeNaam: null` is geldig.
    - Test 8 ("SubEffortAdviesSchema domein enum"): `.parse({ …, domein:"invalid" })` faalt.
    - Test 9 ("Stap4ResultSchema backward compat"): `.parse({ consolidatieAdvies:[], citobreedInzicht:[], samenvatting:"x" })` slaagt EN `result.data.subEffortAnalysis` is `[]` (default).
    - Test 10 ("Stap4ResultSchema consolidatieAdvies.context optional"): `.parse({ consolidatieAdvies:[{ clusterTitel:"x", type:"inspanning", aanbeveling:"combineren", reden:"y", context:"User feedback" }], …, samenvatting:"z" })` slaagt, `result.data.consolidatieAdvies[0].context === "User feedback"`.
    - Test 11 ("Stap4ResultSchema subEffortAnalysis present"): `.parse({ …, subEffortAnalysis:[{ groepId:"g1", domein:"mens", actie:"apart_houden", items:["e1"], reden:"x", voorgesteldeNaam:null }] })` slaagt.
  </behavior>
  <action>
1. **Install zod als expliciete dependency:**
   ```bash
   npm install zod@^4.3.6
   ```
   Dit MOET `package.json` `dependencies` uitbreiden met `"zod": "^4.3.6"` en `package-lock.json` updaten. Verifieer met `npm ls zod` dat output begint met `din@0.1.0 … ├── zod@4.3.6` (niet meer transitief via @anthropic-ai/sdk).

2. **Uitbreiden `src/lib/schemas.ts`:**

   a. Direct NA `VermogenClusterItemSchema` (regel 313) wijzigen — uitbreiden enum:
   ```typescript
   export const VermogenClusterItemSchema = z.object({
     clusterTitel: z.string(),
     items: z.array(z.object({ id: z.string(), beschrijving: z.string(), sector: z.string() })),
     batenContext: z.array(z.object({ baat: z.string(), sector: z.string() })),
     advies: z.string(),
     // D-30: markeer_gelijkenis toegevoegd voor cross-analyse vermogen-gelijkenis markering (geen merge)
     aanbeveling: z.enum(["combineren", "afstemmen", "apart_houden", "markeer_gelijkenis"]),
   });
   ```

   b. Direct NA de `VermogenClusterItemSchema` definitie, VOOR `InspanningClusterItemSchema` (rond regel 315), nieuwe schema's toevoegen:
   ```typescript
   // --- Phase 17: VermogenGelijkenisGroep (D-26) ---
   export const VermogenGelijkenisGroepSchema = z.object({
     id: z.string(),
     vermogenIds: z.array(z.string()).min(1),
     gezamenlijkeOmschrijving: z.string(),
     reden: z.string(),
   });

   // --- Phase 17: SubEffortAdvies (D-30) ---
   export const SubEffortAdviesSchema = z.object({
     groepId: z.string(),
     domein: z.enum(["mens", "processen", "data_systemen", "cultuur"]),
     actie: z.enum(["combineren", "apart_houden"]),
     items: z.array(z.string()),
     reden: z.string(),
     voorgesteldeNaam: z.string().nullable().optional(),
   });
   ```

   c. Uitbreiden `Stap2ResultSchema` (regel 433-437):
   ```typescript
   export const Stap2ResultSchema = z.object({
     vermogenClusters: z.array(VermogenClusterItemSchema).optional().default([]),
     hefboomwerking: z.array(CrossAnalyseHefboomItemSchema).optional().default([]),
     // D-30: nieuwe vermogen-gelijkenis groepen (markering, geen merge)
     vermogenGelijkenisGroepen: z.array(VermogenGelijkenisGroepSchema).optional().default([]),
     samenvatting: z.string(),
   });
   ```

   d. Uitbreiden `Stap4ResultSchema` (regel 445-462):
   ```typescript
   export const Stap4ResultSchema = z.object({
     consolidatieAdvies: z.array(z.object({
       clusterTitel: z.string(),
       type: z.enum(["vermogen", "inspanning"]),
       aanbeveling: z.enum(["combineren", "afstemmen", "apart_houden"]),
       reden: z.string(),
       voorgesteldeNaam: z.string().nullable().optional(),
       afstemmingsStappen: z.array(z.string()).optional().default([]),
       // D-19: user-context voor herzie-advies
       context: z.string().optional(),
     })).optional().default([]),
     citobreedInzicht: z.array(z.object({
       domein: z.enum(["mens", "processen", "data_systemen", "cultuur"]),
       titel: z.string(),
       beschrijving: z.string(),
       onderbouwing: z.string(),
       relevanteItems: z.array(z.string()).optional().default([]),
     })).optional().default([]),
     // D-30: sub-effort analyse per VermogenGelijkenisGroep
     subEffortAnalysis: z.array(SubEffortAdviesSchema).optional().default([]),
     samenvatting: z.string(),
   });
   ```

   e. In type-export-sectie (rond regel 895-918) direct NA bestaande `export type VermogenClusterItem`:
   ```typescript
   export type VermogenGelijkenisGroep = z.infer<typeof VermogenGelijkenisGroepSchema>;
   export type SubEffortAdvies = z.infer<typeof SubEffortAdviesSchema>;
   ```

3. **Uitbreiden `src/lib/__tests__/cross-analyse-schema.test.ts`** — ONDER bestaande `describe` blokken (regel 71+), voeg toe:
   ```typescript
   import {
     Stap2ResultSchema,
     Stap4ResultSchema,
     VermogenClusterItemSchema,
     VermogenGelijkenisGroepSchema,
     SubEffortAdviesSchema,
   } from "@/lib/schemas";

   describe("VermogenGelijkenisGroepSchema (D-26)", () => {
     test("gelijkenis groep accepteert valide input met drie sectoren", () => {
       const result = VermogenGelijkenisGroepSchema.safeParse({
         id: "g1",
         vermogenIds: ["cap-po-1", "cap-vo-1", "cap-zak-1"],
         gezamenlijkeOmschrijving: "Medewerker-wendbaarheid",
         reden: "Alle drie sectoren vereisen adaptief vermogen bij digitalisering",
       });
       expect(result.success).toBe(true);
     });
     test("gelijkenis groep faalt bij lege vermogenIds", () => {
       const result = VermogenGelijkenisGroepSchema.safeParse({
         id: "g1", vermogenIds: [], gezamenlijkeOmschrijving: "x", reden: "y",
       });
       expect(result.success).toBe(false);
     });
   });

   describe("Stap2ResultSchema backward compat (D-30)", () => {
     test("backward compat — parse zonder vermogenGelijkenisGroepen defaultet naar []", () => {
       const result = Stap2ResultSchema.safeParse({
         vermogenClusters: [], hefboomwerking: [], samenvatting: "x",
       });
       expect(result.success).toBe(true);
       if (result.success) expect(result.data.vermogenGelijkenisGroepen).toEqual([]);
     });
     test("accepteert vermogenGelijkenisGroepen wanneer aanwezig", () => {
       const result = Stap2ResultSchema.safeParse({
         vermogenClusters: [], hefboomwerking: [], samenvatting: "x",
         vermogenGelijkenisGroepen: [
           { id: "g1", vermogenIds: ["c1"], gezamenlijkeOmschrijving: "a", reden: "b" },
         ],
       });
       expect(result.success).toBe(true);
       if (result.success) expect(result.data.vermogenGelijkenisGroepen).toHaveLength(1);
     });
   });

   describe("VermogenClusterItemSchema markeer_gelijkenis enum (D-30)", () => {
     test("markeer_gelijkenis aanbeveling accepteert", () => {
       const result = VermogenClusterItemSchema.safeParse({
         clusterTitel: "x", items: [], batenContext: [],
         advies: "y", aanbeveling: "markeer_gelijkenis",
       });
       expect(result.success).toBe(true);
     });
     test("bestaande enum-waardes blijven werken", () => {
       for (const av of ["combineren", "afstemmen", "apart_houden"] as const) {
         const result = VermogenClusterItemSchema.safeParse({
           clusterTitel: "x", items: [], batenContext: [], advies: "y", aanbeveling: av,
         });
         expect(result.success).toBe(true);
       }
     });
   });

   describe("SubEffortAdviesSchema (D-30)", () => {
     test("sub effort advies accepteert valide input met voorgesteldeNaam string", () => {
       const result = SubEffortAdviesSchema.safeParse({
         groepId: "g1", domein: "mens", actie: "combineren",
         items: ["eff-1", "eff-2"], reden: "gedeelde training",
         voorgesteldeNaam: "Sector-overstijgende training",
       });
       expect(result.success).toBe(true);
     });
     test("sub effort advies accepteert voorgesteldeNaam null", () => {
       const result = SubEffortAdviesSchema.safeParse({
         groepId: "g1", domein: "processen", actie: "apart_houden",
         items: ["eff-1"], reden: "verschillende contexten", voorgesteldeNaam: null,
       });
       expect(result.success).toBe(true);
     });
     test("sub effort advies faalt bij ongeldig domein", () => {
       const result = SubEffortAdviesSchema.safeParse({
         groepId: "g1", domein: "financien", actie: "combineren",
         items: [], reden: "x", voorgesteldeNaam: null,
       });
       expect(result.success).toBe(false);
     });
   });

   describe("Stap4ResultSchema backward compat (D-19, D-30)", () => {
     test("backward compat — parse zonder subEffortAnalysis defaultet naar []", () => {
       const result = Stap4ResultSchema.safeParse({
         consolidatieAdvies: [], citobreedInzicht: [], samenvatting: "x",
       });
       expect(result.success).toBe(true);
       if (result.success) expect(result.data.subEffortAnalysis).toEqual([]);
     });
     test("accepteert context op consolidatieAdvies items", () => {
       const result = Stap4ResultSchema.safeParse({
         consolidatieAdvies: [
           { clusterTitel: "x", type: "inspanning", aanbeveling: "combineren",
             reden: "y", context: "User wil focus op data-domein" },
         ],
         citobreedInzicht: [], samenvatting: "z",
       });
       expect(result.success).toBe(true);
       if (result.success) expect(result.data.consolidatieAdvies[0].context).toBe("User wil focus op data-domein");
     });
     test("accepteert subEffortAnalysis wanneer aanwezig", () => {
       const result = Stap4ResultSchema.safeParse({
         consolidatieAdvies: [], citobreedInzicht: [], samenvatting: "x",
         subEffortAnalysis: [
           { groepId: "g1", domein: "mens", actie: "apart_houden",
             items: ["e1"], reden: "verschilt", voorgesteldeNaam: null },
         ],
       });
       expect(result.success).toBe(true);
       if (result.success) expect(result.data.subEffortAnalysis).toHaveLength(1);
     });
   });
   ```

4. **Run `npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts`** — alle bestaande + nieuwe tests moeten groen.

5. **Run `npm run build`** — geen TypeScript errors (nieuwe types `VermogenGelijkenisGroep`, `SubEffortAdvies` exporteren correct).
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts -t "gelijkenis groep|backward compat|markeer_gelijkenis|sub effort advies|subEffortAnalysis|context"</automated>
    <automated>npm ls zod</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls zod` output bevat `din@0.1.0 … ├── zod@4.3.6` (OF hogere v4.x — directe dep, niet meer uitsluitend transitief)
    - `package.json` bevat `"zod"` in `dependencies` (grep `"zod":` in package.json regel tussen `dependencies` en `devDependencies`)
    - `src/lib/schemas.ts` bevat `export const VermogenGelijkenisGroepSchema` (grep verifieert)
    - `src/lib/schemas.ts` bevat `export const SubEffortAdviesSchema` (grep verifieert)
    - `src/lib/schemas.ts` bevat `"markeer_gelijkenis"` in `VermogenClusterItemSchema` enum
    - `src/lib/schemas.ts` bevat `vermogenGelijkenisGroepen` in `Stap2ResultSchema`
    - `src/lib/schemas.ts` bevat `subEffortAnalysis` in `Stap4ResultSchema`
    - `src/lib/schemas.ts` bevat `context: z.string().optional()` binnen `consolidatieAdvies` object shape
    - `src/lib/schemas.ts` bevat `export type VermogenGelijkenisGroep = z.infer<typeof VermogenGelijkenisGroepSchema>`
    - `src/lib/schemas.ts` bevat `export type SubEffortAdvies = z.infer<typeof SubEffortAdviesSchema>`
    - `npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts` exit code 0
    - `npm run build` exit code 0
    - Bestaande Phase 8 tests in `cross-analyse-schema.test.ts` blijven groen (test count >= 11 origineel + 12 nieuw)
  </acceptance_criteria>
  <done>
    Zod is expliciete dep; `VermogenGelijkenisGroepSchema`, `SubEffortAdviesSchema`, enum-uitbreiding, context-veld en `subEffortAnalysis` zijn beschikbaar in `schemas.ts` met type-exports; alle bestaande + 12 nieuwe schema-tests groen; build groen.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Nieuwe module `src/lib/consolidation-guards.ts` met guard-stubs, context-types en werkende pure helper `computeAutoApplyResult` + eigen test-file</name>
  <files>
    src/lib/consolidation-guards.ts,
    src/lib/__tests__/consolidation-guards.test.ts
  </files>
  <read_first>
    - src/lib/types.ts (verifieer aanwezigheid van DINEffort, CapabilityEffortMap, VermogenGelijkenisGroep-import-pad)
    - src/components/steps/CrossAnalyseStep.tsx (regel 85-130 — mergeEfforts signature die Wave 1 gaat uitbreiden)
    - src/components/cross-analyse/StapConsolidatie.tsx (regel 108-162 — auto-apply useEffect die Wave 2 gaat refactoren via computeAutoApplyResult)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-CONTEXT.md (D-01, D-02, D-05, D-27 — guard contracten)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-RESEARCH.md (regel 192-253 voor guard-signatures + regel 330-375 voor auto-apply pattern + regel 895-908 voor Wave 0 Gaps checklist)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-VALIDATION.md (Task 17-01-04 voor test-naam)
    - src/lib/__tests__/consolidation.test.ts (regel 1-88 — mock pattern + createMockSession helper die hergebruikt kan worden)
  </read_first>
  <behavior>
    - Test 1 ("module exports present"): `import { SECTOR_NAME_REGEX, MIN_TITLE_LENGTH, validateNeutralTitle, validateSameDomain, validateDrieluikThreshold, computeAutoApplyResult } from "@/lib/consolidation-guards"` resolveert; elke export is defined.
    - Test 2 ("SECTOR_NAME_REGEX locked pattern"): `SECTOR_NAME_REGEX.source` EQUAL `"\\b(PO|VO|Zakelijk|primair onderwijs|voortgezet onderwijs)\\b"`. Flags bevat `i`.
    - Test 3 ("MIN_TITLE_LENGTH locked value"): `MIN_TITLE_LENGTH === 10`.
    - Test 4 ("auto apply skip failure — happy path"): Cluster met 2 valid items + geldige title → result bevat 1 mergedKey, 0 failedKeys.
    - Test 5 ("auto apply skip failure — guard throws captured"): Mock `mergeFn` die throw bij specifieke cluster → result skipt die cluster naar `failedKeys`, reason bevat error-message. Andere valid clusters worden alsnog gemerged.
    - Test 6 ("auto apply skip failure — deduplicatie"): Cluster-key die al in `alreadyMergedKeys` zit wordt overgeslagen zonder mergeFn call.
    - Test 7 ("auto apply skip failure — <2 items"): Cluster met 1 item wordt niet gemerged, niet in failedKeys.
  </behavior>
  <action>
1. **Maak `src/lib/consolidation-guards.ts`** met de volgende inhoud:
   ```typescript
   // Phase 17: pure consolidation guards en helpers.
   // Deze module bevat GEEN React-imports en kan vrij geïmporteerd worden in tests + pure functions.
   //
   // Design notes:
   // - validateNeutralTitle / validateSameDomain / validateDrieluikThreshold zijn in Wave 0 stubs
   //   met de correcte signature; Wave 1 implementeert de throw-logic en verankert ze in mergeEfforts.
   // - computeAutoApplyResult is al WERKEND en unit-testbaar — Wave 2 refactort de useEffect in
   //   StapConsolidatie.tsx om deze helper aan te roepen.

   import type { DINEffort, CapabilityEffortMap, VermogenGelijkenisGroep } from "./types";

   // --- D-01 locked patterns ---
   export const SECTOR_NAME_REGEX =
     /\b(PO|VO|Zakelijk|primair onderwijs|voortgezet onderwijs)\b/i;
   export const MIN_TITLE_LENGTH = 10;

   // --- D-27 context shape ---
   export interface DrieluikContext {
     gelijkenisGroepen: VermogenGelijkenisGroep[];
     capEffortMaps: CapabilityEffortMap[];
   }

   // --- Guard stubs (Wave 1 implementeert throw-logica) ---

   /**
    * D-01: Valideert dat een voorgestelde titel sectoroverstijgend is
    * (geen PO/VO/Zakelijk substrings) en minstens MIN_TITLE_LENGTH lang.
    * Wave 0: stub. Wave 1: implementeer throw Error('Titel bevat sector-naam "X" …').
    */
   export function validateNeutralTitle(_title: string): void {
     // WAVE 1: implementeer conform D-01
     return;
   }

   /**
    * D-02: Valideert dat alle items hetzelfde `domain` hebben.
    * Wave 0: stub. Wave 1: implementeer throw bij cross-domein.
    */
   export function validateSameDomain(_items: DINEffort[]): void {
     // WAVE 1: implementeer conform D-02
     return;
   }

   /**
    * D-27: Valideert dat alle te mergen efforts via capabilityEffortMaps terug-refereren
    * naar vermogens uit DEZELFDE VermogenGelijkenisGroep (ongeacht sectoren — schema garandeert ≥1 per sector).
    * Wave 0: stub. Wave 1: implementeer drempel-check + throw.
    */
   export function validateDrieluikThreshold(
     _effortIds: string[],
     _ctx: DrieluikContext
   ): void {
     // WAVE 1: implementeer conform D-27
     return;
   }

   // --- Auto-apply pure helper (D-05) — WERKEND in Wave 0 ---

   export interface AutoApplyCluster {
     key: string; // vooraf berekende dedup-key, bv. sorted-ids joined met ","
     itemIds: string[];
     suggestedTitle?: string;
   }

   export interface AutoApplyResult {
     mergedKeys: string[];
     failedKeys: string[];
     reasons: Record<string, string>; // key → error message
   }

   /**
    * D-05: Draait de auto-apply loop als pure functie. Gebruikt een injected `mergeFn`
    * zodat tests de throw-gedrag kunnen simuleren zonder session-mocks.
    *
    * Contract:
    * - Cluster met <2 items: skip (niet merged, niet failed).
    * - Cluster met key in alreadyMergedKeys: skip.
    * - mergeFn throw: skip + record reason in failedKeys.
    * - mergeFn success: add key to mergedKeys.
    */
   export function computeAutoApplyResult(
     clusters: AutoApplyCluster[],
     alreadyMergedKeys: Set<string>,
     mergeFn: (itemIds: string[], suggestedTitle?: string) => void
   ): AutoApplyResult {
     const mergedKeys: string[] = [];
     const failedKeys: string[] = [];
     const reasons: Record<string, string> = {};

     for (const cluster of clusters) {
       if (cluster.itemIds.length < 2) continue;
       if (alreadyMergedKeys.has(cluster.key)) continue;
       try {
         mergeFn(cluster.itemIds, cluster.suggestedTitle);
         mergedKeys.push(cluster.key);
       } catch (err) {
         failedKeys.push(cluster.key);
         reasons[cluster.key] = err instanceof Error ? err.message : String(err);
       }
     }

     return { mergedKeys, failedKeys, reasons };
   }
   ```

   **BELANGRIJK:** Verifieer dat `VermogenGelijkenisGroep` import-pad klopt. Het type komt uit `src/lib/schemas.ts` (via `z.infer`), maar `types.ts` re-exporteert vaak. Als Task 1 het type NIET in `types.ts` re-exporteert, dan import direct: `import type { VermogenGelijkenisGroep } from "./schemas";`. Grep eerst: `grep "export type VermogenGelijkenisGroep" src/lib/types.ts src/lib/schemas.ts` — kies juiste pad. Als geen re-export in types.ts → import uit schemas.

2. **Maak `src/lib/__tests__/consolidation-guards.test.ts`:**
   ```typescript
   import { describe, test, expect, vi } from "vitest";
   import {
     SECTOR_NAME_REGEX,
     MIN_TITLE_LENGTH,
     validateNeutralTitle,
     validateSameDomain,
     validateDrieluikThreshold,
     computeAutoApplyResult,
     type AutoApplyCluster,
   } from "@/lib/consolidation-guards";

   describe("consolidation-guards module exports (Wave 0 scaffold)", () => {
     test("Test 1: all named exports resolve", () => {
       expect(validateNeutralTitle).toBeDefined();
       expect(validateSameDomain).toBeDefined();
       expect(validateDrieluikThreshold).toBeDefined();
       expect(computeAutoApplyResult).toBeDefined();
     });

     test("Test 2: SECTOR_NAME_REGEX locked pattern (D-01)", () => {
       expect(SECTOR_NAME_REGEX.source).toBe(
         "\\b(PO|VO|Zakelijk|primair onderwijs|voortgezet onderwijs)\\b"
       );
       expect(SECTOR_NAME_REGEX.flags).toContain("i");
     });

     test("Test 3: MIN_TITLE_LENGTH locked value", () => {
       expect(MIN_TITLE_LENGTH).toBe(10);
     });
   });

   describe("computeAutoApplyResult (D-05) — auto apply skip failure", () => {
     test("happy path — valid cluster gets merged", () => {
       const mergeFn = vi.fn();
       const clusters: AutoApplyCluster[] = [
         { key: "a,b", itemIds: ["a", "b"], suggestedTitle: "Sectoroverstijgende training" },
       ];
       const result = computeAutoApplyResult(clusters, new Set(), mergeFn);
       expect(result.mergedKeys).toEqual(["a,b"]);
       expect(result.failedKeys).toEqual([]);
       expect(mergeFn).toHaveBeenCalledWith(["a", "b"], "Sectoroverstijgende training");
     });

     test("guard throw is captured in failedKeys with reason", () => {
       const mergeFn = vi.fn((ids: string[]) => {
         if (ids.includes("bad")) throw new Error('Titel bevat sector-naam "PO"');
       });
       const clusters: AutoApplyCluster[] = [
         { key: "bad,x", itemIds: ["bad", "x"], suggestedTitle: "PO-training" },
         { key: "c,d", itemIds: ["c", "d"], suggestedTitle: "Neutrale titel lang" },
       ];
       const result = computeAutoApplyResult(clusters, new Set(), mergeFn);
       expect(result.mergedKeys).toEqual(["c,d"]);
       expect(result.failedKeys).toEqual(["bad,x"]);
       expect(result.reasons["bad,x"]).toContain("sector-naam");
     });

     test("skips clusters with <2 items silently", () => {
       const mergeFn = vi.fn();
       const clusters: AutoApplyCluster[] = [
         { key: "a", itemIds: ["a"], suggestedTitle: "x" },
       ];
       const result = computeAutoApplyResult(clusters, new Set(), mergeFn);
       expect(result.mergedKeys).toEqual([]);
       expect(result.failedKeys).toEqual([]);
       expect(mergeFn).not.toHaveBeenCalled();
     });

     test("skips clusters already merged via alreadyMergedKeys", () => {
       const mergeFn = vi.fn();
       const clusters: AutoApplyCluster[] = [
         { key: "a,b", itemIds: ["a", "b"] },
       ];
       const result = computeAutoApplyResult(clusters, new Set(["a,b"]), mergeFn);
       expect(result.mergedKeys).toEqual([]);
       expect(mergeFn).not.toHaveBeenCalled();
     });
   });
   ```

3. **Run `npx vitest run src/lib/__tests__/consolidation-guards.test.ts`** — alle 7 tests groen.

4. **Run `npm run build`** — TypeScript compile zonder errors.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/consolidation-guards.test.ts</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/consolidation-guards.ts` bestaat en bevat: `export const SECTOR_NAME_REGEX`, `export const MIN_TITLE_LENGTH`, `export function validateNeutralTitle`, `export function validateSameDomain`, `export function validateDrieluikThreshold`, `export function computeAutoApplyResult`, `export interface DrieluikContext`
    - `src/lib/__tests__/consolidation-guards.test.ts` bestaat (grep `describe("consolidation-guards module exports`)
    - `npx vitest run src/lib/__tests__/consolidation-guards.test.ts` exit code 0 en test count >= 7
    - `npm run build` exit code 0
    - `grep "validateNeutralTitle" src/components/steps/CrossAnalyseStep.tsx` → geen hits (Wave 1 bedrade dit)
    - `SECTOR_NAME_REGEX.source` in runtime EQUAL `"\\b(PO|VO|Zakelijk|primair onderwijs|voortgezet onderwijs)\\b"` (exact)
    - Geen React-imports in `consolidation-guards.ts` (grep `from "react"` → 0 hits)
  </acceptance_criteria>
  <done>
    Nieuwe module `consolidation-guards.ts` bestaat met guard-stubs (Wave 1-hook) + werkende `computeAutoApplyResult`. Eigen test-file groen. Bestaande suite onaangetast.
  </done>
</task>

</tasks>

<verification>
Na afronding van beide tasks:
1. Quick-run: `npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts src/lib/__tests__/consolidation-guards.test.ts src/lib/__tests__/consolidation.test.ts` — alle groen (3-5s).
2. Full suite: `npx vitest run` — alle 20+ test-files groen (30-60s).
3. Build: `npm run build` — exit code 0.
4. Lint: `npm run lint` — exit code 0.
5. Dep-check: `npm ls zod` — toont `zod@^4.x` als directe dep, niet uitsluitend transitief.
</verification>

<success_criteria>
- `zod` is expliciet in `package.json` `dependencies`
- `VermogenGelijkenisGroepSchema` + `SubEffortAdviesSchema` bestaan met type-exports in `schemas.ts`
- `Stap2ResultSchema` bevat `vermogenGelijkenisGroepen` veld, `Stap4ResultSchema` bevat `subEffortAnalysis` + `context` velden, `VermogenClusterItemSchema` accepteert `markeer_gelijkenis` aanbeveling
- `src/lib/consolidation-guards.ts` bestaat met guard-stubs + werkende `computeAutoApplyResult`
- 12+ nieuwe schema-tests in `cross-analyse-schema.test.ts` + 7 nieuwe tests in `consolidation-guards.test.ts`, alle groen
- Bestaande test-suite ongewijzigd en groen
- `npm run build` + `npm run lint` groen
</success_criteria>

<output>
After completion, create `.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-01-SUMMARY.md` met:
- Exacte `zod` versie die geïnstalleerd is
- Lijst van toegevoegde schemas en hun line-numbers in `schemas.ts`
- Overzicht van nieuwe test-counts (cross-analyse-schema, consolidation-guards)
- Bevestiging dat `consolidation-guards.ts` guard-stubs Wave 1-ready zijn (signatures locked)
- Eventuele import-path-beslissingen (VermogenGelijkenisGroep uit schemas vs types)
</output>
