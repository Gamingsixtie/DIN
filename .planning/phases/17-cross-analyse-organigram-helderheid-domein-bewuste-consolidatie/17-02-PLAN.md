---
phase: 17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie
plan: 02
type: execute
wave: 1
depends_on:
  - "17-01"
files_modified:
  - src/lib/consolidation-guards.ts
  - src/components/steps/CrossAnalyseStep.tsx
  - src/lib/__tests__/consolidation.test.ts
  - src/lib/__tests__/consolidation-guards.test.ts
autonomous: true
requirements:
  - R-CROSS-01
  - R-CROSS-02
requirements_addressed:
  - R-CROSS-01
  - R-CROSS-02
user_setup: []

must_haves:
  truths:
    - "`mergeEfforts` gooit een Error met zichtbare reden bij cross-domein items (mens + data_systemen)"
    - "`mergeEfforts` gooit een Error bij een voorgesteldeNaam met sector-substring (`PO`, `VO`, `Zakelijk`)"
    - "`mergeEfforts` gooit een Error bij een voorgesteldeNaam korter dan 10 chars"
    - "`mergeEfforts` gooit een Error wanneer drieluikContext wordt meegegeven maar geen `VermogenGelijkenisGroep` de te mergen efforts kan koppelen"
    - "`mergeEfforts` ZONDER drieluikContext werkt nog (legacy tests blijven groen)"
    - "`SECTOR_NAME_REGEX` laat 'Protocol', 'VOldoende', 'Automatiseren' (false positives) correct door"
    - "`mergeCapabilities` gooit een Error bij sector-substring titel (D-01 geldt ook hier, zelfs al wordt de functie niet meer vanuit cross-analyse flow aangeroepen)"
  artifacts:
    - path: "src/lib/consolidation-guards.ts"
      provides: "Geïmplementeerde guard functies (validateNeutralTitle, validateSameDomain, validateDrieluikThreshold) met throw-semantiek"
      contains: "throw new Error"
    - path: "src/components/steps/CrossAnalyseStep.tsx"
      provides: "mergeEfforts met optionele DrieluikContext parameter + guard calls; mergeCapabilities met title-guard call"
      contains: "validateNeutralTitle(suggestedTitle)"
    - path: "src/lib/__tests__/consolidation.test.ts"
      provides: "7+ nieuwe test-cases voor guards (D-24)"
      min_lines: 300
  key_links:
    - from: "src/components/steps/CrossAnalyseStep.tsx"
      to: "src/lib/consolidation-guards.ts"
      via: "import { validateNeutralTitle, validateSameDomain, validateDrieluikThreshold, DrieluikContext }"
      pattern: "from \"@/lib/consolidation-guards\""
    - from: "mergeEfforts pure function"
      to: "guard execution order"
      via: "title → same-domain → drieluik-threshold"
      pattern: "validateNeutralTitle.*validateSameDomain.*validateDrieluikThreshold"
---

<objective>
Wave 1 — implementeer de drie guards als harde backstops tegen AI-hallucinatie en user-fouten. `mergeEfforts` krijgt een nieuwe optionele parameter `context?: DrieluikContext` voor D-27 drempel-check, zonder backward-compat te breken. `mergeCapabilities` krijgt óók de title-guard (D-01) zelfs al wordt hij niet meer vanuit cross-analyse flow aangeroepen. Test-file wordt uitgebreid met de 7+ cases uit D-24 zodat elk guard-gedrag gedekt is.

**Purpose:** De prompts uit Wave 2 zijn zachte suggesties; de guards zijn de code-harde afdwinging. Zonder deze Wave zou auto-apply (D-05) stille corruptie kunnen veroorzaken bij elke AI-hallucinatie die `combineren` voorstelt voor cross-domein of sector-specifieke items.

**Output:** Werkende guards in `consolidation-guards.ts` (geen stubs meer). `CrossAnalyseStep.tsx` `mergeEfforts` + `mergeCapabilities` roepen guards aan. Test-suite `consolidation.test.ts` groeit met 7+ nieuwe cases die alle `throw`-paden afdekken. Quick-run + full suite + build groen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-CONTEXT.md
@.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-RESEARCH.md
@.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-VALIDATION.md
@.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-01-SUMMARY.md
@CLAUDE.md
@src/lib/consolidation-guards.ts
@src/components/steps/CrossAnalyseStep.tsx
@src/lib/__tests__/consolidation.test.ts
@src/lib/__tests__/consolidation-guards.test.ts
@src/lib/types.ts

<interfaces>
<!-- Belangrijke shapes die Wave 1 aanraakt. -->

From src/components/steps/CrossAnalyseStep.tsx (huidige signatures — regel 9-130):
```typescript
export function mergeCapabilities(
  session: DINSession,
  clusterItemIds: string[],
  suggestedTitle?: string
): DINSession;

export function mergeEfforts(
  session: DINSession,
  clusterItemIds: string[],
  suggestedTitle?: string
  // ← Wave 1 voegt toe: context?: DrieluikContext
): DINSession;
```

From src/lib/consolidation-guards.ts (Wave 0 stubs → Wave 1 implementaties):
```typescript
export interface DrieluikContext {
  gelijkenisGroepen: VermogenGelijkenisGroep[];
  capEffortMaps: CapabilityEffortMap[];
}

export function validateNeutralTitle(title: string): void;   // throw bij sector-substring OF <10 chars
export function validateSameDomain(items: DINEffort[]): void; // throw bij cross-domein
export function validateDrieluikThreshold(effortIds: string[], ctx: DrieluikContext): void; // throw bij geen drieluik-dekking
```

From src/lib/types.ts (verifieer aanwezigheid):
```typescript
export interface DINEffort { id: string; domain: "mens" | "processen" | "data_systemen" | "cultuur"; ... }
export interface CapabilityEffortMap { capabilityId: string; effortId: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementeer guard-bodies in `consolidation-guards.ts` + bedrade in `mergeEfforts` en `mergeCapabilities`</name>
  <files>
    src/lib/consolidation-guards.ts,
    src/components/steps/CrossAnalyseStep.tsx
  </files>
  <read_first>
    - src/lib/consolidation-guards.ts (volledig — Wave 0 stubs die hier worden geïmplementeerd)
    - src/components/steps/CrossAnalyseStep.tsx (regel 9-146 — mergeCapabilities en mergeEfforts huidige implementatie)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-CONTEXT.md (D-01, D-02, D-03, D-25, D-27 — guard contracten verbatim)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-RESEARCH.md (regel 192-275 voor guard implementaties + regel 525-537 voor Pitfall 2)
    - src/lib/types.ts (verifieer DINEffort.domain enum + CapabilityEffortMap shape)
  </read_first>
  <behavior>
    - Via extending `consolidation-guards.test.ts`:
      - Test 1 ("validateNeutralTitle throws on PO substring"): `validateNeutralTitle("Training PO-leerkrachten")` throws, message bevat `"PO"`.
      - Test 2 ("validateNeutralTitle throws on 'primair onderwijs'"): `validateNeutralTitle("Strategie primair onderwijs breed")` throws.
      - Test 3 ("validateNeutralTitle accepts Protocol"): `validateNeutralTitle("Protocol datakwaliteit cito-breed")` geen throw.
      - Test 4 ("validateNeutralTitle accepts VOldoende"): `validateNeutralTitle("VOldoende inzicht in leervraag")` geen throw.
      - Test 5 ("validateNeutralTitle throws on <10 chars"): `validateNeutralTitle("Kort")` throws, message bevat `"minimum"` of `"te kort"`.
      - Test 6 ("validateSameDomain throws cross-domein"): `validateSameDomain([{ domain: "mens" }, { domain: "data_systemen" }])` throws, message bevat `"mens"` en `"data_systemen"`.
      - Test 7 ("validateSameDomain accepts same-domein"): `validateSameDomain([{ domain: "mens" }, { domain: "mens" }])` geen throw.
      - Test 8 ("validateDrieluikThreshold throws without groep coverage"): effortIds die geen van de groepen dekken → throw met 'drieluik' in message.
      - Test 9 ("validateDrieluikThreshold accepts valid drieluik"): effortIds die via capEffortMaps alle naar vermogens uit 1 groep wijzen → geen throw.
  </behavior>
  <action>
1. **Implementeer guard-bodies in `src/lib/consolidation-guards.ts`** — vervang Wave 0 stubs:

   ```typescript
   // D-01: title-guard
   export function validateNeutralTitle(title: string): void {
     if (!title || title.length < MIN_TITLE_LENGTH) {
       throw new Error(
         `Titel te kort (minimum ${MIN_TITLE_LENGTH} tekens): "${title ?? ""}"`
       );
     }
     const match = title.match(SECTOR_NAME_REGEX);
     if (match) {
       throw new Error(
         `Titel bevat sector-naam "${match[0]}" — sectoroverstijgende titel vereist`
       );
     }
   }

   // D-02: same-domein guard
   export function validateSameDomain(items: DINEffort[]): void {
     if (items.length === 0) return;
     const domains = new Set(items.map((e) => e.domain));
     if (domains.size > 1) {
       const list = Array.from(domains).join(" + ");
       throw new Error(`Cross-domein merge geblokkeerd: ${list}`);
     }
   }

   // D-27: drieluik-drempel guard
   export function validateDrieluikThreshold(
     effortIds: string[],
     ctx: DrieluikContext
   ): void {
     if (effortIds.length === 0) return;

     // Per effort: welke capabilityIds raakt hij?
     const effortCaps = new Map<string, Set<string>>();
     for (const eId of effortIds) {
       const caps = ctx.capEffortMaps
         .filter((m) => m.effortId === eId)
         .map((m) => m.capabilityId);
       effortCaps.set(eId, new Set(caps));
     }

     // Zoek een groep waar ELKE effort minstens één cap raakt.
     // Schema garandeert dat een groep ≥1 vermogen per sector heeft (PO+VO+Zakelijk);
     // hier checken we alleen cap-coverage van de efforts.
     for (const groep of ctx.gelijkenisGroepen) {
       const groepCapIds = new Set(groep.vermogenIds);
       const allReach = effortIds.every((eId) => {
         const caps = effortCaps.get(eId) ?? new Set();
         for (const cId of caps) if (groepCapIds.has(cId)) return true;
         return false;
       });
       if (allReach) return; // geldig drieluik
     }

     throw new Error(
       "Cross-analyse drempel niet gehaald: inspanningen raken geen drieluik van gelijkende sector-vermogens"
     );
   }
   ```

2. **Uitbreiden `consolidation-guards.test.ts`** — onder Wave 0 tests, voeg toe:
   ```typescript
   describe("validateNeutralTitle (D-01)", () => {
     test("throws on PO substring", () => {
       expect(() => validateNeutralTitle("Training PO-leerkrachten")).toThrow(/PO/);
     });
     test("throws on VO substring", () => {
       expect(() => validateNeutralTitle("Curriculum VO-scholen breed")).toThrow(/VO/);
     });
     test("throws on Zakelijk substring", () => {
       expect(() => validateNeutralTitle("Training Zakelijke klanten totaal")).toThrow(/Zakelijk/);
     });
     test("throws on 'primair onderwijs'", () => {
       expect(() => validateNeutralTitle("Strategie primair onderwijs breed")).toThrow(/primair onderwijs/i);
     });
     test("throws on <10 chars", () => {
       expect(() => validateNeutralTitle("Kort")).toThrow(/(te kort|minimum)/i);
     });
     test("word boundary false positives — accepteert Protocol", () => {
       expect(() => validateNeutralTitle("Protocol datakwaliteit cito-breed")).not.toThrow();
     });
     test("word boundary false positives — accepteert VOldoende", () => {
       expect(() => validateNeutralTitle("VOldoende inzicht in leervraag")).not.toThrow();
     });
     test("word boundary false positives — accepteert Automatiseren", () => {
       expect(() => validateNeutralTitle("Automatiseren van rapportageproces")).not.toThrow();
     });
     test("accepteert neutrale titel van exact 10 chars", () => {
       expect(() => validateNeutralTitle("Klantgespr")).not.toThrow();
     });
   });

   describe("validateSameDomain (D-02)", () => {
     test("throws on cross-domein mens + data_systemen", () => {
       expect(() => validateSameDomain([
         { id: "a", domain: "mens" } as any,
         { id: "b", domain: "data_systemen" } as any,
       ])).toThrow(/mens.*data_systemen|data_systemen.*mens/);
     });
     test("accepts same-domein mens + mens", () => {
       expect(() => validateSameDomain([
         { id: "a", domain: "mens" } as any,
         { id: "b", domain: "mens" } as any,
       ])).not.toThrow();
     });
     test("accepts empty items list", () => {
       expect(() => validateSameDomain([])).not.toThrow();
     });
   });

   describe("validateDrieluikThreshold (D-27)", () => {
     test("throws when efforts raken geen groep", () => {
       expect(() => validateDrieluikThreshold(
         ["eff-1", "eff-2"],
         {
           gelijkenisGroepen: [
             { id: "g1", vermogenIds: ["cap-x"], gezamenlijkeOmschrijving: "x", reden: "y" },
           ],
           capEffortMaps: [
             { capabilityId: "cap-y", effortId: "eff-1" },
             { capabilityId: "cap-z", effortId: "eff-2" },
           ],
         }
       )).toThrow(/drieluik|drempel/i);
     });
     test("accepts when efforts all reach one groep", () => {
       expect(() => validateDrieluikThreshold(
         ["eff-1", "eff-2"],
         {
           gelijkenisGroepen: [
             { id: "g1", vermogenIds: ["cap-po", "cap-vo", "cap-zak"], gezamenlijkeOmschrijving: "x", reden: "y" },
           ],
           capEffortMaps: [
             { capabilityId: "cap-po", effortId: "eff-1" },
             { capabilityId: "cap-vo", effortId: "eff-2" },
           ],
         }
       )).not.toThrow();
     });
     test("accepts when efforts all reach SAME cap (within one groep)", () => {
       expect(() => validateDrieluikThreshold(
         ["eff-1", "eff-2"],
         {
           gelijkenisGroepen: [
             { id: "g1", vermogenIds: ["cap-po", "cap-vo"], gezamenlijkeOmschrijving: "x", reden: "y" },
           ],
           capEffortMaps: [
             { capabilityId: "cap-po", effortId: "eff-1" },
             { capabilityId: "cap-po", effortId: "eff-2" },
           ],
         }
       )).not.toThrow();
     });
   });
   ```

3. **Bedrade guards in `src/components/steps/CrossAnalyseStep.tsx`:**

   a. Voeg bovenaan imports toe:
   ```typescript
   import {
     validateNeutralTitle,
     validateSameDomain,
     validateDrieluikThreshold,
     type DrieluikContext,
   } from "@/lib/consolidation-guards";
   ```

   b. Uitbreiden `mergeCapabilities` signature (regel 9-13) — voeg title-guard aan begin:
   ```typescript
   export function mergeCapabilities(
     session: DINSession,
     clusterItemIds: string[],
     suggestedTitle?: string
   ): DINSession {
     if (clusterItemIds.length < 2) return session;

     const itemsToMerge = session.capabilities.filter(c => clusterItemIds.includes(c.id));
     if (itemsToMerge.length < 2) return session;

     // D-01: title-guard — blijft ook hier actief voor legacy/binnen-sector gebruik
     if (suggestedTitle) validateNeutralTitle(suggestedTitle);

     // ... rest onveranderd (regel 19-66)
   }
   ```

   c. Uitbreiden `mergeEfforts` signature (regel 85-89):
   ```typescript
   export function mergeEfforts(
     session: DINSession,
     clusterItemIds: string[],
     suggestedTitle?: string,
     context?: DrieluikContext  // D-27: optioneel voor backward compat (legacy tests sturen geen context)
   ): DINSession {
     if (clusterItemIds.length < 2) return session;

     const itemsToMerge = session.efforts.filter(e => clusterItemIds.includes(e.id));
     if (itemsToMerge.length < 2) return session;

     // D-01: title-guard
     if (suggestedTitle) validateNeutralTitle(suggestedTitle);
     // D-02: same-domein guard
     validateSameDomain(itemsToMerge);
     // D-27: drieluik-drempel (optioneel — alleen als context is meegegeven)
     if (context) validateDrieluikThreshold(clusterItemIds, context);

     // ... rest onveranderd (regel 95-130)
   }
   ```

   **BELANGRIJK:** Raak NIET de bestaande body-logica aan — alleen guards toevoegen VÓÓR de bestaande logic, zodat throw vóór mutation gebeurt.

4. **Run quick-run** en verifieer: `npx vitest run src/lib/__tests__/consolidation-guards.test.ts` → alle 7 + 15 nieuwe = 22 tests groen.

5. **Run `npm run build`** — TypeScript compile zonder errors.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/consolidation-guards.test.ts -t "validateNeutralTitle|validateSameDomain|validateDrieluikThreshold|word boundary"</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/consolidation-guards.ts` bevat `throw new Error` in drie functies (grep `throw new Error` → ≥3 hits)
    - `src/components/steps/CrossAnalyseStep.tsx` importeert uit `@/lib/consolidation-guards` (grep `from "@/lib/consolidation-guards"`)
    - `src/components/steps/CrossAnalyseStep.tsx` `mergeEfforts` signature heeft parameter `context?: DrieluikContext` (grep `context\\?: DrieluikContext` of `context\\?:\\s*DrieluikContext`)
    - `src/components/steps/CrossAnalyseStep.tsx` `mergeEfforts` body roept `validateNeutralTitle`, `validateSameDomain`, `validateDrieluikThreshold` aan (grep per functie ≥1 hit in mergeEfforts scope)
    - `src/components/steps/CrossAnalyseStep.tsx` `mergeCapabilities` body roept `validateNeutralTitle` aan (grep ≥1 hit)
    - `npx vitest run src/lib/__tests__/consolidation-guards.test.ts` exit 0 met ≥15 extra tests (Wave 0 had 7, Wave 1 voegt ≥15 toe voor een totaal van ≥22)
    - `npm run build` exit 0
    - Geen regressies: `npx vitest run src/lib/__tests__/consolidation.test.ts` blijft groen (bestaande test-file niet gewijzigd door deze task)
  </acceptance_criteria>
  <done>
    Guards werken: cross-domein merge throws, sector-specifieke titel throws, <10 chars throws, false-positives passeren, drieluik-drempel throws bij ontbreken coverage, geldig drieluik passeert. `mergeEfforts` en `mergeCapabilities` roepen de guards aan in correcte volgorde. Build + test-suite groen.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Uitbreiden `consolidation.test.ts` met guard-integratie via `mergeEfforts` / `mergeCapabilities` callsites (D-24 coverage)</name>
  <files>
    src/lib/__tests__/consolidation.test.ts
  </files>
  <read_first>
    - src/lib/__tests__/consolidation.test.ts (volledig — bestaande mock-patterns op regel 1-88, bestaande mergeEfforts describe regel 182-200)
    - src/components/steps/CrossAnalyseStep.tsx (post-Task 1 signatures — mergeEfforts met context-param)
    - src/lib/consolidation-guards.ts (post-Task 1 guards)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-CONTEXT.md (D-24 — lijst van nieuwe test-cases)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-VALIDATION.md (Task 17-02-01..17-02-07 — test-namen voor `-t` matching)
  </read_first>
  <behavior>
    - Test 1 ("title guard rejects sector name"): `mergeEfforts(session, [id1, id2], "Training PO-leerkrachten")` throws, message bevat `"PO"`.
    - Test 2 ("title guard min length"): `mergeEfforts(session, [id1, id2], "Kort")` throws, message bevat `"minimum"` of `"te kort"`.
    - Test 3 ("word boundary false positives"): `mergeEfforts(session, [id1, id2], "Protocol datakwaliteit cito-breed")` throws NIET op de title-guard (mag wel op andere guards throwen als setup dat vereist).
    - Test 4 ("domain guard rejects"): Session met effort A (mens) + effort B (data_systemen) → `mergeEfforts(session, [A.id, B.id], "Sectoroverstijgende inspanning")` throws, message bevat `"mens"` en `"data_systemen"` (of omgekeerd).
    - Test 5 ("domain guard accepts"): Session met twee `mens`-efforts → `mergeEfforts(session, [a.id, b.id], "Sectoroverstijgende training")` slaagt en creëert shared effort (mockUUID).
    - Test 6 ("drieluik threshold throws"): `mergeEfforts(session, [id1, id2], "Neutrale titel lang", { gelijkenisGroepen: [], capEffortMaps: [...] })` throws met `/drieluik|drempel/i`.
    - Test 7 ("drieluik threshold accepts"): `mergeEfforts(session, [eff-po-1, eff-vo-1], "Sector-overstijgende training", drieluikCtxMetValidCoverage)` slaagt.
    - Test 8 ("mergeEfforts backward compat zonder context"): `mergeEfforts(session, [eff-po-1, eff-vo-1])` — bestaande call zonder context werkt nog (title + domain guards draaien, drieluik niet).
    - Test 9 ("mergeCapabilities title guard"): `mergeCapabilities(session, [cap-po-1, cap-vo-1], "Training PO")` throws.
    - Test 10 ("mergeCapabilities zonder title werkt nog"): `mergeCapabilities(session, [cap-po-1, cap-vo-1])` — geen title = geen title-guard = werkt (legacy).
  </behavior>
  <action>
1. **Uitbreiden `src/lib/__tests__/consolidation.test.ts`:**

   a. Voeg boven de bestaande imports een extra type-import toe:
   ```typescript
   import type { DrieluikContext } from "@/lib/consolidation-guards";
   ```

   b. ONDER de bestaande `describe("undoMergeEfforts", …)` blok (na regel 224), voeg deze volledige sectie toe:
   ```typescript
   // ============================================================
   // Phase 17: Guards (D-01, D-02, D-27) via mergeEfforts / mergeCapabilities
   // ============================================================

   describe("mergeEfforts title guard (D-01)", () => {
     it("title guard rejects sector name (Training PO-leerkrachten)", () => {
       const session = createMockSession();
       expect(() =>
         mergeEfforts(session, ["eff-po-1", "eff-vo-1"], "Training PO-leerkrachten")
       ).toThrow(/PO/);
     });

     it("title guard min length — <10 chars throws", () => {
       const session = createMockSession();
       expect(() =>
         mergeEfforts(session, ["eff-po-1", "eff-vo-1"], "Kort")
       ).toThrow(/(te kort|minimum)/i);
     });

     it("word boundary false positives — Protocol passes title guard", () => {
       const session = createMockSession();
       // Protocol bevat 'PO' als substring maar niet op word boundary → moet passeren
       // (mag wel throwen op andere guards; deze test isoleert via try/catch op message)
       let err: Error | null = null;
       try {
         mergeEfforts(session, ["eff-po-1", "eff-vo-1"], "Protocol datakwaliteit cito-breed");
       } catch (e) {
         err = e as Error;
       }
       if (err) {
         expect(err.message).not.toMatch(/sector-naam/i);
       }
     });
   });

   describe("mergeEfforts domain guard (D-02)", () => {
     it("domain guard rejects cross-domein merge (mens + data_systemen)", () => {
       const session = createMockSession({
         efforts: [
           { id: "eff-mens", sectorId: "PO", title: "t1", description: "d1",
             domain: "mens" as const, status: "gepland" as const, dependencies: [] },
           { id: "eff-data", sectorId: "VO", title: "t2", description: "d2",
             domain: "data_systemen" as const, status: "gepland" as const, dependencies: [] },
         ],
         capabilityEffortMaps: [],
       });
       expect(() =>
         mergeEfforts(session, ["eff-mens", "eff-data"], "Sectoroverstijgende inspanning")
       ).toThrow(/mens.*data_systemen|data_systemen.*mens/);
     });

     it("domain guard accepts same-domein merge (mens + mens)", () => {
       const session = createMockSession(); // beide eff-po-1 en eff-vo-1 zijn 'mens'
       const result = mergeEfforts(session, ["eff-po-1", "eff-vo-1"], "Sectoroverstijgende training");
       expect(result.efforts.find((e) => e.id === mockUUID)).toBeDefined();
     });
   });

   describe("mergeEfforts drieluik threshold (D-27)", () => {
     it("drieluik threshold throws when geen groep dekking", () => {
       const session = createMockSession();
       const ctx: DrieluikContext = {
         gelijkenisGroepen: [
           { id: "g-empty", vermogenIds: ["cap-nonexistent"],
             gezamenlijkeOmschrijving: "x", reden: "y" },
         ],
         capEffortMaps: session.capabilityEffortMaps,
       };
       expect(() =>
         mergeEfforts(session, ["eff-po-1", "eff-vo-1"], "Sectoroverstijgende training", ctx)
       ).toThrow(/drieluik|drempel/i);
     });

     it("drieluik threshold accepts valid coverage", () => {
       const session = createMockSession();
       const ctx: DrieluikContext = {
         gelijkenisGroepen: [
           { id: "g-valid", vermogenIds: ["cap-po-1", "cap-vo-1", "cap-zak-1"],
             gezamenlijkeOmschrijving: "Klantgesprek-methodiek", reden: "Alle drie sectoren" },
         ],
         capEffortMaps: session.capabilityEffortMaps, // eff-po-1 → cap-po-1; eff-vo-1 → cap-vo-1
       };
       const result = mergeEfforts(session, ["eff-po-1", "eff-vo-1"], "Sectoroverstijgende training", ctx);
       expect(result.efforts.find((e) => e.id === mockUUID)).toBeDefined();
     });

     it("mergeEfforts backward compat zonder context — werkt nog", () => {
       const session = createMockSession();
       // Legacy call zonder context: title + domain guards draaien, drieluik niet
       const result = mergeEfforts(session, ["eff-po-1", "eff-vo-1"], "Sectoroverstijgende training");
       expect(result.efforts.find((e) => e.id === mockUUID)).toBeDefined();
     });
   });

   describe("mergeCapabilities title guard (D-01, D-25)", () => {
     it("mergeCapabilities title guard — rejects sector name", () => {
       const session = createMockSession();
       expect(() =>
         mergeCapabilities(session, ["cap-po-1", "cap-vo-1"], "Training PO")
       ).toThrow(/PO/);
     });

     it("mergeCapabilities zonder title — geen title-guard = werkt (legacy)", () => {
       const session = createMockSession();
       const result = mergeCapabilities(session, ["cap-po-1", "cap-vo-1"]);
       expect(result.capabilities.find((c) => c.id === mockUUID)).toBeDefined();
     });
   });
   ```

   **LET OP:** De test-namen MOETEN exact matchen met `-t` patterns uit `17-VALIDATION.md`:
   - `"title guard rejects sector name"` → Task 17-02-01 grep-target
   - `"title guard min length"` → Task 17-02-02
   - `"word boundary false positives"` → Task 17-02-03
   - `"domain guard rejects"` → Task 17-02-04
   - `"domain guard accepts"` → Task 17-02-05
   - `"drieluik threshold throws"` → Task 17-02-06 (match via substring in "drieluik threshold throws when geen groep dekking")
   - `"drieluik threshold accepts"` → Task 17-02-07 (match via substring in "drieluik threshold accepts valid coverage")

2. **Run quick-run:**
   ```bash
   npx vitest run src/lib/__tests__/consolidation.test.ts -t "title guard|domain guard|drieluik threshold|backward compat|Phase 17"
   ```
   Alle bovenstaande tests moeten groen.

3. **Run full suite:** `npx vitest run` — geen regressies, alle bestaande tests groen, test-count ≥ Wave 0 + 10 nieuwe = 22+ nieuwe tests over beide suites.

4. **Run `npm run build`** — TypeScript compile zonder errors.

5. **Run `npm run lint`** — geen lint errors (let op: geen `any`-warnings, gebruik `as any` spaarzaam zoals in guard-test patronen).
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/consolidation.test.ts -t "title guard rejects sector name"</automated>
    <automated>npx vitest run src/lib/__tests__/consolidation.test.ts -t "title guard min length"</automated>
    <automated>npx vitest run src/lib/__tests__/consolidation.test.ts -t "word boundary false positives"</automated>
    <automated>npx vitest run src/lib/__tests__/consolidation.test.ts -t "domain guard rejects"</automated>
    <automated>npx vitest run src/lib/__tests__/consolidation.test.ts -t "domain guard accepts"</automated>
    <automated>npx vitest run src/lib/__tests__/consolidation.test.ts -t "drieluik threshold throws"</automated>
    <automated>npx vitest run src/lib/__tests__/consolidation.test.ts -t "drieluik threshold accepts"</automated>
    <automated>npx vitest run</automated>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/__tests__/consolidation.test.ts` bevat `describe("mergeEfforts title guard` (grep)
    - `src/lib/__tests__/consolidation.test.ts` bevat `describe("mergeEfforts domain guard`
    - `src/lib/__tests__/consolidation.test.ts` bevat `describe("mergeEfforts drieluik threshold`
    - `src/lib/__tests__/consolidation.test.ts` bevat `describe("mergeCapabilities title guard`
    - `src/lib/__tests__/consolidation.test.ts` bevat `import type { DrieluikContext }`
    - Alle 7 `-t` patterns uit VALIDATION.md exit 0 (title guard rejects sector name, title guard min length, word boundary false positives, domain guard rejects, domain guard accepts, drieluik threshold throws, drieluik threshold accepts)
    - `npx vitest run` exit 0 (geen regressies in andere test-files)
    - `npm run build` exit 0
    - `npm run lint` exit 0
    - Bestaande tests in `consolidation.test.ts` (mergeCapabilities, undoMergeCapabilities, mergeEfforts, undoMergeEfforts — regel 90-224) blijven groen
  </acceptance_criteria>
  <done>
    Alle 7 VALIDATION.md `-t` patterns voor Plan 02 groen. Guards zijn integratief getest via mergeEfforts/mergeCapabilities callsites. Bestaande suite zonder regressies. Wave 1 gate green (build + lint + full suite).
  </done>
</task>

</tasks>

<verification>
Wave 1 afsluitende checks:
1. Quick-run voor deze wave: `npx vitest run src/lib/__tests__/consolidation.test.ts src/lib/__tests__/consolidation-guards.test.ts` — alle groen.
2. Full suite: `npx vitest run` — alle 20+ test-files groen.
3. Build + lint: `npm run build && npm run lint` — beiden exit 0.
4. Grep cross-check: `grep -n "validateNeutralTitle\\|validateSameDomain\\|validateDrieluikThreshold" src/components/steps/CrossAnalyseStep.tsx` → minimaal 4 hits (imports + 3 guard-calls in mergeEfforts; 1 call in mergeCapabilities voor title-guard).
5. Handmatige smoke: `npx vitest run --reporter=verbose src/lib/__tests__/consolidation.test.ts src/lib/__tests__/consolidation-guards.test.ts` en verifieer in output dat de 7 kritieke test-namen uit VALIDATION.md allemaal als "passed" staan.
</verification>

<success_criteria>
- Guards geïmplementeerd (geen stubs meer) in `consolidation-guards.ts`
- `mergeEfforts` signature uitgebreid met `context?: DrieluikContext`; roept `validateNeutralTitle`, `validateSameDomain`, `validateDrieluikThreshold` aan in juiste volgorde
- `mergeCapabilities` roept `validateNeutralTitle` aan (D-01 geldt ook hier)
- 15+ nieuwe tests in `consolidation-guards.test.ts` + 10+ nieuwe tests in `consolidation.test.ts`
- Alle 7 VALIDATION.md `-t` patterns (Plan 02) exit 0
- `npm run build` + `npm run lint` + `npx vitest run` groen
- Legacy `mergeEfforts(session, ids)` calls zonder context werken nog
</success_criteria>

<output>
After completion, create `.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-02-SUMMARY.md` met:
- Exacte error-message strings die de guards gooien (zodat Wave 3 UI hun error-banner copy kan matchen)
- Lijst van toegevoegde test-beschrijvingen en hun line-numbers
- Bevestiging dat `mergeEfforts` backward-compat is (legacy call zonder context werkt)
- Opmerking of er edge cases zijn ontdekt tijdens implementation (bv. lege effortIds array bij validateDrieluikThreshold)
</output>
