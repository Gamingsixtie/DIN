---
phase: 17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie
plan: 04
type: execute
wave: 3
depends_on:
  - "17-01"
  - "17-02"
  - "17-03"
files_modified:
  - src/components/cross-analyse/StapSectorVertaling.tsx
  - src/components/cross-analyse/StapConsolidatie.tsx
  - src/components/cross-analyse/shared/ClusterCard.tsx
  - src/components/cross-analyse/shared/ConsolidationActionBar.tsx
  - src/components/cross-analyse/CrossAnalyseWizard.tsx
autonomous: false
requirements:
  - R-CROSS-01
  - R-CROSS-02
requirements_addressed:
  - R-CROSS-01
  - R-CROSS-02
user_setup: []

must_haves:
  truths:
    - "Per `VermogenGelijkenisGroep` rendert het organigram drie parallelle sector-vermogen-kaarten (PO+VO+Zakelijk) met `SectorBadge` — geen shared cap meer"
    - "Boven de drie vermogen-kaarten: rationale-kop met `gezamenlijkeOmschrijving` + domein-balans-badge `Dekt N van 4 domeinen`"
    - "Onder de drie vermogen-kaarten: color-coded hefboomlaag per domein (Mens/Processen/Data & Systemen/Cultuur) met gebundelde inspanningen uit `subEffortAnalysis`"
    - "Elke gebundelde inspanning draagt een `Hefboom: raakt 3 sectoren` badge met tooltip die de drie vermogens opsomt"
    - "Handmatige merge-fail toont een inline rode error-banner met exacte guard-reason, Combineren-knop is disabled"
    - "Auto-apply toont een samenvattende toast `N samengevoegd, M vereisen review`; guard-failers krijgen `Vereist review` badge op ClusterCard"
    - "Auto-apply gebruikt 3-stappen pattern: (1) pure compute BUITEN reducer, (2) updateSession met voorberekende updatedSession, (3) aparte setMergedEffClusters statement — GEEN setState binnen reducer"
    - "Per cluster een context-textarea + `Herzie advies` knop die `/api/din-suggest?type=consolidatie-herzien` aanroept en het cluster in `stap4Result.consolidatieAdvies` overschrijft met de response"
    - "Na Herzie-advies wordt `subEffortAnalysis` voor de getroffen `VermogenGelijkenisGroep(en)` geïnvalideerd (D-12), zodat volgende Analyseer-run deze opnieuw genereert"
    - "Legacy sessies met `capabilities[].consolidated=true` tonen amber badge `Legacy: vermogens-merge (niet meer toegepast in cross-analyse)` — geen crash"
  artifacts:
    - path: "src/components/cross-analyse/StapSectorVertaling.tsx"
      provides: "Drieluik-rendering per VermogenGelijkenisGroep + hefboomlaag + domein-balans-badge + legacy-warning"
      contains: "vermogenGelijkenisGroepen"
      min_lines: 250
    - path: "src/components/cross-analyse/shared/ConsolidationActionBar.tsx"
      provides: "Inline error banner + Combineren-knop disabled-state + Herzie-knop met loading"
      contains: "guardError"
    - path: "src/components/cross-analyse/shared/ClusterCard.tsx"
      provides: "Context-textarea + Herzie-advies knop bedrading + Vereist review badge"
      contains: "onHerzieAdvies"
    - path: "src/components/cross-analyse/StapConsolidatie.tsx"
      provides: "Auto-apply via computeAutoApplyResult (3-stappen pattern) + summary toast + failedClusters state + herzie-advies bedrading met subEffortAnalysis invalidatie"
      contains: "computeAutoApplyResult"
    - path: "src/components/cross-analyse/CrossAnalyseWizard.tsx"
      provides: "Stap 4 AI-call stuurt vermogenGelijkenisGroepen + capabilityEffortMaps mee; response.subEffortAnalysis persisted in stap4Result"
      contains: "vermogenGelijkenisGroepen"
  key_links:
    - from: "src/components/cross-analyse/StapSectorVertaling.tsx"
      to: "stap2Result.vermogenGelijkenisGroepen + stap4Result.subEffortAnalysis"
      via: "prop-lezing in component + iteratie in render"
      pattern: "vermogenGelijkenisGroepen.*map"
    - from: "src/components/cross-analyse/StapConsolidatie.tsx"
      to: "src/lib/consolidation-guards.ts (computeAutoApplyResult)"
      via: "import + gebruik in useEffect (pure-compute fase van 3-stappen pattern)"
      pattern: "computeAutoApplyResult"
    - from: "src/components/cross-analyse/shared/ClusterCard.tsx"
      to: "/api/din-suggest?type=consolidatie-herzien"
      via: "Herzie-advies fetch call"
      pattern: "consolidatie-herzien"
    - from: "src/components/cross-analyse/CrossAnalyseWizard.tsx"
      to: "/api/cross-analyse stap 4 payload"
      via: "body.stap2Result.vermogenGelijkenisGroepen + body.capabilityEffortMaps"
      pattern: "vermogenGelijkenisGroepen"
    - from: "StapConsolidatie.onHerzieAdvies handler"
      to: "stap4Result.subEffortAnalysis (cache-invalidatie D-12)"
      via: "filter op affectedGroepIds via capabilityEffortMap + vermogenGelijkenisGroepen lookup"
      pattern: "subEffortAnalysis.*filter"
---

<objective>
Wave 3 — UI rewrite die de methodische correctie zichtbaar maakt. Drieluik-rendering vervangt Variant A/B in `StapSectorVertaling.tsx`. `ConsolidationActionBar` krijgt een inline guard-error banner met `Herzie advies` fallback. `ClusterCard` krijgt context-textarea + `Herzie advies` knop. `StapConsolidatie` gebruikt de `computeAutoApplyResult` helper uit Wave 0 voor een schoon auto-apply via het **3-stappen pattern** (pure compute → updateSession → setMergedEffClusters — GEEN setState binnen reducer). `CrossAnalyseWizard` stuurt de juiste payload (`vermogenGelijkenisGroepen` + `capabilityEffortMaps`) mee in stap 4 en persisted `subEffortAnalysis` terug in session.

**Purpose:** Zonder deze Wave zijn de methodische regels wel afgedwongen in code (Wave 1) en spreekt de AI ze wel uit (Wave 2), maar ziet de gebruiker de hefboomwerking niet en kan zij niet feedback geven op AI-adviezen. Wave 3 is waar R-CROSS-01 (helderheid) tastbaar wordt.

Daarnaast: React 19 Strict Mode dispatcht reducers tweemaal tijdens development; een `setState` call binnen een `updateSession(prev => ...)` reducer leidt tot dubbele UI-state updates en is een concrete productie-bug. Deze wave borgt het correcte pattern.

**Output:** Drie UI-componenten herschreven/uitgebreid (StapSectorVertaling volledige rewrite van vermogen-sectie; ConsolidationActionBar + ClusterCard + StapConsolidatie surgical edits); CrossAnalyseWizard payload-upgrade; human-verify checkpoint met 7 concrete UAT-cases uit VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-CONTEXT.md
@.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-RESEARCH.md
@.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-VALIDATION.md
@.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-01-SUMMARY.md
@.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-02-SUMMARY.md
@.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-03-SUMMARY.md
@CLAUDE.md
@src/components/cross-analyse/StapSectorVertaling.tsx
@src/components/cross-analyse/StapConsolidatie.tsx
@src/components/cross-analyse/shared/ClusterCard.tsx
@src/components/cross-analyse/shared/ConsolidationActionBar.tsx
@src/components/cross-analyse/shared/SectorBadge.tsx
@src/components/cross-analyse/CrossAnalyseWizard.tsx
@src/lib/consolidation-guards.ts

<interfaces>
<!-- Contracten die Wave 3 gebruikt. Alle components zijn "use client". -->

From src/components/cross-analyse/shared/SectorBadge.tsx:
```typescript
export interface SectorBadgeProps { sector: string; } // rendert PO/VO/Zakelijk kleuren
```

From src/lib/schemas.ts (post-Wave-0):
```typescript
export type VermogenGelijkenisGroep = { id: string; vermogenIds: string[]; gezamenlijkeOmschrijving: string; reden: string };
export type SubEffortAdvies = { groepId: string; domein: EffortDomain; actie: "combineren"|"apart_houden"; items: string[]; reden: string; voorgesteldeNaam: string|null };
```

From src/components/cross-analyse/StapSectorVertaling.tsx (huidig — regel 22-34):
```typescript
// DOMAIN_COLORS + DOMAIN_LABELS — hergebruikt in hefboomlaag:
const DOMAIN_COLORS: Record<EffortDomain, { bg: string; border: string; text: string }> = {
  mens:          { bg: "bg-blue-50",   border: "border-blue-200",   text: "text-blue-700" },
  processen:     { bg: "bg-green-50",  border: "border-green-200",  text: "text-green-700" },
  data_systemen: { bg: "bg-purple-50", border: "border-purple-200", text: "text-purple-700" },
  cultuur:       { bg: "bg-amber-50",  border: "border-amber-200",  text: "text-amber-700" },
};
const DOMAIN_LABELS: Record<EffortDomain, string> = {
  mens: "Mens", processen: "Processen", data_systemen: "Data & Systemen", cultuur: "Cultuur",
};
```

From src/components/cross-analyse/shared/ConsolidationActionBar.tsx (state machine — regel 32-35):
```typescript
const [showConfirm, setShowConfirm] = useState(false);
const [showAfstemmingsAdvies, setShowAfstemmingsAdvies] = useState(false);
const [generatedAdvice, setGeneratedAdvice] = useState<string[] | null>(null);
const [isGenerating, setIsGenerating] = useState(false);
// ← Wave 3 voegt toe: guardError + isHerzienLoading
```

From src/components/cross-analyse/shared/ClusterCard.tsx (props — regel 21-90):
```typescript
// Bestaande props. Wave 3 voegt OPTIONELE herzie-props toe:
// onHerzieAdvies?: (context: string) => Promise<void>
// isHerzienLoading?: boolean
// savedContext?: string
// requiresReview?: boolean
```

From src/lib/consolidation-guards.ts (Wave 1):
```typescript
export function computeAutoApplyResult(
  clusters: AutoApplyCluster[],
  alreadyMergedKeys: Set<string>,
  mergeFn: (itemIds: string[], suggestedTitle?: string) => void
): { mergedKeys: string[]; failedKeys: string[]; reasons: Record<string, string> };
```

From src/lib/types.ts (voor cache-invalidatie D-12):
```typescript
// session.capabilityEffortMaps is een direct array: Array<{ capabilityId: string; effortId: string }>
// (gebruikt consistent in din-service.ts, demo-data.ts, CrossAnalyseStep.tsx)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StapSectorVertaling.tsx — volledige rewrite van vermogen-sectie naar drieluik-rendering per VermogenGelijkenisGroep + hefboomlaag + domein-balans-badge + hefboom-badge + legacy warning</name>
  <files>
    src/components/cross-analyse/StapSectorVertaling.tsx
  </files>
  <read_first>
    - src/components/cross-analyse/StapSectorVertaling.tsx (volledig — de bestaande Variant A/B logica regel 104-493 wordt VERVANGEN; behoud de header/focus-doel/baten logica)
    - src/components/cross-analyse/shared/SectorBadge.tsx (verifieer prop-signature en kleur-output)
    - src/lib/schemas.ts (VermogenGelijkenisGroep + SubEffortAdvies types)
    - src/lib/stap5-focus.ts (`getConsolidationOrigins` voor legacy-warning detectie, hergebruikt pattern)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-CONTEXT.md (D-28, D-29, D-32, D-23 — exacte UI-contracten)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-RESEARCH.md (regel 378-475 voor Pattern 4/5 drieluik + hefboom-pijlen + badge; regel 762-799 voor DomainBalanceBadge + legacy warning code)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-VALIDATION.md (Task 17-04-01, 17-04-05 — UAT scope)
    - .claude/skills/interface-design/SKILL.md (voor visuele craft bij drieluik layout)
  </read_first>
  <behavior>
    - Visueel gedrag wordt gedekt in Task 3 (human-verify). Deze task levert de code die Task 3 kan testen.
    - Typescript-level assertions (via `npm run build` + lint):
      - Component compileert met strict TS (nieuwe types `VermogenGelijkenisGroep` en `SubEffortAdvies` correct geïmporteerd uit `@/lib/schemas`).
      - Nieuwe interne helpers hebben expliciete return types.
  </behavior>
  <action>
1. **Lees eerst `src/components/cross-analyse/StapSectorVertaling.tsx` volledig** (~700 regels). Identificeer:
   - Header + focus-doel sectie (regel 1-100 ongeveer) → **BEHOUDEN, niet wijzigen**.
   - Oude Variant A/B logica (regel 122-140 + 232-493 ongeveer) → **VERWIJDEREN** en vervangen door drieluik-rendering.
   - Per-sector baten-rendering → **BEHOUDEN**.
   - DOMAIN_COLORS + DOMAIN_LABELS maps → **BEHOUDEN**, hergebruiken in hefboomlaag.

2. **Uitbreiden imports** bovenaan:
   ```typescript
   import type {
     VermogenGelijkenisGroep,
     SubEffortAdvies,
   } from "@/lib/schemas";
   import SectorBadge from "@/components/cross-analyse/shared/SectorBadge";
   // (SectorBadge is al geïmporteerd in wizard-context; als niet in dit file, voeg toe)
   ```

3. **Voeg interne helpers toe** (direct na DOMAIN_COLORS map):
   ```typescript
   const DOMEIN_ORDER: EffortDomain[] = ["mens", "processen", "data_systemen", "cultuur"];

   function DomainBalanceBadge({
     groepId,
     subEffortAnalysis,
   }: {
     groepId: string;
     subEffortAnalysis: SubEffortAdvies[];
   }) {
     const coveredDomains = new Set(
       subEffortAnalysis
         .filter((s) => s.groepId === groepId && s.actie === "combineren")
         .map((s) => s.domein)
     );
     const missing = DOMEIN_ORDER.filter((d) => !coveredDomains.has(d));
     const count = coveredDomains.size;
     const style =
       count >= 3
         ? "bg-green-50 text-green-700 border-green-200"
         : count === 2
         ? "bg-amber-50 text-amber-700 border-amber-200"
         : "bg-red-50 text-red-700 border-red-200";
     return (
       <span
         className={`inline-block text-[10px] font-semibold border rounded-full px-2 py-0.5 ${style} mt-2`}
         data-testid={`domain-balance-badge-${groepId}`}
       >
         Dekt {count} van 4 domeinen
         {missing.length > 0 ? ` — mist ${missing.map((d) => DOMAIN_LABELS[d]).join(", ")}` : ""}
       </span>
     );
   }

   function HefboomBadge({
     groep,
     vermogens,
   }: {
     groep: VermogenGelijkenisGroep;
     vermogens: Array<{ id: string; sectorId: string; title?: string; description: string }>;
   }) {
     return (
       <span className="relative group inline-block">
         <span className="inline-flex items-center gap-1 text-[10px] font-semibold text-teal-700 border border-teal-300 bg-teal-50 rounded-full px-2 py-0.5 mt-1">
           <svg className="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
             <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2}
               d="M13 10V3L4 14h7v7l9-11h-7z" />
           </svg>
           Hefboom: raakt 3 sectoren via gelijkende vermogens
         </span>
         <span className="invisible group-hover:visible absolute left-0 top-full mt-1 z-10 bg-gray-900 text-white text-xs rounded p-2 shadow-lg min-w-[200px]">
           Gelijkende vermogens:
           <ul className="mt-1 space-y-0.5">
             {vermogens.map((v) => (
               <li key={v.id}>{v.sectorId}: {v.title || v.description}</li>
             ))}
           </ul>
         </span>
       </span>
     );
   }

   function HefboomPijlen() {
     return (
       <div className="hidden md:grid grid-cols-3 gap-3 my-2">
         {[0, 1, 2].map((i) => (
           <div key={i} className="flex justify-center">
             <svg className="w-6 h-6 text-teal-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
               <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2}
                 d="M5 10l7-7m0 0l7 7m-7-7v18" />
             </svg>
           </div>
         ))}
       </div>
     );
   }
   ```

4. **Vervang de Variant A/B sectie** (regel 122-493 ongeveer — lees exact eerst) door een iteratie over `vermogenGelijkenisGroepen`:
   ```tsx
   {/* --- Drieluik-rendering per VermogenGelijkenisGroep (D-28) --- */}
   {(stap2Result?.vermogenGelijkenisGroepen ?? []).map((groep) => {
     const groepVermogens = session.capabilities.filter((c) =>
       groep.vermogenIds.includes(c.id)
     );
     if (groepVermogens.length === 0) return null;

     const groepSubAnalyses = (stap4Result?.subEffortAnalysis ?? []).filter(
       (s) => s.groepId === groep.id
     );

     return (
       <section
         key={groep.id}
         className="bg-teal-50/30 border border-teal-200 rounded-xl p-5 mb-4"
         data-testid={`vermogen-gelijkenis-groep-${groep.id}`}
       >
         {/* BOVEN: rationale-kop + domein-balans */}
         <div className="mb-4 text-center">
           <p className="text-[10px] font-bold text-teal-700 uppercase tracking-wider mb-1">
             Gelijkende vermogens — hefboomgroep
           </p>
           <p className="text-sm font-semibold text-gray-800">
             {groep.gezamenlijkeOmschrijving}
           </p>
           <p className="text-xs text-gray-500 mt-1 italic">{groep.reden}</p>
           <DomainBalanceBadge
             groepId={groep.id}
             subEffortAnalysis={stap4Result?.subEffortAnalysis ?? []}
           />
         </div>

         {/* MIDDEN: drie parallelle sector-vermogen-kaarten */}
         <div className="grid grid-cols-1 md:grid-cols-3 gap-3 mb-3">
           {groepVermogens.map((cap) => (
             <div
               key={cap.id}
               className="bg-white border-2 border-teal-300 rounded-lg p-3"
               data-testid={`vermogen-card-${cap.id}`}
             >
               <div className="flex items-center justify-between mb-1">
                 <SectorBadge sector={cap.sectorId} />
                 {cap.consolidated && (
                   <span className="inline-block text-[10px] font-semibold bg-amber-50 text-amber-700 border border-amber-200 rounded px-1.5 py-0.5">
                     Legacy: vermogens-merge
                   </span>
                 )}
               </div>
               <p className="text-sm font-medium text-gray-800">
                 {cap.title || cap.description}
               </p>
               {cap.title && cap.description && cap.description !== cap.title && (
                 <p className="text-xs text-gray-500 mt-1">{cap.description}</p>
               )}
             </div>
           ))}
         </div>

         {/* Hefboom-pijlen (desktop only) */}
         <HefboomPijlen />

         {/* ONDER: hefboomlaag per domein */}
         <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-2 mt-2">
           {DOMEIN_ORDER.map((domein) => {
             const advies = groepSubAnalyses.find((s) => s.domein === domein);
             const colors = DOMAIN_COLORS[domein];

             if (!advies) {
               return (
                 <div
                   key={domein}
                   className="border border-dashed border-gray-300 rounded-lg p-3 opacity-60"
                 >
                   <p className={`text-[10px] font-bold uppercase tracking-wider ${colors.text} mb-1`}>
                     {DOMAIN_LABELS[domein]}
                   </p>
                   <p className="text-xs text-gray-400">Geen gezamenlijke inspanning</p>
                 </div>
               );
             }

             const relatedEfforts = session.efforts.filter((e) => advies.items.includes(e.id));

             return (
               <div
                 key={domein}
                 className={`border ${colors.border} ${colors.bg} rounded-lg p-3`}
                 data-testid={`hefboomlaag-${groep.id}-${domein}`}
               >
                 <div className="flex items-center justify-between mb-1">
                   <p className={`text-[10px] font-bold uppercase tracking-wider ${colors.text}`}>
                     {DOMAIN_LABELS[domein]}
                   </p>
                   <span
                     className={`text-[9px] font-semibold px-1.5 py-0.5 rounded ${
                       advies.actie === "combineren"
                         ? "bg-green-100 text-green-700"
                         : "bg-gray-100 text-gray-600"
                     }`}
                   >
                     {advies.actie === "combineren" ? "Combineren" : "Apart"}
                   </span>
                 </div>
                 {advies.voorgesteldeNaam && (
                   <p className="text-xs font-semibold text-gray-800 mb-1">
                     {advies.voorgesteldeNaam}
                   </p>
                 )}
                 <p className="text-[11px] text-gray-600 mb-2">{advies.reden}</p>
                 <ul className="text-[11px] text-gray-700 space-y-0.5">
                   {relatedEfforts.map((eff) => (
                     <li key={eff.id}>• {eff.title || eff.description}</li>
                   ))}
                 </ul>
                 {advies.actie === "combineren" && (
                   <HefboomBadge groep={groep} vermogens={groepVermogens} />
                 )}
               </div>
             );
           })}
         </div>
       </section>
     );
   })}

   {/* Fallback: als er GEEN vermogenGelijkenisGroepen zijn, toon empty state */}
   {(stap2Result?.vermogenGelijkenisGroepen ?? []).length === 0 && (
     <div className="bg-gray-50 border border-gray-200 rounded-lg p-4 text-center">
       <p className="text-sm text-gray-600">
         Nog geen vermogen-gelijkenis-groepen gedetecteerd. Doorloop stap 2 om gelijkende
         sector-vermogens te markeren.
       </p>
     </div>
   )}
   ```

5. **Verwijder de oude Variant B detection logic** (regel 122-140 in huidige file) en de sharedCaps berekening waar deze niet meer gebruikt wordt. Let op: behoud `getConsolidationOrigins` call als die al gebruikt wordt voor `MergeHerkomst` elders in de file — dat is legacy warning detectie.

6. **Run `npm run build`** — exit 0 (strict TS). Fix eventuele type-errors.

7. **Run `npm run lint`** — exit 0.

8. **Run `npx vitest run`** — alle bestaande tests groen (deze file heeft geen directe unit tests, dekking via Task 3 human-verify).
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/cross-analyse/StapSectorVertaling.tsx` bevat `vermogenGelijkenisGroepen` iteratie (grep `vermogenGelijkenisGroepen.*map`)
    - Bevat `import type { VermogenGelijkenisGroep, SubEffortAdvies } from "@/lib/schemas"` (grep)
    - Bevat `function DomainBalanceBadge` + `function HefboomBadge` + `function HefboomPijlen` definities (grep per functie)
    - Bevat `data-testid="vermogen-gelijkenis-groep-` (grep — voor UAT hooks)
    - Bevat `data-testid="hefboomlaag-` (grep)
    - Bevat string `"Hefboom: raakt 3 sectoren via gelijkende vermogens"` (grep)
    - Bevat string `"Legacy: vermogens-merge"` (grep)
    - Bevat string `"Dekt"` + `"van 4 domeinen"` (D-29 exacte copy)
    - Bevat string `"Geen gezamenlijke inspanning"` (empty-state)
    - Oude Variant B detection is VERWIJDERD: grep `sharedEffortGroups` → 0 hits (of enkel in legacy detect helper die blijft voor MergeHerkomst)
    - `npm run build` exit 0
    - `npm run lint` exit 0
    - `npx vitest run` exit 0 (geen regressies)
  </acceptance_criteria>
  <done>
    Drieluik-rendering vervangt Variant A/B volledig. Per groep: 3 parallelle sector-kaarten, hefboom-pijlen (desktop), per-domein hefboomlaag met `subEffortAnalysis`-koppeling, hefboom-badge bij combineren-advies, domein-balans-badge boven de groep, legacy-warning bij `consolidated: true`. Build + lint + test-suite groen.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consolidation UX — ConsolidationActionBar guard-error banner + ClusterCard context-textarea/herzie-knop + StapConsolidatie auto-apply via 3-stappen pattern + subEffortAnalysis invalidatie + CrossAnalyseWizard payload-upgrade</name>
  <files>
    src/components/cross-analyse/shared/ConsolidationActionBar.tsx,
    src/components/cross-analyse/shared/ClusterCard.tsx,
    src/components/cross-analyse/StapConsolidatie.tsx,
    src/components/cross-analyse/CrossAnalyseWizard.tsx
  </files>
  <read_first>
    - src/components/cross-analyse/shared/ConsolidationActionBar.tsx (volledig — state machine regel 32-35, buttons en condities)
    - src/components/cross-analyse/shared/ClusterCard.tsx (volledig — prop-signature regel 21-90, bestaande acties-sectie)
    - src/components/cross-analyse/StapConsolidatie.tsx (volledig — met focus op auto-apply useEffect regel 108-162 en handleMerge handlers)
    - src/components/cross-analyse/CrossAnalyseWizard.tsx (volledig — stap 4 fetch call rond regel 244-249; zoek `stap: 4` of `stap===4` payload)
    - src/lib/consolidation-guards.ts (post-Wave-1: `computeAutoApplyResult`, `AutoApplyCluster`, `AutoApplyResult`, `DrieluikContext`)
    - src/lib/types.ts (verifieer dat `session.capabilityEffortMaps` een directe array is, NIET een nested object)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-CONTEXT.md (D-04 inline banner, D-05 auto-apply skip, D-06..D-09 herzie regen, D-12 cache-invalidatie na herzie)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-RESEARCH.md (regel 286-375 voor Pattern 2/3 state-machine + auto-apply extraction)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-VALIDATION.md (Task 17-04-02, 17-04-03, 17-04-04 — UAT scope)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-02-SUMMARY.md (exacte guard error-message strings — die komen terug in de UI banner-copy)
  </read_first>
  <behavior>
    - `ConsolidationActionBar`: bij `tryMerge()` catch van throw → `guardError` state bevat de exacte error-message uit Wave 1 → inline rood banner toont tekst → Combineren-knop is disabled zolang `guardError !== null`.
    - `ClusterCard`: nieuwe optionele props `onHerzieAdvies`, `isHerzienLoading`, `savedContext`, `requiresReview` — alleen gerenderd wanneer props aanwezig. Textarea input state lokaal, button call `onHerzieAdvies(contextText)`.
    - `StapConsolidatie` auto-apply (3-stappen pattern, W-5 fix): Stap A: pure `computeAutoApplyResult` call buiten React state, muteert alleen een lokale `updatedSession` variable via `mergeFn` closure. Stap B: één `updateSession(() => result.updatedSession)` reducer (reducer is puur, returnt alleen). Stap C: aparte `setMergedEffClusters(prev => result.newMergeKeyMap)` statement buiten reducer. Geen setState binnen reducer.
    - `StapConsolidatie.onHerzieAdvies` handler (D-12 fix, B-4): na het overschrijven van `consolidatieAdvies[i]` wordt `subEffortAnalysis` gefilterd — verwijder alle entries waarvan `groepId` hoort bij een `VermogenGelijkenisGroep` waarvan ≥1 `vermogenId` via `capabilityEffortMap` een `effortId` in het herziene cluster raakt.
    - `CrossAnalyseWizard`: stap 4 fetch-payload bevat `stap2Result: session.crossAnalyseWizard?.stepResults?.stap2` (zodat API route `vermogenGelijkenisGroepen` kan lezen) + `capabilityEffortMaps: session.capabilityEffortMaps`.
  </behavior>
  <action>
1. **Uitbreiden `src/components/cross-analyse/shared/ConsolidationActionBar.tsx`:**

   a. In state-machine sectie (regel 32-35), voeg toe:
   ```typescript
   const [guardError, setGuardError] = useState<string | null>(null);
   const [isHerzienLoading, setIsHerzienLoading] = useState(false);
   ```

   b. Wrap de bestaande merge-call (zoek `onMerge(` in de file) in try/catch:
   ```typescript
   async function tryMerge() {
     try {
       onMerge(itemIds);
       setGuardError(null);
       setShowConfirm(false);
     } catch (err) {
       setGuardError(err instanceof Error ? err.message : "Merge gefaald");
       setShowConfirm(false);
     }
   }
   // Vervang bestaande directe `onMerge(...)` oproepen met `tryMerge()`
   ```

   c. Render inline error banner BOVEN de bestaande sub-states:
   ```tsx
   {guardError && (
     <div className="mt-3 p-3 bg-red-50 border border-red-200 rounded-lg" data-testid="guard-error-banner">
       <p className="text-xs font-semibold text-red-800">Samenvoeging niet mogelijk</p>
       <p className="text-xs text-red-700 mt-1">{guardError}</p>
       <div className="flex gap-2 mt-2">
         {onHerzieAdvies && (
           <button
             onClick={async () => {
               setIsHerzienLoading(true);
               try { await onHerzieAdvies(""); } finally { setIsHerzienLoading(false); }
               setGuardError(null);
             }}
             disabled={isHerzienLoading}
             className="text-xs px-3 py-1 rounded bg-red-600 text-white hover:bg-red-700 disabled:opacity-50"
           >
             {isHerzienLoading ? "Bezig..." : "Herzie advies"}
           </button>
         )}
         <button
           onClick={() => setGuardError(null)}
           className="text-xs px-3 py-1 rounded bg-gray-100 text-gray-700 hover:bg-gray-200"
         >
           Sluiten
         </button>
       </div>
     </div>
   )}
   ```

   d. Voor de `Combineren`-button: voeg `disabled={guardError !== null || suggestedTitle == null}` toe (combineer met bestaande disabled logic).

   e. Als `ConsolidationActionBarProps` nog geen `onHerzieAdvies?: (context: string) => Promise<void>` prop heeft, voeg toe als OPTIONELE prop (backward compat).

2. **Uitbreiden `src/components/cross-analyse/shared/ClusterCard.tsx`:**

   a. Props-signature uitbreiden (OPTIONEEL voor backward compat):
   ```typescript
   export interface ClusterCardProps {
     // ... bestaande props ...
     onHerzieAdvies?: (userContext: string) => Promise<void>;
     isHerzienLoading?: boolean;
     savedContext?: string;
     requiresReview?: boolean;
   }
   ```

   b. Aan het einde van de card render (NA de bestaande acties-sectie, bv. onder `ConsolidationActionBar`), voeg toe:
   ```tsx
   {requiresReview && (
     <div className="border-l-4 border-red-500 bg-red-50 px-3 py-2 my-2 rounded" data-testid="requires-review-badge">
       <p className="text-xs font-semibold text-red-700">Vereist review</p>
       <p className="text-[11px] text-red-600">Auto-apply kon dit cluster niet veilig samenvoegen — bekijk het advies en pas handmatig aan.</p>
     </div>
   )}

   {onHerzieAdvies && (
     <div className="mt-3 border-t border-gray-200 pt-3" data-testid="herzie-advies-section">
       {savedContext && (
         <p className="text-[11px] text-gray-600 mb-2 italic">
           <span className="font-semibold">Herzien met context:</span> {savedContext}
         </p>
       )}
       <HerzieAdviesInput
         onSubmit={onHerzieAdvies}
         isLoading={isHerzienLoading ?? false}
       />
     </div>
   )}
   ```

   c. Voeg een kleine `HerzieAdviesInput` sub-component toe bovenaan de file of net boven default export:
   ```typescript
   function HerzieAdviesInput({
     onSubmit,
     isLoading,
   }: {
     onSubmit: (context: string) => Promise<void>;
     isLoading: boolean;
   }) {
     const [context, setContext] = useState("");
     return (
       <div className="flex flex-col gap-2">
         <textarea
           value={context}
           onChange={(e) => setContext(e.target.value)}
           disabled={isLoading}
           placeholder="Voeg context toe voor een herzien advies (bv. focus op data-domein)..."
           className="w-full text-xs border border-gray-300 rounded p-2 disabled:opacity-50"
           rows={2}
         />
         <button
           onClick={async () => { await onSubmit(context); }}
           disabled={isLoading}
           className="self-start text-xs px-3 py-1 rounded bg-teal-600 text-white hover:bg-teal-700 disabled:opacity-50"
         >
           {isLoading ? (
             <span className="inline-flex items-center gap-1">
               <svg className="animate-spin w-3 h-3" viewBox="0 0 24 24">
                 <circle cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" fill="none" className="opacity-25" />
                 <path d="M4 12a8 8 0 018-8" stroke="currentColor" strokeWidth="4" fill="none" />
               </svg>
               Bezig...
             </span>
           ) : "Herzie advies"}
         </button>
       </div>
     );
   }
   ```

3. **Uitbreiden `src/components/cross-analyse/StapConsolidatie.tsx` — CRITICAL: 3-stappen pattern voor auto-apply (W-5 fix):**

   a. Imports aanvullen:
   ```typescript
   import { computeAutoApplyResult, type DrieluikContext, type AutoApplyCluster } from "@/lib/consolidation-guards";
   import { mergeEfforts } from "@/components/steps/CrossAnalyseStep";
   ```

   b. Voeg state toe voor failed-clusters + contexts:
   ```typescript
   const [failedClusterKeys, setFailedClusterKeys] = useState<Set<string>>(new Set());
   const [clusterReasons, setClusterReasons] = useState<Record<string, string>>({});
   const [clusterContexts, setClusterContexts] = useState<Record<string, string>>({});
   ```

   c. **Herschrijf de auto-apply `useEffect` (regel 108-162) naar het 3-stappen pattern:**

   ```typescript
   useEffect(() => {
     if (autoApplied) return;
     const hasCombineren = inspanningClusters.some((c) => c.aanbeveling === "combineren");
     if (!hasCombineren) return;

     setAutoApplied(true);

     // === STAP A — Pure compute BUITEN React state ===
     // Muteert alleen een lokale `updatedSession` en een lokale `newMergeKeyMap` via mergeFn-closure.
     // Geen enkele setState-call binnen deze scope.
     const drieluikCtx: DrieluikContext = {
       gelijkenisGroepen: stap2Result?.vermogenGelijkenisGroepen ?? [],
       capEffortMaps: session.capabilityEffortMaps,
     };

     const clusters: AutoApplyCluster[] = inspanningClusters
       .filter((c) => c.aanbeveling === "combineren")
       .map((c) => {
         const ids = c.items.map((it) => it.id);
         return {
           key: [...ids].sort().join(","),
           itemIds: ids,
           suggestedTitle: findSuggestedTitle(c.clusterTitel),
         };
       });

     let updatedSession = session;
     const newMergeKeyMap = new Map(mergedEffClusters);
     const alreadyMerged = new Set(mergedEffClusters.keys());

     const mergeFn = (ids: string[], title?: string) => {
       updatedSession = mergeEfforts(updatedSession, ids, title, drieluikCtx);
       const sharedId = updatedSession.efforts[updatedSession.efforts.length - 1]?.id;
       if (sharedId) {
         newMergeKeyMap.set([...ids].sort().join(","), sharedId);
       }
     };

     const result = computeAutoApplyResult(clusters, alreadyMerged, mergeFn);

     // === STAP B — updateSession met voorberekende session (reducer is PUUR) ===
     updateSession(() => updatedSession);

     // === STAP C — aparte setState calls BUITEN de reducer ===
     setMergedEffClusters(() => newMergeKeyMap);
     setFailedClusterKeys(new Set(result.failedKeys));
     setClusterReasons(result.reasons);

     const mergedN = result.mergedKeys.length;
     const failedN = result.failedKeys.length;
     const summary = failedN > 0
       ? `${mergedN} cluster${mergedN !== 1 ? "s" : ""} samengevoegd, ${failedN} vereisen review`
       : `${mergedN} cluster${mergedN !== 1 ? "s" : ""} samengevoegd`;
     if (mergedN > 0 || failedN > 0) setToastMessage(summary);

     // Opmerking D-25: vermogen-clusters met "combineren" worden NIET meer auto-toegepast.
     // `mergeCapabilities` blijft geëxporteerd voor legacy handmatige use, maar cross-analyse
     // flow stelt geen "combineren" meer voor op vermogens (Wave 2 prompt).
   }, [stap2Result, stap3Result, stap4Result, autoApplied]); // eslint-disable-line react-hooks/exhaustive-deps
   ```

   **BELANGRIJK waarom 3 stappen (W-5):**
   - React 19 Strict Mode dispatcht reducers tweemaal tijdens development. Als je `setMergedEffClusters` binnen `updateSession(prev => ...)` plaatst, wordt de outer setState twee keer aangeroepen — en de inner setState binnen een state-updater van een andere state is een anti-pattern dat tot ongedefinieerd gedrag leidt.
   - Pure compute in Stap A: `computeAutoApplyResult` met een mergeFn-closure die *alleen* lokale vars muteert. Geen enkele React state call. Idempotent — veilig om dubbel te draaien bij Strict Mode.
   - Stap B: reducer is `() => updatedSession` — volledig puur, returnt alleen de voorberekende nieuwe session.
   - Stap C: aparte statements. Elke `setFoo` wordt dan correct batched door React.

   d. Voor elke `ClusterCard` render, pass de nieuwe props door — **LET OP B-4 cache-invalidatie in onHerzieAdvies**:

   ```tsx
   <ClusterCard
     cluster={cluster}
     // ... bestaande props ...
     requiresReview={failedClusterKeys.has([...cluster.items.map(it => it.id)].sort().join(","))}
     savedContext={clusterContexts[cluster.clusterTitel]}
     onHerzieAdvies={async (userContext) => {
       setClusterContexts((p) => ({ ...p, [cluster.clusterTitel]: userContext }));
       const advies = stap4Result?.consolidatieAdvies?.find(
         (a) => a.clusterTitel === cluster.clusterTitel
       );
       const res = await fetch("/api/din-suggest", {
         method: "POST",
         headers: { "Content-Type": "application/json" },
         body: JSON.stringify({
           type: "consolidatie-herzien",
           clusterTitel: cluster.clusterTitel,
           clusterItems: cluster.items,
           origineelAdvies: advies,
           userContext,
           kibGoals: session.goals,
           kibScope: session.scope,
         }),
       });
       const data = await res.json();
       if (data.success && stap4Result) {
         // === D-12 / B-4 fix — invalideer subEffortAnalysis voor getroffen groep(en) ===
         // Bepaal welke VermogenGelijkenisGroep(en) via capabilityEffortMap gelinkt zijn aan de
         // inspanningen in dit herziene cluster. subEffortAnalysis-entries met die groepId worden
         // verwijderd zodat volgende Analyseer-run ze opnieuw genereert (D-12 sticky cache-exit).
         const vermogenGelijkenisGroepen = stap2Result?.vermogenGelijkenisGroepen ?? [];
         const clusterEffortIds = cluster.items.map((it) => it.id);
         const affectedCapIds = clusterEffortIds.flatMap((iid) =>
           session.capabilityEffortMaps
             .filter((m) => m.effortId === iid)
             .map((m) => m.capabilityId)
         );
         const affectedGroepIds = new Set(
           vermogenGelijkenisGroepen
             .filter((g) => g.vermogenIds.some((vid) => affectedCapIds.includes(vid)))
             .map((g) => g.id)
         );
         const filteredSub =
           stap4Result.subEffortAnalysis?.filter(
             (s) => !affectedGroepIds.has(s.groepId)
           ) ?? [];

         // Overschrijf cluster in consolidatieAdvies + schrijf gefilterde subEffortAnalysis terug
         updateSession((prev) => {
           const updated = prev.crossAnalyseWizard?.stepResults?.stap4;
           if (!updated) return prev;
           const newConsolidatieAdvies = updated.consolidatieAdvies.map((a) =>
             a.clusterTitel === cluster.clusterTitel
               ? { ...a, ...data.data, context: userContext }
               : a
           );
           return {
             ...prev,
             crossAnalyseWizard: {
               ...prev.crossAnalyseWizard!,
               stepResults: {
                 ...prev.crossAnalyseWizard!.stepResults,
                 stap4: {
                   ...updated,
                   consolidatieAdvies: newConsolidatieAdvies,
                   subEffortAnalysis: filteredSub,
                 },
               },
             },
           };
         });
       }
     }}
   />
   ```

   **BELANGRIJK waarom invalideren (B-4):** D-12 stelt dat `subEffortAnalysis` sticky is, behalve bij een expliciete user-actie: (a) stap 4 Analyseer opnieuw, (b) Herzie advies op een cluster. Zonder deze filter blijft een stale sub-analyse zichtbaar die gebaseerd is op het oude advies — de user ziet dan twee tegenstrijdige uitkomsten in het organigram.

4. **Uitbreiden `src/components/cross-analyse/CrossAnalyseWizard.tsx`:**

   Zoek de stap 4 fetch-call (rond regel 244-249; zoek `stap: 4` of `"stap": 4`). Voeg aan de body toe:
   ```typescript
   body: JSON.stringify({
     // ... bestaande velden ...
     stap: 4,
     stap2Result: session.crossAnalyseWizard?.stepResults?.stap2,
     capabilityEffortMaps: session.capabilityEffortMaps,
     // ...
   }),
   ```

   Na de response-verwerking, verifieer dat `subEffortAnalysis` correct in `stap4Result` wordt opgeslagen (via bestaande `updateSession` met de response-data). Als de bestaande flow `data.analysis` direct spreidt: geen wijziging nodig — de Zod-parse zorgt automatisch dat `subEffortAnalysis` leeg defaultet wanneer ontbrekend.

5. **Run:**
   - `npm run build` → exit 0 (Let op: nieuwe type-imports moeten resolveren; drieluikCtx type bestaat in `consolidation-guards.ts`).
   - `npm run lint` → exit 0 (let op ongebruikte imports — verwijder als niet langer nodig).
   - `npx vitest run` → exit 0 (bestaande suite).
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/cross-analyse/shared/ConsolidationActionBar.tsx` bevat `const [guardError, setGuardError] = useState<string | null>(null)` (grep)
    - Bevat `data-testid="guard-error-banner"` (grep — voor UAT hook)
    - Bevat `tryMerge` of equivalent try/catch om `onMerge(` call (grep `try {` binnen file + `setGuardError` in catch)
    - `src/components/cross-analyse/shared/ClusterCard.tsx` bevat `onHerzieAdvies?:` in props (grep)
    - Bevat `function HerzieAdviesInput` (grep)
    - Bevat `data-testid="herzie-advies-section"` en `data-testid="requires-review-badge"` (grep per string)
    - `src/components/cross-analyse/StapConsolidatie.tsx` bevat `import { computeAutoApplyResult` (grep)
    - Bevat `computeAutoApplyResult(` call in auto-apply useEffect (grep)
    - Bevat `DrieluikContext` type gebruik (grep)
    - Bevat nieuwe state `failedClusterKeys` (grep)
    - Bevat fetch met `type: "consolidatie-herzien"` (grep `"consolidatie-herzien"`)
    - **W-5 fix — 3-stappen pattern:** auto-apply useEffect body bevat ALLEEN ÉÉN `updateSession(...)` call, en deze reducer-body is puur `() => updatedSession` (grep `updateSession\\(\\(\\) => updatedSession\\)` of vergelijkbaar pattern — geen setState tussen haakjes). `setMergedEffClusters` call staat BUITEN de `updateSession` call (grep: aantal regels tussen `updateSession(` en `setMergedEffClusters(` > 0 en op dezelfde indent-niveau, niet genest).
    - `setFailedClusterKeys`, `setClusterReasons`, `setToastMessage` staan BUITEN de `updateSession` reducer body (dezelfde regel-afstand-check)
    - **B-4 fix — subEffortAnalysis invalidatie:** `onHerzieAdvies` async closure bevat een filter op `stap4Result.subEffortAnalysis` (grep `stap4Result.*subEffortAnalysis.*filter` OR `subEffortAnalysis?\\.filter`) gecombineerd met `affectedGroepIds` set-berekening via `capabilityEffortMaps` (grep `affectedGroepIds`)
    - `onHerzieAdvies` schrijft `subEffortAnalysis: filteredSub` terug in `updateSession` (grep `subEffortAnalysis:\\s*filteredSub`)
    - `src/components/cross-analyse/CrossAnalyseWizard.tsx` bevat `stap2Result: session.crossAnalyseWizard` payload (grep `stap2Result`) binnen stap 4 call
    - Bevat `capabilityEffortMaps: session.capabilityEffortMaps` binnen stap 4 payload (grep)
    - `npm run build` exit 0
    - `npm run lint` exit 0
    - `npx vitest run` exit 0 (geen regressies)
  </acceptance_criteria>
  <done>
    Guard-error banner, context-textarea+herzie-knop, auto-apply via 3-stappen pure helper pattern (geen setState in reducer), Vereist review badge, herzie-advies fetch bedrading met subEffortAnalysis cache-invalidatie voor getroffen groep(en), en stap 4 payload-upgrade zijn geïmplementeerd. Wave 2 API's worden daadwerkelijk vanuit de UI aangesproken. React 19 Strict Mode safe.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT — 7 visuele/functionele verificatie-punten uit VALIDATION.md</name>
  <files>
    src/components/cross-analyse/StapSectorVertaling.tsx,
    src/components/cross-analyse/StapConsolidatie.tsx,
    src/components/cross-analyse/shared/ClusterCard.tsx,
    src/components/cross-analyse/shared/ConsolidationActionBar.tsx
  </files>
  <read_first>
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-VALIDATION.md (Manual-Only Verifications tabel — 8 cases)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-CONTEXT.md (D-04, D-05, D-23, D-28, D-29, D-32 — exacte UI-copy)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-02-SUMMARY.md (exacte guard-error strings die banner moet tonen)
  </read_first>
  <what-built>
    - Drieluik-rendering in StapSectorVertaling (3 parallelle sector-vermogen-kaarten + hefboomlaag + hefboom-badge + domein-balans)
    - Guard-error banner in ConsolidationActionBar bij cross-domein of sector-specifieke-titel merge
    - Context-textarea + `Herzie advies` knop in ClusterCard die `/api/din-suggest?type=consolidatie-herzien` aanroept
    - subEffortAnalysis cache-invalidatie bij Herzie advies voor getroffen groep(en) (D-12)
    - Auto-apply via 3-stappen pattern (pure compute → updateSession → setMergedEffClusters, geen setState in reducer — React 19 Strict Mode safe)
    - Auto-apply summary toast `N samengevoegd, M vereisen review` + `Vereist review` badge op failed clusters
    - Domein-balans-badge per VermogenGelijkenisGroep met groen/amber/rood kleurconventie (≥3 / 2 / ≤1)
  </what-built>
  <action>
Dit is een checkpoint-task. Claude voert GEEN code-wijzigingen uit; Claude instrueert de mens om de onderstaande UAT uit te voeren en de resume-signal af te geven. Claude MOET:

1. `npm run dev` uitvoeren en bevestigen dat server opstart op `localhost:3000` (in background of met time-limited run).
2. De onderstaande UAT-lijst presenteren aan de mens (via chat-output).
3. Wachten op resume-signal voordat Phase 17 als complete wordt gemarkeerd.
  </action>
  <how-to-verify>
    Start dev-server:
    ```bash
    npm run dev
    ```

    **UAT-1 — Drieluik-rendering (R-CROSS-01, VALIDATION 17-04-01):**
    1. Open een sessie waarin stap 4 van cross-analyse voltooid is met `vermogenGelijkenisGroepen` aanwezig (maak zo nodig een test-sessie via demo-loader).
    2. Navigeer naar stap 5 (Sector-vertaling/organigram).
    3. Verifieer: per groep een `bg-teal-50/30` kaart met (a) rationale-kop `gezamenlijkeOmschrijving`, (b) drie parallelle sector-vermogen-kaarten (op desktop naast elkaar, op mobile gestapeld) elk met een correcte `SectorBadge` (PO/VO/Zakelijk met verschillende kleuren), (c) hefboom-pijlen zichtbaar op desktop (verborgen op mobile), (d) vier domein-secties in de hefboomlaag.
    4. PASS-criterium: drie verschillende sector-kleuren zichtbaar binnen één groep; géén "shared cap"-achtige merged vermogen-weergave.

    **UAT-2 — Hefboom-badge tooltip (R-CROSS-01, VALIDATION 17-04-01):**
    1. In dezelfde view, hover over een `Hefboom: raakt 3 sectoren via gelijkende vermogens` badge.
    2. Verifieer: tooltip somt de drie sector-vermogens op (bv. `PO: Klantgesprek-methodiek PO`, `VO: Klantgesprek-methodiek VO`, `Zakelijk: Marktanalyse`).
    3. PASS-criterium: alle drie sector-namen in tooltip, geen duplicaten.

    **UAT-3 — Guard-error banner bij cross-domein merge (R-CROSS-02, VALIDATION 17-04-02):**
    1. Open stap 4 consolidatie-stap.
    2. Forceer een cross-domein merge: pas lokaal een `inspanningCluster` aan met items uit verschillende domeinen (bv. via developer console `session.efforts[…].domain = "data_systemen"` op één item + `= "mens"` op andere).
    3. Klik `Combineren` op die cluster.
    4. Verifieer: rode error-banner `Samenvoeging niet mogelijk` met tekst `Cross-domein merge geblokkeerd: mens + data_systemen` (of omgekeerde volgorde).
    5. Verifieer: `Combineren`-knop is disabled.
    6. PASS-criterium: exacte tekst in banner + disabled knop.

    **UAT-4 — Herzie-advies knop + context-textarea + cache-invalidatie (R-CROSS-01, VALIDATION 17-04-03):**
    1. Op een cluster met bestaand `consolidatieAdvies` en bestaande `subEffortAnalysis` voor de getroffen VermogenGelijkenisGroep, scroll naar `Herzie advies` sectie.
    2. Typ in de textarea: `Focus op data-domein — integreer met Phase 12 projecten`.
    3. Klik `Herzie advies`.
    4. Verifieer: knop toont loading-spinner + tekst `Bezig...`, textarea is disabled.
    5. Na response: cluster toont (a) nieuw advies van AI, (b) bewaard context als italic regel onder cluster (`Herzien met context: Focus op data-domein…`).
    6. **Verifieer via developer tools (React DevTools of console inspect):** `session.crossAnalyseWizard.stepResults.stap4.subEffortAnalysis` bevat NIET meer de entries van de getroffen groep (hun groepId is uit de array verwijderd). Entries van andere groepen blijven intact.
    7. PASS-criterium: complete round-trip werkt, context blijft zichtbaar, subEffortAnalysis voor getroffen groep is geïnvalideerd.

    **UAT-5 — Auto-apply summary toast + Vereist review badge + 3-stappen pattern (R-CROSS-02, VALIDATION 17-04-04):**
    1. Open React DevTools → Profiler → start meten.
    2. Laad een sessie met gemengde clusters (enkele valid combineren + enkele met sector-specifieke titel of cross-domein).
    3. Ga naar stap 4 consolidatie (auto-apply moet triggeren).
    4. Verifieer: toast verschijnt met tekst `X samengevoegd, Y vereisen review` waar X en Y correct zijn geteld.
    5. Verifieer: clusters die faalden hebben een `Vereist review` badge met rode linker-border.
    6. **Verifieer in React DevTools Profiler:** maar ÉÉN render-flush door StapConsolidatie na de auto-apply (geen duplicate renders die wijzen op reducer-dubbele-dispatch).
    7. PASS-criterium: correcte counts in toast + correcte badges op failed clusters + geen visuele flicker.

    **UAT-6 — Domein-balans badge kleurconventie (R-CROSS-01, VALIDATION 17-04-05):**
    1. In organigram, verifieer per groep de `Dekt N van 4 domeinen — mist X, Y`-badge:
       - 3 of 4 gedekte domeinen → groene achtergrond (`bg-green-50 text-green-700`)
       - 2 gedekte domeinen → amber achtergrond (`bg-amber-50 text-amber-700`)
       - 0 of 1 gedekt domein → rode achtergrond (`bg-red-50 text-red-700`)
    2. Tekst toont `N` als getal en `X, Y` als komma-gescheiden ontbrekende domeinen in Nederlandse labels (bv. `mist Data & Systemen, Cultuur`).
    3. PASS-criterium: kleur matcht count-range + tekst is in Nederlands.

    **UAT-7 — Legacy warning badge (R-CROSS-02 smoke):**
    1. Laad een legacy sessie die `session.capabilities[…].consolidated === true` heeft (Phase 8 merge).
    2. Ga naar stap 5 organigram.
    3. Verifieer: op elke consolidated-cap een amber badge `Legacy: vermogens-merge` (beknopt).
    4. Verifieer: GEEN crash, sessie laadt normaal.
    5. PASS-criterium: badge zichtbaar zonder app-breuk.

    **Run na UAT:**
    ```bash
    npx vitest run && npm run build && npm run lint
    ```
    Alle drie MOETEN exit 0.
  </how-to-verify>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
    <automated>npx vitest run</automated>
    <manual>Mens voert UAT-1 t/m UAT-7 uit en keurt af of goed (zie how-to-verify)</manual>
  </verify>
  <acceptance_criteria>
    - `npm run build` exit 0
    - `npm run lint` exit 0
    - `npx vitest run` exit 0 (alle ≥20 test-files groen)
    - Mens bevestigt `UAT-1` (drieluik-rendering) met 3 sector-kaarten en 3 verschillende kleuren
    - Mens bevestigt `UAT-2` (hefboom-tooltip) toont alle 3 sector-vermogens
    - Mens bevestigt `UAT-3` (cross-domein error-banner) met exacte tekst `Cross-domein merge geblokkeerd: mens + data_systemen` of inverse
    - Mens bevestigt `UAT-4` (herzie-advies round-trip) werkt, context blijft zichtbaar, subEffortAnalysis voor getroffen groep is geïnvalideerd
    - Mens bevestigt `UAT-5` (auto-apply toast met correcte counts) + `Vereist review` badge zichtbaar + geen duplicate render-flush in Profiler
    - Mens bevestigt `UAT-6` (domein-balans badge) met correcte kleurconventie groen/amber/rood
    - Mens bevestigt `UAT-7` (legacy badge) geen crash op oude sessies
    - Resume-signal ontvangen met `approved` OF lijst van falende UATs
  </acceptance_criteria>
  <done>
    Alle 7 UAT-cases groen door mens bevestigd; build + lint + test-suite groen; Phase 17 kan in ROADMAP als complete worden gemarkeerd; 17-04-SUMMARY.md geschreven.
  </done>
  <resume-signal>Type `approved` om Phase 17 af te ronden, of beschrijf welke UAT-cases falen + de observatie (screenshot of exacte tekst). Bij deelresultaten: lijst welke UATs groen (`UAT-1..UAT-7: groen behalve UAT-3 faalt op ...`).</resume-signal>
</task>

</tasks>

<verification>
Wave 3 gate = Wave 1 + Wave 2 green + Wave 3 code-merges + Human UAT groen:
1. `npm run build` exit 0
2. `npm run lint` exit 0
3. `npx vitest run` — alle 20+ test-files groen
4. Human verifies 7 UAT-cases (5 kern + 2 smoke) allemaal pass
5. Grep cross-check:
   - `grep -n "vermogenGelijkenisGroepen" src/components/cross-analyse/StapSectorVertaling.tsx` → ≥2 hits
   - `grep -n "computeAutoApplyResult" src/components/cross-analyse/StapConsolidatie.tsx` → ≥1 hit
   - `grep -n "consolidatie-herzien" src/components/cross-analyse/StapConsolidatie.tsx` → ≥1 hit
   - `grep -n "guardError" src/components/cross-analyse/shared/ConsolidationActionBar.tsx` → ≥2 hits
   - `grep -n "affectedGroepIds" src/components/cross-analyse/StapConsolidatie.tsx` → ≥1 hit (B-4 cache-invalidatie)
   - `grep -n "subEffortAnalysis: filteredSub" src/components/cross-analyse/StapConsolidatie.tsx` → ≥1 hit
</verification>

<success_criteria>
- Drieluik rendert per `VermogenGelijkenisGroep` in `StapSectorVertaling.tsx` — Variant A/B logica verwijderd
- Hefboom-badge met tooltip zichtbaar op elke gebundelde inspanning
- Domein-balans-badge met correcte kleurconventie (groen/amber/rood)
- Guard-error banner verschijnt bij cross-domein of sector-specifieke-titel merge (exacte Wave 1 error-message)
- Herzie-advies knop + context-textarea doen succesvolle round-trip naar `/api/din-suggest?type=consolidatie-herzien` en overschrijven het cluster
- **B-4 fix:** Na Herzie advies wordt `subEffortAnalysis` voor de getroffen `VermogenGelijkenisGroep(en)` geïnvalideerd (D-12)
- **W-5 fix:** Auto-apply gebruikt 3-stappen pattern (pure compute → updateSession → setMergedEffClusters); geen setState binnen reducer; React 19 Strict Mode safe
- Auto-apply toont samenvattende toast + `Vereist review` badge
- Legacy warning badge zichtbaar bij `consolidated: true` caps
- CrossAnalyseWizard stuurt `stap2Result` + `capabilityEffortMaps` mee naar stap 4 API
- Alle 7 UAT-cases groen (human-verified)
- `npm run build` + `npm run lint` + `npx vitest run` groen
</success_criteria>

<output>
After completion, create `.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-04-SUMMARY.md` met:
- Welke UAT-cases groen waren en welke (zo ja) rework vereisten
- Screenshots of expliciete tekst-bevestigingen van drieluik-rendering + guard-error banner (optioneel)
- Bevestiging dat auto-apply in React 19 Strict Mode geen dubbele render-flush veroorzaakt (UAT-5 Profiler observatie)
- Bevestiging dat Herzie advies subEffortAnalysis voor getroffen groep invalideert (UAT-4 observatie)
- Opmerkingen over responsive gedrag (desktop vs mobile drieluik-stapeling)
- Eventuele UX-verfijningen die discretionair zijn gedaan (Tailwind-classes, spacing)
- Bevestiging dat legacy sessies (Phase 8-merges) zonder crash laden
- Final phase verification: all build/lint/test/uat green → phase ready to mark complete in ROADMAP
</output>
