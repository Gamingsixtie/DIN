---
phase: 17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie
plan: 03
type: execute
wave: 2
depends_on:
  - "17-01"
  - "17-02"
files_modified:
  - src/lib/prompts.ts
  - src/app/api/din-suggest/route.ts
  - src/app/api/cross-analyse/route.ts
  - src/lib/__tests__/cross-analyse-schema.test.ts
autonomous: true
requirements:
  - R-CROSS-01
  - R-CROSS-02
requirements_addressed:
  - R-CROSS-01
  - R-CROSS-02
user_setup: []

must_haves:
  truths:
    - "Stap 2 prompt instrueert AI om vermogens ALLEEN te markeren via `vermogenGelijkenisGroepen` + `aanbeveling: markeer_gelijkenis`, NOOIT `combineren`"
    - "Stap 3 prompt instrueert AI dat inspannings-clusters ALLEEN geldig zijn wanneer items gekoppeld zijn aan vermogens in eenzelfde `VermogenGelijkenisGroep` die alle drie sectoren (PO+VO+Zakelijk) bevat"
    - "Stap 4 prompt produceert alleen `type: 'inspanning'` in `consolidatieAdvies`; `voorgesteldeNaam` is sectoroverstijgend (geen PO/VO/Zakelijk substrings)"
    - "Nieuwe `SUB_EFFORT_ANALYSE_PROMPT` accepteert een `VermogenGelijkenisGroep` + gekoppelde efforts en produceert `SubEffortAdvies[]` per domein"
    - "`/api/din-suggest` met `type === 'consolidatie-herzien'` retourneert Zod-gevalideerde `{aanbeveling, reden, voorgesteldeNaam, afstemmingsStappen}` JSON"
    - "`/api/cross-analyse` stap 4 branch roept na `CROSS_ANALYSE_STAP4_PROMPT` parallel `SUB_EFFORT_ANALYSE_PROMPT` aan per `VermogenGelijkenisGroep` en merged `subEffortAnalysis[]` in de response"
    - "Empty groep (geen gekoppelde efforts) skipt AI-call en produceert leeg advies (geen verspilde tokens, D-13)"
  artifacts:
    - path: "src/lib/prompts.ts"
      provides: "Herziene stap 2/3/4 prompts + nieuwe CONSOLIDATIE_HERZIEN_PROMPT + SUB_EFFORT_ANALYSE_PROMPT"
      contains: "export const SUB_EFFORT_ANALYSE_PROMPT"
    - path: "src/app/api/din-suggest/route.ts"
      provides: "Nieuwe tak `type === 'consolidatie-herzien'`"
      contains: "consolidatie-herzien"
    - path: "src/app/api/cross-analyse/route.ts"
      provides: "Stap 4 uitbreiding met parallelle SUB_EFFORT_ANALYSE per VermogenGelijkenisGroep"
      contains: "subEffortAnalysis"
  key_links:
    - from: "src/app/api/din-suggest/route.ts"
      to: "callClaudeWithValidation + ConsolidatieHerzienResponseSchema"
      via: "consolidatie-herzien branch"
      pattern: "type === \"consolidatie-herzien\""
    - from: "src/app/api/cross-analyse/route.ts"
      to: "SubEffortAdviesSchema + SUB_EFFORT_ANALYSE_PROMPT"
      via: "stap === 4 branch na CROSS_ANALYSE_STAP4_PROMPT"
      pattern: "Promise\\.all.*SUB_EFFORT_ANALYSE_PROMPT"
    - from: "CROSS_ANALYSE_STAP2_PROMPT body"
      to: "schema instructie"
      via: "tekst die AI forceert naar `markeer_gelijkenis`"
      pattern: "markeer_gelijkenis"
---

<objective>
Wave 2 — AI-pipeline. Alle prompt-herzieningen en twee nieuwe API-routebranches landen hier. Na deze Wave produceert `/api/cross-analyse` stap 4 een response met `subEffortAnalysis[]` per `VermogenGelijkenisGroep`, en kan de UI via `/api/din-suggest` met `type: "consolidatie-herzien"` een enkel cluster laten herzien inclusief user-context.

**Purpose:** De AI is de enige bron van (a) gelijkenis-detectie tussen sector-vermogens en (b) sub-effort advies per domein. Zonder deze Wave produceert stap 4 alleen het bestaande `consolidatieAdvies` en mist de UI de drieluik-invulling die Wave 3 nodig heeft om het organigram te renderen.

**Output:** `prompts.ts` uitgebreid met 2 nieuwe exports (`CONSOLIDATIE_HERZIEN_PROMPT`, `SUB_EFFORT_ANALYSE_PROMPT`) + herziene stap 2/3/4 prompts (D-25/D-27/D-31). `/api/din-suggest/route.ts` heeft nieuwe `consolidatie-herzien` tak. `/api/cross-analyse/route.ts` stap 4 branch draait parallelle sub-effort analyse. Schema-tests in `cross-analyse-schema.test.ts` uitgebreid met integratie-casus voor `subEffortAnalysis` door de AI-gegenereerde shape. Build + full suite groen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-CONTEXT.md
@.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-RESEARCH.md
@.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-VALIDATION.md
@.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-02-SUMMARY.md
@CLAUDE.md
@src/lib/prompts.ts
@src/app/api/din-suggest/route.ts
@src/app/api/cross-analyse/route.ts
@src/lib/ai-client.ts
@src/lib/prompt-assembly.ts
@src/lib/schemas.ts

<interfaces>
<!-- Existing AI pipeline interfaces — executor needs these contracts to wire new branches. -->

From src/lib/ai-client.ts (hergebruik — niet wijzigen):
```typescript
export async function callClaudeWithValidation<T>(
  schema: z.ZodType<T>,
  systemPrompt: string,
  userMessage: string,
  opts?: { maxTokens?: number; maxRetries?: number; model?: string }
): Promise<{ success: true; data: T } | { success: false; error: string }>;
```

From src/lib/prompt-assembly.ts (hergebruik):
```typescript
export function assembleSystemPrompt(
  basePrompt: string,
  useCase: "din-mapping" | "cross-analyse" | "batenprofiel" | "consolidatie" | ...,
  sectorwerk?: SectorwerkContext | undefined,
  kib?: KiBContext,
  completedGoals?: CompletedGoalsContext
): string;
```

From src/lib/schemas.ts (Wave 0 toegevoegd — gebruiken in Wave 2):
```typescript
export const VermogenGelijkenisGroepSchema = z.object({ id, vermogenIds, gezamenlijkeOmschrijving, reden });
export const SubEffortAdviesSchema = z.object({ groepId, domein, actie, items, reden, voorgesteldeNaam });
export const Stap2ResultSchema = z.object({ ..., vermogenGelijkenisGroepen, ... });
export const Stap4ResultSchema = z.object({ ..., subEffortAnalysis, ... });
```

From src/app/api/din-suggest/route.ts (regel 32-67 — pattern voor consolidatie-herzien tak):
```typescript
if (type === "consolidatie-advies" && body.clusterItems) {
  if (!process.env.ANTHROPIC_API_KEY) return NextResponse.json({ success: false, error: "..." }, { status: 503 });
  const systemPrompt = assembleSystemPrompt(CONSOLIDATIE_ADVIES_PROMPT, "cross-analyse", undefined, kibContext);
  const userMessage = [ /* cluster info */ ].join("\n\n");
  // Free-form JSON array response
  const message = await anthropic.messages.create({ model: "claude-sonnet-4-6", maxTokens, system: systemPrompt, messages: [{ role: "user", content: userMessage }] });
  // ... return JSON
}
```

From src/app/api/cross-analyse/route.ts (exacte variable-namen, lees regel 95-225):
```typescript
// Lokale variabelen binnen POST handler body:
const kibContext = extractKiBContext({ goals: body.kibGoals, scope: body.kibScope }); // regel 68
const capsData = (body.capabilities || []).map(...);                                    // regel 102
const effortsData = (body.efforts || []).map(...);                                      // regel 105
// body.capabilityEffortMaps (direct uit request body, niet als lokale variabele)       // regel 121 in koppelingen-object
// body.stap2Result: optionele cumulative-context veld uit buildCumulativeContext (regel 43-46)

// Stap 4 branch structure (regel 127-225):
if (stap && stap >= 1 && stap <= 5) {
  const config = getStepConfig(stap);
  // ... stap-5-specific payload narrowing ...
  const result = await callClaudeWithValidation(
    config.schema,           // = Stap4ResultSchema voor stap===4
    assembleSystemPrompt(config.prompt, "cross-analyse", undefined, kibContext),
    userMessage,
    { maxTokens: 16384, model: "claude-opus-4-6" }
  );
  if (!result.success) return NextResponse.json({ success: false, error: result.error, retryable: true }, { status: 422 });
  return NextResponse.json({ success: true, data: { analysis: result.data, stap } });
}
// ← Wave 2 Task 2: splits de stap===4 branch uit het generieke pad en draai PARALLEL de SUB_EFFORT_ANALYSE calls
```

**VARIABLE NAMES (W-4 fix — verbatim uit route.ts):**
- `kibContext` (niet `kib` of `ctx`)
- `capsData` (niet `capabilities` of `caps`)
- `effortsData` (niet `efforts`)
- `body.capabilityEffortMaps` (direct uit request; geen lokale variable)
- `body.stap2Result` (cumulatieve context; bevat `vermogenGelijkenisGroepen` na Wave 0 schema-uitbreiding)
- `result.data` (= gevalideerde Stap4Result)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Herziene prompts (stap 2/3/4) + nieuwe SUB_EFFORT_ANALYSE_PROMPT + CONSOLIDATIE_HERZIEN_PROMPT in `prompts.ts`</name>
  <files>
    src/lib/prompts.ts
  </files>
  <read_first>
    - src/lib/prompts.ts **regel 166-315 VOLLEDIG** (CROSS_ANALYSE_STAP2_PROMPT regel 166-191; CROSS_ANALYSE_STAP3_PROMPT regel 193-224; CROSS_ANALYSE_STAP4_PROMPT regel 226-315). Nodig voor verbatim quote-matching in stap 2/3/4 edits hieronder.
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-CONTEXT.md (D-25, D-26, D-30, D-31 — prompt-contracten)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-RESEARCH.md (regel 650-697 voor CONSOLIDATIE_HERZIEN example, regel 700-755 voor SUB_EFFORT_ANALYSE flow)
    - src/lib/schemas.ts (VermogenGelijkenisGroepSchema + SubEffortAdviesSchema shape na Wave 0)
    - src/lib/prompt-assembly.ts (verifieer `"cross-analyse"` en `"consolidatie"` useCase labels bestaan)
  </read_first>
  <behavior>
    - Lichte integratie-test (optioneel): `import { CROSS_ANALYSE_STAP2_PROMPT, CROSS_ANALYSE_STAP3_PROMPT, CROSS_ANALYSE_STAP4_PROMPT, SUB_EFFORT_ANALYSE_PROMPT, CONSOLIDATIE_HERZIEN_PROMPT } from "@/lib/prompts"` resolveert; elk is een non-lege string.
    - Concrete inhoudelijke assertions (die we wél borgen via grep/acceptance_criteria):
      - `CROSS_ANALYSE_STAP2_PROMPT` bevat de string `"markeer_gelijkenis"` en expliciete instructie dat vermogens NIET worden gecombineerd in cross-analyse (D-25).
      - `CROSS_ANALYSE_STAP2_PROMPT` bevat instructie om `vermogenGelijkenisGroepen` te vullen met groepen die minimaal één vermogen per sector (PO, VO, Zakelijk) bevatten.
      - `CROSS_ANALYSE_STAP2_PROMPT` bevat NIET meer de oude instructie `Cluster vermogens die semantisch op hetzelfde neerkomen over sectoren heen.` (= conflicteert met D-25/D-31).
      - `CROSS_ANALYSE_STAP3_PROMPT` bevat instructie dat inspanning-clusters ALLEEN geldig zijn wanneer items gekoppeld zijn aan vermogens in eenzelfde `VermogenGelijkenisGroep` die alle drie sectoren bevat, EN alleen binnen-domein geclusterd mag worden.
      - `CROSS_ANALYSE_STAP4_PROMPT` bevat instructie dat `consolidatieAdvies` UITSLUITEND `type: "inspanning"` produceert; de oude voorbeeldregel met `"type": "vermogen"` is verwijderd.
      - `SUB_EFFORT_ANALYSE_PROMPT` bevat instructie om per `VermogenGelijkenisGroep` een advies per domein (`mens`, `processen`, `data_systemen`, `cultuur`) te geven met `actie: combineren|apart_houden` en `voorgesteldeNaam` verplicht bij `combineren`.
  </behavior>
  <action>

**CRITICAL (D-25/D-31):** Deze task moet UNAMBIGU zijn — geen "vervang of voeg". Voor elke prompt (STAP2/STAP3/STAP4) staat hieronder EXPLICIET welke regels VERWIJDERD moeten worden (verbatim quote) en welke regels TOEGEVOEGD moeten worden (verbatim tekst). Geen keuzeruimte.

**STAP 0 — Open en bestudeer `src/lib/prompts.ts`.**

Verifieer voor commit dat de prompts op exact de onderstaande regel-ranges staan. Als line-numbers in een latere revisie zijn verschoven: gebruik de verbatim quote-matching (niet de regel-nummers) als waarheid.

<hr/>

### STAP 1 — Vervang `CROSS_ANALYSE_STAP2_PROMPT` (regel 166-191)

**BEHOUD** (letterlijk, niet wijzigen):
- Regel 166: `export const CROSS_ANALYSE_STAP2_PROMPT = `\`` `Je bent een expert in programmamanagement (DIN-methodiek).`
- Regel 170: `Gebruik de id-velden om items te identificeren in je clusters.`
- Regel 173-189 (de JSON-output-schema blok), MET de volgende wijzigingen:
  - Behoud `"vermogenClusters"`, `"hefboomwerking"`, `"samenvatting"` top-level keys en hun inhoud.

**VERWIJDER** (verbatim, deze regels staan letterlijk in de huidige file en MOETEN weg omdat ze conflicteren met D-25/D-31):

1. Regel 167 verbatim: `Analyseer welke vermogens door meerdere sectoren gedeeld worden en waar hefboomwerking zit.`
2. Regel 169 verbatim: `Cluster vermogens die semantisch op hetzelfde neerkomen over sectoren heen.`
3. Regel 171 verbatim: `Identificeer inspanningen die aan meerdere baten bijdragen (hefboomwerking).`
4. Regel 190 verbatim: `aanbeveling MOET exact een van: "combineren", "afstemmen", "apart_houden" zijn.` — (wordt VERVANGEN door een uitgebreide enum die `markeer_gelijkenis` bevat; zie TOEVOEGEN hieronder).
5. In de voorbeeld JSON (regel 181): `"aanbeveling": "combineren"` — VERVANG door `"aanbeveling": "markeer_gelijkenis"`.

**VOEG TOE, direct NA regel 170 (`Gebruik de id-velden om items te identificeren in je clusters.`) en VOOR de `Antwoord ALLEEN als JSON-object met EXACT deze structuur:` regel** — de volgende verbatim blok:

```
BELANGRIJK — Vermogens worden NIET samengevoegd in cross-analyse (D-25).
De drie sector-vermogens blijven aparte records. Markeer uitsluitend gelijkenis via `vermogenGelijkenisGroepen`.

Voor elke groep van gelijkende sector-vermogens:
- Minimaal ÉÉN vermogen per sector uit {PO, VO, Zakelijk}. Als je geen drieluik kunt samenstellen (bv. één sector mist), laat die vermogens ongeclusterd.
- `gezamenlijkeOmschrijving`: waarom deze vermogens inhoudelijk op elkaar lijken (zelfde capaciteit, zelfde doel-keten).
- `reden`: korte onderbouwing (1-2 zinnen, methodiek-conform).

Voor `vermogenClusters[].aanbeveling`: gebruik UITSLUITEND `"markeer_gelijkenis"` wanneer het een cross-sector gelijkenis betreft. `"combineren"` is NIET toegestaan voor vermogens in cross-analyse. De enum-waardes `"afstemmen"` en `"apart_houden"` zijn toegestaan.
```

**VOEG TOE in de JSON-voorbeeld-structuur**, direct na de `hefboomwerking`-array en VOOR `samenvatting`:
```
  "vermogenGelijkenisGroepen": [
    {
      "id": "g1",
      "vermogenIds": ["<cap-po-id>", "<cap-vo-id>", "<cap-zak-id>"],
      "gezamenlijkeOmschrijving": "Medewerker-wendbaarheid bij digitalisering",
      "reden": "Alle drie sectoren vereisen adaptief vermogen bij snelle digitaliseringstrajecten."
    }
  ],
```

**VOEG TOE, als VERVANGER voor de verwijderde enum-regel (5) hierboven**, direct vóór `BELANGRIJK: Gebruik de werkelijke id-velden uit de data.`:
```
aanbeveling MOET exact een van: "markeer_gelijkenis", "afstemmen", "apart_houden" zijn. "combineren" is NIET toegestaan voor vermogens in cross-analyse (D-25).
```

<hr/>

### STAP 2 — Vervang `CROSS_ANALYSE_STAP3_PROMPT` (regel 193-224)

**BEHOUD** (letterlijk, niet wijzigen):
- Regel 193: `export const CROSS_ANALYSE_STAP3_PROMPT = `\`` `Je bent een expert in programmamanagement (DIN-methodiek).`
- Regel 194: `Analyseer overlap in inspanningen over sectoren en koppel lopende projecten aan het DIN-netwerk.`
- Regel 196: `Cluster inspanningen die semantisch overlappen over sectoren.`
- Regel 197: `Gebruik de id-velden om items te identificeren in je clusters.`
- Regel 198: `Koppel lopende projecten aan DIN-inspanningen (match/geen match).`
- Regel 200-221 (de JSON-output-schema blok): BEHOUDEN ongewijzigd.
- Regel 223: `aanbeveling MOET exact een van: "combineren", "afstemmen", "apart_houden" zijn.` — BEHOUDEN.
- Regel 224: `BELANGRIJK: Gebruik de werkelijke id-velden uit de data. Antwoord in het Nederlands.` — BEHOUDEN.

**GEEN regels verwijderen** in STAP 3 (het bestaande instruct-frame is compatibel). Alleen TOEVOEGEN.

**VOEG TOE, direct NA regel 198 (`Koppel lopende projecten aan DIN-inspanningen (match/geen match).`) en VOOR de `Antwoord ALLEEN als JSON-object met EXACT deze structuur:` regel** — de volgende verbatim blok:

```
Cluster-regel inspanningen (D-27, D-31):
Een inspannings-cluster is ALLEEN geldig wanneer:
1. Alle items hetzelfde `domain` hebben (Mens, Processen, Data & Systemen, Cultuur). Cross-domein clusters zijn methodisch fout.
2. Alle items via `capabilityEffortMap` terug-refereren naar vermogens uit dezelfde `VermogenGelijkenisGroep` (zie stap 2 output) die alle drie sectoren (PO, VO, Zakelijk) bevat.

Als een inspanning geen drieluik-gekoppelde vermogens raakt, laat die inspanning ongeclusterd (of in een single-item cluster met `aanbeveling: "apart_houden"`).

`clusterTitel` + eventuele `voorgesteldeNaam` zijn ALTIJD sectoroverstijgend (geen `PO`/`VO`/`Zakelijk`/`primair onderwijs`/`voortgezet onderwijs` substrings, minimum 10 tekens).
```

<hr/>

### STAP 3 — Vervang `CROSS_ANALYSE_STAP4_PROMPT` (regel 226-315)

**BEHOUD** (letterlijk, niet wijzigen):
- Regel 226-250 (de header + "Bij aanbeveling 'afstemmen'..." + de CITO-BREED INZICHT PER DOMEIN sectie).
- Regel 277-305 (de `citobreedInzicht` voorbeeldstructuur en de vier domein-blokken).
- Regel 307: `"samenvatting": "Korte samenvatting van het consolidatie-advies (2-3 zinnen)"`.
- Regel 311: `aanbeveling MOET exact een van: "combineren", "afstemmen", "apart_houden" zijn.`
- Regel 312-315 (de laatste regels over `afstemmingsStappen` en `citobreedInzicht` vereisten).

**VERWIJDER** (verbatim, deze regels staan letterlijk en moeten weg omdat ze conflicteren met D-25/D-31):

1. Regel 260-267 verbatim (het eerste `consolidatieAdvies` voorbeeld-object met `"type": "vermogen"`):
```
    {
      "clusterTitel": "Titel van het cluster",
      "type": "vermogen",
      "aanbeveling": "combineren",
      "reden": "Concrete onderbouwing waarom dit advies",
      "voorgesteldeNaam": "Voorgestelde naam na samenvoeging (alleen bij combineren)",
      "afstemmingsStappen": []
    },
```
Verwijder dit hele object INCLUSIEF de trailing komma. Het tweede voorbeeld-object (met `"type": "inspanning"`, regel 268-275) BLIJFT staan — maar haal de trailing komma van het vorige weg zodat JSON valide blijft.

2. Regel 310 verbatim: `type MOET exact "vermogen" of "inspanning" zijn.` — VERWIJDER deze regel volledig (vervangen door nieuwe regel hieronder).

**VOEG TOE, direct NA regel 250 (`- "cultuur": gedrag, mindset, waarden, leiderschapsontwikkeling`) en VOOR `Voor elk domein:`** — de volgende verbatim blok:

```
Consolidatie-advies regels (D-25, D-30, D-31):
- `consolidatieAdvies[].type` is UITSLUITEND `"inspanning"` in cross-analyse. Produceer GEEN `type: "vermogen"` advies — vermogens worden niet meer samengevoegd in cross-analyse (zie D-25). Het bestaande `type: "inspanning"` voorbeeld hieronder is het enige geldige patroon.
- `aanbeveling: "combineren"` is ALLEEN toegestaan wanneer ALLE cluster-items hetzelfde `domain` hebben.
- `voorgesteldeNaam` is verplicht bij `"combineren"` en MOET sectoroverstijgend zijn (geen `PO`/`VO`/`Zakelijk`/`primair onderwijs`/`voortgezet onderwijs` substrings, minimum 10 tekens).
- Bij `"afstemmen"` of `"apart_houden"`: `voorgesteldeNaam` mag `null` of weggelaten worden.
```

**VOEG TOE, als vervanger voor de verwijderde regel 310 (`type MOET exact "vermogen" of "inspanning" zijn.`)** — direct vóór regel 311:
```
type MOET exact "inspanning" zijn in cross-analyse-output (D-25). "vermogen" is NIET toegestaan.
```

<hr/>

### STAP 4 — Voeg nieuwe `CONSOLIDATIE_HERZIEN_PROMPT` toe

Plaats direct NA het einde van `CROSS_ANALYSE_STAP4_PROMPT` (na regel 315, vóór `CROSS_ANALYSE_STAP5_PROMPT` op regel 317):

```typescript
export const CONSOLIDATIE_HERZIEN_PROMPT = `Je bent een expert in programmamanagement (DIN-methodiek) en herziet één consolidatie-advies op basis van gebruikerscontext.

Input krijg je:
- Cluster-titel en cluster-items (met sector en domein)
- Het originele AI-advies (aanbeveling, reden, voorgesteldeNaam, afstemmingsStappen)
- Optioneel: gebruikerscontext die verklaart waarom het oorspronkelijke advies niet past

Lever ALTIJD een herzien advies in dit JSON-schema:
{
  "aanbeveling": "combineren" | "afstemmen" | "apart_houden",
  "reden": "<onderbouwing — mag verwijzen naar de gebruikerscontext>",
  "voorgesteldeNaam": "<sectoroverstijgende titel, verplicht bij combineren>" | null,
  "afstemmingsStappen": ["<3-5 concrete stappen>"]
}

Regels (D-25, D-31):
- Respecteer methodiek-regels: geen cross-domein combineren, geen sector-substrings in voorgesteldeNaam.
- Voor vermogen-clusters (legacy): NOOIT "combineren" adviseren — kies "afstemmen" of "apart_houden".
- Als de gebruikerscontext het originele advies bevestigt: herbevestig met dezelfde aanbeveling maar versterk de reden.
- Produceer ALLEEN geldige JSON, geen prose errom.`;
```

<hr/>

### STAP 5 — Voeg nieuwe `SUB_EFFORT_ANALYSE_PROMPT` toe

Plaats direct NA `CONSOLIDATIE_HERZIEN_PROMPT`:

```typescript
export const SUB_EFFORT_ANALYSE_PROMPT = `Je bent een expert in programmamanagement (DIN-methodiek) en analyseert inspanningen per inspannings-domein onder een VermogenGelijkenisGroep.

Een VermogenGelijkenisGroep bevat drie (of meer) sector-vermogens die inhoudelijk op elkaar lijken (PO + VO + Zakelijk). Inspanningen die aan deze vermogens gekoppeld zijn kunnen per domein worden geclusterd of apart blijven.

Input krijg je (JSON):
{
  "groep": { "id": "<groepId>", "vermogenIds": [...], "gezamenlijkeOmschrijving": "...", "reden": "..." },
  "vermogens": [{ "id": "...", "sectorId": "PO|VO|Zakelijk", "title"|"description": "..." }],
  "efforts":   [{ "id": "...", "sectorId": "...", "domain": "mens|processen|data_systemen|cultuur", "title": "...", "description": "..." }]
}

Lever een array \`SubEffortAdvies[]\` met één entry per (groep × domein) waar minstens één gekoppelde inspanning staat:
{
  "groepId": "<zelfde als input.groep.id>",
  "domein": "mens" | "processen" | "data_systemen" | "cultuur",
  "actie": "combineren" | "apart_houden",
  "items": ["<effort-id>", "..."],
  "reden": "<methodiek-conforme onderbouwing>",
  "voorgesteldeNaam": "<sectoroverstijgende titel>" | null
}

Regels (D-11, D-31):
- ÉÉN advies per domein binnen een groep. Geen cross-domein combineren.
- \`voorgesteldeNaam\` is VERPLICHT bij \`actie: "combineren"\` en MOET sectoroverstijgend zijn (geen PO/VO/Zakelijk substrings, min 10 chars).
- Bij \`actie: "apart_houden"\`: \`voorgesteldeNaam\` is \`null\`.
- Gebruik cross-domein context ALLEEN om je reden te versterken ("Mens-training ondersteunt Data-implementatie"), maar nooit als justificatie voor cross-domein merge.
- Als een domein geen gekoppelde inspanningen heeft, laat dat domein WEG uit de response (geen lege entries — dat doet de client).
- Produceer ALLEEN geldige JSON, geen prose errom.\`;
```

**LET OP de escapes:** `SubEffortAdvies[]`, `actie: "combineren"`, etc. moeten escaped worden (`\`SubEffortAdvies[]\``) zodat backticks binnen het template-literal NIET de string vroegtijdig sluiten. Zie RESEARCH.md Pitfall 2.

<hr/>

### STAP 6 — Verifieer

1. Elke nieuwe `export const` eindigt op een backtick-string (`` ` ``) en puntkomma.
2. Geen typo in de enum-waarden (`data_systemen` met underscore, niet `data-systemen`).
3. Alle backticks binnen de template-literals zijn escaped met `` \` ``.
4. `grep "type\": \"vermogen\"" src/lib/prompts.ts` → 0 hits in de STAP 4 prompt (alleen in de STAP 1 `type` field van schema-voorbeelden mag het nog staan, maar NIET in de STAP 4 consolidatieAdvies voorbeeld).
5. `grep "Cluster vermogens die semantisch" src/lib/prompts.ts` → 0 hits (oude STAP 2 instructie is weg).

### STAP 7 — Tests

1. **Run `npm run build`** — TypeScript compile zonder errors. Template-literal escapes moeten valide blijven.
2. **Run `npx vitest run`** — alle tests groen (geen prompt-string heeft tests gebroken).
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/prompts.ts` bevat literal `"markeer_gelijkenis"` in `CROSS_ANALYSE_STAP2_PROMPT` body (grep case-sensitive)
    - `src/lib/prompts.ts` `CROSS_ANALYSE_STAP2_PROMPT` bevat string `vermogenGelijkenisGroepen`
    - `src/lib/prompts.ts` `CROSS_ANALYSE_STAP2_PROMPT` bevat NIET meer `Cluster vermogens die semantisch op hetzelfde neerkomen over sectoren heen.` (grep → 0 hits)
    - `src/lib/prompts.ts` `CROSS_ANALYSE_STAP2_PROMPT` bevat NIET meer `Analyseer welke vermogens door meerdere sectoren gedeeld worden en waar hefboomwerking zit.` (grep → 0 hits)
    - `src/lib/prompts.ts` `CROSS_ANALYSE_STAP3_PROMPT` bevat string `VermogenGelijkenisGroep` (met instructie over drieluik-drempel)
    - `src/lib/prompts.ts` `CROSS_ANALYSE_STAP3_PROMPT` bevat string `alle drie sectoren` OF `PO, VO, Zakelijk`
    - `src/lib/prompts.ts` `CROSS_ANALYSE_STAP4_PROMPT` bevat string `UITSLUITEND` met `"inspanning"` (grep `UITSLUITEND.*inspanning` of `"inspanning".*UITSLUITEND`)
    - `src/lib/prompts.ts` `CROSS_ANALYSE_STAP4_PROMPT` bevat NIET meer `"type": "vermogen"` voorbeeldregel (grep → 0 hits in STAP4 scope; mag wel in andere prompts zoals STAP1 voorkomen als type-label)
    - `src/lib/prompts.ts` `CROSS_ANALYSE_STAP4_PROMPT` bevat NIET meer `type MOET exact "vermogen" of "inspanning" zijn.` — is VERVANGEN door `type MOET exact "inspanning" zijn in cross-analyse-output`
    - `src/lib/prompts.ts` bevat `export const CONSOLIDATIE_HERZIEN_PROMPT`
    - `src/lib/prompts.ts` bevat `export const SUB_EFFORT_ANALYSE_PROMPT`
    - `CONSOLIDATIE_HERZIEN_PROMPT` bevat alle enum-waardes: `combineren`, `afstemmen`, `apart_houden` EN `voorgesteldeNaam`
    - `SUB_EFFORT_ANALYSE_PROMPT` bevat alle 4 domein-enum-waardes: `mens`, `processen`, `data_systemen`, `cultuur`
    - `SUB_EFFORT_ANALYSE_PROMPT` bevat `groepId` + `SubEffortAdvies` of `actie`
    - `npm run build` exit 0
    - `npx vitest run` exit 0 (geen regressies)
  </acceptance_criteria>
  <done>
    `prompts.ts` heeft 2 nieuwe exports (`CONSOLIDATIE_HERZIEN_PROMPT`, `SUB_EFFORT_ANALYSE_PROMPT`) en 3 herziene stap-prompts (2/3/4). Oude instructies die conflicteerden met D-25/D-31 zijn verbatim verwijderd; nieuwe instructies zijn verbatim toegevoegd. Geen "als/dan" keuzeruimte voor de executor. Build + test-suite groen.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API wiring — `/api/din-suggest` `consolidatie-herzien` tak + `/api/cross-analyse` stap 4 parallelle `SUB_EFFORT_ANALYSE` calls + schema-integratie-test</name>
  <files>
    src/app/api/din-suggest/route.ts,
    src/app/api/cross-analyse/route.ts,
    src/lib/__tests__/cross-analyse-schema.test.ts
  </files>
  <read_first>
    - src/app/api/din-suggest/route.ts (volledig — pattern voor nieuwe tak; hergebruik ANTHROPIC_API_KEY guard + assembleSystemPrompt + extractKiBContext uit de `consolidatie-advies` tak)
    - src/app/api/cross-analyse/route.ts (volledig — post-handler regel 64-262; let op de EXACTE lokale namen: `kibContext` (regel 68), `capsData` (regel 102), `effortsData` (regel 105), `body.capabilityEffortMaps` (regel 121), `body.stap2Result` (regel 43))
    - src/lib/ai-client.ts (`callClaudeWithValidation` signature)
    - src/lib/prompt-assembly.ts (`assembleSystemPrompt`, `extractKiBContext`)
    - src/lib/schemas.ts (post-Wave-0: `VermogenGelijkenisGroepSchema`, `SubEffortAdviesSchema`, `Stap4ResultSchema`)
    - src/lib/prompts.ts (post-Task-1: `CONSOLIDATIE_HERZIEN_PROMPT`, `SUB_EFFORT_ANALYSE_PROMPT`)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-CONTEXT.md (D-08, D-10, D-11, D-13 — API contracten)
    - .planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-RESEARCH.md (regel 650-755 voor Example 2 + 3 verified code + Pitfall 5 over `Promise.all` timeout-risico)
  </read_first>
  <behavior>
    - Schema-integratie-test in `cross-analyse-schema.test.ts`: simuleer een volledige stap 4 response shape met `subEffortAnalysis: SubEffortAdvies[]` en verifieer dat `Stap4ResultSchema.parse(response.analysis)` slaagt.
    - Behavior van nieuwe tak (`consolidatie-herzien`):
      - POST `/api/din-suggest` met `{ type: "consolidatie-herzien", clusterTitel, clusterItems, origineelAdvies, userContext? }` → 200 + Zod-gevalideerde response.
      - Bij ontbreken ANTHROPIC_API_KEY → 503.
      - Bij Zod-validatie-fail → 422 + `{ retryable: true }`.
    - Behavior van stap 4 uitbreiding:
      - Wanneer body bevat `stap2Result.vermogenGelijkenisGroepen` met N groepen, wordt voor elke groep (met ≥1 gekoppelde effort) een `SUB_EFFORT_ANALYSE_PROMPT` call gedaan.
      - Lege groep (geen efforts) → skipt call, leeg result.
      - Response bevat `data.analysis.subEffortAnalysis: SubEffortAdvies[]` (platte array, gevuld met alle groep-resultaten).
  </behavior>
  <action>
1. **Uitbreiden `src/app/api/din-suggest/route.ts`** — voeg de `consolidatie-herzien` tak toe DIRECT NA de bestaande `consolidatie-advies` tak (pattern uit regel 32-67 klonen):

   a. Voeg de Zod response schema toe bovenaan de file (naast imports, na andere schemas of net voor de POST handler):
   ```typescript
   import { z } from "zod";
   import { CONSOLIDATIE_HERZIEN_PROMPT } from "@/lib/prompts"; // ← toevoegen aan bestaande prompts-import
   // ...
   const ConsolidatieHerzienResponseSchema = z.object({
     aanbeveling: z.enum(["combineren", "afstemmen", "apart_houden"]),
     reden: z.string(),
     voorgesteldeNaam: z.string().nullable().optional(),
     afstemmingsStappen: z.array(z.string()).optional().default([]),
   });
   ```

   b. BINNEN de POST handler, NA de `type === "consolidatie-advies"` tak, voeg toe:
   ```typescript
   if (type === "consolidatie-herzien" && body.clusterItems && body.origineelAdvies) {
     if (!process.env.ANTHROPIC_API_KEY) {
       return NextResponse.json(
         { success: false, error: "ANTHROPIC_API_KEY niet geconfigureerd." },
         { status: 503 }
       );
     }

     const systemPrompt = assembleSystemPrompt(
       CONSOLIDATIE_HERZIEN_PROMPT,
       "cross-analyse",
       undefined,
       extractKiBContext({ goals: body.kibGoals, scope: body.kibScope })
     );

     const userMessage = [
       `Cluster: "${body.clusterTitel ?? "onbekend"}"`,
       `Items:\n${(body.clusterItems as Array<{ beschrijving: string; sector: string; domein?: string }>)
         .map((it) => `- ${it.beschrijving} (${it.sector}${it.domein ? `, ${it.domein}` : ""})`)
         .join("\n")}`,
       `Origineel AI-advies:\n${JSON.stringify(body.origineelAdvies, null, 2)}`,
       body.userContext ? `Gebruikerscontext voor herziening:\n${body.userContext}` : "",
     ]
       .filter(Boolean)
       .join("\n\n");

     const result = await callClaudeWithValidation(
       ConsolidatieHerzienResponseSchema,
       systemPrompt,
       userMessage,
       { maxTokens: 2048, maxRetries: 1 }
     );

     if (!result.success) {
       return NextResponse.json(
         { success: false, error: result.error, retryable: true },
         { status: 422 }
       );
     }
     return NextResponse.json({ success: true, data: result.data });
   }
   ```

   c. Verifieer dat `callClaudeWithValidation` en `assembleSystemPrompt` al geïmporteerd zijn (zo niet, voeg toe).

2. **Uitbreiden `src/app/api/cross-analyse/route.ts`** — hoist de stap 4 branch buiten de generieke `stap>=1 && stap<=5` loop, zodat we een apart post-hook kunnen draaien.

   a. Voeg bovenaan de file imports toe:
   ```typescript
   import { SUB_EFFORT_ANALYSE_PROMPT } from "@/lib/prompts"; // voeg toe aan bestaande prompts-import
   import { SubEffortAdviesSchema } from "@/lib/schemas"; // voeg toe aan bestaande schemas-import
   import { z } from "zod";
   ```

   b. **EXACTE variable-namen (W-4 fix — deze zijn verbatim in route.ts regel 68-121):**
   - `kibContext` — lokale variable (regel 68)
   - `capsData` — lokale variable (regel 102)
   - `effortsData` — lokale variable (regel 105)
   - `body.capabilityEffortMaps` — direct uit request body (geen lokale var)
   - `body.stap2Result` — cumulatieve context veld, bevat `vermogenGelijkenisGroepen` na Wave 0 schema-uitbreiding

   c. BINNEN de huidige `if (stap && stap >= 1 && stap <= 5)` branch (regel 127-225), plaats direct NA de bestaande `const result = await callClaudeWithValidation(...)` call (regel 207-212) en VÓÓR de `if (!result.success)` check (regel 214), voeg toe:

   ```typescript
   // --- D-10/D-11: sub-effort analyse per VermogenGelijkenisGroep (parallel, alleen stap 4) ---
   let subEffortAnalysisFlat: Array<z.infer<typeof SubEffortAdviesSchema>> = [];

   if (stap === 4 && result.success) {
     const groepen = (body.stap2Result as { vermogenGelijkenisGroepen?: Array<{
       id: string;
       vermogenIds: string[];
       gezamenlijkeOmschrijving: string;
       reden: string;
     }> } | undefined)?.vermogenGelijkenisGroepen ?? [];

     const capEffortMapsLocal = (body.capabilityEffortMaps ?? []) as Array<{
       capabilityId: string;
       effortId: string;
     }>;

     const subAnalyses = await Promise.all(
       groepen.map(async (groep) => {
         const capIdSet = new Set(groep.vermogenIds);
         const groepVermogens = capsData.filter((c: { id: string }) => capIdSet.has(c.id));
         const groepEffortIdSet = new Set(
           capEffortMapsLocal
             .filter((m) => capIdSet.has(m.capabilityId))
             .map((m) => m.effortId)
         );
         const groepEfforts = effortsData.filter((e: { id: string }) => groepEffortIdSet.has(e.id));

         if (groepEfforts.length === 0) {
           // D-13: skip AI-call — geen efforts gekoppeld
           return [];
         }

         const subSystemPrompt = assembleSystemPrompt(
           SUB_EFFORT_ANALYSE_PROMPT,
           "cross-analyse",
           undefined,
           kibContext
         );

         const subUserMessage = JSON.stringify(
           { groep, vermogens: groepVermogens, efforts: groepEfforts },
           null,
           2
         );

         const subResult = await callClaudeWithValidation(
           z.array(SubEffortAdviesSchema),
           subSystemPrompt,
           subUserMessage,
           { maxTokens: 4096 }
         );

         return subResult.success ? subResult.data : [];
       })
     );

     subEffortAnalysisFlat = subAnalyses.flat();
   }
   ```

   d. Vervang de bestaande `return NextResponse.json({ success: true, data: { analysis: result.data, stap } });` (regel 221-224) door:
   ```typescript
   return NextResponse.json({
     success: true,
     data: {
       analysis: stap === 4
         ? { ...(result.data as object), subEffortAnalysis: subEffortAnalysisFlat }
         : result.data,
       stap,
     },
   });
   ```

   e. **Pitfall 5 defense:** `Promise.all` kan de 300s maxDuration overschrijden bij ≥10 groepen. Research (17-RESEARCH.md Open Question 1) adviseert `Promise.all` voor typische N≤5. Documenteer in SUMMARY als runtime-profiel groter wordt.

3. **Uitbreiden `src/lib/__tests__/cross-analyse-schema.test.ts`** met een integratie-casus voor sub-effort payload:
   ```typescript
   describe("Stap4ResultSchema sub effort advies integration (D-30)", () => {
     test("volledig stap 4 response met subEffortAnalysis parseert succesvol", () => {
       const mockResponse = {
         consolidatieAdvies: [
           {
             clusterTitel: "Medewerkerstraining cluster",
             type: "inspanning" as const,
             aanbeveling: "combineren" as const,
             reden: "Drie trainingen met identiek doel",
             voorgesteldeNaam: "Sector-overstijgende klantgesprek-training",
             afstemmingsStappen: ["Stem agenda af", "Plan kwartaalslot"],
             context: "User wil focus op data-domein erbij betrekken",
           },
         ],
         citobreedInzicht: [],
         subEffortAnalysis: [
           {
             groepId: "g1",
             domein: "mens" as const,
             actie: "combineren" as const,
             items: ["eff-po-1", "eff-vo-1", "eff-zak-1"],
             reden: "Drie trainingen met identieke inhoud",
             voorgesteldeNaam: "Sector-overstijgende training",
           },
           {
             groepId: "g1",
             domein: "processen" as const,
             actie: "apart_houden" as const,
             items: ["eff-proc-1"],
             reden: "Proces-gebonden aan sector-governance",
             voorgesteldeNaam: null,
           },
         ],
         samenvatting: "Consolidatie mogelijk in Mens-domein",
       };
       const result = Stap4ResultSchema.safeParse(mockResponse);
       expect(result.success).toBe(true);
       if (result.success) {
         expect(result.data.subEffortAnalysis).toHaveLength(2);
         expect(result.data.subEffortAnalysis[0].groepId).toBe("g1");
         expect(result.data.consolidatieAdvies[0].context).toBe("User wil focus op data-domein erbij betrekken");
       }
     });
   });
   ```

4. **Run:**
   - `npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts -t "sub effort advies"` → groen.
   - `npm run build` → exit 0 (nieuwe tak compilable, geen type-errors).
   - `npx vitest run` → alle suites groen.
   - `npm run lint` → geen errors.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts -t "sub effort advies"</automated>
    <automated>npx vitest run</automated>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/din-suggest/route.ts` bevat string `"consolidatie-herzien"` (grep case-sensitive)
    - `src/app/api/din-suggest/route.ts` importeert `CONSOLIDATIE_HERZIEN_PROMPT` uit `@/lib/prompts`
    - `src/app/api/din-suggest/route.ts` bevat `ConsolidatieHerzienResponseSchema` (Zod object met 4 velden)
    - `src/app/api/din-suggest/route.ts` retourneert status 503 bij ontbrekende `ANTHROPIC_API_KEY` in de nieuwe tak
    - `src/app/api/din-suggest/route.ts` retourneert status 422 bij Zod-validatie-fail met `retryable: true` in body
    - `src/app/api/cross-analyse/route.ts` importeert `SUB_EFFORT_ANALYSE_PROMPT` uit `@/lib/prompts`
    - `src/app/api/cross-analyse/route.ts` importeert `SubEffortAdviesSchema` uit `@/lib/schemas`
    - `src/app/api/cross-analyse/route.ts` bevat `Promise.all(` binnen stap 4 branch (grep)
    - `src/app/api/cross-analyse/route.ts` bevat response-veld `subEffortAnalysis` in `NextResponse.json` payload (grep `subEffortAnalysis`)
    - `src/app/api/cross-analyse/route.ts` bevat skip-gate `if (groepEfforts.length === 0) return [];` of equivalent (grep `length === 0` binnen map-closure)
    - `src/app/api/cross-analyse/route.ts` gebruikt verbatim `capsData`, `effortsData`, `kibContext` (grep, bestaande namen niet overschreven)
    - `src/lib/__tests__/cross-analyse-schema.test.ts` bevat `describe("Stap4ResultSchema sub effort advies integration`
    - `npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts -t "sub effort advies"` exit 0
    - `npm run build` exit 0
    - `npm run lint` exit 0
    - `npx vitest run` exit 0 (geen regressies)
  </acceptance_criteria>
  <done>
    `/api/din-suggest` heeft werkende `consolidatie-herzien` tak met Zod-gevalideerde response; `/api/cross-analyse` stap 4 draait parallelle sub-effort analyse per groep met skip-gate voor empty groups; schema-test dekt integratie-shape. Build + lint + full suite groen.
  </done>
</task>

</tasks>

<verification>
Wave 2 afsluitende checks:
1. Per-task quick-run: `npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts -t "sub effort advies|backward compat"` — groen.
2. Per-task build: `npm run build` — exit 0.
3. Full suite: `npx vitest run && npm run build && npm run lint` — alle drie groen.
4. Grep cross-check:
   - `grep -c "SUB_EFFORT_ANALYSE_PROMPT" src/app/api/cross-analyse/route.ts` ≥ 1
   - `grep -c "CONSOLIDATIE_HERZIEN_PROMPT" src/app/api/din-suggest/route.ts` ≥ 1
   - `grep -c "subEffortAnalysis" src/app/api/cross-analyse/route.ts` ≥ 2 (import payload + response)
   - `grep "Cluster vermogens die semantisch" src/lib/prompts.ts` = 0 hits (oude STAP2 instructie weg)
   - `grep "type MOET exact \"vermogen\" of \"inspanning\"" src/lib/prompts.ts` = 0 hits (oude STAP4 regel weg)
5. Manual smoke (optioneel): `curl -X POST http://localhost:3000/api/din-suggest -H "Content-Type: application/json" -d '{"type":"consolidatie-herzien","clusterTitel":"test","clusterItems":[{"beschrijving":"x","sector":"PO"}],"origineelAdvies":{"aanbeveling":"combineren","reden":"x"}}'` — retourneert 200 of 503 (afhankelijk van key aanwezigheid).
</verification>

<success_criteria>
- `prompts.ts` bevat nieuwe `CONSOLIDATIE_HERZIEN_PROMPT` + `SUB_EFFORT_ANALYSE_PROMPT` + herziene stap 2/3/4 prompts met methodiek-correctie; oude conflicterende instructies zijn verbatim verwijderd
- `/api/din-suggest` `consolidatie-herzien` tak werkt, Zod-gevalideerd
- `/api/cross-analyse` stap 4 response bevat `subEffortAnalysis[]` (platte array van alle groep-resultaten)
- Empty-groep skip werkt (D-13)
- Schema-tests in `cross-analyse-schema.test.ts` dekken integratie-shape
- `npm run build` + `npm run lint` + `npx vitest run` groen
</success_criteria>

<output>
After completion, create `.planning/phases/17-cross-analyse-organigram-helderheid-domein-bewuste-consolidatie/17-03-SUMMARY.md` met:
- Exacte tekst van de instructies toegevoegd aan stap 2/3/4 prompts (copy-paste blocks) zodat Wave 3 UI hun copy/toelichting consistent kan houden
- Exact lijst van verwijderde regels per STAP (voor audit trail)
- De exacte response-shape van `/api/din-suggest?type=consolidatie-herzien`
- De exacte response-shape van `/api/cross-analyse?stap=4` na uitbreiding (met voorbeeld `subEffortAnalysis`)
- Keuze over `Promise.all` vs sequentieel (inclusief eventuele maxDuration-overwegingen)
- Bevestiging dat lokale variabel-namen in `cross-analyse/route.ts` (capsData, effortsData, kibContext, body.capabilityEffortMaps) ongewijzigd zijn gebleven
</output>
