---
phase: 08-cross-analyse-semantische-matching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/schemas.ts
  - src/lib/prompts.ts
  - src/app/api/cross-analyse/route.ts
  - src/lib/types.ts
  - src/lib/__tests__/cross-analyse-schema.test.ts
autonomous: true
requirements: [CROSS-01, CROSS-02, CROSS-03]

must_haves:
  truths:
    - "AICrossAnalyseSchema accepteert nieuwe velden (vermogenClusters, inspanningClusters, projectMatching) als optioneel met defaults"
    - "Bestaande crossAnalyse JSON data parst zonder fouten tegen het nieuwe schema (backward compat)"
    - "DINCapabilitySchema en DINEffortSchema accepteren consolidated en consolidatedInto velden"
    - "Cross-analyse API stuurt entity IDs mee naar AI en vraagt om ze terug te geven in clusters"
    - "CROSS_ANALYSE_PROMPT bevat semantische matching instructies voor vermogens en inspanningen"
    - "Baten worden als context meegestuurd maar NIET gematcht (per D-03)"
  artifacts:
    - path: "src/lib/schemas.ts"
      provides: "Extended DINCapabilitySchema, DINEffortSchema, AICrossAnalyseSchema with consolidation and cluster fields"
      contains: "vermogenClusters"
    - path: "src/lib/prompts.ts"
      provides: "Upgraded CROSS_ANALYSE_PROMPT with semantic matching instructions"
      contains: "vermogenClusters"
    - path: "src/app/api/cross-analyse/route.ts"
      provides: "Updated API route sending entity IDs, maxTokens 16384"
      contains: "maxTokens: 16384"
    - path: "src/lib/__tests__/cross-analyse-schema.test.ts"
      provides: "Unit tests for extended schema validation and backward compat"
      contains: "vermogenClusters"
  key_links:
    - from: "src/lib/prompts.ts"
      to: "src/lib/schemas.ts"
      via: "AI response JSON structure must match AICrossAnalyseSchema"
      pattern: "vermogenClusters.*inspanningClusters.*projectMatching"
    - from: "src/app/api/cross-analyse/route.ts"
      to: "src/lib/schemas.ts"
      via: "callClaudeWithValidation validates AI response against AICrossAnalyseSchema"
      pattern: "AICrossAnalyseSchema"
---

<objective>
Extend the cross-analyse data layer: Zod schemas get consolidation flags and new AI cluster sections, the prompt gets semantic matching instructions, and the API route sends entity IDs with increased token budget.

Purpose: Build the data foundation for AI-driven semantic matching. Without correct schemas and prompts, the UI (Plan 02) has nothing to render.
Output: Extended schemas, upgraded prompt, updated API route, unit tests confirming backward compat and new schema validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cross-analyse-semantische-matching/08-CONTEXT.md
@.planning/phases/08-cross-analyse-semantische-matching/08-RESEARCH.md
@.planning/phases/08-cross-analyse-semantische-matching/08-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/schemas.ts (current DINCapabilitySchema, line 123):
```typescript
export const DINCapabilitySchema = z.object({
  id: z.string(),
  sectorId: z.string(),
  title: z.string().optional(),
  description: z.string(),
  relatedSectors: z.array(z.string()),
  currentLevel: z.number().optional(),
  targetLevel: z.number().optional(),
  profiel: VermogensProfielSchema.optional(),
});
```

From src/lib/schemas.ts (current DINEffortSchema, line 134):
```typescript
export const DINEffortSchema = z.object({
  id: z.string(),
  sectorId: z.string(),
  title: z.string().optional(),
  description: z.string(),
  domain: EffortDomainSchema,
  quarter: z.string().optional(),
  responsibleSector: z.string().optional(),
  status: EffortStatusSchema,
  dependencies: z.array(z.string()),
  votes: z.number().optional(),
  opmerking: z.string().optional(),
  approvalStatus: ApprovalStatusSchema.optional(),
  approvalDate: z.string().optional(),
  dossier: InspanningsDossierSchema.optional(),
});
```

From src/lib/schemas.ts (current AICrossAnalyseSchema, line 491):
```typescript
export const AICrossAnalyseSchema = z.object({
  synergie: z.object({ titel, toelichting, items: z.array(CrossAnalyseSynergieItemSchema) }).optional().default(...),
  gaps: z.object({ titel, toelichting, doelenZonderBaten, batenZonderVermogens, vermogensZonderInspanningen }).optional().default(...),
  hefboomwerking: z.object({ titel, toelichting, items: z.array(CrossAnalyseHefboomItemSchema) }).optional().default(...),
  domeinBalans: z.object({ titel, toelichting, domeinen: z.array(CrossAnalyseDomeinItemSchema) }).optional().default(...),
  sectorOverlap: z.object({ titel, toelichting, items: z.array(CrossAnalyseSectorOverlapItemSchema) }).optional().default(...),
  externeProjecten: z.object({ titel, toelichting, items: z.array(CrossAnalyseExternItemSchema) }).optional().default(...),
});
```

From src/lib/ai-client.ts (central AI call pattern):
```typescript
export function callClaudeWithValidation<T>(
  schema: z.ZodType<T>,
  systemPrompt: string,
  userMessage: string,
  options?: { maxTokens?: number; model?: string }
): Promise<{ success: boolean; data?: T; error?: string }>;
```

From src/app/api/cross-analyse/route.ts (current default cross-analyse call, line 195-209):
```typescript
const userMessage = `Analyseer de volgende DIN-data:\n${JSON.stringify(body, null, 2).slice(0, 15000)}`;
const result = await callClaudeWithValidation(
  AICrossAnalyseSchema,
  assembleSystemPrompt(CROSS_ANALYSE_PROMPT, "cross-analyse", undefined, kibContext),
  userMessage,
  { maxTokens: 8192, model: "claude-opus-4-6" }
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Zod schemas with consolidation flags and AI cluster response sections</name>
  <files>src/lib/schemas.ts, src/lib/types.ts, src/lib/__tests__/cross-analyse-schema.test.ts</files>
  <read_first>
    - src/lib/schemas.ts (full file -- see all existing schemas and the pattern for .optional().default())
    - src/lib/types.ts (re-exports from schemas.ts -- new types must be added here)
    - src/lib/__tests__/schemas.test.ts (existing schema test patterns)
  </read_first>
  <behavior>
    - Test 1: AICrossAnalyseSchema.parse({...existing valid data...}) succeeds (backward compat, no vermogenClusters/inspanningClusters/projectMatching present)
    - Test 2: AICrossAnalyseSchema.parse({...with vermogenClusters array...}) succeeds and returns typed clusters
    - Test 3: AICrossAnalyseSchema.parse({...with inspanningClusters array...}) succeeds and returns typed clusters
    - Test 4: AICrossAnalyseSchema.parse({...with projectMatching array...}) succeeds and returns typed matches
    - Test 5: VermogenClusterItemSchema validates cluster with items containing id, beschrijving, sector, plus batenContext, advies, aanbeveling
    - Test 6: InspanningClusterItemSchema validates cluster with items containing id, beschrijving, sector, domein, plus batenContext, advies, aanbeveling
    - Test 7: ProjectMatchItemSchema validates project with heeftMatch boolean, optional gekoppeldAan, advies
    - Test 8: DINCapabilitySchema.parse({...existing data without consolidated...}) succeeds (backward compat)
    - Test 9: DINCapabilitySchema.parse({...with consolidated: true, consolidatedInto: "abc"...}) succeeds
    - Test 10: DINEffortSchema.parse({...with consolidated: true, consolidatedInto: "abc"...}) succeeds
  </behavior>
  <action>
    **A) Add consolidation fields to DINCapabilitySchema (after profiel field, around line 132):**
    ```typescript
    consolidated: z.boolean().optional(),
    consolidatedInto: z.string().optional(),
    ```

    **B) Add consolidation fields to DINEffortSchema (after dossier field, around line 149):**
    ```typescript
    consolidated: z.boolean().optional(),
    consolidatedInto: z.string().optional(),
    ```

    **C) Create new sub-schemas for AI cross-analyse cluster response (insert after CrossAnalyseExternItemSchema, before CrossAnalyseResultSchema):**

    ```typescript
    // --- Semantische matching cluster schemas (Phase 8 — D-04, D-06) ---

    export const VermogenClusterItemSchema = z.object({
      clusterTitel: z.string(),
      items: z.array(z.object({
        id: z.string(),
        beschrijving: z.string(),
        sector: z.string(),
      })),
      batenContext: z.array(z.object({
        baat: z.string(),
        sector: z.string(),
      })),
      advies: z.string(),
      aanbeveling: z.enum(["combineren", "afstemmen", "apart_houden"]),
    });

    export const InspanningClusterItemSchema = z.object({
      clusterTitel: z.string(),
      items: z.array(z.object({
        id: z.string(),
        beschrijving: z.string(),
        sector: z.string(),
        domein: z.string(),
      })),
      batenContext: z.array(z.object({
        baat: z.string(),
        sector: z.string(),
      })),
      advies: z.string(),
      aanbeveling: z.enum(["combineren", "afstemmen", "apart_houden"]),
    });

    export const ProjectMatchItemSchema = z.object({
      project: z.string(),
      heeftMatch: z.boolean(),
      gekoppeldAan: z.string().optional(),
      type: z.string().optional(),
      advies: z.string(),
    });
    ```

    **D) Extend AICrossAnalyseSchema (add after externeProjecten section, around line 551):**

    Add these three new optional sections following the exact same `.optional().default(...)` pattern used by the existing sections:

    ```typescript
    vermogenClusters: z
      .object({
        titel: z.string().optional().default(""),
        toelichting: z.string().optional().default(""),
        items: z.array(VermogenClusterItemSchema).optional().default([]),
      })
      .optional()
      .default(() => ({ titel: "", toelichting: "", items: [] })),
    inspanningClusters: z
      .object({
        titel: z.string().optional().default(""),
        toelichting: z.string().optional().default(""),
        items: z.array(InspanningClusterItemSchema).optional().default([]),
      })
      .optional()
      .default(() => ({ titel: "", toelichting: "", items: [] })),
    projectMatching: z
      .object({
        titel: z.string().optional().default(""),
        toelichting: z.string().optional().default(""),
        items: z.array(ProjectMatchItemSchema).optional().default([]),
      })
      .optional()
      .default(() => ({ titel: "", toelichting: "", items: [] })),
    ```

    **E) Add new type exports at the bottom of schemas.ts (after existing type exports):**

    ```typescript
    export type VermogenClusterItem = z.infer<typeof VermogenClusterItemSchema>;
    export type InspanningClusterItem = z.infer<typeof InspanningClusterItemSchema>;
    export type ProjectMatchItem = z.infer<typeof ProjectMatchItemSchema>;
    ```

    **F) Add new type re-exports in types.ts:**

    Add to the `export type { ... } from "./schemas"` block:
    ```typescript
    VermogenClusterItem,
    InspanningClusterItem,
    ProjectMatchItem,
    ```

    **G) Create test file src/lib/__tests__/cross-analyse-schema.test.ts** with the 10 test cases listed in the behavior block. Use vitest. Test backward compat by parsing data WITHOUT the new fields. Test new fields by parsing data WITH the new sections. Test consolidation flags on both DINCapabilitySchema and DINEffortSchema.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/schemas.ts contains `consolidated: z.boolean().optional()` inside DINCapabilitySchema
    - src/lib/schemas.ts contains `consolidated: z.boolean().optional()` inside DINEffortSchema
    - src/lib/schemas.ts contains `export const VermogenClusterItemSchema`
    - src/lib/schemas.ts contains `export const InspanningClusterItemSchema`
    - src/lib/schemas.ts contains `export const ProjectMatchItemSchema`
    - src/lib/schemas.ts AICrossAnalyseSchema contains `vermogenClusters:` field
    - src/lib/schemas.ts AICrossAnalyseSchema contains `inspanningClusters:` field
    - src/lib/schemas.ts AICrossAnalyseSchema contains `projectMatching:` field
    - src/lib/types.ts re-exports VermogenClusterItem, InspanningClusterItem, ProjectMatchItem
    - src/lib/__tests__/cross-analyse-schema.test.ts exists with all 10 test cases
    - `npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts -x` exits 0
  </acceptance_criteria>
  <done>All 10 schema tests pass. DINCapabilitySchema/DINEffortSchema accept consolidated flags. AICrossAnalyseSchema accepts new cluster sections while remaining backward compatible with old data.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade CROSS_ANALYSE_PROMPT and API route for semantic matching</name>
  <files>src/lib/prompts.ts, src/app/api/cross-analyse/route.ts</files>
  <read_first>
    - src/lib/prompts.ts (full CROSS_ANALYSE_PROMPT, lines 46-137 -- the exact prompt text to upgrade)
    - src/app/api/cross-analyse/route.ts (full file -- the default cross-analyse handler at lines 194-215)
    - src/lib/prompt-assembly.ts (assembleSystemPrompt signature and cross-analyse use case)
    - src/lib/ai-client.ts (callClaudeWithValidation signature)
    - src/lib/schemas.ts (updated AICrossAnalyseSchema from Task 1)
  </read_first>
  <action>
    **A) Upgrade CROSS_ANALYSE_PROMPT in prompts.ts (replace lines 46-137):**

    Keep the existing structure (synergie, gaps, hefboomwerking, domeinBalans, sectorOverlap, externeProjecten) but ADD three new sections to the JSON template and add semantic matching instructions. The updated prompt must:

    1. Keep the opening line: expert in programmamanagement (DIN-methodiek, Doelen-Inspanningennetwerk, Wijnen & Van der Tak, 2002).
    2. Add explicit instruction: "Identificeer SEMANTISCH vergelijkbare vermogens en inspanningen over sectoren heen -- niet alleen exacte woordmatch, maar ook conceptueel gelijke items die anders geformuleerd zijn."
    3. Add instruction per D-03: "Baten worden NIET gematcht of samengevoegd. Baten zijn per sector verschillend -- dat is methodisch correct. Toon baten als context bij geconsolideerde vermogens/inspanningen om de hefboomwerking zichtbaar te maken."
    4. Add instruction per D-07/D-08: "Koppel lopende projecten aan vermogens of baten. Geef expliciet aan als een project NIET thuishoort in het DIN-netwerk."
    5. Add instruction: "Gebruik de meegegeven id-velden om items te identificeren in je clusters. Retourneer altijd het exacte id van het item."
    6. Extend the JSON response structure with these three new sections AFTER externeProjecten:

    ```json
    "vermogenClusters": {
      "titel": "Gedeelde vermogens",
      "toelichting": "Korte samenvatting van semantisch vergelijkbare vermogens over sectoren (1-2 zinnen)",
      "items": [
        {
          "clusterTitel": "Beschrijvende naam van het gedeelde vermogen",
          "items": [
            {"id": "exact-uuid-van-het-vermogen", "beschrijving": "Vermogen beschrijving", "sector": "PO"},
            {"id": "exact-uuid-van-het-vermogen", "beschrijving": "Vergelijkbaar vermogen", "sector": "VO"}
          ],
          "batenContext": [
            {"baat": "Sector-specifieke baat waar dit vermogen aan bijdraagt", "sector": "PO"},
            {"baat": "Andere sector-specifieke baat", "sector": "VO"}
          ],
          "advies": "Concreet consolidatie-advies: waarom combineren/afstemmen/apart houden",
          "aanbeveling": "combineren"
        }
      ]
    },
    "inspanningClusters": {
      "titel": "Gedeelde inspanningen",
      "toelichting": "Korte samenvatting van semantisch vergelijkbare inspanningen over sectoren (1-2 zinnen)",
      "items": [
        {
          "clusterTitel": "Beschrijvende naam van de gedeelde inspanning",
          "items": [
            {"id": "exact-uuid-van-de-inspanning", "beschrijving": "Inspanning beschrijving", "sector": "PO", "domein": "mens"},
            {"id": "exact-uuid-van-de-inspanning", "beschrijving": "Vergelijkbare inspanning", "sector": "VO", "domein": "mens"}
          ],
          "batenContext": [
            {"baat": "Baat waar deze inspanning (via vermogen) aan bijdraagt", "sector": "PO"}
          ],
          "advies": "Concreet consolidatie-advies",
          "aanbeveling": "combineren"
        }
      ]
    },
    "projectMatching": {
      "titel": "Projectkoppeling",
      "toelichting": "Korte samenvatting van de koppeling tussen lopende projecten en het DIN-netwerk (1-2 zinnen). Als er geen projecten zijn, schrijf dat op.",
      "items": [
        {
          "project": "Naam van het project",
          "heeftMatch": true,
          "gekoppeldAan": "Naam van het vermogen of de baat",
          "type": "vermogen",
          "advies": "Concreet advies over hoe dit project bijdraagt"
        }
      ]
    }
    ```

    7. Add to the BELANGRIJK section at the bottom:
       - "Clusters bevatten alleen items die bij MEERDERE sectoren terugkomen. Eenmalige items zijn geen cluster."
       - "aanbeveling is altijd een van: 'combineren', 'afstemmen', of 'apart_houden'"
       - "projectMatching.heeftMatch is false als een project NIET thuishoort in het DIN-netwerk"

    **B) Update the default cross-analyse handler in cross-analyse/route.ts (replace lines 194-215):**

    1. Build a structured user message instead of raw JSON.stringify. Format capabilities and efforts with their IDs explicitly:

    ```typescript
    // Build structured user message with entity IDs for reliable matching
    const goalsData = (body.goals || []).map((g: { id: string; name: string; description?: string }) => ({
      id: g.id, name: g.name, description: g.description || "",
    }));
    const benefitsData = (body.benefits || []).map((b: { id: string; goalId: string; sectorId: string; title?: string; description: string }) => ({
      id: b.id, goalId: b.goalId, sectorId: b.sectorId, title: b.title || "", description: b.description,
    }));
    const capsData = (body.capabilities || []).map((c: { id: string; sectorId: string; title?: string; description: string }) => ({
      id: c.id, sectorId: c.sectorId, title: c.title || "", description: c.description,
    }));
    const effortsData = (body.efforts || []).map((e: { id: string; sectorId: string; title?: string; description: string; domain: string }) => ({
      id: e.id, sectorId: e.sectorId, title: e.title || "", description: e.description, domain: e.domain,
    }));
    const projectsData = (body.externalProjects || []).map((p: { id: string; name: string; description: string; sectorId: string; status: string }) => ({
      id: p.id, name: p.name, description: p.description, sectorId: p.sectorId, status: p.status,
    }));

    const structuredData = {
      doelen: goalsData,
      baten: benefitsData,
      vermogens: capsData,
      inspanningen: effortsData,
      lopendeProjecten: projectsData,
      koppelingen: {
        goalBenefitMaps: body.goalBenefitMaps || [],
        benefitCapabilityMaps: body.benefitCapabilityMaps || [],
        capabilityEffortMaps: body.capabilityEffortMaps || [],
      },
    };

    const userMessage = `Analyseer de volgende DIN-data over alle sectoren heen.\nGebruik de id-velden om items te identificeren in je clusters.\n\n${JSON.stringify(structuredData, null, 2).slice(0, 20000)}`;
    ```

    2. Add optional userFeedback to the user message (same as existing pattern used in sector-integratie):
    ```typescript
    if (body.userFeedback) {
      userMessage += `\n\nExtra instructies van de gebruiker: ${body.userFeedback}`;
    }
    ```

    3. Increase maxTokens from 8192 to 16384:
    ```typescript
    const result = await callClaudeWithValidation(
      AICrossAnalyseSchema,
      assembleSystemPrompt(CROSS_ANALYSE_PROMPT, "cross-analyse", undefined, kibContext),
      userMessage,
      { maxTokens: 16384, model: "claude-opus-4-6" }
    );
    ```

    4. Also pass kibGoals and kibScope from body (pattern from Phase 4 -- extract before the default handler, already done at line 12).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/prompts.ts CROSS_ANALYSE_PROMPT contains "vermogenClusters"
    - src/lib/prompts.ts CROSS_ANALYSE_PROMPT contains "inspanningClusters"
    - src/lib/prompts.ts CROSS_ANALYSE_PROMPT contains "projectMatching"
    - src/lib/prompts.ts CROSS_ANALYSE_PROMPT contains "semantisch" (semantic matching instruction)
    - src/lib/prompts.ts CROSS_ANALYSE_PROMPT contains "Baten worden NIET gematcht"
    - src/lib/prompts.ts CROSS_ANALYSE_PROMPT contains "id-velden"
    - src/app/api/cross-analyse/route.ts contains `maxTokens: 16384`
    - src/app/api/cross-analyse/route.ts contains `id: g.id` or `id: c.id` (entity IDs sent to AI)
    - src/app/api/cross-analyse/route.ts contains `.slice(0, 20000)` (increased data budget)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>CROSS_ANALYSE_PROMPT includes semantic matching instructions for vermogens and inspanningen with new JSON output sections. API route sends entity IDs in structured format with 16384 maxTokens. Build passes.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts -x` -- all 10 schema tests pass
2. `npm run build` -- TypeScript compilation succeeds with new schema types and prompt changes
3. Backward compat: old AICrossAnalyseSchema JSON without vermogenClusters/inspanningClusters/projectMatching parses successfully
</verification>

<success_criteria>
- Extended AICrossAnalyseSchema validates both old (no clusters) and new (with clusters) AI responses
- DINCapabilitySchema and DINEffortSchema accept consolidated/consolidatedInto fields
- CROSS_ANALYSE_PROMPT instructs AI to perform semantic matching and return clusters with entity IDs
- API route sends structured data with IDs and uses 16384 maxTokens
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/08-cross-analyse-semantische-matching/08-01-SUMMARY.md`
</output>
