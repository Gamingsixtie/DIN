---
phase: 8
slug: cross-analyse-semantische-matching
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-03
---

# Phase 8 — UI Design Contract

> Visual and interaction contract for Cross-Analyse Semantische Matching. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Tailwind CSS 4 @theme tokens) |
| Preset | not applicable |
| Component library | none (custom components, inline SVG icons) |
| Icon library | Heroicons (inline SVG, stroke variant, 24x24 viewBox) |
| Font | `Inter`, `Segoe UI`, `system-ui`, `-apple-system`, `sans-serif` |

**Source:** Existing `globals.css` @theme block + CLAUDE.md stack definition.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, badge internal padding |
| sm | 8px | Compact element spacing, gap between badges |
| md | 16px | Default element spacing, card content padding |
| lg | 24px | Section padding, card header padding-x (px-5 = 20px, use px-6 = 24px for new sections) |
| xl | 32px | Layout gaps between major sections (space-y-8) |
| 2xl | 48px | Page-level vertical rhythm |
| 3xl | 64px | Not used in this phase |

**Component-level minimum constraint (not a spacing token):** Consolidation action buttons ("Combineren", "Afstemmen", "Apart houden", "Ongedaan maken") enforce a 44px minimum touch target height via `min-h-[44px]`. This is an accessibility constraint on interactive elements, not a layout spacing value, and does not appear in the spacing scale.

---

## Typography

Declared weights: 400 (regular) and 600 (semibold). Maximum 2 weights enforced.

| Role | Size | Weight | Line Height | Tailwind Class |
|------|------|--------|-------------|----------------|
| Body | 14px | 400 (regular) | 1.5 | `text-sm` |
| Label | 10px | 600 (semibold) | 1.4 | `text-[10px] font-semibold` |
| Heading | 14px | 600 (semibold) | 1.3 | `text-sm font-semibold` |
| Display | 18px | 600 (semibold) | 1.2 | `text-lg font-semibold` |

**Source:** Existing CrossAnalyseStep patterns. Display size used for the main "Cross-analyse" heading. Heading size used for section headers. Label size used for sector badges, domain labels, and stat counters. Body size used for descriptions and advice text.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#f5f7fa` (cito-bg) | Page background |
| Secondary (30%) | `#ffffff` (cito-surface) | Cards, section containers, stat blocks |
| Accent (10%) | `#003366` (cito-blue) | Primary CTA button, section headings, active states |
| Destructive | `#dc2626` (red-600) | Gap warnings, error states, "Niet afgedekt" warnings |

Accent reserved for:
- "AI Cross-analyse uitvoeren" primary CTA button background
- "Samenvoegen" consolidation action button background
- Section heading text color (`text-cito-blue`)
- Stat counter numbers for Doelen

### Semantic Section Colors (existing pattern, maintained)

Each analysis section uses a distinct border/header color. Phase 8 adds two new section types:

| Section | Border | Header BG | Icon BG | Text |
|---------|--------|-----------|---------|------|
| Synergie (existing) | `border-amber-200` | `bg-amber-50` | `bg-amber-100` | `text-amber-800` |
| Gaps (existing) | `border-red-200` | `bg-red-50` | `bg-red-100` | `text-red-800` |
| Hefboomwerking (existing) | `border-green-200` | `bg-green-50` | `bg-green-100` | `text-green-800` |
| Domeinbalans (existing) | `border-purple-200` | `bg-purple-50` | `bg-purple-100` | `text-purple-800` |
| Sector-overlap (existing) | `border-blue-200` | `bg-blue-50` | `bg-blue-100` | `text-blue-800` |
| Externe projecten (existing) | `border-cyan-200` | `bg-cyan-50` | `bg-cyan-100` | `text-cyan-800` |
| **Vermogen-clusters (NEW)** | `border-teal-200` | `bg-teal-50` | `bg-teal-100` | `text-teal-800` |
| **Inspanning-clusters (NEW)** | `border-indigo-200` | `bg-indigo-50` | `bg-indigo-100` | `text-indigo-800` |

### Consolidation State Colors

| State | Badge Style | Usage |
|-------|------------|-------|
| Cluster identified (pre-merge) | `bg-amber-100 text-amber-700 border-amber-200` | AI has found a cluster, user has not acted |
| Consolidated (merged) | `bg-teal-100 text-teal-700 border-teal-200` | Items successfully merged into shared item |
| No match (project) | `bg-gray-100 text-gray-500 border-gray-200` | External project has no DIN match |
| Match found (project) | `bg-green-100 text-green-700 border-green-200` | External project matches a vermogen/baat |

### Sector Colors (existing, unchanged)

| Sector | Style |
|--------|-------|
| PO | `bg-blue-100 text-blue-800 border-blue-200` |
| VO | `bg-green-100 text-green-800 border-green-200` |
| Zakelijk | `bg-purple-100 text-purple-800 border-purple-200` |

### Domain Colors (existing, unchanged)

| Domain | Bar Color | BG | Text |
|--------|-----------|-----|------|
| Mens | `#2563eb` | `bg-blue-50` | `text-blue-700` |
| Processen | `#059669` | `bg-green-50` | `text-green-700` |
| Data & Systemen | `#7c3aed` | `bg-purple-50` | `text-purple-700` |
| Cultuur | `#d97706` | `bg-amber-50` | `text-amber-700` |

---

## Visual Focal Points

### Pre-analysis state (before AI has run)

When the user first lands on the Cross-Analyse step and no analysis results are available yet, the primary visual anchor is the **"AI Cross-analyse uitvoeren" CTA button**. This button uses `bg-cito-blue text-white` and is the only accent-colored element on the page in this state. The stats strip above it provides secondary orientation (counts of Doelen, Baten, Vermogens, Inspanningen) but the CTA is the clear action focal point.

| Screen State | Primary Focal Point | Secondary Elements |
|--------------|--------------------|--------------------|
| Pre-analysis (no results) | "AI Cross-analyse uitvoeren" CTA button (accent bg, center-positioned) | Stats strip showing DIN item counts |
| Post-analysis (results visible) | First analysis section (Synergie) | Section headers with colored icons guide scanning |
| Empty state (no DIN data) | Empty state heading "Nog geen DIN-data beschikbaar" | Guidance text directing user to DIN-Mapping step |

---

## Copywriting Contract

All copy in Dutch (nl-NL) per CLAUDE.md constraint.

| Element | Copy |
|---------|------|
| Primary CTA | "AI Cross-analyse uitvoeren" (existing, unchanged) |
| Secondary CTA (consolidation) | "Samenvoegen" |
| Undo CTA | "Ongedaan maken" |
| Empty state heading | "Nog geen DIN-data beschikbaar" (existing, unchanged) |
| Empty state body | "Vul eerst het DIN-netwerk in via de DIN-Mapping stap om een cross-analyse uit te voeren." (existing, unchanged) |
| Error state | "Analyse mislukt" heading + specific error message + "Geef extra instructies mee voor een nieuwe poging" retry prompt (existing pattern, unchanged) |
| Consolidation confirmation | "Samenvoegen": "{N} items worden samengevoegd tot een gedeeld item dat gekoppeld wordt aan {sector-list}. Originele items blijven bewaard." (inline explanation, no modal) |
| Confirmation confirm button | "Bevestigen" |
| Confirmation cancel button | "Toch niet samenvoegen" |
| Undo confirmation | No confirmation needed -- undo is non-destructive (removes consolidated flag) |

### New Copywriting Elements for Phase 8

| Element | Copy |
|---------|------|
| Vermogen-clusters section title | "Gedeelde vermogens" |
| Vermogen-clusters section subtitle | "Vermogens die bij meerdere sectoren terugkomen -- kandidaten voor consolidatie." |
| Inspanning-clusters section title | "Gedeelde inspanningen" |
| Inspanning-clusters section subtitle | "Inspanningen die meerdere sectoren bedienen -- samen sterker door consolidatie." |
| Project-matching section title | "Projectkoppeling" |
| Project-matching section subtitle | "Lopende projecten gekoppeld aan het DIN-netwerk -- welke passen, welke niet." |
| Cluster advice label | "AI-advies:" |
| Cluster action label (combine) | "Combineren -- voeg samen tot een gedeeld item" |
| Cluster action label (align) | "Afstemmen -- houd apart maar stem aanpak af" |
| Cluster action label (keep separate) | "Apart houden -- geen consolidatie nodig" |
| Consolidated badge | "Geconsolideerd" |
| Consolidated chain label | "Draagt bij aan:" |
| No DIN match badge (project) | "Geen DIN-match" |
| DIN match badge (project) | "Gekoppeld aan {vermogen/baat naam}" |
| Toast on merge success | "Items samengevoegd" |
| Toast on undo success | "Samenvoeging ongedaan gemaakt" |
| Empty cluster state | "Geen gedeelde {vermogens/inspanningen} gevonden over sectoren." |
| Empty project state | "Geen lopende projecten beschikbaar om te koppelen." |

---

## Component Inventory

### Existing Components (reused, not modified)

| Component | Location | Usage in Phase 8 |
|-----------|----------|------------------|
| `LoadingOverlay` | CrossAnalyseStep.tsx (inner) | Shown during AI analysis |
| `SectorBadge` | CrossAnalyseStep.tsx (inner) | Sector indicators on cluster items |
| `SynergieSection` | CrossAnalyseStep.tsx (inner) | Existing synergy display (kept) |
| `GapsSection` | CrossAnalyseStep.tsx (inner) | Existing gap display (kept) |
| `HefboomSection` | CrossAnalyseStep.tsx (inner) | Existing leverage display (kept) |
| `DomeinBalansSection` | CrossAnalyseStep.tsx (inner) | Existing domain balance (kept) |
| `SectorOverlapSection` | CrossAnalyseStep.tsx (inner) | Existing sector overlap (kept) |
| `ExterneProjectenSection` | CrossAnalyseStep.tsx (inner) | Replaced by ProjectMatchingSection |
| `MarkdownContent` | components/ui/MarkdownContent.tsx | AI advice rendering |
| `Toast` | components/ui/Toast.tsx | Consolidation feedback |

### New Components (to be created)

| Component | Description | Visual Pattern |
|-----------|-------------|----------------|
| `VermogenClusterSection` | Renders AI-identified capability clusters with consolidation actions | Teal-bordered section card (same pattern as existing sections). Each cluster is a card with: cluster description, list of matched items with sector badges, AI advice text, action buttons. |
| `InspanningClusterSection` | Renders AI-identified effort clusters with consolidation actions | Indigo-bordered section card. Same internal structure as VermogenClusterSection. |
| `ProjectMatchingSection` | Replaces ExterneProjectenSection with richer match/no-match display | Cyan-bordered section card (reuses existing color). Each project shows: name, match status badge (green/gray), matched vermogen/baat name, AI advice. |
| `ClusterCard` | Reusable card for a single cluster (vermogen or inspanning) | White card with left border accent (teal for vermogens, indigo for inspanningen). Contains: title, matched items list, sector badges, AI advice, action button row. |
| `ConsolidatedBadge` | Badge indicating an item has been consolidated | `bg-teal-100 text-teal-700 border border-teal-200 rounded px-1.5 py-0.5 text-[10px] font-semibold` with a link icon. |
| `ConsolidationActionBar` | Row of action buttons for a cluster | Flex row with 3 buttons: "Combineren" (primary, cito-blue bg), "Afstemmen" (secondary, gray border), "Apart houden" (ghost, text only). All buttons enforce `min-h-[44px]` touch target. |
| `ChainIndicator` | Shows which sector-baten a consolidated item contributes to | Horizontal flex of colored dots + baat names, grouped by sector. Pattern: `[SectorBadge] baat-titel` repeated per connected baat. |

---

## Interaction Contracts

### 1. AI Cross-analyse (existing flow, unchanged trigger)

| Step | User Action | System Response |
|------|-------------|-----------------|
| 1 | Clicks "AI Cross-analyse uitvoeren" | LoadingOverlay appears. API call fires. |
| 2 | Waits | AI processes all DIN items in one call (D-05, D-06). |
| 3 | Response received | LoadingOverlay disappears. New sections render: vermogen-clusters, inspanning-clusters, project-matching. Existing sections (synergie, gaps, hefboom, domeinbalans) also update. |
| 4 | Error | Error banner with retry textarea (existing pattern). |

### 2. Consolidation Flow (NEW)

| Step | User Action | System Response |
|------|-------------|-----------------|
| 1 | Reviews a cluster in VermogenClusterSection or InspanningClusterSection | Sees matched items, sector badges, AI advice, and recommended action. |
| 2 | Clicks "Combineren" on a cluster | Inline confirmation text appears below the button: "{N} items worden samengevoegd tot een gedeeld item dat gekoppeld wordt aan {sector-list}. Originele items blijven bewaard." + "Bevestigen" / "Toch niet samenvoegen" buttons. |
| 3 | Clicks "Bevestigen" | Original items get `consolidated: true` flag + `consolidatedInto` link to new shared item. New shared item created with multi-sector mappings. Toast: "Items samengevoegd". ClusterCard collapses to a single-line consolidated summary. |
| 4 | Clicks "Afstemmen" on a cluster | No data mutation. AI advice badge changes to "Afgestemd" (visual acknowledgement). Cluster card gets a subtle gray overlay to indicate it has been reviewed. |
| 5 | Clicks "Apart houden" on a cluster | No data mutation. AI advice badge changes to "Apart gehouden". Cluster card gets a subtle gray overlay. |

### 3. Undo Consolidation (NEW)

| Step | User Action | System Response |
|------|-------------|-----------------|
| 1 | Clicks "Ongedaan maken" on a consolidated cluster summary | Consolidated flag removed from original items. Shared item removed. Mappings restored. Toast: "Samenvoeging ongedaan gemaakt". ClusterCard re-expands to show original items. |

### 4. Project Matching (NEW)

| Step | User Action | System Response |
|------|-------------|-----------------|
| 1 | Views ProjectMatchingSection after AI analysis | Each external project shows a match status: green badge + linked vermogen/baat name, OR gray badge "Geen DIN-match" + AI explanation. |
| 2 | No user action needed | Project matching is read-only display of AI results. No consolidation actions on projects. |

### 5. Consolidated Items in Existing Sections

| Context | Visual Treatment |
|---------|------------------|
| Vermogen-Synergie Matrix table | Consolidated items show `ConsolidatedBadge` next to title. Non-consolidated items in the same cluster are visually dimmed (`opacity-50`). |
| Inspanningen per Domein cards | Consolidated efforts show `ConsolidatedBadge`. Count reflects deduplicated total. |
| Anywhere an item appears | If `consolidated === true`, show badge + `ChainIndicator` showing which sector-baten it serves (D-11). |

---

## Layout Contract

### Section Order in CrossAnalyseStep

The step renders sections in this order (top to bottom):

1. **Header + stats strip** (existing, unchanged)
2. **Error state** (existing, shown conditionally)
3. **AI Analysis Results** (when available):
   - 3a. Synergie section (existing)
   - 3b. Gaps section (existing)
   - 3c. Hefboomwerking section (existing)
   - 3d. **Vermogen-clusters section (NEW)** -- positioned after hefboomwerking because it provides actionable follow-up
   - 3e. **Inspanning-clusters section (NEW)** -- paired with vermogen-clusters
   - 3f. DomeinBalans section (existing)
   - 3g. SectorOverlap section (existing)
   - 3h. **Project-matching section (NEW, replaces ExterneProjectenSection)**
4. **Vermogen-Synergie Matrix** (existing local analysis, unchanged)
5. **Inspanningen per Domein** (existing local analysis, unchanged)
6. **Gap-analyse** (existing local analysis, unchanged)

### ClusterCard Internal Layout

```
+------------------------------------------------------------------+
| [teal/indigo left border 4px]                                     |
|                                                                    |
|  Cluster titel (text-sm font-semibold)                            |
|  AI-advies: advies tekst (text-xs text-gray-500 italic)          |
|                                                                    |
|  +-----------------------------------------------------------+   |
|  | Item 1: beschrijving          [PO] [VO]                   |   |
|  +-----------------------------------------------------------+   |
|  | Item 2: beschrijving          [Zakelijk]                   |   |
|  +-----------------------------------------------------------+   |
|                                                                    |
|  Draagt bij aan:                                                  |
|  [PO] baat-titel  [VO] baat-titel  [Zakelijk] baat-titel        |
|                                                                    |
|  [Combineren]  [Afstemmen]  [Apart houden]                       |
+------------------------------------------------------------------+
```

### ConsolidationActionBar Button Styles

All consolidation buttons enforce `min-h-[44px]` for accessibility touch targets.

| Button | Style |
|--------|-------|
| Combineren (primary) | `px-3 min-h-[44px] bg-cito-blue text-white rounded-lg text-xs font-semibold hover:bg-cito-blue-light transition-colors` |
| Afstemmen (secondary) | `px-3 min-h-[44px] border border-gray-300 text-gray-700 rounded-lg text-xs font-semibold hover:bg-gray-50 transition-colors` |
| Apart houden (ghost) | `px-3 min-h-[44px] text-gray-500 text-xs font-semibold hover:text-gray-700 transition-colors` |
| Ongedaan maken | `px-3 min-h-[44px] text-teal-600 text-xs font-semibold hover:text-teal-800 underline transition-colors` |
| Bevestigen (confirm) | `px-3 min-h-[44px] bg-cito-blue text-white rounded-lg text-xs font-semibold` |
| Toch niet samenvoegen (cancel) | `px-3 min-h-[44px] text-gray-500 text-xs font-semibold` |

---

## State Management Contract

### New Fields on DIN Entities

Per D-10, consolidated items preserve originals with a flag:

```typescript
// Added to DINCapability and DINEffort schemas
consolidated?: boolean;        // true when merged into a shared item
consolidatedInto?: string;     // id of the shared item this was merged into
```

### Session State Updates

All consolidation actions use `updateSession(prev => ...)` functional updater pattern (established in Phase 2).

| Action | State Mutation |
|--------|---------------|
| Merge cluster | Create new shared item. Set `consolidated: true` + `consolidatedInto` on originals. Add multi-sector mappings for shared item. |
| Undo merge | Remove shared item. Clear `consolidated` + `consolidatedInto` on originals. Remove shared item mappings. |
| Mark as "afgestemd" / "apart gehouden" | No session mutation. Local component state only (reviewed clusters set). |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| No registry | N/A | not applicable |

No shadcn or third-party registries are used. All components are custom-built with Tailwind CSS utility classes.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
