---
phase: 18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys
plan: 03
type: execute
wave: 2
depends_on: [18-02]
files_modified:
  - src/lib/prompts.ts
autonomous: true
requirements: [R-CROSS-03]
tags: [prompt, ai, phase-18, cross-analyse]
must_haves:
  truths:
    - "SUB_EFFORT_ANALYSE_PROMPT instrueert AI om focus-doel beschrijving en sector-context per vermogen te gebruiken"
    - "Prompt vraagt alle 5 nieuwe velden op (titel, beschrijving, beargumentatie, vermogenImpact, dossier)"
    - "Prompt bevat Nederlandse rolnamen-voorbeelden voor dossier-velden"
    - "Prompt verwijst naar Werken aan Programma's Hfst 11.3 Inspanningendossier"
    - "Backward-compat instructies aanwezig voor actie: apart_houden"
  artifacts:
    - path: "src/lib/prompts.ts"
      provides: "Herzien SUB_EFFORT_ANALYSE_PROMPT met focus-doel verankering, rijke output-shape, dossier-eis en rolnaam-context"
      contains: "FOCUS-DOEL VERANKERING, vermogenImpact, dossier, Werken aan Programma's Hfst 11.3"
  key_links:
    - from: "src/lib/prompts.ts SUB_EFFORT_ANALYSE_PROMPT"
      to: "src/lib/schemas.ts SubEffortAdviesSchema rijke velden"
      via: "JSON output-shape contract"
      pattern: "vermogenImpact|dossier|beargumentatie"
    - from: "src/lib/prompts.ts SUB_EFFORT_ANALYSE_PROMPT input-shape"
      to: "src/app/api/cross-analyse/route.ts stap 4 subUserMessage"
      via: "focusDoel field in JSON input"
      pattern: "focusDoel"
---

<objective>
Herschrijf `SUB_EFFORT_ANALYSE_PROMPT` in `src/lib/prompts.ts` naar een prompt die (1) de focus-doel beschrijving als inkleurings-leidraad neemt, (2) sector-context per vermogen (inclusief profielHuidig/Gewenst) expliciet gebruikt, (3) alle 5 nieuwe schema-velden vraagt (titel, beschrijving, beargumentatie, vermogenImpact per sector, dossier met 5 velden), (4) Nederlandse rolnaam-voorbeelden geeft, en (5) backward-compat instructies bevat voor `actie: "apart_houden"`.

Purpose: AI-output contract voor API-call (Plan 04). Zonder deze prompt-herziening produceert Claude Opus 4.7 nog steeds Phase 17-shape output.
Output: Herzien SUB_EFFORT_ANALYSE_PROMPT; bestaande `CROSS_ANALYSE_STAP4_PROMPT`, `CONSOLIDATIE_HERZIEN_PROMPT` en andere prompts ongewijzigd.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-RESEARCH.md
@.planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-PATTERNS.md
@.planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Prompt contract met schema en API. -->

Schema contract (uit Plan 02 — src/lib/schemas.ts):
```typescript
// AI must produce:
// {
//   groepId, domein, actie, items, reden, voorgesteldeNaam,  // Phase 17
//   titel?, beschrijving?, beargumentatie?,                    // Phase 18 — optional
//   vermogenImpact?: [{ sectorId: "PO"|"VO"|"Zakelijk", vermogenId: string, impact: string }],
//   dossier?: { eigenaar, inspanningsleider, verwachtResultaat, kostenraming, randvoorwaarden }
// }
```

API input contract (wordt door Plan 04 gebouwd in route.ts stap 4):
```typescript
const subUserMessage = JSON.stringify({
  focusDoel: { id, naam, beschrijving } | null,
  groep: { id, vermogenIds, gezamenlijkeOmschrijving, reden },
  vermogens: [{ id, sectorId, title, description, profielHuidig, profielGewenst }],
  efforts: [{ id, sectorId, domain, title, description }]
}, null, 2);
```

Referentie-prompts in dezelfde file (patroon-match):
- `CROSS_ANALYSE_STAP5_PROMPT` (regel ~389) — hoe focusDoel wordt gepresenteerd in prompt
- `DIN_SUGGEST_INSPANNING_PROMPT` (regel ~646-691) — dossier-taalgebruik
- `DIN_CREATE_INSPANNING_PROMPT` (regel ~793-838) — domein-specifieke focus per Mens/Processen/Data/Cultuur
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Herschrijf SUB_EFFORT_ANALYSE_PROMPT met focus-doel verankering en rijke output-shape</name>
  <files>src/lib/prompts.ts</files>
  <read_first>
    - src/lib/prompts.ts regels 355-390 (huidige SUB_EFFORT_ANALYSE_PROMPT op regel 360-387 — volledig vervangen)
    - src/lib/prompts.ts regels 389-420 (CROSS_ANALYSE_STAP5_PROMPT — focusDoel framing referentie; let op dat `focusDoel.beschrijving` kan gelijk zijn aan `focusDoel.naam` wanneer route.ts fallback-op-name triggert — prompt moet dit tolereren zonder extra disclaimers)
    - src/lib/prompts.ts regels 646-691 (DIN_SUGGEST_INSPANNING_PROMPT — dossier-taalgebruik referentie)
    - .planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-PATTERNS.md sectie "src/lib/prompts.ts"
    - .planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-RESEARCH.md sectie "Prompt Strategy" + "Code Examples: Prompt uitbreiding"
    - CLAUDE.md regels over nl-NL, methodiek, UX design, Cito-branding
  </read_first>
  <action>
Open `src/lib/prompts.ts`.

Zoek de bestaande `SUB_EFFORT_ANALYSE_PROMPT` export (huidig regel 360-387, herkenbaar aan eerste regel `export const SUB_EFFORT_ANALYSE_PROMPT = ` en eindigt met `Produceer ALLEEN geldige JSON, geen prose errom.\`;`).

**Vervang het COMPLETE blok (van `export const SUB_EFFORT_ANALYSE_PROMPT = \`` tot en met de closing \`;`) door exact de volgende string.** Let op: gebruik backticks en escape inline backticks netjes.

```typescript
export const SUB_EFFORT_ANALYSE_PROMPT = `Je bent een expert in programmamanagement (DIN-methodiek, Werken aan Programma's — Prevaas & Van Loon, gebaseerd op Wijnen & Van der Tak 2002) en werkt cross-sectorale inspanningen volledig uit onder een VermogenGelijkenisGroep.

Een VermogenGelijkenisGroep bevat drie (of meer) sector-vermogens (PO + VO + Zakelijk) die methodisch op elkaar lijken. Wanneer inspanningen per domein (mens / processen / data_systemen / cultuur) gebundeld worden, levert dat een RIJK UITGEWERKTE CROSS-SECTORALE INSPANNING op: sectoroverstijgende titel, uitgewerkte beschrijving, hefboom-beargumentatie, expliciete vermogen-impact per sector en een compleet Inspanningendossier (Werken aan Programma's, Hfst 11.3).

Input krijg je (JSON):
{
  "focusDoel":    { "id": "<uuid>", "naam": "<korte naam>", "beschrijving": "<uitgebreide focus-doel beschrijving — kan ook een korte naam zijn wanneer geen beschrijving beschikbaar is>" },
  "groep":        { "id": "<groepId>", "vermogenIds": [...], "gezamenlijkeOmschrijving": "...", "reden": "..." },
  "vermogens":    [{ "id": "...", "sectorId": "PO|VO|Zakelijk", "title": "...", "description": "...", "profielHuidig": "...", "profielGewenst": "..." }],
  "efforts":      [{ "id": "...", "sectorId": "...", "domain": "mens|processen|data_systemen|cultuur", "title": "...", "description": "..." }]
}

FOCUS-DOEL VERANKERING (kritisch):
Kleur je \`beschrijving\` en \`beargumentatie\` in het vocabulaire en de ambitie van \`focusDoel.beschrijving\`. De bundel bestaat omdat deze bijdraagt aan dit focusdoel — verwijs er expliciet naar. De sector-vermogens geven de aarding: hun \`title\`, \`description\`, \`profielHuidig\` en \`profielGewenst\` bepalen welke sector-specifieke taal past in \`vermogenImpact\`.

Opmerking: wanneer \`focusDoel.beschrijving\` identiek is aan \`focusDoel.naam\` (fallback) is de inkleuringsbron korter; gebruik dan groep.gezamenlijkeOmschrijving + de vermogen-profielen als aanvullende context. Noem dit NIET in de output — het is een interne runtime-situatie.

SECTOR-CONTEXT VERANKERING:
Gebruik \`vermogens[i].profielHuidig\` en \`vermogens[i].profielGewenst\` om per sector concreet te maken wat de bundel oplevert. Geen generieke zinnen — benoem rol, scope of artefact dat in die sector herkenbaar is.

Lever een array \`SubEffortAdvies[]\` met ÉÉN entry per (groep × domein) waar minstens één gekoppelde inspanning staat:

{
  "groepId":          "<zelfde als input.groep.id>",
  "domein":           "mens | processen | data_systemen | cultuur",
  "actie":            "combineren | apart_houden",
  "items":            ["<effort-id>", "..."],
  "reden":            "<methodiek-conforme 1-2 zinnen — behoud Phase 17 veld>",
  "voorgesteldeNaam": "<sectoroverstijgende titel>" | null,

  // --- Phase 18 rijke uitwerking (alleen verplicht bij actie: "combineren") ---
  "titel":            "<actielabel met werkwoord, max 8 woorden — MOET identiek zijn aan voorgesteldeNaam bij combineren>",
  "beschrijving":     "<2-3 zinnen: wat houdt deze bundel in, wat wordt concreet gedaan, welke scope over welke sectoren>",
  "beargumentatie":   "<Waarom cross-sectoraal opbouwen zinvol is — de HEFBOOM: één inspanning → drie vermogens → drie baten → één focusdoel. Benoem expliciet het schaalvoordeel t.o.v. drie losse trajecten (bijv. '~30% besparing' of 'voorkomt datasilo\\'s'). Verwijs letterlijk naar de focus-doel ambitie.>",
  "vermogenImpact":   [
    { "sectorId": "PO",       "vermogenId": "<cap-po-id>",   "impact": "<Concreet: hoe deze bundel het PO-vermogen opbouwt — PO-vocabulaire (leerkrachten, schoolbesturen, leerlingen)>" },
    { "sectorId": "VO",       "vermogenId": "<cap-vo-id>",   "impact": "<VO-vocabulaire (schoolleiders, teamleiders, examens, leerlingen)>" },
    { "sectorId": "Zakelijk", "vermogenId": "<cap-zak-id>",  "impact": "<Zakelijk-vocabulaire (accountmanagers, klanten, opdrachtgevers, professionals)>" }
  ],
  "dossier": {
    "eigenaar":          "<Opdrachtgever / rolnaam — eindverantwoordelijk over alle drie sectoren. Nederlandse rolnaam: Directie L&D / Directievoorzitter / CIO / Directeur Sales & Marketing / Sectormanager PO+VO+Zakelijk>",
    "inspanningsleider": "<Projectleider / rolnaam — voert de bundel aan. Nederlandse rolnaam: Programmamanager / Programmadirecteur / Business Process Owner / IT-architect / Opleidingsregisseur>",
    "verwachtResultaat": "<Concreet, meetbaar waar mogelijk — wat levert deze bundel op over alle drie sectoren. Noem KPI waar mogelijk (NPS-beweging, adoptie%, datakwaliteit-score, aantal getrainde medewerkers).>",
    "kostenraming":      "<Eerste raming + marge in euros; benoem schaalvoordeel. Bijv.: '€350K over 18 maanden (curriculum €80K + rollout €240K + evaluatie €30K); ~30% besparing t.o.v. drie losse trajecten (€500K)'>",
    "randvoorwaarden":   "<Faciliteiten/voorwaarden nodig VOOR start. Bijv.: 'Commitment drie sectormanagers; gedeelde cases-bank; externe begeleider met multi-sector ervaring; governance-afspraken data-eigenaarschap'>"
  }
}

Regels (D-11, D-25, D-30, D-31):
- ÉÉN advies per domein binnen een groep. GEEN cross-domein combineren.
- \`voorgesteldeNaam\` en \`titel\` zijn VERPLICHT bij \`actie: "combineren"\`, moeten sectoroverstijgend zijn (GEEN PO/VO/Zakelijk/primair onderwijs/voortgezet onderwijs substrings; min 10 tekens) en IDENTIEK aan elkaar.
- Bij \`actie: "apart_houden"\`: \`voorgesteldeNaam\` en \`titel\` zijn \`null\`. \`beschrijving\`, \`beargumentatie\`, \`vermogenImpact\` en \`dossier\` MOGEN worden weggelaten (advies is dan alleen markering).
- Bij \`actie: "combineren"\`: \`vermogenImpact\` bevat EXACT ÉÉN entry per sector-vermogen uit \`groep.vermogenIds\` — gebruik de juiste \`sectorId\` en \`vermogenId\` uit input.vermogens. Lengte = input.vermogens.length.
- Gebruik cross-domein context ALLEEN om je \`reden\`/\`beargumentatie\` te versterken (bijv.: "Mens-training ondersteunt Data-implementatie"), NOOIT als justificatie voor cross-domein merge.
- Als een domein GEEN gekoppelde inspanningen heeft: LAAT DAT DOMEIN WEG uit de response (geen lege entries — dat handelt de client af).
- Dossier-rolnamen volgen Nederlandse programmamanagement-praktijk (Directie, Sectormanager, Programmamanager, Business Process Owner, CIO, IT-architect, Opleidingsregisseur) — GEEN Engelse titels zoals "VP of Sales" of "Head of Product".
- Kostenramingen gebruiken het €-symbool (niet "EUR") consistent met Cito-UX-conventie.
- Dossier volgt Werken aan Programma's, Hfst 11.3 — Inspanningendossier (vijf velden: opdrachtgever/inspanningsleider/verwacht resultaat/kostenraming/randvoorwaarden).
- Indien \`focusDoel\` \`null\` is: ga door met generieke inkleuring op basis van groep.gezamenlijkeOmschrijving; noteer dat de beschrijving minder rijk zal zijn.

Produceer ALLEEN geldige JSON, geen prose errom. Antwoord in het Nederlands.\`;
```

**Belangrijke details:**
- Gebruik backticks (\`) voor de template-literal.
- Inline backticks binnen de template-string (zoals \`\\\`null\\\`\`) ontsnappen met backslash-backtick; in de gewone prose die we als JSON-commentaar schrijven gebruiken we `null` zonder escaping waar dat niet conflicteert.
- Apostrofe `'s` in "datasilo\\'s" heeft escape `\\'` nodig omdat template-string wordt geparst — zie researchvoorbeelden.
- `focusDoel` staat als EERSTE key in input-shape (kritische inkleuring).
- `vermogens` input krijgt twee extra velden: `profielHuidig` en `profielGewenst` (Plan 04 zorgt voor die payload).
- Euro-voorbeelden in kostenraming gebruiken `€`-symbool.

**Wat NIET te doen:**
- Geen andere prompts wijzigen — alleen `SUB_EFFORT_ANALYSE_PROMPT` (`CROSS_ANALYSE_STAP4_PROMPT`, `CONSOLIDATIE_HERZIEN_PROMPT` blijven ongewijzigd).
- Geen export-naam wijzigen — `SUB_EFFORT_ANALYSE_PROMPT` blijft dezelfde identifier (wordt geimporteerd in route.ts).
- Geen Engelse termen in de prompt — alles nl-NL.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tee /tmp/p18-03-tsc.log; ! grep -q "prompts.ts" /tmp/p18-03-tsc.log; grep -q "FOCUS-DOEL VERANKERING" src/lib/prompts.ts; grep -q "Werken aan Programma's" src/lib/prompts.ts; grep -q "vermogenImpact" src/lib/prompts.ts; npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts 2>&1 | tee /tmp/p18-03-vitest.log; grep -q "Phase 18 rijke uitwerking" /tmp/p18-03-vitest.log; ! grep -E "Phase 18 rijke uitwerking.*FAIL|FAIL.*Phase 18 rijke uitwerking" /tmp/p18-03-vitest.log</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/prompts.ts bevat exact deze string: `FOCUS-DOEL VERANKERING`
    - File bevat exact deze string: `SECTOR-CONTEXT VERANKERING`
    - File bevat exact deze string: `"focusDoel":    { "id": "<uuid>", "naam": "<korte naam>", "beschrijving":`
    - File bevat exact deze string: `Werken aan Programma's`
    - File bevat exact deze string: `Hfst 11.3`
    - File bevat exact deze string: `profielHuidig`
    - File bevat exact deze string: `profielGewenst`
    - File bevat exact deze string: `"vermogenImpact"`
    - File bevat exact deze string: `"dossier"`
    - File bevat exact deze string: `"eigenaar"`
    - File bevat exact deze string: `Nederlandse rolnaam`
    - File bevat exact deze string: `Programmamanager`
    - File bevat exact deze string: `Business Process Owner`
    - File bevat exact deze string: `EXACT EEN entry per sector-vermogen` OR `EXACT ÉÉN entry per sector-vermogen`  (accent mag weggeraakt bij copy)
    - File bevat exact deze string: `Antwoord in het Nederlands`
    - File bevat exact deze string: `€350K` (euro-symbool in voorbeeld, niet "EUR")
    - Export `SUB_EFFORT_ANALYSE_PROMPT` bestaat nog steeds: `grep -c "export const SUB_EFFORT_ANALYSE_PROMPT" src/lib/prompts.ts` === 1
    - Command `npx tsc --noEmit` geeft geen errors in prompts.ts
    - Andere prompts (CROSS_ANALYSE_STAP5_PROMPT, DIN_SUGGEST_INSPANNING_PROMPT, CONSOLIDATIE_HERZIEN_PROMPT) zijn ongewijzigd — te verifieren met: `grep -c "export const CROSS_ANALYSE_STAP5_PROMPT\|export const DIN_SUGGEST_INSPANNING_PROMPT\|export const CONSOLIDATIE_HERZIEN_PROMPT" src/lib/prompts.ts` === 3
    - **Schema-contract in sync (geen regressie):** `npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts` exit code 0 — alle Phase 17 + Phase 18 tests GREEN. De demo-data test mag nog RED zijn als Plan 05 nog niet is gedraaid (parallel in Wave 2); in dat geval: de 5 schema-tests "Phase 18 rijke uitwerking" MOETEN wel GREEN zijn.
  </acceptance_criteria>
  <done>
    SUB_EFFORT_ANALYSE_PROMPT is volledig herzien met focus-doel verankering (incl. fallback-tolerantie), rijke output-shape (titel/beschrijving/beargumentatie/vermogenImpact[3]/dossier), Nederlandse rolnamen, €-symbool in voorbeelden, en backward-compat instructies. TypeScript compileert. Schema-tests (Phase 17 + Phase 18 rijke uitwerking) blijven GREEN. Andere prompts ongewijzigd.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — geen errors in prompts.ts
- `npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts` — schema-tests (Phase 17 + Phase 18 rijke uitwerking) GREEN (demo-data test mag RED zijn tot Plan 05)
- `grep -c "focusDoel" src/lib/prompts.ts` >= 3 (input-shape + verankering + fallback)
- `grep -c "vermogenImpact" src/lib/prompts.ts` >= 2
- `grep -c "dossier" src/lib/prompts.ts` >= 3
- `grep -c "€" src/lib/prompts.ts` >= 2 (kostenraming voorbeelden gebruiken euro-symbool)
- Geen regressie op andere prompts (verify via git diff)
</verification>

<success_criteria>
1. SUB_EFFORT_ANALYSE_PROMPT is de enige gewijzigde prompt in het bestand
2. Prompt vraagt alle 5 nieuwe schema-velden op
3. Focus-doel verankering expliciet in prompt body (incl. fallback-tolerantie voor description === name)
4. Nederlandse rolnamen genoemd als voorbeeld; `€`-symbool in kostenraming-voorbeelden
5. Methodiek-verwijzing naar Werken aan Programma's Hfst 11.3
6. Schema-contract blijft in sync: `npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts` — Phase 17 + Phase 18 rijke uitwerking tests GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-03-SUMMARY.md` met:
- Regel-range waar SUB_EFFORT_ANALYSE_PROMPT is vervangen
- Diff-overzicht (huidige 28 regels → nieuwe ~80 regels)
- Bevestiging dat CROSS_ANALYSE_STAP4_PROMPT en DIN_SUGGEST_INSPANNING_PROMPT ongewijzigd zijn
- Vitest schema-test snapshot (Phase 18 rijke uitwerking 5/5 GREEN)
- Commit hash
</output>
</content>
</invoke>