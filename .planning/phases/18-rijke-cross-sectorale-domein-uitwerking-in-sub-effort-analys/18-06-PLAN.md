---
phase: 18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys
plan: 06
type: execute
wave: 3
depends_on: [18-02, 18-05]
files_modified:
  - src/components/cross-analyse/StapSectorVertaling.tsx
autonomous: true
requirements: [R-CROSS-03]
tags: [ui, component, render, phase-18, cross-analyse]
must_haves:
  truths:
    - "Domein-kaart rendert rijk per Phase 18 shape: titel, beschrijving, beargumentatie, vermogenImpact-lijst, dossier"
    - "Dossier-sectie is altijd-zichtbaar onder de kaart (geen expand/collapse per R4 scope-keuze)"
    - "Grid veranderd van lg:grid-cols-4 naar lg:grid-cols-2 voor leesbaarheid (R4)"
    - "Backward-compat: Phase 17 sessies zonder rijke velden renderen zonder crash via conditional rendering"
    - "Data-testid hooks aanwezig voor UAT: sub-effort-rich, sub-effort-titel, sub-effort-dossier, vermogen-impact"
    - "npm run build en npx vitest run slagen beide groen"
  artifacts:
    - path: "src/components/cross-analyse/StapSectorVertaling.tsx"
      provides: "Rijke domein-kaart renderer met titel/beschrijving/beargumentatie/vermogen-impact/dossier en backward-compat guards"
      contains: "sub-effort-rich, VermogenImpactSectie, DossierSectie, lg:grid-cols-2, advies.titel && , advies.dossier &&"
  key_links:
    - from: "src/components/cross-analyse/StapSectorVertaling.tsx"
      to: "src/lib/schemas.ts SubEffortDossier, SubEffortVermogenImpact"
      via: "type import uit Phase 18 schema exports"
      pattern: "import type.*SubEffortDossier"
    - from: "src/components/cross-analyse/StapSectorVertaling.tsx"
      to: "src/components/cross-analyse/shared/SectorBadge"
      via: "vermogenImpact lijst"
      pattern: "<SectorBadge sector="
---

<objective>
Herschrijf de domein-kaart renderer in `StapSectorVertaling.tsx` (regel ~356-422) naar een rijke kaart-layout die alle 5 Phase 18 velden toont: titel (Cito blauw), beschrijving, beargumentatie (als "Waarom cross-sectoraal?"), vermogen-impact (lijst met `SectorBadge`), en dossier (altijd zichtbaar, 5 velden). Wijzig grid naar `lg:grid-cols-2` voor rijke-content-leesbaarheid. Behoud backward-compat door conditional rendering op elk Phase 18 veld. Voer aan het einde `npm run build` + `npx vitest run` uit en bevestig beide GREEN.

Purpose: Visuele payoff van de fase. Zonder rendering is de rijke AI-output onzichtbaar voor de gebruiker; met deze plan zien stakeholders in de demo hoe focus-doel-geankerde cross-sectorale inspanningen met volledig dossier eruit zien.
Output: Uitgebreide `StapSectorVertaling.tsx`; alle tests groen; build slaagt; fase klaar voor UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-RESEARCH.md
@.planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-PATTERNS.md
@.planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-02-SUMMARY.md
@.planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Types en componenten die de rijke renderer consumeert. -->

Types uit src/lib/schemas.ts (na Plan 02):
```typescript
export type SubEffortAdvies = {
  // Phase 17
  groepId: string;
  domein: "mens" | "processen" | "data_systemen" | "cultuur";
  actie: "combineren" | "apart_houden";
  items: string[];
  reden: string;
  voorgesteldeNaam?: string | null;
  // Phase 18
  titel?: string;
  beschrijving?: string;
  beargumentatie?: string;
  vermogenImpact?: SubEffortVermogenImpact[];
  dossier?: SubEffortDossier;
};

export type SubEffortVermogenImpact = {
  sectorId: "PO" | "VO" | "Zakelijk";
  vermogenId: string;
  impact: string;
};

export type SubEffortDossier = {
  eigenaar: string;          // default "" — altijd string
  inspanningsleider: string;
  verwachtResultaat: string;
  kostenraming: string;
  randvoorwaarden: string;
};
```

Huidige imports en structuur (src/components/cross-analyse/StapSectorVertaling.tsx:1-16):
```tsx
"use client";
import React, { useState } from "react";
import { SectorBadge } from "./shared";
import { computeFocusView } from "@/lib/stap5-focus";
import type { ConsolidationOrigin } from "@/lib/stap5-focus";
import type {
  DINSession, Stap2Result, Stap4Result, Stap5Result,
  SectorName, DINCapability, EffortDomain,
} from "@/lib/types";
import type { VermogenGelijkenisGroep, SubEffortAdvies } from "@/lib/schemas";
```

Bestaande DOMAIN_COLORS map (regels 34-39) — hergebruiken verbatim:
```tsx
const DOMAIN_COLORS: Record<EffortDomain, { bg: string; border: string; text: string }> = {
  mens: { bg: "bg-blue-50", border: "border-blue-200", text: "text-blue-700" },
  processen: { bg: "bg-green-50", border: "border-green-200", text: "text-green-700" },
  data_systemen: { bg: "bg-purple-50", border: "border-purple-200", text: "text-purple-700" },
  cultuur: { bg: "bg-amber-50", border: "border-amber-200", text: "text-amber-700" },
};
```

Huidige domein-loop die vervangen wordt (regel 356-422, zie Read output above): 4-koloms grid met Phase 17 shape render.

Huidige grid (regel 356): `<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-2 mt-2">` — wijzigen naar `lg:grid-cols-2`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Voeg type-imports SubEffortDossier en SubEffortVermogenImpact toe + DossierSectie + VermogenImpactSectie helpers</name>
  <files>src/components/cross-analyse/StapSectorVertaling.tsx</files>
  <read_first>
    - src/components/cross-analyse/StapSectorVertaling.tsx regels 1-50 (imports, DOMAIN_COLORS, DOMAIN_LABELS)
    - src/components/cross-analyse/StapSectorVertaling.tsx regels 340-425 (huidige domein-loop die wordt vervangen)
    - src/components/cross-analyse/shared/SectorBadge.tsx (om props-signature te kennen voor `<SectorBadge sector={v.sectorId} />`)
    - .planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-PATTERNS.md sectie "StapSectorVertaling.tsx" — bevat exacte JSX skeleton
    - .planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-RESEARCH.md sectie "Rendering Strategy" + "Code Examples: Rendering"
  </read_first>
  <action>
Open `src/components/cross-analyse/StapSectorVertaling.tsx`.

**Stap 1 — Breid type-imports uit.** Zoek regel 16:
```tsx
import type { VermogenGelijkenisGroep, SubEffortAdvies } from "@/lib/schemas";
```
Vervang door:
```tsx
import type {
  VermogenGelijkenisGroep,
  SubEffortAdvies,
  SubEffortDossier,
  SubEffortVermogenImpact,
} from "@/lib/schemas";
```

**Stap 2 — Voeg 2 helper-componenten toe bovenaan het bestand, NA de DOMAIN_COLORS/DOMEIN_ORDER constanten (huidig rond regel 41) en VOOR `function DomainBalanceBadge(...)` (huidig rond regel 44).** Plaats dit blok:

```tsx
// --- Phase 18 (R-CROSS-03): VermogenImpact lijst per sub-effort advies ---
function VermogenImpactSectie({
  impacts,
  testIdBase,
}: {
  impacts: SubEffortVermogenImpact[];
  testIdBase: string;
}): React.ReactElement | null {
  if (!impacts || impacts.length === 0) return null;
  return (
    <section className="mt-3 pt-3 border-t border-gray-200">
      <p className="text-[10px] font-semibold text-gray-500 uppercase tracking-wider mb-2">
        Vermogen-impact per sector
      </p>
      <ul className="space-y-1.5">
        {impacts.map((v) => (
          <li
            key={`${v.sectorId}-${v.vermogenId}`}
            className="flex items-start gap-2"
            data-testid={`${testIdBase}-${v.sectorId}`}
          >
            <SectorBadge sector={v.sectorId} />
            <span className="text-[12px] text-gray-700 flex-1 leading-snug">{v.impact}</span>
          </li>
        ))}
      </ul>
    </section>
  );
}

// --- Phase 18 (R-CROSS-03): Dossier sectie — altijd zichtbaar per R4 scope-keuze ---
function DossierSectie({
  dossier,
  testId,
}: {
  dossier: SubEffortDossier;
  testId: string;
}): React.ReactElement | null {
  const entries: Array<[string, string]> = [
    ["Opdrachtgever", dossier.eigenaar ?? ""],
    ["Inspanningsleider", dossier.inspanningsleider ?? ""],
    ["Verwacht resultaat", dossier.verwachtResultaat ?? ""],
    ["Kostenraming", dossier.kostenraming ?? ""],
    ["Randvoorwaarden", dossier.randvoorwaarden ?? ""],
  ];
  const hasAny = entries.some(([, v]) => v && v.length > 0);
  if (!hasAny) return null;

  return (
    <section
      className="mt-3 pt-3 border-t border-gray-200"
      data-testid={testId}
    >
      <p className="text-[10px] font-semibold text-[#003366] uppercase tracking-wider mb-2">
        Inspanningendossier
      </p>
      <p className="text-[9px] text-gray-400 italic mb-2">
        Rolnamen zijn AI-voorstel; pas aan op jouw Cito-context.
      </p>
      <dl className="space-y-1.5">
        {entries.map(([label, value]) =>
          value && value.length > 0 ? (
            <div
              key={label}
              className="grid grid-cols-[130px_1fr] gap-2 text-[12px]"
            >
              <dt className="font-semibold text-gray-500">{label}</dt>
              <dd className="text-gray-700 leading-snug">{value}</dd>
            </div>
          ) : null
        )}
      </dl>
    </section>
  );
}
```

**Belangrijke design-keuzes:**
- `DossierSectie`: altijd zichtbaar (géén expand/collapse), per R4-beslissing Phase 18 scope-tight.
- `VermogenImpactSectie`: guardt tegen lege array met `return null`.
- UI-disclaimer "Rolnamen zijn AI-voorstel; pas aan op jouw Cito-context." per R2-beslissing.
- Cito blauw `#003366` voor de dossier-heading.

**Stap 3 — Vervang de domein-loop JSX (huidig regel ~355-422).** Zoek de regel:
```tsx
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-2 mt-2">
```
Wijzig naar:
```tsx
<div className="grid grid-cols-1 lg:grid-cols-2 gap-3 mt-2">
```

Vervolgens zoek het blok binnen deze grid dat begint met `{DOMEIN_ORDER.map((domein) => {` en de afsluitende `})}` op regel ~422. Vervang de bestaande `return ( <div ... /> )` voor de ADVIES-STATE (huidig regel 380-422, dus NIET de empty-state op regel 361-374) door het volgende blok. Let op: behoud de empty-state render verbatim; alleen de advies-state wordt vervangen.

Vervang de advies-state return (huidig regel 380-421):
```tsx
return (
  <div
    key={domein}
    className={`border ${colors.border} ${colors.bg} rounded-lg p-4`}
    data-testid={`sub-effort-rich-${groep.id}-${domein}`}
  >
    {/* Header-strip: domein + actie-badge + hefboom-badge */}
    <div className="flex items-center justify-between mb-3 gap-2 flex-wrap">
      <p className={`text-[10px] font-bold uppercase tracking-wider ${colors.text}`}>
        {DOMAIN_LABELS[domein]}
      </p>
      <div className="flex items-center gap-1">
        <span
          className={`text-[9px] font-semibold px-1.5 py-0.5 rounded ${
            advies.actie === "combineren"
              ? "bg-green-100 text-green-700"
              : "bg-gray-100 text-gray-600"
          }`}
        >
          {advies.actie === "combineren" ? "Combineren" : "Apart"}
        </span>
        {advies.actie === "combineren" && (
          <HefboomBadge vermogens={groepVermogens} />
        )}
      </div>
    </div>

    {/* Titel (Phase 18) — fallback op voorgesteldeNaam (Phase 17) */}
    {(advies.titel || advies.voorgesteldeNaam) && (
      <h4
        className="text-sm font-semibold text-[#003366] mb-2 leading-snug"
        data-testid={`sub-effort-titel-${groep.id}-${domein}`}
      >
        {advies.titel || advies.voorgesteldeNaam}
      </h4>
    )}

    {/* Beschrijving (Phase 18) */}
    {advies.beschrijving && (
      <p className="text-[13px] text-gray-700 leading-relaxed mb-3">
        {advies.beschrijving}
      </p>
    )}

    {/* Beargumentatie (Phase 18) */}
    {advies.beargumentatie && (
      <section className="mb-3 pt-3 border-t border-gray-200">
        <p className="text-[10px] font-semibold text-gray-500 uppercase tracking-wider mb-1">
          Waarom cross-sectoraal opbouwen?
        </p>
        <p className="text-[12px] text-gray-700 leading-relaxed">
          {advies.beargumentatie}
        </p>
      </section>
    )}

    {/* VermogenImpact per sector (Phase 18) */}
    {advies.vermogenImpact && advies.vermogenImpact.length > 0 && (
      <VermogenImpactSectie
        impacts={advies.vermogenImpact}
        testIdBase={`vermogen-impact-${groep.id}-${domein}`}
      />
    )}

    {/* Dossier (Phase 18) */}
    {advies.dossier && (
      <DossierSectie
        dossier={advies.dossier}
        testId={`sub-effort-dossier-${groep.id}-${domein}`}
      />
    )}

    {/* Legacy reden (Phase 17 backward-compat) — alleen tonen als beargumentatie ontbreekt */}
    {!advies.beargumentatie && advies.reden && (
      <p className="text-[11px] text-gray-600 mb-2 leading-snug">
        {advies.reden}
      </p>
    )}

    {/* Gebundelde inspanningen (legacy bullet-lijst) */}
    {relatedEfforts.length > 0 && (
      <section className="mt-3 pt-3 border-t border-gray-200">
        <p className="text-[10px] font-semibold text-gray-500 uppercase tracking-wider mb-1">
          Gebundelde inspanningen
        </p>
        <ul className="text-[11px] text-gray-700 space-y-0.5">
          {relatedEfforts.map((eff) => (
            <li key={eff.id}>• {eff.title || eff.description}</li>
          ))}
        </ul>
      </section>
    )}
  </div>
);
```

**Belangrijke backward-compat regels:**
- Elk Phase 18 blok heeft een guard: `{advies.titel && ...}`, `{advies.beschrijving && ...}`, etc. Zonder rijke velden valt het blok stil terug op Phase 17 render (voorgesteldeNaam + reden + relatedEfforts).
- `relatedEfforts` blijft altijd tonen als `items` gevuld is (legacy-keten).
- De empty-state (regel 361-374 — kaart zonder `advies`) blijft onaangeroerd.

**Wat NIET te doen:**
- GEEN verwijdering van de empty-state render (dashed border, "Geen gezamenlijke inspanning").
- GEEN wijziging aan `HefboomBadge`, `DomainBalanceBadge`, `HefboomPijlen` of andere helpers buiten de domein-loop.
- GEEN wijziging aan `computeFocusView`, `groepVermogens` mapping, of enige data-flow buiten de render.
- GEEN wijziging aan grid-klassen buiten het domein-grid (bv. groep-container grid).
- GEEN nieuwe padding/margin tokens buiten wat gespecificeerd is.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tee /tmp/p18-06-tsc.log; ! grep -q "StapSectorVertaling" /tmp/p18-06-tsc.log</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/cross-analyse/StapSectorVertaling.tsx bevat exact deze string: `SubEffortDossier,`
    - File bevat exact deze string: `SubEffortVermogenImpact,`
    - File bevat exact deze string: `function VermogenImpactSectie({`
    - File bevat exact deze string: `function DossierSectie({`
    - File bevat exact deze string: `data-testid={\`sub-effort-rich-${'${groep.id}'}-${'${domein}'}\`}` OR equivalent template literal with `sub-effort-rich-`
    - `grep -c "sub-effort-rich" src/components/cross-analyse/StapSectorVertaling.tsx` >= 1
    - `grep -c "sub-effort-titel" src/components/cross-analyse/StapSectorVertaling.tsx` >= 1
    - `grep -c "sub-effort-dossier" src/components/cross-analyse/StapSectorVertaling.tsx` >= 1
    - `grep -c "vermogen-impact" src/components/cross-analyse/StapSectorVertaling.tsx` >= 1
    - File bevat exact deze string: `lg:grid-cols-2`
    - File bevat NIET MEER: `lg:grid-cols-4` (grid is gewijzigd)
    - File bevat exact deze string: `Waarom cross-sectoraal opbouwen?`
    - File bevat exact deze string: `Vermogen-impact per sector`
    - File bevat exact deze string: `Inspanningendossier`
    - File bevat exact deze string: `Rolnamen zijn AI-voorstel`
    - File bevat exact deze string: `text-[#003366]` (Cito blauw voor titel en dossier-heading)
    - File bevat alle 4 guards: `advies.titel`, `advies.beschrijving`, `advies.beargumentatie`, `advies.dossier` (via `grep -c`)
    - File bevat exact deze string: `{!advies.beargumentatie && advies.reden &&` (backward-compat legacy fallback)
    - Command `npx tsc --noEmit` geeft geen StapSectorVertaling errors
  </acceptance_criteria>
  <done>
    StapSectorVertaling.tsx rendert rijke domein-kaart met 5 Phase 18 blokken (titel/beschrijving/beargumentatie/vermogenImpact/dossier), backward-compat guards, Cito blauw accent, `lg:grid-cols-2`. TypeScript compileert zonder errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Eindgate — npm run build + npx vitest run + visuele sanity check demo</name>
  <what-built>
    Alle Phase 18 wijzigingen over 6 plans:
    - Schema-uitbreiding (Plan 02)
    - Prompt-herziening (Plan 03)
    - API-wiring met focus-doel + profiel + 8192 tokens (Plan 04)
    - Demo-data met 4 rijke entries (Plan 05)
    - Rendering met rijke kaart + dossier (deze plan)
  </what-built>
  <how-to-verify>
    **Stap 1 — Automated gates (verplicht GREEN):**
    1. Run `npm run build` in project root. Bevestig "Compiled successfully" of Next.js build exit code 0. Noteer eventuele warnings.
    2. Run `npx vitest run`. Bevestig ALLE tests groen — inclusief:
       - `Phase 18 rijke uitwerking (R-CROSS-03)` — 5 tests GREEN
       - `Stap4ResultSchema Phase 18 integratie` — 2 tests GREEN (incl. demo-data parse)
       - Phase 17 tests blijven GREEN (backward-compat)
    3. Run `grep -c "Phase 18\\|R-CROSS-03" src/lib/schemas.ts src/lib/prompts.ts src/app/api/cross-analyse/route.ts src/components/cross-analyse/StapSectorVertaling.tsx src/lib/demo-data.ts src/lib/__tests__/cross-analyse-schema.test.ts` — er moet minstens 1 Phase 18 of R-CROSS-03 markering in elk bestand staan.

    **Stap 2 — Visuele sanity check in browser:**
    1. Run `npm run dev`. Open http://localhost:3000 .
    2. Klik "Demo laden" op de homepage → open de demo-sessie.
    3. Navigeer naar stap 3 (cross-analyse). Als er al een wizard-state is: klik door tot stap 4 (sector-vertaling) in de CrossAnalyseWizard.
    4. Verifieer visueel per groep × domein (verwacht 4 domein-kaarten: mens / cultuur / data_systemen / processen):
       - **Titel** in Cito blauw (`#003366`) zichtbaar
       - **Beschrijving** (2-3 zinnen grijs) zichtbaar
       - **"Waarom cross-sectoraal opbouwen?"** sectie met beargumentatie zichtbaar
       - **"Vermogen-impact per sector"** sectie met 3 rijen (PO badge + VO badge + Zakelijk badge) zichtbaar
       - **"Inspanningendossier"** sectie met 5 velden (Opdrachtgever / Inspanningsleider / Verwacht resultaat / Kostenraming / Randvoorwaarden) zichtbaar
       - Domein-kleuren kloppen: mens=blauw, processen=groen, data_systemen=paars, cultuur=amber (tints)
       - Grid is 2-koloms op desktop (`lg:grid-cols-2`)
    5. Console moet vrij zijn van React errors/warnings gerelateerd aan Phase 18.

    **Stap 3 — Backward-compat check:**
    Als je een bestaande localStorage-sessie hebt (Phase 17 era zonder rijke velden): laad die en bevestig dat de render niet crasht. Zonder rijke velden vallen titel/beschrijving/beargumentatie/vermogenImpact/dossier secties stil terug; reden + relatedEfforts blijven zichtbaar. Als geen zodanige sessie beschikbaar: skip deze stap en noteer in resume-signal.
  </how-to-verify>
  <resume-signal>Type "approved" als alle 3 stappen slagen en visuele sanity check positief is; anders beschrijf welke verificatie faalt (incl. screenshot-beschrijving of error-log).</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` slaagt
- `npx vitest run` — alle tests GREEN
- File heeft: 2 nieuwe helper-functies (VermogenImpactSectie, DossierSectie), uitgebreide type-import, gewijzigde grid-klasse, 5 Phase 18 rendering blokken
- Backward-compat: verify via git diff dat empty-state onaangeroerd is
- Geen React warnings in browser console tijdens demo-rendering
</verification>

<success_criteria>
1. Rijke kaart rendert alle 5 velden correct bij Phase 18 data (demo)
2. Backward-compat rendert zonder crash bij Phase 17 data (geen rijke velden)
3. Grid is 2-koloms op desktop; kaarten zijn leesbaar (niet te smal)
4. Data-testid hooks aanwezig voor UAT
5. Build + tests GREEN
6. Human verify checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-06-SUMMARY.md` met:
- Line-ranges waar VermogenImpactSectie en DossierSectie zijn toegevoegd
- Line-ranges waar grid en domein-loop zijn gewijzigd
- Uitkomst `npm run build` + `npx vitest run`
- Human-verify approval status
- Commit hash
</output>
