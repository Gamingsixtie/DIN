---
phase: 18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/__tests__/cross-analyse-schema.test.ts
autonomous: true
requirements: [R-CROSS-03]
tags: [zod, schema, test, phase-18, cross-analyse]
must_haves:
  truths:
    - "Test suite kent een Phase 18 describe-blok dat rijke velden verifieert"
    - "Bestaande Phase 17 tests blijven groen onder het uitgebreide schema"
    - "Tests falen (RED) voordat Plan 02 de schema-uitbreiding toevoegt"
  artifacts:
    - path: "src/lib/__tests__/cross-analyse-schema.test.ts"
      provides: "Phase 18 describe-blok met 6 nieuwe tests + 1 backward-compat confirm"
      contains: "SubEffortAdviesSchema Phase 18 rijke uitwerking"
  key_links:
    - from: "src/lib/__tests__/cross-analyse-schema.test.ts"
      to: "src/lib/schemas.ts (SubEffortAdviesSchema, SubEffortVermogenImpactSchema, SubEffortDossierSchema)"
      via: "zod import + SchemaName.safeParse pattern"
      pattern: "SubEffortVermogenImpactSchema|SubEffortDossierSchema"
    - from: "src/lib/__tests__/cross-analyse-schema.test.ts"
      to: "src/lib/demo-data.ts (createDemoSession)"
      via: "Stap4ResultSchema.safeParse op demo.crossAnalyseWizard.stepResults.stap4"
      pattern: "createDemoSession"
---

<objective>
Breid `cross-analyse-schema.test.ts` uit met een Phase 18 describe-blok dat de rijke-velden-uitbreiding van `SubEffortAdviesSchema` valideert (RED state). Tests zijn het contract voor Plan 02 (schema-uitbreiding) en Plan 05 (demo-data).

Purpose: Nyquist-compliance — elke volgende task heeft een automated verify; backward-compat wordt expliciet geborgd voordat code gewijzigd wordt.
Output: Uitgebreide test-file met 7 nieuwe tests (RED verwacht op 6; 1 is backward-compat confirm die al GREEN moet zijn).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-RESEARCH.md
@.planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-PATTERNS.md
@.planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Sleutelinterfaces die de tests consumeren. Geen codebase-exploration nodig. -->

From src/lib/schemas.ts (HUIDIG — Phase 17 shape, wordt uitgebreid in Plan 02):
```typescript
export const SubEffortAdviesSchema = z.object({
  groepId: z.string(),
  domein: z.enum(["mens", "processen", "data_systemen", "cultuur"]),
  actie: z.enum(["combineren", "apart_houden"]),
  items: z.array(z.string()),
  reden: z.string(),
  voorgesteldeNaam: z.string().nullable().optional(),
});

export const Stap4ResultSchema = z.object({
  consolidatieAdvies: /* ... */,
  citobreedInzicht: /* ... */,
  samenvatting: z.string(),
  subEffortAnalysis: z.array(SubEffortAdviesSchema).optional().default([]),
});

export const SectorNameSchema = z.enum(["PO", "VO", "Zakelijk"]);
```

From src/lib/schemas.ts (te exporteren door Plan 02, tests referen hier):
```typescript
export const SubEffortVermogenImpactSchema = z.object({
  sectorId: SectorNameSchema,
  vermogenId: z.string(),
  impact: z.string(),
});

export const SubEffortDossierSchema = z.object({
  eigenaar: z.string().optional().default(""),
  inspanningsleider: z.string().optional().default(""),
  verwachtResultaat: z.string().optional().default(""),
  kostenraming: z.string().optional().default(""),
  randvoorwaarden: z.string().optional().default(""),
});
```

Existing test pattern (cross-analyse-schema.test.ts:373-407, verbatim):
```typescript
describe("SubEffortAdviesSchema (D-30)", () => {
  test("sub effort advies accepteert valide input met voorgesteldeNaam string", () => {
    const result = SubEffortAdviesSchema.safeParse({ /* Phase 17 shape */ });
    expect(result.success).toBe(true);
  });
  // ...
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend cross-analyse-schema.test.ts met Phase 18 describe-blok</name>
  <files>src/lib/__tests__/cross-analyse-schema.test.ts</files>
  <read_first>
    - src/lib/__tests__/cross-analyse-schema.test.ts (volledig) — zie bestaande describe("SubEffortAdviesSchema (D-30)") op regel 373-407 en describe("Stap4ResultSchema sub effort advies integration (D-30)") op regel 465-512. Volg exact dezelfde stijl.
    - .planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-PATTERNS.md — sectie "src/lib/__tests__/cross-analyse-schema.test.ts" (bevat 6 aanbevolen nieuwe tests)
    - .planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-RESEARCH.md — sectie "Schema Strategy" (backward-compat test-matrix) + "Demo Data Strategy" (Stap4ResultSchema parse test)
    - src/lib/schemas.ts regels 1-60 (om te zien welke schemas al bestaan — `SectorNameSchema`, `InspanningsDossierSchema`) en 320-337 (huidige SubEffortAdviesSchema)
    - src/lib/demo-data.ts regels 755-796 (huidige subEffortAnalysis — voor test 7 die demo parseert)
  </read_first>
  <behavior>
    - Test 1 (Rich fields accepted): `SubEffortAdviesSchema.safeParse` met álle 5 nieuwe velden gevuld (`titel`, `beschrijving`, `beargumentatie`, `vermogenImpact: [{sectorId:"PO", vermogenId:"v1", impact:"..."}]`, `dossier: {eigenaar:"...", inspanningsleider:"...", verwachtResultaat:"...", kostenraming:"...", randvoorwaarden:"..."}`) → success === true.
    - Test 2 (Backward-compat Phase 17 shape): `SubEffortAdviesSchema.safeParse` met UITSLUITEND de 6 Phase 17 velden (geen titel, geen dossier, geen vermogenImpact) → success === true. Commentaar: "Phase 18 backward compat — oude sessies parseren zonder rijke velden".
    - Test 3 (Empty dossier parsed with defaults): `SubEffortAdviesSchema.safeParse` met `dossier: {}` → success === true en `result.data.dossier.eigenaar === ""` (default "").
    - Test 4 (Empty vermogenImpact accepted): `SubEffortAdviesSchema.safeParse` met `vermogenImpact: []` → success === true.
    - Test 5 (Invalid sectorId rejected): `SubEffortAdviesSchema.safeParse` met `vermogenImpact: [{sectorId: "FINANCIEN", vermogenId: "v1", impact: "x"}]` → success === false.
    - Test 6 (Stap4 integration with rich entry): `Stap4ResultSchema.safeParse` met 1 rijke subEffortAnalysis entry → success === true en `result.data.subEffortAnalysis[0].dossier.eigenaar` exact de meegegeven string.
    - Test 7 (Demo data parses): importeer `createDemoSession` uit `@/lib/demo-data`, roep aan, `Stap4ResultSchema.safeParse(demo.crossAnalyseWizard?.stepResults?.stap4)` → success === true.
  </behavior>
  <action>
Open `src/lib/__tests__/cross-analyse-schema.test.ts` en voeg de volgende INHOUD toe ná regel 512 (einde van `describe("Stap4ResultSchema sub effort advies integration (D-30)")` blok), vóór een eventueel EOF:

```typescript
// ============================================================
// Phase 18 — SubEffortAdviesSchema rijke uitwerking (R-CROSS-03)
// Uitbreiding met titel, beschrijving, beargumentatie, vermogenImpact[], dossier{}.
// Alle nieuwe velden zijn .optional() voor backward compat.
// ============================================================

describe("SubEffortAdviesSchema Phase 18 rijke uitwerking (R-CROSS-03)", () => {
  test("accepteert volledig rijke input met titel, beschrijving, beargumentatie, vermogenImpact en dossier", () => {
    const result = SubEffortAdviesSchema.safeParse({
      groepId: "g-klantpartnerschap",
      domein: "mens",
      actie: "combineren",
      items: ["eff-1", "eff-2", "eff-3"],
      reden: "gedeelde training",
      voorgesteldeNaam: "Sectoroverstijgende outside-in training",
      titel: "Sectoroverstijgende outside-in training",
      beschrijving:
        "Gezamenlijk curriculum outside-in gespreksvaardigheden met sector-specifieke casuistiek.",
      beargumentatie:
        "Een traject levert de hefboom voor drie sectoren tegelijk; circa 30% besparing t.o.v. drie losse trajecten.",
      vermogenImpact: [
        { sectorId: "PO", vermogenId: "cap-po-1", impact: "Leerkrachten voeren outside-in gesprekken met ouders." },
        { sectorId: "VO", vermogenId: "cap-vo-1", impact: "Schoolleiders benoemen inhoudelijke kansen vroeg." },
        { sectorId: "Zakelijk", vermogenId: "cap-zak-1", impact: "Accountmanagers stellen klantvraag scherp." },
      ],
      dossier: {
        eigenaar: "Directie L&D Cito BV",
        inspanningsleider: "Programmamanager L&D",
        verwachtResultaat: "NPS +5 binnen 12 maanden.",
        kostenraming: "EUR 350K over 18 maanden.",
        randvoorwaarden: "Commitment van alle drie sectormanagers.",
      },
    });
    expect(result.success).toBe(true);
    if (result.success) {
      expect(result.data.titel).toBe("Sectoroverstijgende outside-in training");
      expect(result.data.vermogenImpact).toHaveLength(3);
      expect(result.data.dossier?.eigenaar).toBe("Directie L&D Cito BV");
    }
  });

  test("backward compat — Phase 17 shape zonder rijke velden parseert succesvol", () => {
    const result = SubEffortAdviesSchema.safeParse({
      groepId: "g1",
      domein: "mens",
      actie: "combineren",
      items: ["eff-1"],
      reden: "gedeelde training",
      voorgesteldeNaam: "Sector-overstijgende training",
      // Phase 18 velden expliciet afwezig
    });
    expect(result.success).toBe(true);
    if (result.success) {
      expect(result.data.titel).toBeUndefined();
      expect(result.data.dossier).toBeUndefined();
      expect(result.data.vermogenImpact).toBeUndefined();
    }
  });

  test("dossier leeg object parseert met default lege strings per veld", () => {
    const result = SubEffortAdviesSchema.safeParse({
      groepId: "g1",
      domein: "processen",
      actie: "combineren",
      items: ["eff-1"],
      reden: "gedeelde processen",
      voorgesteldeNaam: "Gedeelde klantinformatie-proces standaardisatie",
      dossier: {},
    });
    expect(result.success).toBe(true);
    if (result.success) {
      expect(result.data.dossier?.eigenaar).toBe("");
      expect(result.data.dossier?.inspanningsleider).toBe("");
      expect(result.data.dossier?.verwachtResultaat).toBe("");
      expect(result.data.dossier?.kostenraming).toBe("");
      expect(result.data.dossier?.randvoorwaarden).toBe("");
    }
  });

  test("vermogenImpact lege array parseert succesvol", () => {
    const result = SubEffortAdviesSchema.safeParse({
      groepId: "g1",
      domein: "cultuur",
      actie: "apart_houden",
      items: ["eff-1"],
      reden: "sector-specifieke cultuur",
      voorgesteldeNaam: null,
      vermogenImpact: [],
    });
    expect(result.success).toBe(true);
    if (result.success) {
      expect(result.data.vermogenImpact).toEqual([]);
    }
  });

  test("vermogenImpact faalt bij ongeldige sectorId", () => {
    const result = SubEffortAdviesSchema.safeParse({
      groepId: "g1",
      domein: "mens",
      actie: "combineren",
      items: ["eff-1"],
      reden: "x",
      voorgesteldeNaam: "Sector-overstijgende training",
      vermogenImpact: [
        { sectorId: "FINANCIEN", vermogenId: "cap-1", impact: "x" }, // ongeldig — moet PO|VO|Zakelijk zijn
      ],
    });
    expect(result.success).toBe(false);
  });
});

describe("Stap4ResultSchema Phase 18 integratie (R-CROSS-03)", () => {
  test("Stap4Result met rijke subEffortAnalysis entry parseert succesvol", () => {
    const mockResponse = {
      consolidatieAdvies: [],
      citobreedInzicht: [],
      samenvatting: "Phase 18 rijke uitwerking demo",
      subEffortAnalysis: [
        {
          groepId: "g1",
          domein: "mens" as const,
          actie: "combineren" as const,
          items: ["eff-1"],
          reden: "gedeelde training",
          voorgesteldeNaam: "Sector-overstijgende training",
          titel: "Sector-overstijgende training",
          beschrijving: "Een cross-sectorale training",
          beargumentatie: "Hefboom over 3 sectoren",
          vermogenImpact: [
            { sectorId: "PO" as const, vermogenId: "cap-po-1", impact: "PO-impact" },
            { sectorId: "VO" as const, vermogenId: "cap-vo-1", impact: "VO-impact" },
            { sectorId: "Zakelijk" as const, vermogenId: "cap-zak-1", impact: "Zakelijk-impact" },
          ],
          dossier: {
            eigenaar: "Directie L&D",
            inspanningsleider: "Programmamanager",
            verwachtResultaat: "NPS +5",
            kostenraming: "EUR 350K",
            randvoorwaarden: "Drie sectormanagers commitment",
          },
        },
      ],
    };
    const result = Stap4ResultSchema.safeParse(mockResponse);
    expect(result.success).toBe(true);
    if (result.success) {
      expect(result.data.subEffortAnalysis).toHaveLength(1);
      expect(result.data.subEffortAnalysis[0].dossier?.eigenaar).toBe("Directie L&D");
      expect(result.data.subEffortAnalysis[0].vermogenImpact).toHaveLength(3);
    }
  });

  test("demo-data stap4Result.subEffortAnalysis parseert onder Stap4ResultSchema", async () => {
    const { createDemoSession } = await import("@/lib/demo-data");
    const demo = createDemoSession();
    const stap4 = demo.crossAnalyseWizard?.stepResults?.stap4;
    expect(stap4).toBeTruthy();
    const result = Stap4ResultSchema.safeParse(stap4);
    expect(result.success).toBe(true);
    if (result.success) {
      expect(result.data.subEffortAnalysis.length).toBeGreaterThanOrEqual(4);
      // Phase 18: elke entry moet rijke velden hebben (dossier, titel, vermogenImpact)
      result.data.subEffortAnalysis.forEach((entry) => {
        expect(entry.titel).toBeDefined();
        expect(entry.beschrijving).toBeDefined();
        expect(entry.beargumentatie).toBeDefined();
        expect(entry.dossier).toBeDefined();
        expect(entry.vermogenImpact).toBeDefined();
      });
    }
  });
});
```

**Belangrijk — imports aan top van file:**
- Controleer dat `SubEffortAdviesSchema` en `Stap4ResultSchema` al in de bestaande import-regel staan (regel ~1-10 van het bestand). Als ze er al zijn: geen import-wijziging.
- Géén nieuwe imports nodig voor `createDemoSession` — die wordt dynamisch geimporteerd in de test zelf (`await import("@/lib/demo-data")`) om circulaire deps te voorkomen.

**RED verwacht:** Na schrijven faalt `npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts` op de Phase 18 tests omdat `SubEffortAdviesSchema` de nieuwe velden nog niet accepteert (Zod strict of throw op undefined key — afhankelijk van zod gedrag). De backward-compat test en de demo-test kunnen óók falen als demo-data nog Phase 17 shape heeft (dat is OK en wordt door Plan 05 opgelost). Het is expliciet de bedoeling dat deze tests RED zijn vóór Plan 02 en Plan 05.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts 2>&1 | tee /tmp/p18-01-test.log; grep -q "Phase 18 rijke uitwerking" /tmp/p18-01-test.log</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/__tests__/cross-analyse-schema.test.ts bevat exact deze string: `describe("SubEffortAdviesSchema Phase 18 rijke uitwerking (R-CROSS-03)"`
    - File bevat exact deze string: `describe("Stap4ResultSchema Phase 18 integratie (R-CROSS-03)"`
    - File bevat exact deze string: `expect(result.data.dossier?.eigenaar).toBe("Directie L&D Cito BV")`
    - File bevat exact deze string: `await import("@/lib/demo-data")`
    - File bevat exact deze string: `sectorId: "FINANCIEN"` (test 5 — invalid sectorId)
    - Command `npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts` produceert RED output voor minstens één Phase 18 test (exit code != 0 of failed count >= 1) — dit is verwacht in Wave 0 RED state
    - Bestaande tests `describe("SubEffortAdviesSchema (D-30)")` en `describe("Stap4ResultSchema backward compat (D-19, D-30)")` blijven groen
    - Geen nieuwe imports toegevoegd aan test-file (alleen inline dynamic import voor demo-data)
  </acceptance_criteria>
  <done>
    Phase 18 describe-blokken zijn toegevoegd aan cross-analyse-schema.test.ts. Test-suite bevat minstens 7 nieuwe test-cases. Running vitest produceert RED voor Phase 18 tests (verwacht — Plan 02 maakt ze GREEN). Phase 17 tests blijven groen.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/__tests__/cross-analyse-schema.test.ts` moet Phase 17 tests groen tonen en Phase 18 tests als RED (verwacht RED state in Wave 0)
- Het bestand heeft geen syntax errors: `npx tsc --noEmit src/lib/__tests__/cross-analyse-schema.test.ts 2>&1 | head -20` geeft geen typescript-errors voor dit bestand
- Grep-verify: `grep -c "Phase 18" src/lib/__tests__/cross-analyse-schema.test.ts` >= 3
</verification>

<success_criteria>
1. 7 nieuwe tests toegevoegd in 2 nieuwe describe-blokken gemarkeerd met `Phase 18`
2. Bestaande Phase 17 tests zijn niet gewijzigd
3. Phase 18 tests zijn RED (verwacht — wordt GREEN via Plan 02 en Plan 05)
4. Geen nieuwe top-level imports — dynamic import gebruikt voor createDemoSession
</success_criteria>

<output>
After completion, create `.planning/phases/18-rijke-cross-sectorale-domein-uitwerking-in-sub-effort-analys/18-01-SUMMARY.md` met:
- Aantal tests toegevoegd (verwacht: 7)
- Welke tests RED zijn (expected: alle 6 rijke-velden tests + demo-data test)
- Welke tests GREEN zijn (expected: backward-compat test Phase 17 shape)
- Commit hash
</output>
